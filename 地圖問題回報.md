# UD v3.81 問題回報清單

給地圖作者的問題清單。全部以 `UD_v3_81_opt.w3x` 為準，方法是直接解開 MPQ、
讀 `war3map.j` 與物件資料（`war3map.w3a` / `w3t` / `w3u`）比對。

行號指的是從 v3.81 解出來的 `war3map.j`，作者的原始碼行號可能不同，
所以每一條都附上可以直接搜尋的原文。

分成三類：

- **A. 程式錯誤** —— 程式碼跟自己的意圖不符，功能實際上是壞的
- **B. 說明與實作不符** —— 程式碼是對的，但說明文字會誤導玩家
- **C. 文字問題** —— 純粹是文案，不影響數值

---

## A. 程式錯誤

### A-1. 零時迷子的「冰凍」分支永遠不會生效

**道具**：`I086` 零時迷子 / «Окалиновый конструкт»
**位置**：`FrostUnit`，第 1577 行與第 1632 行（共兩處）

```jass
if LoadInteger(hash,d_Id,'I086')>=1 then
    if LoadInteger(hash,d_Id,48)>=1 then      // <-- 這裡
        set cof=cof+0.50
    endif
endif
```

零時迷子在四種狀態各有一段一模一樣的程式碼，差別只在檢查哪一個 key：

| 狀態 | 檢查的 key | 有沒有人寫入這個整數槽 |
|---|---|---|
| 點燃 `Burn_Dmg` / `BurnUnit` | 27 | ✅ 有（`ApplyStaticItemStats` 等 27 處） |
| 流血 `Bleed_Dmg` | 29 | ✅ 有 |
| 疾病 `Disease_Dmg` | 45 | ✅ 有 |
| **冰凍 `FrostUnit`** | **48** | ❌ **完全沒有** |

整份 `war3map.j` 裡 `SaveInteger(hash,…,48,…)` 出現 **0 次**，只有 `SaveReal`。
而「抵抗冰凍」的旗標其實是寫在整數 key **28**：

```jass
// ApplyStaticItemStats，鏡面項鍊 I06Z、液態冰 k3m2、永恆之夢 ram3、
// 超凡護符 ram4、永恆聖木 rat3 都是這樣寫的
call SaveInteger(hash,u_Id,28,LoadInteger(hash,u_Id,28)+sign*1)
```

所以帶著零時迷子 ＋ 任何一件「抵抗冰凍」裝備的英雄，冰凍傷害拿不到那 +50%。
其他三種狀態都正常。

**建議修法**：把兩處的 `LoadInteger(hash,d_Id,48)` 改成 `LoadInteger(hash,d_Id,28)`。

> RU: `FrostUnit` (стр. 1577, 1632) проверяет целочисленный ключ 48, но
> `SaveInteger(hash,…,48,…)` не встречается в скрипте ни разу — флаг
> «сопротивление заморозке» пишется в целочисленный ключ **28**.
> Бонус +50% от «Окалинового конструкта» для заморозки не работает никогда.

---

### A-2. 風暴之靴檢查的是施加者，不是受害者

**道具**：`I09A` 風暴之靴 / «Штормовые сапоги»
**說明文字**：「英雄身上的電擊負面效果減弱 50%」
（«Ослабляет негативный эффект шока на герое на 50%.»）
**位置**：`ShockUnit`，第 2623 行

```jass
call UnitAddAbility(target,'S015')
if LoadInteger(hash,GetHandleId(damager),'I09A')>=1 then   // <-- damager
    set cof=0.50
endif
call SaveReal(hash,u_Id,4,LoadReal(hash,u_Id,4)-0.20*cof)
call SaveReal(hash,u_Id,6,LoadReal(hash,u_Id,6)-0.30*cof)
call SaveReal(hash,GetHandleId(target),47,LoadReal(hash,GetHandleId(target),47)-1.00*cof)
```

`cof` 控制的是這次電擊有多強，而 `damager` 是**施加電擊的那一方**。
所以現在的行為是「帶著風暴之靴時，**你打出去的電擊**弱一半」，
跟說明寫的「你身上的電擊弱一半」剛好相反。

對玩家來說這件裝備目前是負面的：想減免電擊的人買了沒效果，
而有在用電擊輸出的人（例如帶雷霆之刃 `I0AM`）買了反而砍自己一半。

**建議修法**：`GetHandleId(damager)` 改成 `GetHandleId(target)`。

> RU: `ShockUnit` (стр. 2623) проверяет `damager`, а не `target` —
> сапоги ослабляют шок, который герой **накладывает**, а не получает.

---

### ~~A-3.「向聖光祈願」的冷卻欄位是空的~~（查證後確認是文字問題，移到 C-3）

---

### A-4. 流血／疾病的傷害加成寫進了錯誤的變數

**位置**：`Bleed_Dmg` 第 2048 / 2069 行、`Disease_Dmg` 第 2253 / 2267 行

三個狀態的結算函式結構一樣：迴圈跑過目標身上每一段疊加的狀態，
每一段都有自己的施加者 `u`，`cof` 從 `dmg_cof` 複製出來當這一段的係數。
差別在最後一步：

```jass
// Burn_Dmg 第 1805 行 —— 正確
set cof=cof+LoadReal(hash,u_Id,27)        // 加進 cof
if cof<0.20 then
    set cof=0.20
endif
call UnitDamageTarget(u,u2,dmg*cof,...)   // 用 cof

// Bleed_Dmg 第 2069 行 —— 加錯變數
set dmg_cof=dmg_cof+LoadReal(hash,u_Id,29)   // 加進 dmg_cof
...
call UnitDamageTarget(u,u2,dmg*cof,...)      // 但傷害用的是 cof
```

`cof` 在第 2048 行就已經從 `dmg_cof` 複製好了，之後才改 `dmg_cof`，
所以這一段狀態**永遠吃不到自己施加者的加成**。而且因為 `dmg_cof` 是迴圈外
的區域變數，被改掉的值會留到下一圈 —— 加成會漏給**下一段**狀態，
而下一段常常是別的玩家施加的。

實際後果：「流血傷害 +X%」與「疾病傷害 +X%」這兩個屬性對自己幾乎沒用，
只有點燃是正常的。目前地圖裡有 3 件裝備給流血傷害、2 件給疾病傷害。

**建議修法**：兩處都改成 `set cof=cof+LoadReal(hash,u_Id,29)` /
`...,45)`，跟 `Burn_Dmg` 一致。

> RU: `Bleed_Dmg` (стр. 2069) и `Disease_Dmg` (стр. 2267) прибавляют бонус
> к `dmg_cof`, но урон считается по `cof`, скопированному раньше (стр. 2048 /
> 2253). Бонусы «+X% урона от кровотечения/болезни» не применяются к своему
> же экземпляру статуса и утекают в следующий. В `Burn_Dmg` (стр. 1805)
> сделано правильно.

---

### A-5. 狀態堆疊會被靜默丟掉（點燃／流血／疾病都有）

**位置**：`BurnUnit` 第 1965 行、`Burn_Dmg` 第 1821 行
（`BleedUnit` / `Bleed_Dmg`、`DiseaseUnit` / `Disease_Dmg` 是同一份程式碼）

目標身上的狀態是用「槽位」存的：`hash[目標]['burn']` 存**目前有幾段**，
`Burn_Dmg` 的迴圈就跑到那個數字為止：

```jass
// Burn_Dmg
local integer count=LoadInteger(hash,u2_Id,'burn')
...
exitwhen i==count
```

但 `BurnUnit` 在重複利用一個已過期的空槽時，把那個「數量」覆寫成了**槽位編號**：

```jass
loop
    set i=i+1
    if LoadInteger(hash,Id,i)==0 then          // 找到空槽 i
        call SaveUnitHandle(hash,Id,i,damager)
        call SaveInteger(hash,Id,i,count)
        call SaveReal(hash,Id,i,dmg)
        call SaveInteger(hash,t_Id,'burn',i)   // <-- 把「數量」寫成了「槽號」
        set i=count2
        set i2=1
    endif
    exitwhen i==count2
endloop
```

舉例：目標身上有 5 段點燃，第 2 段先燒完空出來，新的一段補進槽 2 →
`'burn'` 從 5 變成 2 → `Burn_Dmg` 之後只跑槽 1、2，**槽 3、4、5 雖然還沒燒完，
卻永遠不再結算**，也不會被清掉，最後整個計時器連同雜湊表一起銷毀。

被丟掉的常常是別的玩家施加的狀態。任何會對同一目標高頻施加狀態的組合都會踩到
（主教的天堂之光 9 秒內 18 次、神聖之焰 16 秒 32 次、拜火者的烈焰旋風 40 次…）。

**建議修法**：重用空槽時不要改寫 `'burn'`，只有在 `i2==0`（真的要開新槽）
那條路徑才需要更新數量。

> RU: `BurnUnit` (стр. 1965) при переиспользовании освободившегося слота
> записывает в `hash[цель]['burn']` **номер слота** вместо количества стаков.
> `Burn_Dmg` (стр. 1821) идёт циклом до этого числа, поэтому все стаки с
> большими номерами перестают тикать, хотя ещё активны. То же самое в
> `BleedUnit`/`Bleed_Dmg` и `DiseaseUnit`/`Disease_Dmg`.

---

### A-6. 「冰之不朽」的技能強度係數少了兩個 0

**技能**：`A10E` 冰之不朽 / «Незыблемость льда»
（`H00L` 寒冰魔像 —— 長工 H01H 的皮膚形態；`aare` = 99999，等於全圖光環）
**位置**：`Trig_HeroTakeDamage_Actions` 第 19853 與 19857 行

```jass
if GetUnitAbilityLevel(a,'B048')>=1 then
    set L=GetPlayerId(GetOwningPlayer(a))+1
    set DefCof=DefCof+(0.02+0.02*I2R(GetUnitAbilityLevel(udg_Hero[L],'A10E'))
                       +udg_ItemBonusDMG[L]*0.01)     // <-- 這裡
endif
```

技能說明寫的是「**每 100 技能強度 +1%**」（«+1% за каждые 100 силы умений»），
但 `udg_ItemBonusDMG[L]*0.01` 是**每 1 技能強度 +1%**，差了 100 倍。
係數應該是 `*0.0001`。

實際影響：1000 點技能強度 = 建築與機械單位的造成傷害 **+1000%**、
受到傷害同樣往下調 1000%（`DefCof` 沒有上限箝制，只有 `<0.20` 的下限）。

順帶一提這個技能的說明還有兩處值沒填：
`Защита от атак и умений: %;`、`Наносимый урон от атак и умений: %;`
—— 百分比數字是空的。

> RU: `Trig_HeroTakeDamage_Actions` (стр. 19853, 19857): `udg_ItemBonusDMG[L]*0.01`
> даёт +1% за **каждую единицу** силы умений, тогда как в описании — «+1% за
> каждые 100 силы умений». Коэффициент должен быть `*0.0001`.

---

### A-7. 9 個「施法時觸發」道具的內部冷卻完全沒生效

**位置**：`Trig_UseSkillsEndcast_Actions`，第 28893 / 28918 / 28938 / 28979 /
28984 / 28989 / 28994 / 28999 / 29086 行

「攻擊時觸發」的道具（`Trig_ItemAttacksFromHero_Actions`，40 多件）
都是先檢查冷卻再開始計時：

```jass
if LoadInteger(hash,Id,'I04C')>=1 then
    if LoadReal(hash,Id,'I04C')==0. then       // <-- 先確認冷卻跑完
        call StartModCooldown(Id,'I04C',3.)
        …效果…
```

但「施法時觸發」的這 9 件少了中間那一層，變成每次施法都重新計時、
而且**每次都觸發效果**：

```jass
if LoadInteger(hash,Id,'odef')>=1 then
    call StartModCooldown(Id,'odef',10.)       // <-- 沒有先檢查
    set dmg=I2R(GetHeroStr(u,true))*1.50*cof
```

| 道具 | 稀有度 | 說明寫的冷卻 | 實際 |
|---|---|---|---|
| 獅子星座 `ofro` | 神器 lv.5 | 1 秒 | 每次施法 |
| 水晶法杖 `I06X` | 特殊 lv.5 | 1 秒 | 每次施法 |
| 海克斯科技 `shtm` | 特殊 lv.5++ | 15 秒 | 每次施法 |
| 平紋褲 `I01T` | 普通 | 10 秒 | 每次施法 |
| 侍僧之褲 `hcun` | 稀有 | 10 秒 | 每次施法 |
| 術士之褲 `clfm` | 獨特 | 10 秒 | 每次施法 |
| 大法師之褲 `odef` | 傳說 | 10 秒 | 每次施法 |
| 林后之冠 `oslo` | 特殊 lv.5++ | 15 秒 | 每次施法 |
| 治癒之球 `glsk` | 復活節 | 5 秒 | 每次施法 |

影響最大的是那四件褲子與治癒之球：對能連續施法的英雄（女術士開執政官後
魔法閃光冷卻只有 1.5 秒）等於法力回復與治療量各自變成說明的好幾倍。

**建議修法**：照 27615-27616 的寫法，在 `StartModCooldown` 外面補一層
`if LoadReal(hash,Id,'<道具ID>')==0. then`。

> RU: 9 предметов в `Trig_UseSkillsEndcast_Actions` вызывают
> `StartModCooldown`, но без проверки `LoadReal(hash,Id,'<item>')==0.`,
> которая есть у всех предметов в `Trig_ItemAttacksFromHero_Actions`
> (см. стр. 27615-27616). Их внутренний кулдаун не работает вообще.

---

### A-8. 「不穩定充能」把「魔力充盈」的加成算了兩次

**技能**：`A016` 不穩定充能（女術士 H00Q）／天賦 `A0JD` 魔力充盈
**位置**：`Trig_HeroSkills12_Actions` 第 50255 行 ＋ `Hero12E` 第 50083-50086 行

```jass
// Trig_HeroSkills12_Actions：dmg_cof 這時已經是 1.75
if GetUnitAbilityLevel(u,'A0JD')==1 then
    if GetUnitManaPercent(u)>=80.00 then
        set dmg_cof=dmg_cof+0.75
    endif
endif
…
call SaveReal(hash,Id,1,(60.+60.*lvl+SP*0.50)*dmg_cof)   // 第一次 ×1.75

// Hero12E（命中時）
if GetUnitAbilityLevel(u,'A0JD')==1 then
    if GetUnitManaPercent(u)>=80.00 then
        set dmg=dmg*1.75                                  // 第二次 ×1.75
    endif
endif
```

結果是 **×3.0625** 而不是 ×1.75。同一個天賦對魔法閃光（第 50203 行）
與超新星（`Hero12W` 第 49976 行）都只乘一次，所以這一支明顯是重複。

順帶一提 `Hero12E_Talent` 第 50032-50033 行還有第三層 ×1.75；
目前兩個天賦互斥（選完一個另一個就被 `UnitRemoveAbility` 移除）所以是死碼，
但如果之後開放同時擁有，分裂傷害會變成 ×1.75³×0.75 ≈ ×4.02。

---

### A-9. 遺忘之矛的「5% 當前生命值」實際上是 0

**道具**：`hbth` 遺忘之矛（特殊 lv.5++，2750 金）
**說明**：「英雄的攻擊有 50% 機率對目標造成相當於其**當前生命值 5%** 的物理傷害」
**位置**：`Trig_ItemAttacksFromHero_Actions` 第 28330 行

```jass
set dmg=GetUnitLife(d)*0.05
```

但 `GetUnitLife`（第 3784 行）讀的是雜湊表 key 37：

```jass
function GetUnitLife takes unit u returns integer
    local integer a=LoadInteger(hash,GetHandleId(u),37)
    return a
endfunction
```

而寫進 key 37 的只有 `SetUnitLife`（第 3969 行），它存的是**這一次呼叫的增量**，
不是單位的生命值、也不是累計的生命上限加成，而且每次呼叫都直接覆寫：

```jass
function SetUnitLife takes unit c,integer hp returns nothing
    set hp=R2I(I2R(hp)-(GetUnitState((c),UNIT_STATE_MAX_LIFE)))
    call SaveInteger(hash,GetHandleId(c),37,hp)     // 覆寫，不是累加
```

所以對絕大多數單位（從來沒被 `SetUnitLife` 動過的）key 37 = 0，
這件 2750 金道具的招牌效果**完全沒有傷害**。

**建議修法**：`GetUnitLife(d)` 改成 `GetWidgetLife(d)`。

> RU: `hbth` использует `GetUnitLife(d)*0.05`, но `GetUnitLife` (стр. 3784)
> читает ключ 37, куда `SetUnitLife` (стр. 3969) пишет **разницу** последнего
> вызова, а не запас здоровья. Для большинства целей там 0.

---

### A-10. 屬性隨機（-random str/agi/int）的「已被選走」判斷讀錯英雄

**位置**：`RandomStr` / `RandomAgi` / `RandomInt`，以及 `Trig_HeroPick_Actions`

三個屬性池各自有自己的 `RH[]` 編號：

```jass
// RandomInt
set RH[1]='H00O'   set RH[2]='H00X'   set RH[3]='H012'  …
…
if udg_RHcheck[L]==true then          // L 是 RandomInt 自己的編號
```

但 `udg_RHcheck` 是在 `Trig_HeroPick_Actions` 用**另一套編號**寫入的 ——
那套編號對應的是英雄的技能觸發器序號（`gg_trg_HeroSkills1`、`2`、`3`…）：

```jass
set udg_RHcheck[1]=true      // 這裡的 1 是「技能觸發器 1 號」的那隻英雄
```

兩套編號不同，所以屬性隨機在判斷「這隻是不是已經被別人選走」時讀到的是別隻英雄。
`RandomAll` 的編號剛好與技能觸發器序號一致（例如 `RH[23]='H01F'`，
而樹人長者的技能是 `Hero23Q`），所以只有 `-random` 是對的。

同一段還有兩個小問題：

- **`RandomInt` 裡 `N01K`（深淵戰士）出現兩次**（`RH[7]` 與 `RH[8]`），
  被 `-random int` 抽中的機率是其他智力英雄的兩倍。
- **樹人長者 `H01F` 不在任何一個屬性池裡**，只有 `RandomAll` 有它 ——
  `-random int` 永遠抽不到樹人長者。

---

### A-11. ClearUnit 的疾病分支清錯了雜湊表 key

**位置**：`ClearUnit`

```jass
if GetUnitAbilityLevel(u,'B006')==1 then      // B006 = 疾病
    …
    set t=LoadTimerHandle(hash,u_Id,'disa')   // 讀 disa
    …
    call RemoveSavedHandle(hash,u_Id,'blud')  // 卻刪 blud（流血的 key）
```

疾病的計時器 handle 留在雜湊表裡沒被清掉，而流血的 key 被多刪了一次。
點燃（`burt`）與流血（`blud`）兩個分支都是對的，只有疾病這段是複製貼上沒改到。

---

### A-12. 冠軍手套沒被算進「傳說道具件數」

**位置**：`ApplyLegendaryItems`（第 22784-22993 行）

地圖裡有 11 件「傳說」道具，這個函式為其中 **10 件**維護一個計數器：

```jass
call SaveInteger(hash,u_Id,'iLEG',LoadInteger(hash,u_Id,'iLEG')+sign*1)
```

唯一漏掉的是**冠軍手套 `brac`** —— 它的分支有處理屬性，但沒有那一行。

這個計數器只有一個讀者，就是生命之靈「覺醒」天賦下的治療連結：

```jass
// Hero7Q 第 48199 行
set item_cof=item_cof+0.15*I2R(LoadInteger(hash,GetHandleId(u),'iLEG'))
```

所以帶著冠軍手套的生命之靈，**拿不到那 +15% 治療量**，
但帶其他任何一件傳說都拿得到。

對照組：「折射」類的 `iBLU` 計數器在 `ApplyStaticItemStats` 裡 17 件全部都有，
沒有遺漏 —— 所以這是 `iLEG` 這一支單獨漏掉的。

> RU: `ApplyLegendaryItems` ведёт счётчик `iLEG` для 10 из 11 легендарных
> предметов — у `brac` (Champion's Gloves) этой строки нет. Талант
> «Пробуждение» Духа жизни (`Hero7Q`, стр. 48199) даёт +15% лечения за
> каждый легендарный предмет, и перчатки в него не попадают.
> Для «преломлённых» (`iBLU`) все 17 предметов учтены корректно.

---

### A-13. 英勇警鐘的加成是負數，實際會削弱自己與隊友

**道具**：`I0AK` 英勇警鐘（特殊 lv.5，1800 金）
**說明**：「使用時強化英雄與周圍友軍，**攻擊力提升 30%、護甲提升 6 點**，持續 15 秒」
**位置**：`war3map.w3a` 的 `A0SY`（基底 `Acht` 咆哮）

```
Roa1 = -0.30      (Damage Increase)
Roa2 = -6         (Armor Bonus)
atar = air,ground,friend,self
```

兩個數值都是**負的**，而目標是自己與友軍。地圖裡其他所有指向友軍的咆哮
全部是正值：

| 技能 | Roa1 | Roa2 | 目標 |
|---|---|---|---|
| `A0EE` | +0.30 | — | 友軍 |
| `A0LT` | +0.35 | — | 友軍 |
| `A06V` | +0.50 | — | 友軍 |
| `A0BP` | +0.75 | +5 | 友軍 |
| **`A0SY`（英勇警鐘）** | **−0.30** | **−6** | **友軍＋自己** |

只有指向敵人的「震懾咆哮」（`A07L` / `A0MP`）才是負值。
所以這件 1800 金的道具照實作是**讓自己與周圍隊友 −30% 攻擊力、−6 護甲** 15 秒。

**建議修法**：`Roa1` 改成 `0.30`、`Roa2` 改成 `6`。

> RU: `A0SY` («Набат доблести», предмет `I0AK`) имеет `Roa1 = -0.30` и
> `Roa2 = -6` при `atar = friend,self`, тогда как в описании — «+30% к силе
> атаки и +6 к защите». Все остальные «рёвы» на союзников в карте
> положительные; отрицательные — только у «Устрашающего рёва» на врагов.

---

### A-14. 著魔結束時會退還「從來沒加上去的」加成，可永久扣光 40% 裝備技能威力

**技能**：`A04T` 著魔（惡魔獵手 Edem）／天賦 `A0YG` 精湛劍術（標記技能 `A0YH`）
**位置**：`Trig_HeroSkills1_Actions` 第 46658-46662 行 ＋ `HeroR1_EndBuff` 第 46614-46618 行

加成只在**施法當下**有精湛劍術才給：

```jass
// Trig_HeroSkills1_Actions，A04T 分支
if GetUnitAbilityLevel(u,'A0YH')==1 then
    call SaveReal(hash,GetHandleId(u),1,LoadReal(hash,GetHandleId(u),1)-0.20)
    call SaveReal(hash,GetHandleId(u),18,LoadReal(hash,GetHandleId(u),18)+0.40)
    call SetUnitAbilityLevel(u,'A0S1',2)
endif
```

但 20 秒後的收尾函式是**重新檢查一次**才退還：

```jass
// HeroR1_EndBuff
if GetUnitAbilityLevel(u,'A0YH')==1 then
    call SaveReal(hash,GetHandleId(u),1,LoadReal(hash,GetHandleId(u),1)+0.20)
    call SaveReal(hash,GetHandleId(u),18,LoadReal(hash,GetHandleId(u),18)-0.40)
    call SetUnitAbilityLevel(u,'A0S1',1)
endif
```

兩邊檢查的是**不同時間點**的狀態。只要玩家：

1. 在還沒選天賦（或選了虛空之刃後又改選）時放著魔 —— 沒有加成
2. 在那 20 秒內取得精湛劍術
3. 等 buff 自然結束 —— 收尾函式看到 `A0YH==1`，就**退還了從來沒加上的東西**

結果是 key 18 變成 **−0.40**、key 1 變成 **1.20**，而且是**永久**的
（沒有任何地方會重算這兩個值）。消費端 `cof = LoadReal(hash,Id,18)+1.`
於是變成 0.60 —— 所有道具觸發傷害少 40%，道具技能冷卻多 20%。
再放一次著魔會再扣一次，可以疊到更低。

**建議修法**：把「這次施放有沒有加成」記在計時器的雜湊表裡
（例如 `SaveInteger(hash,Id,2,1)`），收尾時只讀那個旗標，不要重新檢查 `A0YH`。

> RU: `Trig_HeroSkills1_Actions` (стр. 46658) добавляет −0.20 к ключу 1 и
> +0.40 к ключу 18 только если талант `A0YH` уже взят на момент каста, а
> `HeroR1_EndBuff` (стр. 46614) проверяет `A0YH` заново и возвращает бонус
> безусловно. Если взять талант во время действия «Метаморфозы», ключ 18
> станет −0.40 навсегда: весь урон от срабатываний предметов −40%.

---

### A-15. 烈焰領主的「引爆」讀到隔壁玩家的技能強度

**技能**：`A0GZ` 引爆（烈焰領主 N03R）
**位置**：`Trig_HeroAttack34_Actions`

```jass
local integer n=GetPlayerId(pl)          // <-- 少了 +1
...
local real dmg=25.+25.*I2R(GetUnitAbilityLevel(u,'A0GZ'))+udg_ItemBonusDMG[n]*0.20
```

`udg_ItemBonusDMG` 這個陣列全地圖都是用 `GetPlayerId(pl)+1` 當索引 ——
同一隻英雄的其他函式（`Trig_HeroSkills34_Actions`、`Hero34Q`、`Hero34R`、
`ResFirelord`）也全都有那個 `+1`，只有這一支漏了。

後果：引爆的技能強度加成讀的是**下一個玩家**的技能強度。
紅色玩家（id 0）讀 index 0，那個槽位從初始化後就沒被寫過，恆為 0 ——
**引爆完全吃不到自己的技能強度**。

**建議修法**：`GetPlayerId(pl)` 改成 `GetPlayerId(pl)+1`。

> RU: `Trig_HeroAttack34_Actions` использует `GetPlayerId(pl)` без `+1`,
> тогда как `udg_ItemBonusDMG` во всей карте индексируется как
> `GetPlayerId(pl)+1`. «Детонация» читает силу умений соседнего игрока;
> у красного (id 0) — всегда 0.

---

### A-16. 引爆的攻擊分支會蓋掉過載的傷害公式與座標

**技能**：`A0HC` 過載 ＋ `A0HU` 致命閃電（暴風使者 N03S）
**位置**：`Trig_Hero35DAttack_Actions`

同一個攻擊處理器裡，過載先算好自己的傷害與「目標座標」：

```jass
set dmg=GetHeroInt(u,true)*1.5+udg_ItemBonusDMG[n]*0.20   // 過載用
...
// 致命閃電那一支接著覆寫了同兩組變數
set dmg=30.+40.*I2R(GetUnitAbilityLevel(u,'A0HU'))+udg_ItemBonusDMG[n]*0.20
set x=GetUnitX(u)      // 從「被攻擊目標」改成「施法者自己」
set y=GetUnitY(u)
```

兩個效果在同一次攻擊同時觸發時，過載的範圍爆炸會：
(a) 用致命閃電的公式算傷害（智力高於 153 時等於降級），
(b) 炸在暴風使者自己腳下，而不是被攻擊的目標身上。

**建議修法**：兩支各自用自己的區域變數。

---

### A-17. 受詛之刃對同一個目標只能生效一次，之後永久失效

**技能**：`Amgr` 受詛之刃（黑暗主教 Nngs）
**位置**：`Trig_HeroAttack47_Actions` 第 61079 行、`HeroE47_EndDebuff` 第 61046 行

疊層數存在目標身上的整數槽 `'Nngs'`，滿 3 層就早退：

```jass
if LoadInteger(hash,GetHandleId(u),'Nngs')==3 then
    …
    return          // 不再施加
endif
```

但 debuff 到期的收尾只清了**計時器 handle**：

```jass
// HeroE47_EndDebuff
call SaveTimerHandle(hash,GetHandleId(u),'Nngs',null)
```

魔獸的雜湊表裡 `SaveTimerHandle` 與 `SaveInteger` 是**兩張不同的子表**，
即使 key 名稱同樣是 `'Nngs'` 也互不影響 —— 所以整數槽永遠停在 3。
全腳本對它的寫入只有第 61099 行（設 1）與第 61102 行（+1），**沒有任何歸零處**。

後果：任何單位一旦吃滿 3 層，debuff 結束後 `LoadInteger` 永遠回傳 3，
**受詛之刃對那個單位再也不會生效**。對 handle 長壽的 BOSS 與敵方英雄來說，
等於這招每個目標一場只能用一次。而且第 61073 行的 3.5 秒內部冷卻是在早退
**之前**就設好的，所以那 3.5 秒照樣被扣掉。

**建議修法**：`HeroE47_EndDebuff` 裡加一行
`call SaveInteger(hash,GetHandleId(u),'Nngs',0)`。

> RU: счётчик стаков «Проклятого клинка» хранится в целочисленном слоте
> `'Nngs'`, а `HeroE47_EndDebuff` обнуляет только слот таймера с тем же
> именем (это разные подтаблицы). Целое значение навсегда остаётся равным 3,
> и способность перестаёт действовать на эту цель до конца игры.

---

### A-18. 暗影獵手的 `'O01C'` 雜湊鍵被兩套邏輯共用

**位置**：`Trig_HeroSkills24_Actions` 第 53896 行、第 53994 行；
`Trig_HeroKills24_Actions` 第 54029 / 54087 / 54099 行

同一個 key 名稱在兩種單位身上是完全不同的東西：

| 存在誰身上 | 意義 | 寫入處 |
|---|---|---|
| **暗影／分身** `u2` | 「我是邪祟的」的標記（值恆為 1） | 53994 |
| **英雄本人** `u` | 生暗影失敗的**保底計數器**（每次沒生出來 +1，生出來歸 0） | 54087、54099 |

而暗影法球的傷害公式用它來判斷「施法者是不是邪祟暗影」：

```jass
if LoadInteger(hash,GetHandleId(u),'O01C')==1 then
    set dmg=(…)*0.50*(1.00+LoadReal(hash,GetHandleId(pl),'A09H'))   // 邪祟：乘天賦倍率
else
    set dmg=(…)*0.50+LoadReal(hash,GetHandleId(pl),'A0OO')*0.50     // 一般：加汲取累積
endif
```

當 `u` 是**英雄本人**時，這一行讀到的是保底計數器。後果：

- 帶「邪祟暗影 `A091`」天賦時，英雄自己的法球**只有在保底計數器剛好等於 1
  的那段期間**才吃得到 `×(1+A09H)`，其餘時間天賦對法球完全沒有加成。
- 帶「暗影汲取 `A0ON`」天賦時反過來 —— 計數器等於 1 的期間，
  累積的 `A0OO` 加成會整個消失。
- 邪祟分身自己也會踩到：它殺人一次（不論有沒有生出暗影）就會改寫自己的
  `'O01C'`，身上的「1」被洗掉，法球的天賦倍率隨即消失。

**建議修法**：兩者用不同的 key 名稱，例如標記用 `'SHDW'`、計數器維持 `'O01C'`。

> RU: ключ `'O01C'` используется одновременно как «этот юнит — зловещая тень»
> (на тени, стр. 53994) и как счётчик неудачных попыток создать тень
> (на герое, стр. 54087/54099). Формула урона сферы (стр. 53896) читает его
> у кастера, поэтому у самого героя талант работает только в те моменты,
> когда счётчик случайно равен 1.

---

### A-19. 惡魔步伐會把全域傷害管線關掉（影響所有玩家）

**道具**：`I070` 惡魔步伐（特殊 lv.5+，2150 金）
**位置**：`Trig_HeroTakeDamage_Actions` 第 20032-20047 行

```jass
call DisableTrigger(GetTriggeringTrigger())          // 先關掉自己
if LoadInteger(hash,GetHandleId(d),'I070')>=1 then   // 受害者帶惡魔步伐
    set L=GetRandomInt(1,100)
    if L<=20 then
        …格擋＋點燃…
    endif
    …
    return                    // <-- 在 20% 判定「外面」，無條件執行
endif
…
call EnableTrigger(GetTriggeringTrigger())           // 正常路徑在函式結尾才重新啟用
```

這一支有兩個問題：

**① `return` 的位置不對。** 它在 `if L<=20` 的外面，所以**只要受害者帶著這件
道具，整個 DefCof／穿透／苦難面具反彈／道具受傷觸發就全部跳過**，不是只有
那 20% 的機率。持有者永遠吃不到自己的減傷，敵人的穿透也打不到他。

**② 那條路徑沒有 `EnableTrigger`。** 函式開頭已經 `DisableTrigger` 了自己，
正常路徑在結尾（第 20692 行附近）才重新啟用，但這裡直接 `return` 跳過了。
於是 `gg_trg_HeroTakeDamage` 就留在停用狀態，**所有玩家**的傷害管線一起失效，
直到別處某個 `DisableTrigger`／`EnableTrigger` 配對（`BurnUnit`、反傷…）
偶然把它打開。

**建議修法**：把 `return` 移進 `if L<=20` 裡面，並在 `return` 之前補上
`call EnableTrigger(GetTriggeringTrigger())`。

> RU: `Trig_HeroTakeDamage_Actions` (стр. 20032-20047): `return` в ветке
> `I070` находится **вне** проверки 20%, и на этом пути нет
> `EnableTrigger`. Любой удар по носителю «Демонической поступи» оставляет
> `gg_trg_HeroTakeDamage` выключенным для **всех** игроков.

---

### A-20. 皇家守衛的死亡團隊回復判斷了別隻英雄的技能，永遠不會執行

**位置**：`Trig_DieHero_Actions` 第 46501 行

```jass
if GetUnitTypeId(u)=='Hapm' and GetUnitAbilityLevel(u,'A01H')==1 then
    …全隊回 50% 生命與法力、每個活著的英雄讓復活時間 −3 秒…
```

`A01H` 是**元帥的天賦書**（`Aspb`，選項 `A0I3,A0IA,A0LE`），只出現在
`Hlgr` / `Emfr` / `H01D` 三個單位的 `uabi` 裡。皇家守衛的天賦書是 **`A0R6`**。

全腳本沒有任何 `UnitAddAbility(u,'A01H')`（只有三處 `UnitRemoveAbility`），
所以皇家守衛永遠不可能持有 `A01H` —— **這整段死亡回復是死碼**。

**建議修法**：`'A01H'` 改成皇家守衛實際的那個天賦 ID。

---

### A-21. 「磐石」的傷害加成寫進了減傷欄位，變成第二段減傷

**位置**：`Trig_HeroGiveSkills44_Actions` 第 60195 行（軍團特使）

```jass
if GetUnitAbilityLevel(u,'A0II')==1 then
    call SaveReal(hash,Id,12,LoadReal(hash,Id,12)+0.08)
    call SaveReal(hash,Id,12,LoadReal(hash,Id,12)+0.12)
else
    call SaveReal(hash,Id,12,LoadReal(hash,Id,12)+0.04)
    call SaveReal(hash,Id,12,LoadReal(hash,Id,12)+0.06)
endif
```

技能說明是兩件事：

```
對 0-1 級單位的防護：+8%   （每級 8 / 12 / 16 / 20 / 24）
對 0-1 級單位的傷害：+12%  （每級 12 / 18 / 24 / 30 / 36）
```

兩次 `SaveReal` 都寫進 **key 12**。key 12 只有一個消費者：

```jass
if GetUnitLevel(a)<2 then
    set DefCof=DefCof-LoadReal(hash,GetHandleId(d),12)
endif
```

也就是「被 0-1 級單位攻擊時的減傷」。所以實際效果是：

| 等級 | 說明寫的 | 實際 |
|---|---|---|
| 1 | 減傷 8%＋傷害 12% | 減傷 **20%**、傷害 **+0%** |
| 5 | 減傷 24%＋傷害 36% | 減傷 **60%**、傷害 **+0%** |

（每級的增量 `+0.04 / +0.06` 本身是對的，對應說明的 8→12→16 與 12→18→24，
所以結構沒問題，純粹是第二個 `SaveReal` 的 key 填錯。）

**建議修法**：第二個 `SaveReal` 改寫進「對低階單位增傷」的 key
（管線裡目前沒有這個欄位，需要在 `Trig_HeroTakeDamage_Actions` 的
`GetUnitLevel(a)<2` 那段旁邊補一個對稱的攻擊方判斷）。

---

### A-22. 「軍團遺產」套裝數錯道具：算進了隨機寶箱，漏掉了一件遺物

**位置**：`Trig_HeroComplectAdd44_Actions` 第 60219 行、
`Trig_HeroComplectRemove44_Actions` 第 60249 行（兩處清單一模一樣）

```jass
if ItemID=='I00T' or ItemID=='I012' or ItemID=='I03D' or ItemID=='I029'
   or ItemID=='spro' or ItemID=='pman' or ItemID=='tbak' or ItemID=='ssan'
   or ItemID=='phea' or ItemID=='spre' or ItemID=='modt' or ItemID=='rej3'
   or ItemID=='wlsd' then
```

清單裡 13 個 ID，其中 12 個的 `ides` 都是 `Класс: Реликвия`，只有一個例外：

- **`I012`「Большая посылка」** —— `Класс: Случайный предмет`，
  售價 525，作用是開出一件隨機的「特殊」道具。它不是遺物，
  卻會讓「Наследие Легиона N/6」的計數 +1。

反過來，`I028`「Молот веры」（1500 金、`Класс: Реликвия`、
+20% 對英雄傷害／+15 攻擊／+200 生命）**不在清單裡**，
拿了不會計入套裝。

兩個 ID 的原型都是 `rde1`，看起來是同一次複製貼上時打錯的。

**建議修法**：清單裡的 `'I012'` 改成 `'I028'`（Add 與 Remove 兩處都要改）。

---

### A-23. 「寒冰魔像」的 1000 門檻把冰凍傷害加成寫成 250 倍

**位置**：`Trig_HeroSkills27_Actions` 第 55230 行（長工，`A0C3` 變身分支）

```jass
if udg_ItemBonusDMG[n]>=1000 then
    call UnitAddAbility(u2,'A0ZU')
    call SaveReal(hash,GetHandleId(u2),28,250.)
endif
```

key 28（實數）是**冰凍傷害加成**，在傷害管線裡直接進係數：

```jass
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
```

`cof` 是 1.0 = 100% 的倍率。全腳本其他寫入 key 28 的地方都是這個量級：

```jass
call SaveReal(hash,GetHandleId(u),28,0.25)                       // 21585
call SaveReal(hash,u_Id,28,LoadReal(hash,u_Id,28)+sign*0.15)     // 23928
```

只有這一行是 `250.`，等於 **+25000%**。魔像的冰凍傷害會直接把
`cof` 從 1 推到 251，任何目標都是一擊。
看數字應該是想寫 `2.50`（+250%）或 `0.250`。

**建議修法**：`250.` 改成想要的倍率；順便確認一下這個加成該不該寫在
魔像 `u2` 身上（`u2` 是召喚出來的分身，本體 `u` 被隱藏了）。

---

### A-24. 「毀滅砲擊」的 −15% 減傷懲罰基本上不會生效

**位置**：`Trig_HeroGiveSkills27_Actions` 第 55022 行

```jass
function Trig_HeroGiveSkills27_Actions takes nothing returns nothing
    local unit u=GetLearningUnit()
    if GetUnitAbilityLevel(u,'A0C0')==1 then
        call SaveReal(hash,GetHandleId(u),4,LoadReal(hash,GetHandleId(u),4)-0.15)
    endif
    call DisableTrigger(GetTriggeringTrigger())
    set u=null
endfunction
```

`DisableTrigger` 在 `if` **外面**，所以這支觸發器在**第一次有人學任何技能**
時就把自己關掉了。只有「A0C0 剛好是這個觸發器收到的第一個學習事件」時
懲罰才會套上；正常玩法（先點別的技能、或別的英雄先點技能）都不會生效。

觸發器物件是全域唯一的（`gg_trg_HeroGiveSkills27`），多個單位註冊到同一支上，
所以一旦被關，對所有人都失效。

**建議修法**：把 `DisableTrigger` 移進 `if` 裡面，或改用
`SaveInteger` 記旗標避免重複加成。

---

### A-25. 猛衝的「對英雄雙倍」判斷的是自己，不是被打的人

**位置**：`Skill43Q` 第 59604 行（血騎士女族長），第 59659 行有同樣一份

```jass
loop
    set u3=FirstOfGroup(ug)
    exitwhen u3==null
    if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
        if IsUnitType(u,UNIT_TYPE_HERO)==true then
            call UnitDamageTarget(u,u3,dmg*2.00,…)
        else
            call UnitDamageTarget(u,u3,dmg,…)
        endif
    endif
```

迴圈變數是 `u3`（被撞到的敵人），但 `IsUnitType` 問的是 `u` —— **衝鋒的英雄自己**。
血騎士女族長永遠是英雄，所以 `else` 分支是死碼，**×2.00 對所有目標無條件生效**，
雜兵與建築一樣吃雙倍。

技能說明寫的是「敵方英雄受到的傷害提高 100%」。

**建議修法**：`IsUnitType(u,…)` 改成 `IsUnitType(u3,…)`（兩處都要改）。

---

### A-26. 閃耀護盾借用了「抵抗冰凍／抵抗流血」的旗標當內部開關

**位置**：`Trig_HeroAttack43_Actions` 第 59832 行、`Hero43E` 第 59802 行、
`Hero43E2` 第 59799 行（血騎士女族長）

護盾用**整數** key 28 當「現在不能再開」的旗標、key 29 當「護盾生效中」：

```jass
if GetUnitAbilityLevel(u,'Amgl')>0 then
    if LoadInteger(hash,GetHandleId(u),28)!=1 then      // 閘門
        …
        call SaveInteger(hash,GetHandleId(u),28,1)      // 硬寫 1
        call SaveInteger(hash,GetHandleId(u),29,1)      // 硬寫 1
```

```jass
// Hero43E：10 跳結束
call SaveInteger(hash,GetHandleId(u),29,0)             // 硬寫 0
…10 秒後…
// Hero43E2
call SaveInteger(hash,GetHandleId(u),28,0)             // 硬寫 0
```

但整數 key 28 / 29 在全地圖是**抵抗冰凍 / 抵抗流血的計數器**，
道具處理器是用 `+1 / −1` 累加的：

```java
elseif ItemID=='ram4' then                              // 超凡護符
    call SaveInteger(hash,u_Id,27,LoadInteger(hash,u_Id,27)+sign*1)
    call SaveInteger(hash,u_Id,28,LoadInteger(hash,u_Id,28)+sign*1)
    call SaveInteger(hash,u_Id,29,LoadInteger(hash,u_Id,29)+sign*1)
    call SaveInteger(hash,u_Id,30,LoadInteger(hash,u_Id,30)+sign*1)
```

（`ram3` 永恆之夢也是 key 28 `+sign*1`，另外還有幾件。）

於是有三種壞法：

1. **帶「恰好 1 件」抵抗冰凍道具 → 閃耀護盾永遠不會啟動。**
   計數器剛好是 1，閘門 `!=1` 直接不成立。連帶猛衝的兩道力量波
   （條件是 key 29 == 1）也永遠不會出現。
2. **帶 2 件以上 → 護盾能開，但開完就把冰凍抗性抹掉。**
   `Hero43E2` 是 `SaveInteger(…,28,0)`，不是 `−1`。
3. **流血抗性一定會被抹掉。** 護盾結束時 `Hero43E` 無條件把 key 29 寫成 0。

（反過來說，帶 1 件同時 +1 到 28 與 29 的道具（例如超凡護符），
護盾就永久關閉、力量波永久開啟 —— 這是可以被玩家利用的組合。）

**建議修法**：護盾的兩個旗標換成沒被用過的 key（或用
`SaveInteger(hash,Id,'Amgl',…)` 這種字串 key），不要碰 28 / 29。

---

### A-27. 反攻的減傷被套用了兩次

**位置**：`Trig_HeroSkills43_Actions` 第 59745 行寫入、
`Trig_HeroTakeDamage_Actions` 第 19805 行再算一次

```jass
// 施放時，寫進實數 key 4
set dmg=0.12+0.08*I2R(GetUnitAbilityLevel(u,Skill))
call SaveReal(hash,GetHandleId(u),4,LoadReal(hash,GetHandleId(u),4)+dmg)
```

```jass
// 傷害管線裡，key 4 已經被減過一次：
//     set DefCof=DefCof-LoadReal(hash,GetHandleId(d),4)
// 然後又因為 buff B03I 再減同一條算式：
if GetUnitAbilityLevel(d,'B03I')==1 then
    set DefCof=DefCof-(0.12+0.08*I2R(GetUnitAbilityLevel(d,'Absk')))
endif
```

`B03I` 是反攻自己的 buff（全腳本只有這一處引用），所以屏障生效期間
同一個係數被扣兩次。滿級：`1.00 − 0.52 − 0.52 = −0.04`，
被第 19881 行的 `if DefCof<0.20 then DefCof=0.20` 夾住，
**實際是 80% 減傷，說明寫的是 52%**。

進出是對稱的（施放時 +、`Skill43W` 到期時 −），所以不會外洩到屏障之外。

**建議修法**：拿掉第 19805 行那段，或改成不要寫 key 4，二選一。

---

### A-28. 冠軍鎧甲的反擊讀的是攻擊者的力量，不是穿戴者的

**位置**：`Trig_ItemAttacksToHero_Actions` 第 28579 行

```jass
if LoadInteger(hash,Id,'clsd')>=1 then
    if LoadReal(hash,Id,'clsd')==0. then
        call StartModCooldown(Id,'clsd',4.)
        set dmg=I2R(GetHeroStr(a,true))*2.50*cof
        …
        call UnitDamageTarget(d,a,dmg,true,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_NORMAL,…)
```

`d` 是穿戴者（他打出這一下），`a` 是攻擊者。旁邊兩個同構的分支都用 `d`：

```jass
elseif 'axas' → set dmg=I2R(GetHeroAgi(d,true))*3.00*cof
elseif 'wcyc' → set dmg=I2R(GetHeroStr(d,true))*…
```

只有 `clsd` 寫成 `GetHeroStr(a,true)`。後果：

- 被**非英雄**攻擊時 `GetHeroStr` 回 0 → **這個「250% 力量」的反擊完全不造成傷害**。
  守線打雜兵的場合等於這件道具的主動效果不存在。
- 被敵方英雄攻擊時才有傷害，而且吃的是**對方的**力量。

道具說明寫的是穿戴者的力量。

**建議修法**：`GetHeroStr(a,true)` 改成 `GetHeroStr(d,true)`。

---

### A-29. 衝擊火箭的「英雄 25 級」被動完全沒有實作

**技能**：`A0XO` 衝擊火箭 / «Ударная ракета»（超重型坦克 `Nsjs`）

說明最後一段寫：

```
英雄 25 級：每 8 秒一次，攻擊會發射一枚火箭，造成 50% 傷害並暈眩 2 秒（英雄 1 秒）
```

超重型坦克在整份 `war3map.j` 只出現 5 次：圖示登記（8000）、選英雄（45355）、
`ResHero`（46352）、`DieHero`（46568）、擊殺條件（64638）。選英雄那裡註冊的事件只有一個：

```jass
elseif GetUnitTypeId(u)=='Nsjs' then
    set udg_RHcheck[55]=true
    call TriggerRegisterUnitEvent(gg_trg_HeroSkills55,u,EVENT_UNIT_SPELL_EFFECT)
    call SaveInteger(hash,GetHandleId(u),'TAL1',0)
    call SaveInteger(hash,GetHandleId(u),'TAL2',0)
    set unit_global=u
    call TriggerExecute(gg_trg_HeroTurretsActivate55)
```

**沒有任何攻擊事件（`EVENT_UNIT_ATTACKED` / `EVENT_PLAYER_UNIT_ATTACKED`）**，
火箭投射物 `'o00J'` 也只在 `A0XO` 自己的施法分支（64505）與另一隻英雄的技能（47531）
被創造。這段被動是純說明。

（順帶一提：上面那個 `'TAL2'` 初始化之後全腳本沒有任何 Nsjs 相關程式讀寫它，
`'TAL1'` 才是點燃層數。`TAL2` 對這隻是死初始化。）

---

### A-30. 三位一體套用的是改版前的舊套裝數值

**位置**：`ApplyTriad` 第 22994 行，對照 `ApplySetValor` 22579 /
`ApplySetAbyssal` 22363 / `ApplySetStorm` 22467

「三位一體」`I0AC` 同時算三個套裝的成員，所以它有自己一支處理器。
但那支處理器裡的門檻加成**沒有跟著套裝改版一起更新**：

| 套裝 | 正式處理器（＝ `-c1`～`-c3` 說明） | `ApplyTriad` 裡的版本 |
|---|---|---|
| доблесть 英勇 | 2 件 **+10 力量**、4 件 **+20 力量** | 2 件 +6 力量、4 件 +10 力量 +5 智力 |
| бездна 深淵 | 2 件 **+10 敏捷**、4 件 **+20 敏捷** | 2 件 +6 敏捷、4 件 +10 敏捷 +5 力量 |
| шторм 風暴 | 2 件 **+10 智力**、4 件 **+20 智力** | 2 件 +6 智力、4 件 +10 智力 +5 敏捷 |

（`ApplyTriad` 的那組數字正好是改版前的舊值。）

兩個後果：

1. **同一個門檻給多少，取決於是哪件道具跨過去的。** 用三位一體湊到第 2 件，
   拿到的是 +6；用一般套裝道具湊到第 2 件，拿到的是 +10。
2. **拆裝會永久掉屬性。** 加成的加與減是各自的處理器算的：
   用三位一體跨到 2 件（`ApplyTriad` 給 +6 力量），之後改拿掉一件英勇道具，
   跑的是 `ApplySetValor` 的 `sign=-1`，它會扣 **10 力量與 0.10 點燃傷害** ——
   扣掉你從來沒拿到的那 4 點力量與 10% 點燃。反覆穿脫可以一直往下扣。

**建議修法**：`ApplyTriad` 裡三段門檻的數值同步成各套裝正式處理器的版本，
或直接改成呼叫那三支函式。

---

### A-31. 狂戰士在額外攻擊力接近上限時會永久扣掉攻擊力

**位置**：`Trig_HeroSkills22_Actions` 第 53411 行（施放）與 `Hero22Q`（6 秒後收回）

```jass
set n2=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.05)
call SaveInteger(hash,Id,1,n2)
call SetUnitExtraDamage(u,GetUnitExtraDamage(u)+n2)
```

`SetUnitExtraDamage` 有 8191 的硬上限（魔獸的 `Iatt` 疊加上限），
但收回時是 `GetUnitExtraDamage(u) − n2` —— 讀的是**被夾過的**現值。
所以只要「現有額外攻擊力 + 最大生命×5%」超過 8191，
每施放一次狂戰士就會淨損一截攻擊力，而且不會自己還回來。

戰爭首領的最大生命是可以無限累加的（飽食的獸人，見 B-12），
所以這不是理論上的邊界 —— 長局遲早會踩到。

**建議修法**：把加上去的量記成「實際生效的差額」
（`SaveInteger(hash,Id,1, 新值 − 舊值)`），而不是預期值 `n2`。

---

## B. 說明與實作不符

### B-1. 人間煉獄：易燃的「延長時間」對瞬間點燃沒有作用

**天賦**：`A03J` 人間煉獄 / «Ад на земле»（拜火者）
**說明**：「點燃效果會立即造成全部傷害，而非逐漸燃燒。
**易燃所帶來的燃燒時間延長效果同樣會被計入。**」

實際上 `BurnUnit` 裡瞬燃與 DoT 是兩條完全分開的路徑：

```jass
// 第 1884-1885 行：瞬燃路徑，係數與目標身上有沒有易燃無關
if LoadInteger(hash,GetHandleId(damager),'A03M')==1 then
    set cof=1.0*(1.00+(0.50*(1.00+LoadReal(hash,GetHandleId(damager),46))))
    ...
    call UnitDamageTarget(damager,target,dmg*cof,...)
    return          // <-- 直接返回，下面的 count 完全沒跑到
endif

// 第 1934-1938 行：DoT 路徑，這裡才有易燃的跳數加成
set dmg=dmg/16.
set count=16
if GetUnitAbilityLevel(target,'B042')==1 then
    set count=count+R2I(8.0*(1.00+(1.00+LoadReal(hash,GetHandleId(damager),46))))
endif
```

瞬燃的係數固定是 `1 + 0.5×(1+key46)`，不管目標身上有沒有易燃都一樣。
易燃對人間煉獄剩下的作用只有第 1872 行那個 **機率 ×1.5**。

實際落差不小：DoT 版本吃到易燃是 16 跳變 32 跳（傷害整整翻倍），
瞬燃版本則完全吃不到。玩家照說明配裝會做出錯誤判斷。

**建議**：要嘛讓瞬燃路徑也乘上易燃的倍率，要嘛把那句說明拿掉。

---

### B-2. 零時迷子的觸發條件寫得太含糊

**道具**：`I086` 零時迷子
**說明**：「當英雄具備**對應的抗性或狀態**時，狀態對敵人造成的傷害提升 50%。」

程式檢查的是二元的「抵抗X」旗標（整數槽），不是百分比抗性（實數槽）。
兩者是不同的東西：

- 有效：淬鍊鎖甲 `I01A`、熾熱核心 `I06A`、生鏽頭盔 `rnsp`、超凡護符 `ram4`、
  惡魔鎧甲 `shhn`、太陽石 `sorf`、勇氣徽記 `gmfr`、寒冬帷幕 `wolg`
  （這些的屬性列都寫著「抵抗點燃」）
- **無效**：任何只給「+X% 點燃抗性」而沒有「抵抗點燃」的裝備

例如一個帶著 +75% 點燃抗性但沒有「抵抗點燃」旗標的英雄，零時迷子對他完全沒作用，
但照說明讀會以為有。建議把說明改成「當英雄具備對應的**抵抗**時」，
或直接列出是哪一種抗性。

---

### B-3. 律法之字的暴擊是攻擊暴擊，不是技能暴擊

**技能**：`A0OB` 律法之字 / «Буква закона»（白銀執法官）
**說明**：「白銀執法官的**技能**具有吸血效果，並有 20% 機率造成致命一擊（170% 傷害）。」

兩件事被寫在同一句話裡，但實作方式完全不同：

- **技能吸血**是寫死在每個技能裡的 `SetWidgetLife(u,GetWidgetLife(u)+dmg*0.10)`，
  確實是技能的效果，每打到一個目標算一次。
- **暴擊**是 `A0OD`，原型 `ACct`（原生暴擊），由引擎在**普通攻擊**時擲骰。
  整份 `war3map.j` 裡除了 `-i` 指令的顯示和擊殺時升級之外，
  沒有任何地方讀取它 —— 技能傷害不會暴擊。

現在的寫法很容易讀成「技能會暴擊」。建議拆成兩行。

---

### B-4. 均衡者之眼的目標條件與英雄建議屬性衝突

**技能**：`A0OF` 均衡者之眼（白銀執法官）
**位置**：`Hero49R`

```jass
local real life=GetWidgetLife(u)
...
if UnitAlive(u3)and IsUnitEnemy(u3,pl)and GetWidgetLife(u3)>life then
```

技能只打「當前生命值高於施法者」的敵人，而且每 0.5 秒重新讀一次自己的血量。
說明有寫這個條件，沒問題 —— 問題是白銀執法官的建議屬性裡放了「生命值」，
而堆生命值會**縮小**這個大絕的目標清單。兩者方向相反，建議調整其中一邊。

---

### B-5. 大法師之褲回復的是力量不是智力

**道具**：`odef` 大法師之褲 / «Штаны архимага»
**說明**：「使用技能為英雄回復（150% 智力）點法力」
（«восстанавливает герою (150% разум) ед. маны»）
**位置**：`Trig_UseSkillsEndcast_Actions` 第 28995 行

```jass
set dmg=I2R(GetHeroStr(u,true))*1.50*cof     // GetHeroStr，不是 GetHeroInt
```

這件是傳說級的法師裝（+15 智力、+150 技能強度），但回復量吃的是力量。
智力英雄 25 級時力量通常只有 40 多、智力 200 以上，等於回復量只剩約五分之一。

---

### B-6. 魔力潮汐的持續時間寫 0 秒，而且沒說有永久加成

**技能**：`A085` 魔力潮汐（樹人長者 H01F）
**位置**：`Trig_HeroSkills23_Actions` 第 53771-53789 行、`Hero23W`

```jass
set dmg=50.+50.*GetUnitAbilityLevel(u,Skill)      // 滿級 300
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]+dmg   // n = 目標的玩家編號
call TimerStart(t,20.,false,function Hero23W)     // 20 秒
…
set dmg=3.+2.*GetUnitAbilityLevel(u,Skill)        // 滿級 13
call SaveReal(hash,GetHandleId(t),2,dmg)

// Hero23W（20 秒後）
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]-r     // 扣掉全部
if r2 !=0. then
    set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]+r2   // 再加回永久的那份
endif
```

說明寫的是「Время действия: 0 сек.」（持續時間 0 秒），實際是 **20 秒**。
更重要的是說明沒提到那 `r2` —— 每次施放會**永久**留下 `3 + 2×等級` 點技能強度
（滿級 13），沒有上限、死亡不歸零、而且是加在**目標玩家**身上（可以餵給隊友）。
冷卻 25 秒的話等於每分鐘 +31 技能強度，一場長局累積下來比整套裝備還多。

天賦「自然的擁抱」`A0YQ` 會把 `r2` 設成 0，說明只寫「失去永久提升能力」，
沒說被放棄的是每分鐘 31 點的複利。

---

### B-7. 怪物獵人的說明有兩處數字錯誤，而且方向相反

**技能**：`A0HR` 怪物獵人（神射手 H00N）
**位置**：`Trig_HeroKills4_Actions` 第 47787/47789 行、第 44630 行

這隻的「狩獵等級」實際上完全由 `Trig_HeroKills4` 提供（靠**單位型號**判定，
不是靠有沒有這個技能；`A0HR` 的原型是 `Amgl` 空殼，物件欄位是空的）。

**① 底數與指數寫反了**

```jass
set exp=R2I(25.*Pow(1.5,I2R(GetHeroLevel(u2))))   // 英雄
set exp=R2I(10.*Pow(1.5,I2R(GetUnitLevel(u2))))   // 部隊
```

說明寫的是 `10 * (等級)^1.5`（多項式），程式碼是 `10 * 1.5^等級`（**指數**）。

| 敵人等級 | 說明 `10×L^1.5` | 實際 `10×1.5^L` | 倍數差 |
|---|---|---|---|
| 3 | 52 | 33 | 0.6× |
| 8 | 226 | 256 | 1.1× |
| 12 | 416 | 1,297 | 3.1× |
| 15 | 581 | 4,378 | 7.5× |
| 20 | 894 | 33,252 | 37× |
| 25 | 1,250 | 252,511 | 202× |

交叉點在敵人等級 8 —— 之前比說明慢，之後開始指數爆炸。

**② 初始門檻少了一位數**

說明寫「獲得狩獵點數所需的狩獵經驗：**25** (+75)」，程式碼是：

```jass
call SaveInteger(hash,GetHandleId(u),'EXP1',225)
```

**225**，不是 25。門檻本身是線性成長（每點 +75），但經驗是指數成長 ——
兩者一拉開就再也追不回來。

這兩處合起來就是玩家實際感受到的「前期升很慢、後期突然爆升」：
前期雜兵 3～5 級一隻只給 33～75 點而門檻是 225 起跳，要殺 3～7 隻才一點；
後期一隻 15 級的怪 4,378 點，那一擊的 `loop` 會一次連跳好幾點。

**建議**：把說明改成 `10 * 1.5^(等級)` 與 `225 (+75)`，或者把程式碼改成
說明寫的多項式版本 —— 兩者的遊戲體驗差很多，看作者原本想要哪一種。

---

### B-8. `-i` 指令看不到兩個皮膚的狩獵點數

**位置**：第 16714 行（`-i` 顯示）vs 第 47773 行（擊殺處理器）

兩處的型號清單不一致：

| | 型號 |
|---|---|
| 擊殺處理器（會累積） | `H00N` `H040` `Otch` `H03Q` `H041` **`H01I`** **`H02R`** `Ofar` |
| `-i` 顯示 | `H00N` `H040` `Otch` `H03Q` `H041` `Ofar` |

`H01I`（復生陸戰隊員）與 `H02R` 兩個皮膚**會正常累積狩獵點數，但玩家永遠查不到數字**。

---

### B-9. 選「霰彈槍」天賦會清空已累積的狩獵點數

**位置**：`Trig_HeroSkills4_Actions` 第 47489-47522 行

```jass
elseif Skill=='A0J3' then
    call FlushChildHashtable(hash,GetHandleId(u))    // 舊英雄的 hash 整個清掉
    …
    call RemoveUnit(u)
    set u2=CreateUnit(pl,'H03Q',x,y,270)             // 換成全新單位＝全新 handle
    …
    call SaveInteger(hash,GetHandleId(u2),'EXP1',225)
    call SaveInteger(hash,GetHandleId(u2),'EXP2',0)
    call SaveInteger(hash,GetHandleId(u2),'EXP3',0)
```

狩獵點數、狩獵經驗、以及靠 `SetUnitBaseDamage` 累積的那 +N 攻擊力**全部歸零**。
而轉職後的技能仍然照舊讀 `EXP3`（`A01A` +4×點數、`A018` 上限 +5×點數、
`A019` +1.25×點數、`A017` +5×點數），所以是實打實的損失。

另一個天賦「狙擊手 `A0HN`」不換單位，不會重置。

說明完全沒提這件事。因為狩獵經驗是指數成長，**這個重置在前期非常痛、在後期
幾乎無感** —— 但玩家沒有任何線索可以做這個判斷。

---

### B-10. 軍團特使的兩個皮膚都會改變數值，不只是換外觀

**位置**：`HeroR44` 第 59958 行、`Trig_HeroSkills44_Actions` 第 60134 行

齊射的下一發間隔是照皮膚分開寫的：

```jass
if GetUnitTypeId(u)=='Hkal' then
    call TimerStart(t,GetRandomReal(0.33,0.66),false,function HeroR44)
else
    call TimerStart(t,GetRandomReal(0.5,1.0),false,function HeroR44)
endif
```

- **「統兵官」`Hkal`** —— 間隔 0.33–0.66 秒（平均 0.495），
  本體是 0.5–1.0 秒（平均 0.75）。同一段時間內大約 **1.5 倍的齊射次數**。
  另外第 60134 行只有 `Hkal` 會額外召出 `h005` 的半透明幻影。
- **「幽靈劍士」`Huth`** —— 箭矢用的是 `'o002'` 而不是本體的 `'o000'`。
  兩者在 `ProjectileMove` 的命中處理完全不同：

  ```jass
  elseif u2_Id=='o002' then
      if GetUnitAbilityLevel(u,'A0A2')==1 then
          call BleedUnit(u,u3,dmg*1.50,1.00)
      endif
      call FrostUnit(u,u3,0.50)
  ```

  也就是每一箭都帶 **50% 機率冰凍**（`o000` 沒有），
  持有 `A0A2` 時再加 100% 機率、1.5 倍傷害的流血。

三個皮膚在選單上看起來是同一隻英雄的不同外觀，但實際強度差很多。
如果是刻意設計，建議在說明裡寫出來；如果不是，`Hkal` 的間隔與
`Huth` 的投射物應該跟本體一致。

---

### B-11. 側翼砲塔的點燃疊層說明寫 2%，程式碼是 5%

**技能**：`A0XR` 側翼砲塔 / «Боковые турели»（超重型坦克）

說明：

```
每達成 100 次擊殺，點燃傷害會額外乘上 2%。層數之間不會互相相乘。
```

程式碼（`Trig_HeroKills55_Actions` 第 64648 行）：

```jass
set count=LoadInteger(hash,u_Id,'TAL1')+1
if count==100 then
    set count=0
    call SaveReal(hash,u_Id,27,LoadReal(hash,u_Id,27)+0.05)
endif
```

實際是每 100 殺 **+0.05**（＝ +5%），差 2.5 倍，方向對玩家有利。
「不會互相相乘」那句是對的 —— key 27 的實數槽是加法，跟薩弗拉斯（+0.30）、
太陽之書（+0.20）那些道具共用同一格。

另外兩件說明沒寫的事，一併記在這裡：

- **沒有上限，而且死亡不歸零。** 條件只有「死者是敵人」，0 級雜兵也算；
  `'TAL1'` 只在選英雄那一刻初始化一次，`ResHero` 用同一個 unit handle 復活，
  不會清掉。
- **砲塔的擊殺算英雄的擊殺。** `Turret55`（64553）的
  `CreateProjectile(u,'o02C',…)` 裡 `u` 就是英雄本人，所以砲塔自動打死的雜兵
  也會累進 `'TAL1'`。這讓超重型坦克是唯一「站著不動也在疊層」的英雄。

---

### B-12. 飽食的獸人不會「完全回復生命值」

**技能**：`A099` 飽食的獸人 / «Сытый орк»（戰爭首領）

說明寫「讓英雄**完全回復生命值**並提升生命值上限」。程式碼（第 53398 行）：

```jass
set n2=GetPlayerState(pl,PLAYER_STATE_RESOURCE_FOOD_CAP)
set n2=n2+n2/2
call SetUnitState(u,UNIT_STATE_LIFE,(GetUnitState((u),UNIT_STATE_MAX_LIFE)))
call SetUnitLife(u,R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE)))+n2)
```

**先**補到當時的上限，**再**把上限加上 `n2`。所以施放完永遠是
「舊上限 / 新上限」，剛好差 `n2` 點，永遠不會滿血。

順帶記兩件說明沒寫的：

- 加的量是 `floor(1.5 × 目前人口上限)`，而不是固定值；開場人口上限 10
  只有 +15，人口上限 300 時才是 +450。
- 上限用的是 `ah00`（`AIl2` Item Life Bonus）那組永久加法，
  `ResHero` 復活的是同一個 unit handle，**死亡不歸零、也沒有上限**。

**建議修法**：兩行對調，或改成 `SetUnitState(LIFE, 新上限)`。

---

## C. 文字問題

### C-1. 自然的擁抱引用了不存在的技能名稱

**技能**：`A0YP` `A0YQ` 自然的擁抱（樹人長者 H01F）

說明裡寫的是 «Прилив сил»（力量潮汐），但地圖裡沒有這個名稱的技能 ——
樹人長者實際的技能叫 «Прилив магии»（魔力潮汐，`A085`）。

### C-1b. 「魔法護盾」的內部名稱有錯字

**技能**：`A0ES` 魔法護盾（獅鷲守護者 U011）

`aret` 寫的是 «Магический **щ**ит»（正確），但 `atp1` 五個等級全部寫成
«Магический **ш**ит» —— `щ` 打成 `ш`。兩個欄位都會顯示給玩家，
所以同一個技能在不同介面會出現兩種拼法。同樣的錯字也出現在 `A106`
（絕對屏障天賦換上的護盾複製品）。

### ~~C-2. 邪惡聖誕老人的技能說明寫著「聖騎士」~~（誤報，已撤回）

原本回報「`A02U` 正義之怒、`A030` 神聖職責、`A0M5` 光之解放 的說明裡出現
«паладин»，這隻是邪惡聖誕老人，應該是從聖騎士複製過來忘了改」。

**這是我這邊的錯誤，不是地圖的問題。** 這隻英雄 `H00Z` 的 `utip` 寫的就是
«Паладин»，它本來就是聖騎士。「邪惡聖誕老人」是它的基礎物件 `Hpb2` 的名字，
而 `Hpb2` 被地圖改成了蠻族戰士的皮膚 «Злой Санта» —— 我的工具誤把基礎物件的
名稱當成這隻英雄的名稱（跟 `N01K` 是同一類錯誤，見附註）。

圖鑑已改為「聖武士 / Paladin / Паладин」。技能說明沒有任何問題。

---

### C-3. 「向聖光祈願」的說明少了冷卻數字

**技能**：`A01W` 向聖光祈願 / «Обращение к Свету»（血騎士女族長）

說明文字（`arut`）最後一行是 `|cFFA085EDПерезарядка: |r` 後面沒有值。
這是全地圖 384 個技能裡唯一一個有冷卻標籤卻沒填數字的。

冷卻欄位本身是填好的：`A01W.acdn` 第 1 級 = **100.0**（法力 185、持續 22 秒），
所以只是文案漏字，數值沒問題。
（`acdn` 的第 3～7 級還留著 `30.0`，但 `alev = 1`，那幾列用不到。）

**建議修法**：說明補上「Перезарядка: 100 сек.」。

（原本這條編在 A-3「程式錯誤」，查證後確認程式沒問題，改列到文字類。）

---

## 附註：不是 bug 的幾件事

整理過程中一度以為是問題、後來確認沒問題的，一併記下來免得重複回報：

- **宇宙巨龍的技能說明出現「Аурелион»** —— 那是英雄本名 «Аурелион Сол»，是刻意的。
- **`Emoo` 沒有覆寫單位名稱** —— 沿用原版的月之女祭司，技能也全是月之女祭司的
  （群星隕落、艾露恩的庇佑），`utip` 寫的就是 «Жрица Луны»，前後一致。
  （這條原本被我記成 bug，是我自己在圖鑑裡把名字填錯了，已修正。）
- **建造據點的冷卻 300 秒 / 覺醒與折射的 240 秒** —— 對那類技能來說是合理的。
- **「200% / 150% / 125%」那些數字** —— 是傷害倍率不是機率，看錯了。
- **`A0Y5`（聖物守衛）不是死碼。** 有兩位查證者因為 grep 腳本找不到
  `UnitAddAbility(u,'A0Y5')` 而懷疑它拿不到。實際上它寫在物件資料裡：
  `war3map.w3u` 的 `Hapm.uabi = 'A0Y5,A03V,A0R6,AInv'` —— 皇家守衛一被創造
  就有。**只 grep war3map.j 判斷不了技能的來源，要一併看 w3u 的 uabi/uhab。**
- **黑暗主教「每累積 500 點傷害 +15 HP」不是全額回血。** 有人回報
  `SetUnitLife(a, 最大生命+15)` 是把血補滿，但 `SetUnitLife`（第 3969 行）
  第一行就是 `hp = hp − 最大生命`，它設的是**生命上限**（用 `ah00` 加減差額），
  所以傳「最大生命 + 15」就是上限 +15，跟說明一致。
  同一個函式在樹人長者的「自然的擁抱」也是這樣用的。
- **`A0ZS` 讀 `A0C3` 的等級不是 bug。** 長工變身成寒冰魔像後，
  `Skill=='A0ZS'` 分支裡的 `u` 是魔像、`u2` 是本體英雄
  （由 `A0C3` 分支的 `SaveUnitHandle(hash,GetHandleId(u2),1,u)` 存下來），
  所以 `GetUnitAbilityLevel(u2,'A0C3')` 讀的是本體身上的技能等級。
  全腳本沒有任何 `UnitRemoveAbility(u,'A0C3')`，本體不會失去它。
- **`A0QP` 天賦塞進來的兩個「Фундамент ур. 2」不會永久佔背包。**
  `I07Z` 的原型是 `pghe`（強效治療藥水），沒有覆寫 `iper` / `iusa`，
  所以是一次性消耗品，放完生產點就消失。開場前幾分鐘會少 2 格，
  配裝時要把這件事算進去，但它不是漏洞。
- **`StartModCooldown` 的參數不一定是道具 ID。** 我原本在配裝簡報裡寫
  「沒有任何裝備能縮短英雄技能的冷卻」，那句話太絕對：全地圖 78 個
  `StartModCooldown` 呼叫裡有 **7 個傳的是技能 ID** ——
  `ABY3`(28389)、`A0YX`(33610)、`A0ZG`(33625)、`A034`(48001)、
  `A09A`(53491)、`A09D`(53521)、`A08W`(60540)。
  這幾支的「內部冷卻」會被 key 1（裝備技能冷卻）乘上去，下限 0.20。
  魔獸原生的技能冷卻（`acdn`）仍然不受裝備影響，兩者要分開講。
- **「強化」護甲在這張地圖沒有減傷，是相對缺點。** 超重型坦克的
  `Nsjs.udty = fort`，而 `war3mapMisc.txt` 六張倍率表的第 5 欄（城牆／強化）
  **全部都是 1.00**。相對於英雄護甲（穿刺 0.60、魔法 0.60、攻城 0.50、
  法術 0.80），這隻對穿刺與魔法實吃 1.67 倍、對攻城 2 倍。
  卷宗把它列成特色是對的，但不要當成優點看。
- **優雅吊墜 `tfar` 的 +1/3 會每 0.5 秒重算，不是拿起來的快照。**
  有查證者說 `PercentStatsRefresh` 的 `tfar` 分支被 `A0Y5` 包住且沒有 `else`，
  因此建議「湊滿裝再重拿一次」。實際上那個 `else` 在第 21865 行：
  一般英雄走 `(主屬性 − 目前加成)/3`，聖物守衛（`A0Y5`）持有者走 `/3*2`。
  兩條路都是持續刷新的，裝備順序無所謂。
- **大法師權杖的「0.75 秒觸發」有實作。** 有查證者說找不到對應的
  JASS，實際上它不走 JASS：`fgrg.iabi = 'A0GW'`，`A0GW` 的原型是
  `Apxf`（法力斗篷），`acdn = 0.75`、`pxf1 = 125`、`aare = 700`，
  全部由魔獸原生技能處理。**物件資料裡的原生技能不會出現在 war3map.j 裡。**

---

*本清單由 UD 圖鑑（<https://peter-1119.github.io/DarkSheepSearch>）維護者整理，
資料來源為 UD_v3.81 地圖檔本身。*
