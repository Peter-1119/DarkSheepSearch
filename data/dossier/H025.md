# 懲戒者 `H025`（Каратель）

主屬性 **力量** · 背包 **6 格** · 解鎖 50000 · 定位 戰士/法師

| | 初始 | 每級 |
|---|---|---|
| 力量 | 23 | 3 |
| 敏捷 | 15 | 2 |
| 智力 | 20 | 2 |

> 近戰英雄，受到的狀態傷害較低，並具備多種手段。

**縮放**：吃技能強度的技能 ['A0DY', 'A0E0', 'A0E3'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **狀態** —— 走 `Burn_Dmg` 那條，**外面包了 DisableTrigger** → 不吃 DefCof、不帶穿透、被狀態抗性擋。該買的是「狀態傷害 +%」「易燃」「機率倍率」。
- **技能直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且傷害事件數越多，穿透越划算。
- **召喚物** —— 召喚物**不繼承**主人的裝備觸發／狀態／傷害 +%，只吃主人技能公式裡明寫的屬性（通常是最大生命與技能強度）與原生光環。
- **治療** —— 直接寫血量，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。
- **屬性增益** —— 直接改屬性。注意有些是**永久**的（死亡不歸零），長局會滾雪球。

細節見 `data/dossier/_engine.md`。


---

## 追蹤之火 `A0E3`　—　吃技能強度

俄文原名：Преследующие огни

```
產生 8 枚火焰彈，經過短暫準備後隨機擊中英雄周圍 500 範圍內的敵人並使其點燃。若火焰彈找不到敵人，則為英雄回復相當於其所造成傷害 80% 的生命值。火焰彈會優先攻擊處於「詛咒審判」效果下的敵人。

每枚火焰彈的傷害：30 + （15% 技能強度）點；100% 機率施加點燃，傷害為 100%

冷卻：18 秒。
```

每級變動：
  - 第 3 行：30 / 45 / 60 / 75 / 90

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = curse`, `acdn = 18.0`, `alev = 5`, `amcs = [80, 89, 98, 107, 116]`, `aran = 135.0`, `atar = air,ground,debris,enemy,neutral,organic`

呼叫共用引擎函式：`BurnUnit` —— 完整內容見 `_engine.md`。

實作：

`Trig_Ice_Blades_Conditions`　war3map.j:55802
```jass
function Trig_Ice_Blades_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0E3'
endfunction
```

`Trig_Ice_Blades_Move1`　war3map.j:55878
```jass
else
set gg2=CreateGroup()
call GroupEnumUnitsInRange(gg,x,y,500.,null)
loop
set GE=FirstOfGroup(gg)
exitwhen GE==null
if GetUnitAbilityLevel(GE,'B047')==1 and IsUnitEnemy(GE,GetOwningPlayer(GT))then
set fl=true
call GroupAddUnit(gg2,GE)
endif
call GroupRemoveUnit(gg,GE)
endloop
if fl==false then
call GroupEnumUnitsInRange(gg,x,y,500.,null)
loop
set GE=FirstOfGroup(gg)
exitwhen GE==null
if not IsUnitType(GE,UNIT_TYPE_STRUCTURE)and not IsUnitType(GE,UNIT_TYPE_DEAD)and IsPlayerEnemy(GetOwningPlayer(GE),GetOwningPlayer(GT))then
set fl=true
call GroupAddUnit(gg2,GE)
endif
call GroupRemoveUnit(gg,GE)
endloop
endif
loop
set GE=FirstOfGroup(g)
exitwhen GE==null
set t1=CreateTimer()
set h1=GetHandleId(t1)
call SaveUnitHandle(hash,h1,1,GT)
if fl then
call SaveUnitHandle(hash,h1,2,GroupPickRandomUnit(gg2))
else
call SaveUnitHandle(hash,h1,2,GT)
endif
call SaveUnitHandle(hash,h1,3,GE)
call SaveReal(hash,h1,4,15+15*GetUnitAbilityLevel(GT,'A0E3')+udg_ItemBonusDMG[n]*0.15)
call TimerStart(t1,0.01,true,function Trig_Ice_Blades_Move2)
call GroupRemoveUnit(g,GE)
endloop
call DestroyGroup(gg2)
call DestroyGroup(g)
call DestroyTimer(t)
call FlushChildHashtable(hash,h)
endif
```

`Trig_Ice_Blades_Actions`　war3map.j:55932
```jass
function Trig_Ice_Blades_Actions takes nothing returns nothing
local unit GT=GetTriggerUnit()
local timer t=CreateTimer()
local integer h=GetHandleId(t)
local group g=CreateGroup()
local integer i=1
local real x=GetUnitX(GT)
local real y=GetUnitY(GT)
local real ugol=GetUnitFacing(GT)
loop
exitwhen i>8
set bj_lastCreatedUnit=CreateUnit(GetOwningPlayer(GT),'h026',x,y,ugol+180)
call GroupAddUnit(g,bj_lastCreatedUnit)
set i=i+1
endloop
call SaveUnitHandle(hash,h,1,GT)
call SaveGroupHandle(hash,h,2,g)
call SaveInteger(hash,h,3,30)
call TimerStart(t,0.03,true,function Trig_Ice_Blades_Move1)
set t=null
set GT=null
set g=null
endfunction
```

`Trig_Ice_Blades_Conditions`　war3map.j:55802
```jass
function Trig_Ice_Blades_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0E3'
endfunction
function Trig_Ice_Blades_Move2 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer h=GetHandleId(t)
local unit GT=LoadUnitHandle(hash,h,1)
local unit Cel=LoadUnitHandle(hash,h,2)
local unit dammy=LoadUnitHandle(hash,h,3)
local real x=GetUnitX(dammy)
local real y=GetUnitY(dammy)
local real x1=GetUnitX(Cel)
local real y1=GetUnitY(Cel)
local real ugol=LSMagUgolMT(x,x1,y,y1)
if LSMagRastMT(x,x1,y,y1)>25 then
set x1=x+15*Cos(ugol*bj_DEGTORAD)
set y1=y+15*Sin(ugol*bj_DEGTORAD)
call SetUnitFacing(dammy,ugol)
call SetUnitX(dammy,x1)
call SetUnitY(dammy,y1)
else
if GT !=Cel then
call UnitDamageTarget(GT,Cel,LoadReal(hash,h,4),true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call BurnUnit(GT,Cel,LoadReal(hash,h,4),1.00)
else
call SetUnitState(GT,UNIT_STATE_LIFE,GetUnitState(GT,UNIT_STATE_LIFE)+LoadReal(hash,h,4)*0.80)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\HolyBolt\\HolyBoltSpecialArt.mdl",GetUnitX(Cel),GetUnitY(Cel)))
endif
call KillUnit(dammy)
call DestroyTimer(t)
call FlushChildHashtable(hash,h)
endif
set dammy=null
set t=null
set GT=null
set Cel=null
endfunction
```

## 詛咒審判 `A0E0`　—　吃技能強度

俄文原名：Анафема

```
造成傷害，並對目標與附近的敵人施加「詛咒審判」狀態。

傷害：70 + （30% 技能強度）點；目標附近的敵人受到 50% 傷害
目標周圍的作用範圍：100 點
「詛咒審判」狀態：100% 機率（目標附近的敵人為 50% 機率），+15% 受到的傷害，-15% 造成的傷害，+20% 受到的狀態傷害
「詛咒審判」持續時間：8 秒。

冷卻：8 秒。
```

每級變動：
  - 第 3 行：70 / 105 / 140 / 175 / 210
  - 第 4 行：100 / 150 / 200 / 250 / 300
  - 第 5 行：20 / 25 / 30 / 35 / 40

物件欄位（原型 `ANcl`）：`Ncl1 = 0.800000011920929`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.800000011920929`, `Ncl5 = 0`, `Ncl6 = cloudoffog`, `acdn = 8.0`, `alev = 5`, `amcs = [60, 67, 74, 81, 88]`, `atar = air,ground,enemy,organic,neutral`

呼叫共用引擎函式：`AnathemaUnit`, `VulnerabilityUnit` —— 完整內容見 `_engine.md`。

實作：

`Trig_HeroSkills30_Actions`　war3map.j:55722
```jass
if Skill=='A0E0' or Skill=='A0CP' then
set u2=GetSpellTargetUnit()
set x=GetUnitX(u2)
set y=GetUnitY(u2)
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Holy Light.mdx",u2,"origin"))
set dmg=35.+35.*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.30
set r=0.15+0.05*I2R(GetUnitAbilityLevel(u,Skill))
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if GetUnitAbilityLevel(u,'A105')==1 then
call VulnerabilityUnit(u,u2,1.00)
call AnathemaUnit(u,u2,r,1.00)
set x=GetUnitX(u2)
set y=GetUnitY(u2)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,50.+I2R(GetUnitAbilityLevel(u,Skill))*50.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)and not IsUnitType(u3,UNIT_TYPE_STRUCTURE)and u2 !=u3 then
call UnitDamageTarget(u,u3,dmg*0.50,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call VulnerabilityUnit(u,u3,0.50)
call AnathemaUnit(u,u3,r,0.50)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
else
call AnathemaUnit(u,u2,r,1.00)
set x=GetUnitX(u2)
set y=GetUnitY(u2)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,50.+I2R(GetUnitAbilityLevel(u,Skill))*50.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)and not IsUnitType(u3,UNIT_TYPE_STRUCTURE)and u2 !=u3 then
call UnitDamageTarget(u,u3,dmg*0.50,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call AnathemaUnit(u,u3,r,0.50)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
```

## 榮耀光輝 `A0DZ`

俄文原名：Сияние славы

```
在自身周圍生成一個大型力場，場內的英雄與友軍攻擊造成的傷害提高，並獲得狀態傷害加成。

攻擊力提升：+50%
狀態傷害加成：+50%

冷卻：80 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = wispharvest`, `acap = `, `acdn = 80.0`, `alev = 1`, `amcs = 135`, `aran = 0.0`

實作：

`Trig_HeroSkills30_Actions`　war3map.j:55776
```jass
elseif Skill=='A0DZ' then
set x=GetUnitX(u)
set y=GetUnitY(u)
set u3=CreateUnit(pl,'o01I',x,y,270)
call SetUnitVertexColor(u3,255,255,255,125)
call UnitApplyTimedLife(u3,'BTLF',19.)
endif
```

## 淨化 `A0DY`　—　吃技能強度

俄文原名：Очищение

```
以光之洪流轟擊指定區域，持續 6 秒。技能作用期間，作用範圍會逐漸擴大。

傷害：20 + （20% 技能強度）點/秒。
對處於「詛咒審判」效果下敵人的效果：技能每次跳動有 25% 機率對敵人施加點燃，傷害為 200%

冷卻：10 秒。
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `aare = 160.0`, `acdn = 10.0`, `alev = 5`, `amcs = [80, 90, 100, 110, 120]`, `aran = 550.0`

呼叫共用引擎函式：`BurnUnit` —— 完整內容見 `_engine.md`。

實作：

`HeroW30`　war3map.j:55617
```jass
function HeroW30 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real x=LoadReal(hash,Id,1)
local real y=LoadReal(hash,Id,2)
local integer count=LoadInteger(hash,Id,3)
local real aoe=LoadReal(hash,Id,3)
local real dmg
local real x2
local real y2
local unit u3
local integer L
local integer i=0
local group ug=CreateGroup()
if aoe>290. then
set aoe=290.
endif
set dmg=(10+10*I2R(GetUnitAbilityLevel(u,'A0DY'))+udg_ItemBonusDMG[n]*0.20)*0.25
call GroupEnumUnitsInRange(ug,x,y,aoe+10.,null)
if GetUnitAbilityLevel(u,'A105')==1 then
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if GetUnitAbilityLevel(u3,'B047')==1 then
call BurnUnit(u,u3,dmg*2.0,0.25)
endif
if IsUnitType(u3,UNIT_TYPE_HERO)then
set i=1
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
else
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if GetUnitAbilityLevel(u3,'B047')==1 then
call BurnUnit(u,u3,dmg*2.0,0.25)
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
endif
set L=1
loop
exitwhen L>3
set x2=PolarX(x,aoe,I2R(L)*120+I2R(count)*20)
set y2=PolarY(y,aoe,I2R(L)*120+I2R(count)*20)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\HolyBolt\\HolyBoltSpecialArt.mdl",x2,y2))
set L=L+1
endloop
call DestroyGroup(ug)
set count=count-1+i
if count<1 then
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
call SaveInteger(hash,Id,3,count)
call SaveReal(hash,Id,3,aoe+6.5)
endif
set ug=null
set t=null
set u=null
set u3=null
set pl=null
endfunction
```

`Trig_HeroSkills30_Actions`　war3map.j:55765
```jass
elseif Skill=='A0DY' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,x)
call SaveReal(hash,Id,2,y)
call SaveInteger(hash,Id,3,24)
call SaveReal(hash,Id,3,160.)
call TimerStart(t,0.25,true,function HeroW30)
```

## 選擇天賦 `A103`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的強度等級。
```

**天賦選項**：
  - `A102` 光之護盾
    強度等級：T3 屬性加成／每級屬性成長加成： +4 / +2 +0 / +0 +2 / +0  使用後英雄獲得一面護盾，降低受到的傷害，並在每次受到傷害時回復生命值（造成傷害的狀態除外）。  受到傷害降低：20% 每段傷害回復生命值：4 點 持續時間：12 秒  冷卻：30 秒
  - `A104` 拷問官
    強度等級：T3+ 屬性加成／每級屬性成長加成： +5 / +2 +2 / +0 +5 / +2  擊殺處於「詛咒審判」效果下的敵人，會依（敵人等級 * 0.5）的公式提高英雄的技能強度。  「詛咒審判」會額外以與「詛咒審判」狀態相同的機率對敵人施加易傷。  只要作用範圍內存在敵方英雄，「淨化」就不會消耗自身的持續時間。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A102,A104`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills30_Actions`　war3map.j:55705
```jass
if Skill=='A102' then
call SetHeroStr(u,GetHeroStr(u,false)+4,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A103')
call UnitAddAbility(u,'A0DX')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A104' then
call SetHeroStr(u,GetHeroStr(u,false)+5,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+5,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call SaveInteger(hash,GetHandleId(u),'aINT',2)
call UnitRemoveAbility(u,'A103')
call UnitAddAbility(u,'A105')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 純血 `A064`

俄文原名：Чистокровность

```
造成傷害的狀態在英雄身上的持續時間減少 50%。
```

實作：

`BurnUnit`　war3map.j:1939
```jass
if GetUnitAbilityLevel(target,'A064')==1 then
set count=count/2
endif
```

`BleedUnit`　war3map.j:2146
```jass
if GetUnitAbilityLevel(target,'A064')==1 then
set count=8
```

`DiseaseUnit`　war3map.j:2335
```jass
if GetUnitAbilityLevel(target,'A064')==1 then
set count=8
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 光之護盾 `A102`　—　來自天賦「選擇天賦」

俄文原名：Щит света

```
強度等級：T3
屬性加成／每級屬性成長加成：
+4 / +2
+0 / +0
+2 / +0

使用後英雄獲得一面護盾，降低受到的傷害，並在每次受到傷害時回復生命值（造成傷害的狀態除外）。

受到傷害降低：20%
每段傷害回復生命值：4 點
持續時間：12 秒

冷卻：30 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['faeriefireon', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills30_Actions`　war3map.j:55705
```jass
if Skill=='A102' then
call SetHeroStr(u,GetHeroStr(u,false)+4,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A103')
call UnitAddAbility(u,'A0DX')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 拷問官 `A104`　—　來自天賦「選擇天賦」

俄文原名：Дознаватель

```
強度等級：T3+
屬性加成／每級屬性成長加成：
+5 / +2
+2 / +0
+5 / +2

擊殺處於「詛咒審判」效果下的敵人，會依（敵人等級 * 0.5）的公式提高英雄的技能強度。

「詛咒審判」會額外以與「詛咒審判」狀態相同的機率對敵人施加易傷。

只要作用範圍內存在敵方英雄，「淨化」就不會消耗自身的持續時間。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acolyteharvest', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 32`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills30_Actions`　war3map.j:55712
```jass
elseif Skill=='A104' then
call SetHeroStr(u,GetHeroStr(u,false)+5,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+5,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call SaveInteger(hash,GetHandleId(u),'aINT',2)
call UnitRemoveAbility(u,'A103')
call UnitAddAbility(u,'A105')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 詛咒審判 `A0CP`　—　來自皮膚「赤紅軍團指揮官」

俄文原名：Анафема

```
造成傷害，並對目標與附近的敵人施加「詛咒審判」狀態。

傷害：70 +（30% 技能強度）點；目標附近的敵人受到 50% 傷害
目標周圍的作用範圍：100 點
「詛咒審判」狀態：100% 機率（目標附近的敵人為 50% 機率），+15% 受到的傷害，-15% 造成的傷害，+20% 受到的狀態傷害
「詛咒審判」持續時間：8 秒。

冷卻：8 秒。
```

每級變動：
  - 第 3 行：70 / 105 / 140 / 175 / 210
  - 第 4 行：100 / 150 / 200 / 250 / 300
  - 第 5 行：20 / 25 / 30 / 35 / 40

物件欄位（原型 `ANcl`）：`Ncl1 = 0.800000011920929`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.800000011920929`, `Ncl5 = 0`, `Ncl6 = cloudoffog`, `acdn = 10.0`, `alev = 5`, `atar = air,ground,enemy,organic,neutral`

呼叫共用引擎函式：`AnathemaUnit`, `VulnerabilityUnit` —— 完整內容見 `_engine.md`。

實作：

`Trig_HeroSkills30_Actions`　war3map.j:55722
```jass
if Skill=='A0E0' or Skill=='A0CP' then
set u2=GetSpellTargetUnit()
set x=GetUnitX(u2)
set y=GetUnitY(u2)
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Holy Light.mdx",u2,"origin"))
set dmg=35.+35.*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.30
set r=0.15+0.05*I2R(GetUnitAbilityLevel(u,Skill))
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if GetUnitAbilityLevel(u,'A105')==1 then
call VulnerabilityUnit(u,u2,1.00)
call AnathemaUnit(u,u2,r,1.00)
set x=GetUnitX(u2)
set y=GetUnitY(u2)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,50.+I2R(GetUnitAbilityLevel(u,Skill))*50.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)and not IsUnitType(u3,UNIT_TYPE_STRUCTURE)and u2 !=u3 then
call UnitDamageTarget(u,u3,dmg*0.50,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call VulnerabilityUnit(u,u3,0.50)
call AnathemaUnit(u,u3,r,0.50)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
else
call AnathemaUnit(u,u2,r,1.00)
set x=GetUnitX(u2)
set y=GetUnitY(u2)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,50.+I2R(GetUnitAbilityLevel(u,Skill))*50.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)and not IsUnitType(u3,UNIT_TYPE_STRUCTURE)and u2 !=u3 then
call UnitDamageTarget(u,u3,dmg*0.50,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call AnathemaUnit(u,u3,r,0.50)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
```

---

## 這隻召喚／製造的單位

（技能程式碼裡的 `CreateUnit` 目標。數值取自 war3map.w3u，
沒列出的欄位代表地圖沒覆寫、沿用原型。）

### `o01I` DummyElfPaladinR（原型 `ocat`）
  - 生命 1 ／ 骰面 1 ／ 攻擊範圍 1200 ／ 技能 A0E2,Aloc
  - 技能 `A0E2` (сияние славы)　`Cac1 = 0.5`, `aare = 600.0`, `abuf = B01H`

---

## 皮膚

### 赤紅軍團指揮官 `Harf`（預設未開放） —— **會換技能**
  - 詛咒審判 `A0E0` → **詛咒審判** `A0CP`

---

## 同一組的其他實作函式

英雄的實作散在同編號的一組函式裡，上面按技能抽取時抓不到的補在這裡
（常見的是決定門檻、結算加成、清理 buff 的那幾支）。

`Trig_HeroKills30_Conditions`　war3map.j:55960
```jass
function Trig_HeroKills30_Conditions takes nothing returns boolean
return GetUnitTypeId(GetKillingUnit())=='H025' or GetUnitTypeId(GetKillingUnit())=='Harf'
endfunction
```

`Trig_HeroKills30_Actions`　war3map.j:55963
```jass
function Trig_HeroKills30_Actions takes nothing returns nothing
local unit u=GetKillingUnit()
local unit u2=GetDyingUnit()
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local integer lvl
if GetUnitAbilityLevel(u,'A105')==1 and IsUnitEnemy(u2,pl)and GetUnitAbilityLevel(u2,'S01C')==1 then
if IsUnitType(u2,UNIT_TYPE_HERO)then
set lvl=GetHeroLevel(u2)
else
set lvl=GetUnitLevel(u2)
endif
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]+0.50*I2R(lvl)
endif
set u=null
set u2=null
set pl=null
endfunction
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof
  - **4** — 受到傷害 −%〔受害者〕DefCof 減去它 → 值越大越耐打；電擊會扣它

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
