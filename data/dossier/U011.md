# 獅鷲守護者 `U011`（Грифон Хранитель）

主屬性 **智力** · 背包 **6 格** · 解鎖 50000 · 定位 法師/戰士

| | 初始 | 每級 |
|---|---|---|
| 力量 | 21 | 2 |
| 敏捷 | （未覆寫） | 2 |
| 智力 | 26 | 4 |

> 近戰法師，能用自己的法力吸收傷害。

**縮放**：吃技能強度的技能 ['A0EX', 'A0EZ'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **狀態** —— 走 `Burn_Dmg` 那條，**外面包了 DisableTrigger** → 不吃 DefCof、不帶穿透、被狀態抗性擋。該買的是「狀態傷害 +%」「易燃」「機率倍率」。
- **技能直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且傷害事件數越多，穿透越划算。
- **召喚物** —— 召喚物**不繼承**主人的裝備觸發／狀態／傷害 +%，只吃主人技能公式裡明寫的屬性（通常是最大生命與技能強度）與原生光環。
- **屬性增益** —— 直接改屬性。注意有些是**永久**的（死亡不歸零），長局會滾雪球。

細節見 `data/dossier/_engine.md`。


---

## 風暴法球 `A0EX`　—　吃技能強度

俄文原名：Штормовая сфера

```
英雄朝指定方向發射一顆逐漸加速的風暴法球，對敵人造成傷害。

傷害：每次觸發 16 +（8% 技能強度）點
觸發間隔：0.21 秒
法球飛行時間：5 秒

冷卻：16 秒
```

每級變動：
  - 第 3 行：16 / 24 / 32 / 40 / 48

物件欄位（原型 `ANcl`）：`Ncl1 = 0.800000011920929`, `Ncl2 = 2`, `Ncl3 = 1`, `Ncl4 = 0.800000011920929`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acdn = 16.0`, `alev = 5`, `amcs = [90, 100, 110, 120, 130]`, `aran = 800.0`

實作：

`Trig_HeroSkills31_Actions`　war3map.j:56177
```jass
if Skill=='A0EX' then
set x=GetUnitX(u)
set y=GetUnitY(u)
set x2=GetSpellTargetX()
set y2=GetSpellTargetY()
set dmg=8+8*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.08
set degrees=AngleXY(x,y,x2,y2)
set u3=CreateUnit(pl,'o01J',x,y,degrees)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u3)
call SaveReal(hash,Id,1,dmg)
call SaveInteger(hash,Id,2,0)
call SaveInteger(hash,Id,3,168)
call SaveReal(hash,Id,4,degrees)
call SaveReal(hash,GetHandleId(u3),1,12.)
call SaveReal(hash,GetHandleId(u3),2,0.15)
call TimerStart(t,0.03,true,function HeroQ31)
call SaveUnitHandle(hash,GetHandleId(pl),1,u3)
```

`RemoveDummy`　war3map.j:2746
```jass
function RemoveDummy takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call RemoveUnit(u)
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set u=null
set t=null
endfunction
function StunUnit takes unit attacker,unit target,integer seconds,real chanse returns nothing
local integer random
local integer chanse_random
local unit u
local player pl=GetOwningPlayer(attacker)
local timer t=CreateTimer()
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random and IsUnitEnemy(target,pl)and UnitAlive(target)then
set u=CreateUnit(pl,'o010',GetUnitX(target),GetUnitY(target),0.)
call UnitAddAbility(u,'A0Y8')
call SetUnitAbilityLevel(u,'A0Y8',seconds)
call IssueTargetOrder(u,"creepthunderbolt",target)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
call TimerStart(t,2.,false,function RemoveDummy)
endif
set u=null
set pl=null
set t=null
endfunction
```

`HeroQ31`　war3map.j:55999
```jass
function HeroQ31 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u
local unit u2=LoadUnitHandle(hash,Id,2)
local unit u3
local real x=GetUnitX(u2)
local real y=GetUnitY(u2)
local real x2
local real y2
local player pl
local integer n
local real dmg=LoadReal(hash,Id,1)
local real degrees=LoadReal(hash,Id,4)
local integer check=LoadInteger(hash,Id,2)
local integer count=LoadInteger(hash,Id,3)
local real speed=LoadReal(hash,GetHandleId(u2),1)
local real plus_speed=LoadReal(hash,GetHandleId(u2),2)
local group ug
local real r
if plus_speed==0.25 then
set u=LoadUnitHandle(hash,Id,1)
set x2=GetUnitX(u)
set y2=GetUnitY(u)
set degrees=AngleXY(x,y,x2,y2)
set count=10
endif
call SetUnitX(u2,PolarX(x,speed,degrees))
call SetUnitY(u2,PolarY(y,speed,degrees))
call SaveReal(hash,GetHandleId(u2),1,speed+plus_speed)
set check=check+1
set count=count-1
if check==7 then
set check=0
set u=LoadUnitHandle(hash,Id,1)
set pl=GetOwningPlayer(u)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,210,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,null)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
call SaveInteger(hash,GetHandleId(t),2,check)
if plus_speed==0.25 then
if DistanceNative(x,y,x2,y2)<=50 then
set u=LoadUnitHandle(hash,Id,1)
set pl=GetOwningPlayer(u)
set n=GetPlayerId(pl)+1
set dmg=I2R(GetHeroInt(u,true))*1.25+udg_ItemBonusDMG[n]*0.50
if GetUnitAbilityLevel(u,'BNms')==1 then
if GetUnitAbilityLevel(u,'A0ES')==1 then
set dmg=dmg*(1.00+0.10*I2R(GetUnitAbilityLevel(u,'A0ES')))
else
set dmg=dmg*(1.00+0.10*I2R(GetUnitAbilityLevel(u,'A106')))
endif
endif
set x2=GetUnitX(u)
set y2=GetUnitY(u)
if GetUnitAbilityLevel(u,'A10B')==1 then
set dmg=dmg*0.75
set count=1
loop
exitwhen count>5
set x=PolarX(x2,300.,I2R(72*count))
set y=PolarY(y2,300.,I2R(72*count))
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
set count=count+1
endloop
set count=1
loop
exitwhen count>10
set x=PolarX(x2,600.,I2R(36*count))
set y=PolarY(y2,600.,I2R(36*count))
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
set count=count+1
endloop
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x2,y2))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,700.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if IsUnitType(u3,UNIT_TYPE_HERO)then
call StunUnit(u,u3,2,1.00)
else
call StunUnit(u,u3,3,1.00)
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
else
set count=1
loop
exitwhen count>5
set x=PolarX(x2,300.,I2R(72*count))
set y=PolarY(y2,300.,I2R(72*count))
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
set count=count+1
endloop
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x2,y2))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,400.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
call FlushChildHashtable(hash,GetHandleId(u2))
call SaveUnitHandle(hash,GetHandleId(pl),1,null)
call RemoveUnit(u2)
endif
else
if count==0 then
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
call FlushChildHashtable(hash,GetHandleId(u2))
call SaveUnitHandle(hash,GetHandleId(pl),1,null)
call RemoveUnit(u2)
else
call SaveInteger(hash,GetHandleId(t),3,count)
endif
endif
set t=null
set u=null
set u2=null
set u3=null
set pl=null
set ug=null
endfunction
```

## 壓制 `A0F1`

俄文原名：Подавление

```
英雄獲得一個暫時性光環，降低附近敵人的攻擊速度與移動速度，並燒灼其法力值。光環持續 10 秒。

移動速度降低：30%
攻擊速度降低：20%
法力燒灼：12 點／秒

冷卻：25 秒
```

每級變動：
  - 第 3 行：30 / 40 / 50 / 60 / 70
  - 第 4 行：20 / 28 / 36 / 44 / 52
  - 第 5 行：12 / 18 / 24 / 30 / 36

物件欄位（原型 `Absk`）：`abuf = B01L`, `acdn = 25.0`, `adur = [None, 12.0]`, `ahdu = [None, 12.0]`, `aher = 1`, `alev = 5`, `amcs = [80, 90, 100, 110, 120]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Trig_HeroSkills31_Actions`　war3map.j:56202
```jass
elseif Skill=='A0F1' then
call UnitAddAbility(u,'S005')
call UnitAddAbility(u,'A0F2')
call SetUnitAbilityLevel(u,'S005',GetUnitAbilityLevel(u,Skill))
call SetUnitAbilityLevel(u,'A0F2',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,10,false,function HeroW31)
endif
```

`HeroW31`　war3map.j:55987
```jass
function HeroW31 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer i=GetHandleId(t)
local unit u=LoadUnitHandle(hash,i,1)
call UnitRemoveAbility(u,'S005')
call UnitRemoveAbility(u,'A0F2')
call FlushChildHashtable(hash,i)
call PauseTimer(t)
call DestroyTimer(t)
set u=null
set t=null
endfunction
```

## 魔法護盾 `A0ES`

俄文原名：Магический щит

```
英雄以魔法護盾保護自己，部分吸收受到的傷害。

每點法力值可抵擋傷害：3 點
傷害吸收：50%
```

每級變動：
  - 第 3 行：2 / 3 / 4 / 5 / 6

物件欄位（原型 `ACmf`）：`Nms1 = [None, 3.0, 4.0, 5.0, 6.0]`, `Nms2 = 0.5`, `abuf = [None, 'BNms']`, `acdn = 5.0`, `aher = 1`, `alev = 5`, `amcs = 0`, `aran = [None, 128.0]`, `atar = [None, 'ground,friend,vuln,invu']`, `aubx = 2`, `auhk = E`, `aut1 = Развеять Магический щит (|cffffcc00E|r)`

實作：

`HeroQ31`　war3map.j:56055
```jass
if GetUnitAbilityLevel(u,'A0ES')==1 then
set dmg=dmg*(1.00+0.10*I2R(GetUnitAbilityLevel(u,'A0ES')))
```

## 球狀閃電 `A0EW`

俄文原名：Шаровые молнии

```
球狀閃電開始在英雄身邊接連出現，攻擊英雄周圍的區域。技能持續期間，英雄可自由執行其他命令。

球狀閃電數量：28 顆
球狀閃電傷害：75 + (8% max MP)

冷卻：70 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [1.0, None]`, `Ncl2 = [None, 1]`, `Ncl3 = [1, None]`, `Ncl4 = [1.0, None]`, `Ncl5 = [0, None]`, `Ncl6 = ['chemicalrage', None, 'channel']`, `aare = 280.0`, `acdn = [70.0, None, 17.0]`, `alev = 1`, `amcs = [225, None, 80, 90, 100, 110, 120]`, `aran = [800.0, None, 700.0]`, `atar = ['air,ground,friend,neutral,self', None]`

呼叫共用引擎函式：`ShockUnit` —— 完整內容見 `_engine.md`。

實作：

`Trig_ice_attack_Conditions`　war3map.j:56221
```jass
function Trig_ice_attack_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0EW'
endfunction
```

`Trig_ice_attack_Actions`　war3map.j:56360
```jass
function Trig_ice_attack_Actions takes nothing returns nothing
local unit GT=GetTriggerUnit()
local timer t=CreateTimer()
local integer h=GetHandleId(t)
call SaveUnitHandle(hash,h,1,GT)
if GetUnitAbilityLevel(GT,'A10B')==1 then
call SaveInteger(hash,h,4,1)
else
call SaveInteger(hash,h,4,28)
endif
call SaveInteger(hash,h,5,1)
call TimerStart(t,0.25,true,function Trig_ice_attack_Create)
set t=null
set GT=null
endfunction
```

`Trig_ice_attack_Conditions`　war3map.j:56221
```jass
function Trig_ice_attack_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0EW'
endfunction
function Trig_ice_attack_Move2 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer h=GetHandleId(t)
local unit GT=LoadUnitHandle(hash,h,1)
local real x1=LoadReal(hash,h,2)
local real y1=LoadReal(hash,h,3)
local unit dammy=LoadUnitHandle(hash,h,4)
local real x=GetUnitX(dammy)
local real y=GetUnitY(dammy)
local real ugol=LSMagUgolMT(x,x1,y,y1)
local group g
local unit GE
local real uron
if LSMagRastMT(x,x1,y,y1)>25 then
set x=x+15*Cos(ugol*bj_DEGTORAD)
set y=y+15*Sin(ugol*bj_DEGTORAD)
call SetUnitFacing(dammy,ugol)
call SetUnitX(dammy,x)
call SetUnitY(dammy,y)
else
set g=CreateGroup()
call GroupEnumUnitsInRange(g,x1,y1,280,null)
set uron=75+(GetUnitState((GT),UNIT_STATE_MAX_MANA))*0.08
if GetUnitAbilityLevel(GT,'A10B')==1 then
loop
set GE=FirstOfGroup(g)
exitwhen GE==null
if not IsUnitType(GE,UNIT_TYPE_DEAD)and IsPlayerEnemy(GetOwningPlayer(GE),GetOwningPlayer(GT))then
call UnitDamageTarget(GT,GE,uron,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call ShockUnit(GT,GE,0.25)
endif
call GroupRemoveUnit(g,GE)
endloop
else
loop
set GE=FirstOfGroup(g)
exitwhen GE==null
if not IsUnitType(GE,UNIT_TYPE_DEAD)and IsPlayerEnemy(GetOwningPlayer(GE),GetOwningPlayer(GT))then
call UnitDamageTarget(GT,GE,uron,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(g,GE)
endloop
endif
call DestroyGroup(g)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x1,y1))
call RemoveUnit(dammy)
call DestroyTimer(t)
call FlushChildHashtable(hash,h)
endif
set GE=null
set g=null
set dammy=null
set t=null
set GT=null
endfunction
function Trig_ice_attack_Move1 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer h=GetHandleId(t)
local unit GT=LoadUnitHandle(hash,h,1)
local unit dammy=LoadUnitHandle(hash,h,4)
local integer j=LoadInteger(hash,h,5)
local integer kol=LoadInteger(hash,h,6)
local real x1=GetUnitX(GT)
local real y1=GetUnitY(GT)
local timer t1
local integer h1
local real ugol
local real ugolGT=GetUnitFacing(GT)
if kol>0 then
set ugol=GetUnitFacing(dammy)
set x1=x1+80*Cos((ugolGT-j*90)*bj_DEGTORAD)
set y1=y1+80*Sin((ugolGT-j*90)*bj_DEGTORAD)
set x1=x1+80*Cos((ugol+j*6-j*90)*bj_DEGTORAD)
set y1=y1+80*Sin((ugol+j*6-j*90)*bj_DEGTORAD)
call SetUnitX(dammy,x1)
call SetUnitY(dammy,y1)
call SetUnitFacing(dammy,ugol+j*6)
call SaveInteger(hash,h,6,kol-1)
else
set t1=CreateTimer()
set h1=GetHandleId(t1)
call SaveUnitHandle(hash,h1,1,GT)
call SaveReal(hash,h1,2,LoadReal(hash,h,2))
call SaveReal(hash,h1,3,LoadReal(hash,h,3))
call SaveUnitHandle(hash,h1,4,dammy)
call TimerStart(t1,0.01,true,function Trig_ice_attack_Move2)
call DestroyTimer(t)
call FlushChildHashtable(hash,h)
endif
set t=null
set dammy=null
set GT=null
set t1=null
endfunction
function Trig_ice_attack_Create takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer h=GetHandleId(t)
local unit GT=LoadUnitHandle(hash,h,1)
local integer kol=LoadInteger(hash,h,4)
local timer t1
local integer h1
local real x=GetUnitX(GT)
local real y=GetUnitY(GT)
local real dist=GetRandomReal(120,500)
local real angle=GetRandomReal(0,360)
local real x2=PolarX(x,dist,angle)
local real y2=PolarY(y,dist,angle)
if kol>0 then
set bj_lastCreatedUnit=CreateUnit(GetOwningPlayer(GT),'h02A',x,y,angle)
set t1=CreateTimer()
set h1=GetHandleId(t1)
call SaveUnitHandle(hash,h1,1,GT)
call SaveReal(hash,h1,2,x2)
call SaveReal(hash,h1,3,y2)
call SaveUnitHandle(hash,h1,4,bj_lastCreatedUnit)
call SaveInteger(hash,h1,5,LoadInteger(hash,h,5))
call SaveInteger(hash,h1,6,30)
call TimerStart(t1,0.03,true,function Trig_ice_attack_Move1)
call SaveInteger(hash,h,5,-1*LoadInteger(hash,h,5))
if GetUnitAbilityLevel(GT,'A10B')==1 then
if UnitManaPercent(GT)>25.0 then
call SetUnitState(GT,UNIT_STATE_MANA,GetUnitState(GT,UNIT_STATE_MANA)-25.)
else
call SaveInteger(hash,h,4,kol-1)
endif
else
call SaveInteger(hash,h,4,kol-1)
endif
else
call DestroyTimer(t)
call FlushChildHashtable(hash,h)
endif
set t1=null
set t=null
set GT=null
endfunction
function Trig_ice_attack_Actions takes nothing returns nothing
local unit GT=GetTriggerUnit()
local timer t=CreateTimer()
local integer h=GetHandleId(t)
call SaveUnitHandle(hash,h,1,GT)
if GetUnitAbilityLevel(GT,'A10B')==1 then
call SaveInteger(hash,h,4,1)
else
call SaveInteger(hash,h,4,28)
endif
call SaveInteger(hash,h,5,1)
call TimerStart(t,0.25,true,function Trig_ice_attack_Create)
set t=null
set GT=null
endfunction
```

## 選擇天賦 `A107`

俄文原名：Выбрать талант

```
當玩家經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的力量階級。
```

**天賦選項**：
  - `A108` 絕對屏障
    強度等級：T3 屬性加成／每級屬性成長加成： -10 / +0 +0 / +0 +15 / +3  技能「魔法護盾」可吸收 100% 傷害。
  - `A10A` 雷霆使者
    強度等級：T3+ 屬性加成／每級屬性成長加成： +6 / +2 +3 / +0 +6 / +2  技能「球狀閃電」撞上英雄時造成的傷害降低 25%，但作用範圍提升 75%，並使敵人暈眩 3 秒（英雄 2 秒）。  技能「球狀閃電」不再有彈體計數——現在每產生一枚彈體便消耗 25 點法力值，技能會持續到英雄的法力值低於 25% 為止。球狀閃電還會以 25% 機率對被擊中的敵人施加電擊。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A108,A10A`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills31_Actions`　war3map.j:56160
```jass
if Skill=='A108' then
call SetHeroStr(u,GetHeroStr(u,false)-10,true)
call SetHeroInt(u,GetHeroInt(u,false)+15,true)
call SaveInteger(hash,GetHandleId(u),'aINT',3)
call UnitRemoveAbility(u,'A107')
call UnitAddAbility(u,'A109')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A10A' then
call SetHeroStr(u,GetHeroStr(u,false)+6,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+3,true)
call SetHeroInt(u,GetHeroInt(u,false)+6,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call SaveInteger(hash,GetHandleId(u),'aINT',2)
call UnitRemoveAbility(u,'A107')
call UnitAddAbility(u,'A10B')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 法球回收 `A0EZ`　—　吃技能強度

俄文原名：Возврат сферы

```
英雄將風暴法球召回自身，使其加速效果增強 50%。當法球回到獅鷲身邊時會發生爆炸，對周圍敵人造成傷害。

造成傷害：（125% 智力 + 50% 技能強度）點
若爆炸當下「魔法護盾」技能處於啟動狀態：爆炸傷害依「魔法護盾」技能的每一等級提升 10%

冷卻：5 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [1.0, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [1.0, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['charm', 'channel']`, `acdn = [5.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [25, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills31_Actions`　war3map.j:56197
```jass
elseif Skill=='A0EZ' then
set u3=LoadUnitHandle(hash,GetHandleId(pl),1)
if GetUnitTypeId(u3)=='o01J' then
call SaveReal(hash,GetHandleId(u3),2,0.25)
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 絕對屏障 `A108`　—　來自天賦「選擇天賦」

俄文原名：Абсолютный барьер

```
強度等級：T3
屬性加成／每級屬性成長加成：
-10 / +0
+0 / +0
+15 / +3

技能「魔法護盾」可吸收 100% 傷害。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['faeriefireon', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills31_Actions`　war3map.j:56160
```jass
if Skill=='A108' then
call SetHeroStr(u,GetHeroStr(u,false)-10,true)
call SetHeroInt(u,GetHeroInt(u,false)+15,true)
call SaveInteger(hash,GetHandleId(u),'aINT',3)
call UnitRemoveAbility(u,'A107')
call UnitAddAbility(u,'A109')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 雷霆使者 `A10A`　—　來自天賦「選擇天賦」

俄文原名：Вестник грома

```
強度等級：T3+
屬性加成／每級屬性成長加成：
+6 / +2
+3 / +0
+6 / +2

技能「球狀閃電」撞上英雄時造成的傷害降低 25%，但作用範圍提升 75%，並使敵人暈眩 3 秒（英雄 2 秒）。

技能「球狀閃電」不再有彈體計數——現在每產生一枚彈體便消耗 25 點法力值，技能會持續到英雄的法力值低於 25% 為止。球狀閃電還會以 25% 機率對被擊中的敵人施加電擊。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['farsight', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 32`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills31_Actions`　war3map.j:56167
```jass
elseif Skill=='A10A' then
call SetHeroStr(u,GetHeroStr(u,false)+6,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+3,true)
call SetHeroInt(u,GetHeroInt(u,false)+6,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call SaveInteger(hash,GetHandleId(u),'aINT',2)
call UnitRemoveAbility(u,'A107')
call UnitAddAbility(u,'A10B')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof
  - **4** — 受到傷害 −%〔受害者〕DefCof 減去它 → 值越大越耐打；電擊會扣它
  - **5** — 對 0-1 級敵人傷害 +%〔攻擊者〕
  - **6** — 造成傷害 +%〔攻擊者〕；電擊會扣它 → 目標輸出下降

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
