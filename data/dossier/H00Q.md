# 女術士 `H00Q`（Чародейка）

主屬性 **智力** · 背包 **6 格** · 解鎖 0 · 定位 法師

| | 初始 | 每級 |
|---|---|---|
| 力量 | 10 | 1.5 |
| 敏捷 | None | None |
| 智力 | 24 | 3.5 |

> 遠程英雄，用組合技能打出高額魔法傷害。

**縮放**：吃技能強度的技能 ['A016', 'A027', 'A028', 'A029'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 不穩定充能 `A016`　—　吃技能強度

俄文原名：Нестабильный заряд

```
以不穩定的充能衝擊敵人，造成傷害並施加負面狀態。

傷害：120 +（50% 技能強度）點
負面狀態：100% 機率施加易傷，30% 機率施加電擊

冷卻：15 秒
```

每級變動：
  - 第 3 行：120 / 180 / 240 / 300 / 360

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = tranquility`, `acdn = [15.0, 11.0]`, `alev = 5`, `amcs = [75, 85, 95, 105, 115, 140]`, `aran = 800.0`, `atar = ground,enemy,neutral,organic,air`

實作：

`Hero12E`　war3map.j:50059
```jass
function Hero12E takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u2=LoadUnitHandle(hash,Id,2)
local unit u3=LoadUnitHandle(hash,Id,3)
local real x=GetUnitX(u3)
local real y=GetUnitY(u3)
local real x2=GetUnitX(u2)
local real y2=GetUnitY(u2)
local group ug
local group ug2
local real dmg
local integer count
local real r=AngleXY(x,y,x2,y2)
set x=PolarX(x,35,r)
set y=PolarY(y,35,r)
if DistanceNative(x,y,x2,y2)<=18 then
set dmg=LoadReal(hash,Id,1)
call SetUnitX(u3,x2)
call SetUnitY(u3,y2)
call KillUnit(u3)
if GetUnitAbilityLevel(u,'A0JD')==1 then
if GetUnitManaPercent(u)>=80.00 then
set dmg=dmg*1.75
endif
endif
if GetUnitAbilityLevel(u2,'B016')==1 then
call UnitDamageTarget(u,u2,dmg*1.50,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call ClearCharm(u2)
else
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call CharmUnit(u,u2,0.40)
endif
call VulnerabilityUnit(u,u2,1.00)
call ShockUnit(u,u2,0.30)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
if GetUnitAbilityLevel(u,'A0JN')==1 then
set ug=CreateGroup()
set ug2=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,500.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call GroupAddUnit(ug2,u3)
endif
call GroupRemoveUnit(ug,u3)
endloop
set count=3
set r=udg_ItemBonusDMG[n]
loop
exitwhen r<100.
set r=r-100.
set count=count+1
endloop
set ug2=GetRandomSubGroup(count,ug2)
loop
set u3=FirstOfGroup(ug2)
exitwhen u3==null
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u3)
set x2=GetUnitX(u3)
set y2=GetUnitY(u3)
set u2=CreateUnit(pl,'o01W',x,y,AngleXY(x,y,x2,y2))
call SaveUnitHandle(hash,Id,3,u2)
call SaveReal(hash,Id,1,dmg)
call TimerStart(t,0.03,true,function Hero12E_Talent)
call GroupRemoveUnit(ug2,u3)
endloop
call DestroyGroup(ug)
call DestroyGroup(ug2)
endif
else
call SetUnitX(u3,x)
call SetUnitY(u3,y)
call SetUnitFacing(u2,r)
endif
set t=null
set u=null
set u2=null
set pl=null
set u3=null
set ug=null
set ug2=null
endfunction
```

`Trig_HeroSkills12_Actions`　war3map.j:50243
```jass
elseif Skill=='A016' or Skill=='A01F' then
set t=CreateTimer()
set Id=GetHandleId(t)
set u2=GetSpellTargetUnit()
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
set x=GetUnitX(u)
set y=GetUnitY(u)
set x2=GetUnitX(u2)
set y2=GetUnitY(u2)
set u3=CreateUnit(pl,'o01W',x,y,AngleXY(x,y,x2,y2))
call SaveUnitHandle(hash,Id,3,u3)
call SaveReal(hash,Id,1,(60.+60.*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.50)*dmg_cof)
call TimerStart(t,0.03,true,function Hero12E)
```

## 執政官 `A029`　—　吃技能強度

俄文原名：Архонт

```
女術士獲得強大的強化效果，使所有造成的傷害提升 25%，技能冷卻縮短 50%，並使施放技能的法力消耗降低 50%。使用時不會重置冷卻。

持續時間：10 +（2% 技能強度）秒
```

物件欄位（原型 `Absk`）：`abuf = ['', 'B00G']`, `acdn = [80.0, 25.0]`, `adur = [0.10000000149011612, 10.0]`, `ahdu = [0.10000000149011612, 10.0]`, `aher = 1`, `amcs = [75, 80, 90, 100, 110]`, `bsk1 = [0.0, 0.42000001668930054, 0.48000001907348633, 0.5400000214576721, 0.6000000238418579]`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Hero12R`　war3map.j:50151
```jass
function Hero12R takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
call SetUnitVertexColor(u,255,255,255,255)
call UnitRemoveAbility(u,'A0JE')
call SaveReal(hash,GetHandleId(u),6,LoadReal(hash,GetHandleId(u),6)-0.25)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills12_Actions`　war3map.j:50257
```jass
elseif Skill=='A029' then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",u,"overhead"))
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",u,"origin"))
call SetUnitVertexColor(u,70,70,255,125)
call UnitAddAbility(u,'A0JE')
call SaveReal(hash,GetHandleId(u),6,LoadReal(hash,GetHandleId(u),6)+0.25)
set t=CreateTimer()
set Id=GetHandleId(t)
set dmg=10.+udg_ItemBonusDMG[n]*0.02
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,dmg,true,function Hero12R)
endif
```

## 超新星 `A028`　—　吃技能強度

俄文原名：Сверхнова

```
英雄標記指定的區域，3 秒後該處會發生強力的魔法爆炸，對敵人造成傷害。

傷害：50 + 100% 技能強度 + 200% 智力 點
冷卻：42 秒
```

每級變動：
  - 第 3 行：50 / 100 / 150 / 200 / 250
  - 第 4 行：42 / 40 / 38 / 36 / 34

物件欄位（原型 `ANcl`）：`Ncl1 = 0.009999999776482582`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.009999999776482582`, `Ncl5 = 0`, `Ncl6 = charm`, `aare = 325.0`, `acdn = [42.0, 40.0, 38.0, 36.0, 34.0]`, `alev = 5`, `amcs = [155, 182, 209, 236, 263]`, `aran = 750.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Hero12W`　war3map.j:49938
```jass
function Hero12W takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u2=LoadUnitHandle(hash,Id,2)
local unit u3
local real x=LoadReal(hash,Id,3)
local real y=LoadReal(hash,Id,4)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local group ug
local integer Skill=LoadInteger(hash,Id,1)
local real dmg
local timer t2
local integer Id2
local integer count
local real r
if GetUnitAbilityLevel(u,'A0JN')==1 then
set dmg=(50*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]+I2R(GetHeroInt(u,true))*2)*1.50
set t2=CreateTimer()
set Id2=GetHandleId(t2)
call SaveUnitHandle(hash,Id2,1,u)
call SaveReal(hash,Id2,5,x)
call SaveReal(hash,Id2,6,y)
set count=10
set r=udg_ItemBonusDMG[n]
loop
exitwhen r<100.
set r=r-100.
set count=count+1
endloop
call SaveInteger(hash,Id2,3,count)
call SaveInteger(hash,Id2,4,Skill)
call TimerStart(t2,0.25,true,function Hero12WTalent)
else
set dmg=50*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]+I2R(GetHeroInt(u,true))*2
endif
if GetUnitAbilityLevel(u,'A0JD')==1 then
if GetUnitManaPercent(u)>=80.00 then
set dmg=dmg*1.75
endif
endif
call DestroyEffect(AddSpecialEffect("Eternal Explode.mdx",x,y))
call DestroyEffect(AddSpecialEffect("Eternal Explode.mdx",x,y))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,335,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
if GetUnitAbilityLevel(u3,'B016')==1 then
call UnitDamageTarget(u,u3,dmg*1.50,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call ClearCharm(u3)
else
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call CharmUnit(u,u3,0.40)
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
call RemoveUnit(u2)
set t=null
set t2=null
set u=null
set u2=null
set u3=null
set ug=null
set pl=null
endfunction
```

`Trig_HeroSkills12_Actions`　war3map.j:50224
```jass
elseif Skill=='A028' or Skill=='A02B' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
set u3=CreateUnit(pl,'o00M',x,y,0)
call SetUnitScale(u3,3.20,3.20,3.20)
call SetUnitVertexColor(u3,100,100,255,255)
call SetUnitAnimation(u3,"birth")
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u3)
call SaveReal(hash,Id,3,x)
call SaveReal(hash,Id,4,y)
call SaveInteger(hash,Id,1,Skill)
if GetUnitAbilityLevel(u,'A0JN')==1 then
call TimerStart(t,4.5,false,function Hero12W)
else
call TimerStart(t,3.,false,function Hero12W)
endif
```

## 魔法閃光 `A027`　—　吃技能強度

俄文原名：Магическая вспышка

```
產生一道魔法閃光，對小範圍內的敵人造成傷害。

傷害：50 +（25% 技能強度）點
冷卻：3 秒
```

每級變動：
  - 第 3 行：50 / 75 / 100 / 125 / 150

物件欄位（原型 `ANcl`）：`Ncl1 = 0.009999999776482582`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.009999999776482582`, `Ncl5 = 0`, `Ncl6 = channel`, `aare = 150.0`, `acdn = 3.0`, `alev = 5`, `amcs = [65, 75, 85, 95, 105]`, `aran = 700.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`CharmUnit`　war3map.j:49826
```jass
function CharmUnit takes unit damager,unit target,real chanse returns nothing
local timer t
local integer random
local integer chanse_random
local integer u_Id=GetHandleId(target)
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,GetHandleId(target),44)>0 then
set chanse=0.
endif
if LoadInteger(hash,GetHandleId(target),30)>0 then
if LoadInteger(hash,GetHandleId(target),30)>50 then
set chanse=0.
endif
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,GetHandleId(damager),'I09G')>=1 then
set chanse=chanse*1.50
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(target,'B016')==1 then
set t=LoadTimerHandle(hash,GetHandleId(target),'B016')
call TimerStart(t,5.,false,function RemoveCharm)
else
call UnitAddAbility(target,'S016')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,GetHandleId(target),'B016',t)
call TimerStart(t,5.,false,function RemoveCharm)
endif
if LoadInteger(hash,GetHandleId(damager),'I00Y')>=1 then
call FrostUnit(damager,target,0.50)
endif
endif
set t=null
endfunction
function ClearCharm takes unit u returns nothing
local timer t
local integer u_Id=GetHandleId(u)
call UnitRemoveBuffs(u,false,true)
if GetUnitAbilityLevel(u,'B016')==1 then
call UnitRemoveAbility(u,'S016')
call UnitRemoveAbility(u,'B016')
set t=LoadTimerHandle(hash,u_Id,'B016')
call FlushChildHashtable(hash,GetHandleId(t))
call PauseTimer(t)
call DestroyTimer(t)
call RemoveSavedHandle(hash,u_Id,'B016')
endif
set t=null
endfunction
```

`Trig_HeroSkills12_Actions`　war3map.j:50202
```jass
if Skill=='A027' or Skill=='A0JF' then
set dmg=(25.+25.*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.25)*dmg_cof
set x=GetSpellTargetX()
set y=GetSpellTargetY()
call DestroyEffect(AddSpecialEffect("Flamestrike Mystic I.mdx",x,y))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,155.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
if GetUnitAbilityLevel(u3,'B016')==1 then
call UnitDamageTarget(u,u3,dmg*1.50,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call ClearCharm(u3)
else
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call CharmUnit(u,u3,0.40)
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 魅惑 `A025`

俄文原名：Чары

```
英雄的技能有小機率對敵人施加「魅惑」狀態。魅惑會受到敵人身上詛咒抗性的影響。

魅惑施加機率：40%
魅惑持續時間：5 秒
魅惑效果：女術士的技能對帶有「魅惑」狀態的敵人造成傷害時，會移除該狀態，並使技能傷害提高 50%
```

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 選擇天賦 `A0JA`

俄文原名：Выбрать талант

```
當玩家的經驗值達到足夠數量時，你可以選擇一項天賦，大幅強化英雄，使其提升至新的力量階級。
```

**天賦選項**：
  - `A0JC` 魔力充盈
    強度等級：T2 屬性加成／每級屬性成長加成： +0 / +0 +1 / +0 +2 / +1  當法力值在 80% 以上時，女術士的技能造成的傷害提高 75%。
  - `A0JM` 神化
    強度等級：T3 屬性加成／每級屬性成長加成： +1 / +0 +2 / +1 +4 / +1  技能「不穩定充能」會擊中目標附近的 3 名敵人，造成 75% 傷害，並以降低後的機率（原機率的 75%）施加負面狀態。每 100 點技能強度，目標數量增加 1。  技能「超新星」的爆炸醞釀時間延長 50%，造成的傷害提高 50%。此外，超新星在爆炸後會以小型爆炸額外轟擊該區域，每次造成主技能 25% 的傷害（不計入 50% 的傷害提升）。小型爆炸的數量為 10，每 100 點技能強度 +1。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0JC,A0JM`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills12_Actions`　war3map.j:50180
```jass
if Skill=='A0JC' then
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A0JA')
call UnitAddAbility(u,'A0JD')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0JM' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+4,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0JA')
call UnitAddAbility(u,'A0JN')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **3** — 對英雄傷害 +%
  - **4** — 受到傷害 −%（被減的）
  - **5** — 對 0-1 級敵人傷害 +%
  - **6** — 造成傷害 +%
  - **44** — （狀態免疫旗標）

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
