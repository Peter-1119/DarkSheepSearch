# 神射手 `H00N`（Застрельщик）

主屬性 **敏捷** · 背包 **6 格** · 解鎖 0 · 定位 刺客

| | 初始 | 每級 |
|---|---|---|
| 力量 | 14 | 1.5 |
| 敏捷 | 21 | 2.700000047683716 |
| 智力 | 16 | 2.0 |

> 高效的射手，擊殺高危目標可以額外累積自身威力。

**縮放**：吃技能強度的技能 無 ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 給他們點顏色！ `A01A`

俄文原名：Зададим им перцу!

```
英雄每第 5 次攻擊會對敵人造成額外的魔法傷害。

傷害：30 + （X4 狩獵點數）點
```

每級變動：
  - 第 3 行：30 / 60 / 90 / 120 / 150

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_HeroAttack4_Actions`　war3map.j:47634
```jass
if GetUnitAbilityLevel(u,'A01A')>0 then
set count=LoadInteger(hash,Id,'A01A')+1
if count>=5 then
set count=count-5
set dmg=30*I2R(GetUnitAbilityLevel(u,'A01A'))+I2R(points)*4
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
set text=CreateTextTagUnitBJ("|cFF00D8FF"+I2S(R2I(dmg))+"!",u,0,14.00,100,100,100,0)
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
endif
call SaveInteger(hash,Id,'A01A',count)
endif
```

## 霰彈 `A019`

俄文原名：Картечь

```
英雄的攻擊開始波及主要目標附近的敵人（範圍 200）。主要目標同樣會受到本技能的傷害。

傷害：10 + （X1.25 狩獵點數）點
持續時間：16 秒

冷卻：26 秒
```

每級變動：
  - 第 3 行：10 / 20 / 30 / 40 / 50

物件欄位（原型 `Absk`）：`abuf = B004`, `acdn = 26.0`, `adur = 16.0`, `ahdu = 16.0`, `aher = 1`, `alev = 5`, `amcs = [80, 90, 100, 110, 120]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Trig_HeroAttack4_Actions`　war3map.j:47604
```jass
if GetUnitAbilityLevel(u,'B004')>0 then
set dmg=dmg+10.*I2R(GetUnitAbilityLevel(u,'A019'))+I2R(points)*1.25
set r=240.
endif
```

`Trig_HeroAttack4_Actions`　war3map.j:47619
```jass
elseif GetUnitAbilityLevel(u,'B004')>0 then
set dmg=10.*I2R(GetUnitAbilityLevel(u,'A019'))+I2R(points)*1.25
set r=200.
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,r,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Weapons\\GyroCopter\\GyroCopterImpact.mdl",u3,"origin"))
endif
call GroupRemoveUnit(ug,u3)
endloop
endif
```

## 加速 `A018`

俄文原名：Разгон

```
在靜止狀態下連續攻擊會造成額外傷害。若英雄開始移動，加成便會重置。

每次跳動的加成成長：+10 點
最大加成：100 + （X5 狩獵點數）點
```

物件欄位（原型 `Amgl`）：`aher = 1`

實作：

`SetUnitAttackSpeed`　war3map.j:3819
```jass
function SetUnitAttackSpeed takes unit u,integer a returns nothing
local integer Id=GetHandleId(u)
local integer p=8
local integer i=0
local integer r=a
if r>400 then
set r=400
endif
loop
call UnitRemoveAbility(u,setAttribute___abilityAddAttackSpeed[i])
exitwhen i==8
set i=i+1
endloop
if r>0 then
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityAddAttackSpeed[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
call SaveInteger(hash,Id,36,a)
endif
endfunction
```

`Trig_HeroAttack4_Actions`　war3map.j:47649
```jass
if GetUnitAbilityLevel(u,'A018')>0 then
set count=LoadInteger(hash,Id,'A018')
set limit=100+points*5
set x2=LoadReal(hash,Id,24)
set y2=LoadReal(hash,Id,25)
set r2=DistanceNative(x,y,x2,y2)
if r2<5.00 then
if GetUnitAbilityLevel(u,'A0HO')==1 or GetUnitAbilityLevel(u,'A06P')==1 then
set count=count+20
else
set count=count+10
endif
else
set count=0
endif
if count>limit then
set count=limit
endif
if count !=0 then
call UnitDamageTarget(u,u2,I2R(count),true,false,ATTACK_TYPE_HERO,DAMAGE_TYPE_NORMAL,WEAPON_TYPE_WHOKNOWS)
set text=CreateTextTagUnitBJ("|cFFFF5800+"+I2S(count)+"!",u,0,14.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,GetForceOfPlayer(pl))
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
endif
call SaveInteger(hash,Id,'A018',count)
call SaveReal(hash,Id,24,x)
call SaveReal(hash,Id,25,y)
elseif GetUnitAbilityLevel(u,'A0KD')>0 then
set count=LoadInteger(hash,Id,'A018')
set limit=150
set x2=LoadReal(hash,Id,24)
set y2=LoadReal(hash,Id,25)
set r2=DistanceNative(x,y,x2,y2)
if r2<5.00 then
set count=count+3
if count>limit then
set count=limit
else
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)+3)
endif
else
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)-count)
set count=0
endif
if count !=0 then
set text=CreateTextTagUnitBJ("|cFFFF5800"+I2S(count)+"%",u,0,14.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,GetForceOfPlayer(pl))
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
endif
call SaveInteger(hash,Id,'A018',count)
call SaveReal(hash,Id,24,x)
call SaveReal(hash,Id,25,y)
elseif GetUnitAbilityLevel(u,'A0ZW')>0 then
set count=LoadInteger(hash,Id,'A018')
set limit=100+points*5
set x2=LoadReal(hash,Id,24)
set y2=LoadReal(hash,Id,25)
set r2=DistanceNative(x,y,x2,y2)
if r2<5.00 then
if GetUnitAbilityLevel(u,'A0HO')==1 or GetUnitAbilityLevel(u,'A06P')==1 then
set count=count+20
else
set count=count+10
endif
else
set count=0
endif
if count>limit then
set count=limit
endif
if count !=0 then
call UnitDamageTarget(u,u2,I2R(count),true,false,ATTACK_TYPE_HERO,DAMAGE_TYPE_NORMAL,WEAPON_TYPE_WHOKNOWS)
set text=CreateTextTagUnitBJ("|cFFFF5800+"+I2S(count)+"!",u,0,14.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,GetForceOfPlayer(pl))
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
endif
call SaveInteger(hash,Id,'A018',count)
call SaveReal(hash,Id,24,x)
call SaveReal(hash,Id,25,y)
set count=LoadInteger(hash,Id,'A0KD')
set limit=60
if r2<5.00 then
set count=count+3
if count>limit then
set count=limit
else
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)+3)
endif
else
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)-count)
set count=0
endif
call SaveInteger(hash,Id,'A0KD',count)
endif
```

## 燃燒彈 `A017`

俄文原名：Зажигательный снаряд

```
射出爆炸性彈藥，對範圍造成傷害，並以提高的機率點燃敵人。

傷害：60 + （X5 狩獵點數）點
點燃：125% 機率，100% 傷害

冷卻：16 秒
```

每級變動：
  - 第 3 行：60 / 90 / 120 / 150 / 180

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = channel`, `aare = 200.0`, `acap = `, `acdn = 16.0`, `alev = 5`, `amcs = [95, 110, 125, 140, 155]`, `aran = 800.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`HeroQ4_Dmg`　war3map.j:47326
```jass
function HeroQ4_Dmg takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u3=LoadUnitHandle(hash,Id,2)
local real x=GetUnitX(u3)
local real y=GetUnitY(u3)
local real x2=LoadReal(hash,Id,1)
local real y2=LoadReal(hash,Id,2)
local real angle=LoadReal(hash,Id,3)
local group ug
local integer points=LoadInteger(hash,GetHandleId(u),'EXP3')
local integer lvl=LoadInteger(hash,Id,1)
local real dmg=30.+30.*I2R(lvl)+I2R(points)*5
local real aoe=LoadReal(hash,Id,4)
set x=PolarX(x,40.,angle)
set y=PolarY(y,40.,angle)
if DistanceNative(x,y,x2,y2)<=20. then
call SetUnitX(u3,x2)
call SetUnitY(u3,y2)
call KillUnit(u3)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,aoe,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call BurnUnit(u,u3,dmg,1.25)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
call SetUnitX(u3,x)
call SetUnitY(u3,y)
endif
set t=null
set u=null
set pl=null
set u3=null
set ug=null
endfunction
```

`Trig_HeroSkills4_Actions`　war3map.j:47527
```jass
if Skill=='A017' or Skill=='A0HP' then
set x2=GetSpellTargetX()
set y2=GetSpellTargetY()
set angle=AngleXY(x,y,x2,y2)
set u3=CreateUnit(pl,'o00J',x,y,angle)
call SetUnitX(u3,x)
call SetUnitY(u3,y)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u3)
call SaveReal(hash,Id,1,x2)
call SaveReal(hash,Id,2,y2)
call SaveReal(hash,Id,3,angle)
call SaveInteger(hash,Id,1,GetUnitAbilityLevel(u,Skill))
if Skill=='A017' then
call SaveReal(hash,Id,4,205.)
else
call SaveReal(hash,Id,4,305.)
endif
call TimerStart(t,0.03,true,function HeroQ4_Dmg)
```

## 怪物獵人 `A0HR`

俄文原名：Охотник на монстров

```
擊殺敵人可讓英雄獲得狩獵點數。每次獲得加成後，擊殺的需求都會提高。指令「-i」會顯示已累積的狩獵點數。

擊殺獲得的狩獵經驗（部隊）：10 * （敵人等級）^1.5
擊殺獲得的狩獵經驗（英雄）：25 * （英雄等級）^1.5
獲得狩獵點數所需的狩獵經驗：25 (+75)

每層加成：攻擊力 +1，狩獵點數 +1
```

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 選擇天賦 `A0HJ`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的強度等級。
```

**天賦選項**：
  - `A0HN` 狙擊手
    強度等級：T2 屬性加成／每級屬性成長加成： +1 / +0 +1 / +1 +1 / +0  英雄的攻擊距離提升 200 點。  技能「燃燒彈」(Q)（或其同類技能）的施放距離提升 300 點，作用範圍提升 50%。  技能「加速」累積加成的速度加快 2 倍。
  - `A0J3` 霰彈槍
    強度等級：T3 屬性加成／每級屬性成長加成： +12 / +3 -5 / -0.7 -1 / -0.5  英雄的主屬性變更為「力量」。  攻擊距離降低至 250 點，英雄的攻擊帶有永久的「霰彈」效果，其作用範圍縮減為 160 點、傷害為（50% 力量）點；「霰彈」技能本身在啟動時會將作用範圍提升至 240 點，並將自身的傷害數值加到基礎數值上。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0HN,A0J3`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills4_Actions`　war3map.j:47476
```jass
if Skill=='A0HN' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0HJ')
if GetUnitTypeId(u)=='H040' then
call UnitAddAbility(u,'A06P')
else
call UnitAddAbility(u,'A0HO')
endif
call SetPlayerTechResearched(pl,'R00F',1)
call SaveInteger(hash,GetHandleId(pl),15,1)
```

---

## 皮膚

### 黑暗神射手 `H040` —— **會換技能**
  - 燃燒彈 `A017` → **貫穿射擊** `A05R`

### 鼠人 `Otch` —— **會換技能**
  - 加速 `A018` → **加速** `A0KD`

### 復生陸戰隊員 `H01I` —— **會換技能**
  - 加速 `A018` → **加速** `A0ZW`

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **3** — 對英雄傷害 +%
  - **4** — 受到傷害 −%（被減的）

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
