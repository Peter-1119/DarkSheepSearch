# 符文守護者 `O01M`（Рунный защитник）

主屬性 **力量** · 背包 **6 格** · 解鎖 50000 · 定位 坦克/輔助

| | 初始 | 每級 |
|---|---|---|
| 力量 | 35 | 4.5 |
| 敏捷 | 9 | None |
| 智力 | 17 | 2.0 |

> 極為堅固的坦克，專門吸收大量傷害；死亡會受到懲罰。

**縮放**：吃技能強度的技能 無 ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 庇護 `A0G6`

俄文原名：Покровительство

```
符文守護者散發一道光環，保護附近的友軍英雄。受保護的英雄所受到的部分傷害會被轉移到符文守護者身上。對自身無效。

友軍英雄的傷害防護：30%
英雄受到的傷害：光環所擋下傷害的 50%

當啟用「相同英雄」設定時，英雄不會受到傷害，但光環對友軍的效果會減弱兩倍。多個符文守護者的光環不會疊加。
```

物件欄位（原型 `AHad`）：`Had1 = 0.0`, `abuf = B01T`, `alev = 1`, `atar = hero,friend,invulnerable,self,vulnerable`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 不動守衛 `A0G2`

俄文原名：Непоколебимый страж

```
英雄有小機率完全格擋所受到的物理傷害。此外，英雄死亡時你有機率不損失力量。

格擋機率：8%
保留力量的機率：8%
```

每級變動：
  - 第 3 行：8 / 12 / 16 / 20 / 24
  - 第 4 行：8 / 12 / 16 / 20 / 24

物件欄位（原型 `Assk`）：`Ssk1 = [12.0, 16.0, 20.0, 24.0, 8.0]`, `Ssk2 = 0.0`, `Ssk3 = 99999.0`, `Ssk4 = 1`, `Ssk5 = 1`, `aher = 1`, `alev = 5`, `atar = enemy,ally`

實作：

`SetUnitLifeRegeneration`　war3map.j:4167
```jass
function SetUnitLifeRegeneration takes unit u,real a returns nothing
local integer Id=GetHandleId(u)
if a>8191 then
set a=8191
endif
call SaveReal(hash,Id,32,a)
call GroupAddUnit(setAttribute___allUnits,u)
endfunction
```

`ResRuneDefender`　war3map.j:46378
```jass
if GetUnitAbilityLevel(u,'A0G2')>=1 or GetUnitAbilityLevel(u,'A10L')>=1 then
set random=GetRandomInt(1,100)
if GetUnitAbilityLevel(u,'A0G2')>=1 then
set check=GetUnitAbilityLevel(u,'A0G2')*4+4
else
set check=GetUnitAbilityLevel(u,'A10L')*8+8
endif
if random<=check then
call DisplayTimedTextToPlayer(pl,0,0,15.,"|cFF03FDDA[Непоколебимый страж]|r |cFF036BADСила героя сохранена.|r")
if GetUnitAbilityLevel(u,'A10L')>=1 then
call SetHeroStr(u,str+5,true)
call SetUnitLifeRegeneration(u,GetUnitLifeRegeneration(u)+2.5)
endif
else
if GetUnitAbilityLevel(u,'A10N')==1 then
if str<6 then
call SetHeroStr(u,1,true)
else
call SetHeroStr(u,str-5,true)
endif
else
if str<4 then
call SetHeroStr(u,1,true)
else
call SetHeroStr(u,str-3,true)
endif
endif
endif
```

## 堅韌符文 `A0G0`

俄文原名：Руна стойкости

```
英雄清除自身的負面效果，並獲得對一般傷害、狀態、穿透與反傷的防護。

受到的傷害降低：20%
持續時間：10 秒。

冷卻：20 秒。
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `Absk`）：`abuf = B01S`, `acdn = 20.0`, `adur = 10.0`, `ahdu = 10.0`, `aher = 1`, `alev = 5`, `amcs = [55, 62, 69, 76, 83]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Trig_HeroTakeDamage_Actions`　war3map.j:20071
```jass
if GetUnitAbilityLevel(d,'B01S')>0 then
set life=life*(0.90-0.10*I2R(GetUnitAbilityLevel(d,'A0G0')))
endif
```

`Trig_UAttacks_Actions`　war3map.j:20294
```jass
if GetUnitAbilityLevel(u,'B01S')>0 then
set dmg=dmg*(0.90-0.10*I2R(GetUnitAbilityLevel(u,'A0G0')))
endif
```

`HeroW32`　war3map.j:56380
```jass
function HeroW32 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer u_Id=GetHandleId(u)
local real r=LoadReal(hash,Id,1)
call SetUnitVertexColor(u,255,255,255,255)
call SaveReal(hash,u_Id,4,LoadReal(hash,u_Id,4)-r)
call SaveReal(hash,u_Id,47,LoadReal(hash,u_Id,47)-r)
call SaveReal(hash,u_Id,48,LoadReal(hash,u_Id,48)-r)
call SaveReal(hash,u_Id,49,LoadReal(hash,u_Id,49)-r)
call SaveReal(hash,u_Id,50,LoadReal(hash,u_Id,50)-r)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills32_Actions`　war3map.j:56556
```jass
elseif Skill=='A0G0' then
set dmg=0.10+0.10*I2R(GetUnitAbilityLevel(u,Skill))
call SaveReal(hash,u_Id,4,LoadReal(hash,u_Id,4)+dmg)
call SaveReal(hash,u_Id,47,LoadReal(hash,u_Id,47)+dmg)
call SaveReal(hash,u_Id,48,LoadReal(hash,u_Id,48)+dmg)
call SaveReal(hash,u_Id,49,LoadReal(hash,u_Id,49)+dmg)
call SaveReal(hash,u_Id,50,LoadReal(hash,u_Id,50)+dmg)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",u,"origin"))
call SetUnitVertexColor(u,125,125,255,255)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,dmg)
call TimerStart(t,10.,false,function HeroW32)
endif
```

## 地震衝擊 `A0FV`

俄文原名：Сейсмический удар

```
產生多次爆裂，以裂縫的形式自英雄向外擴散，主要朝向英雄面對的方向。

每次爆裂的傷害：30 + （35% 力量）點

冷卻：18 秒。
```

每級變動：
  - 第 3 行：30 / 45 / 60 / 75 / 90

物件欄位（原型 `ANcl`）：`Ncl1 = 0.6000000238418579`, `Ncl3 = 1`, `Ncl4 = 0.6000000238418579`, `Ncl5 = 0`, `Ncl6 = chemicalrage`, `aare = 500.0`, `acdn = 18.0`, `alev = 5`, `amcs = [100, 110, 120, 130, 140]`, `aran = 700.0`, `atar = air,ground,friend,neutral,self`

實作：

`HeroQ32`　war3map.j:56398
```jass
function HeroQ32 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local real dmg=LoadReal(hash,Id,1)
local integer count=LoadInteger(hash,Id,1)+1
local group ug
local unit u3
local real startX=LoadReal(hash,Id,2)
local real startY=LoadReal(hash,Id,3)
local real a1=LoadReal(hash,Id,5)
local real a2=LoadReal(hash,Id,6)
local real a3=LoadReal(hash,Id,7)
local real dist
local real ang
local real px
local real py
local real perp
local integer i
local integer m
local integer sub
local integer num=1
local real step=I2R(count-1)
if count>=6 then
set num=6
elseif count>=2 then
set num=3
endif
set dist=90.+105.*step
set i=0
loop
exitwhen i>=num
if count==1 then
set px=startX
set py=startY
elseif count<=5 then
if i==0 then
set ang=a1
elseif i==1 then
set ang=a2
else
set ang=a3
endif
set ang=ang+11.*Sin(step*1.3+I2R(i)*2.1)
set px=startX+dist*Cos(ang*bj_DEGTORAD)
set py=startY+dist*Sin(ang*bj_DEGTORAD)
set perp=18.*Sin(step*0.9+I2R(i)*1.7)
set px=px+perp*Cos((ang+90.)*bj_DEGTORAD)
set py=py+perp*Sin((ang+90.)*bj_DEGTORAD)
else
set m=i/2
set sub=i-m*2
if m==0 then
set ang=a1
elseif m==1 then
set ang=a2
else
set ang=a3
endif
set ang=ang+(I2R(sub)*2.-1.)*25.+9.*Sin(step*1.1+I2R(i))
set px=startX+dist*Cos(ang*bj_DEGTORAD)
set py=startY+dist*Sin(ang*bj_DEGTORAD)
set perp=14.*Sin(step*0.85+I2R(i)*1.4)
set px=px+perp*Cos((ang+90.)*bj_DEGTORAD)
set py=py+perp*Sin((ang+90.)*bj_DEGTORAD)
endif
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",px,py))
if GetUnitAbilityLevel(u,'A10N')==1 then
set dmg=dmg*LoadReal(hash,Id,'A10N')
endif
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,px,py,225.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set i=i+1
endloop
if count>=LoadInteger(hash,Id,2)then
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
if GetUnitAbilityLevel(u,'A10N')==1 then
call SaveReal(hash,Id,'A10N',LoadReal(hash,Id,'A10N')+0.20)
endif
call SaveInteger(hash,Id,1,count)
endif
set t=null
set u=null
set u3=null
set pl=null
set ug=null
endfunction
```

`Trig_HeroSkills32_Actions`　war3map.j:56532
```jass
if Skill=='A0FV' then
set dmg=15.+15.*I2R(GetUnitAbilityLevel(u,Skill))+I2R(GetHeroStr(u,true))*0.35
set r=GetUnitFacing(u)
set a1=r-72.+GetRandomReal(-12.,8.)
set a2=r+GetRandomReal(-18.,18.)
set a3=r+72.+GetRandomReal(-8.,12.)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,1,0)
if GetUnitAbilityLevel(u,'A10N')==1 then
call SaveInteger(hash,Id,2,13)
call SaveReal(hash,Id,1,dmg*0.50)
call SaveReal(hash,Id,'A10N',1.00)
else
call SaveInteger(hash,Id,2,7)
call SaveReal(hash,Id,1,dmg)
endif
call SaveReal(hash,Id,2,x)
call SaveReal(hash,Id,3,y)
call SaveReal(hash,Id,5,a1)
call SaveReal(hash,Id,6,a2)
call SaveReal(hash,Id,7,a3)
call TimerStart(t,0.10,true,function HeroQ32)
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 野性符文 `A0FU`

俄文原名：Дикие руны

```
英雄在 10 秒內復活，但每次復活都會損失 3 點力量。
```

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 選擇天賦 `A10I`

俄文原名：Выбрать талант

```
當玩家的經驗值達到足夠數量時，你可以選擇一項天賦，大幅強化英雄，使其提升至新的力量階級。
```

**天賦選項**：
  - `A10J` 力量之源
    強度等級：T3 屬性加成／每級屬性成長加成： +6 / +1 +0 / +0 +1 / +0  「不動守衛」不再格擋受到的傷害，但英雄死亡後保留力量的機率加倍；若英雄成功保留力量，則會額外獲得 +5 點力量與 +2.5 點生命值回復。
  - `A10M` 巨人步伐
    強度等級：T3+ 屬性加成／每級屬性成長加成： +20 / +3 +0 / +0 +3 / +1  「地震衝擊」的擴散距離大幅增加（延伸裂縫的長度 +100%），技能傷害降低 50%。「地震衝擊」產生的爆裂距離起始點越遠，造成的傷害越高，最後也最遠的爆裂可獲得最高 +240% 的傷害加成。  「野性符文」在英雄死亡後奪走 5 點力量。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A10J,A10M`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills32_Actions`　war3map.j:56516
```jass
if Skill=='A10J' then
call SetHeroStr(u,GetHeroStr(u,false)+6,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A10I')
call UnitAddAbility(u,'A10K')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A10M' then
call SetHeroStr(u,GetHeroStr(u,false)+20,true)
call SetHeroInt(u,GetHeroInt(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',3)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A10I')
call UnitAddAbility(u,'A10N')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **3** — 對英雄傷害 +%
  - **4** — 受到傷害 −%（被減的）
  - **5** — 對 0-1 級敵人傷害 +%
  - **6** — 造成傷害 +%
  - **47** — 點燃抗性
  - **48** — 冰凍抗性
  - **49** — 流血抗性
  - **50** — 疾病抗性

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
