# 幽魂之狼 `N01P`（Призрачный волк）

主屬性 **敏捷** · 背包 **4 格** · 解鎖 0 · 定位 刺客

| | 初始 | 每級 |
|---|---|---|
| 力量 | 16 | 2.2 |
| 敏捷 | 19 | 2.5 |
| 智力 | 14 | 1.5 |

> 背包格數受限，但攻擊速度極高，能短暫變身。

**縮放**：吃技能強度的技能 ['A08A', 'A08P'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **狀態** —— 走 `Burn_Dmg` 那條，**外面包了 DisableTrigger** → 不吃 DefCof、不帶穿透、被狀態抗性擋。該買的是「狀態傷害 +%」「易燃」「機率倍率」。
- **直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且事件數越多穿透越划算。
- **治療／增益** —— 直接寫數值，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。

細節見 `data/dossier/_engine.md`。


---

## 吞噬 `A08P`　—　吃技能強度

俄文原名：Пожирание

```
英雄可以吞食 0-1 等級的敵人來治療自身，並為「深度創傷」技能取得額外充能。對更強大的目標使用此技能時，狼會造成傷害並取得較少的額外充能。

治療強度：100 + （75% 技能強度）點
對強大敵人的傷害：75 + （60% 技能強度）點
額外充能：1
吞食目標時的額外充能：3

冷卻：15 秒。
```

每級變動：
  - 第 3 行：100 / 150 / 200 / 250 / 300
  - 第 4 行：75 / 125 / 175 / 225 / 275
  - 第 5 行：1 / 2 / 3 / 4 / 5
  - 第 6 行：3 / 4 / 5 / 6 / 7

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acdn = 15.0`, `alev = 5`, `amcs = [50, 55, 60, 65, 70]`, `aran = 110.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53231
```jass
if Skill=='A08P' then
set u2=GetSpellTargetUnit()
call DestroyEffect(AddSpecialEffectTarget("Objects\\Spawnmodels\\Orc\\OrcSmallDeathExplode\\OrcSmallDeathExplode.mdl",u2,"origin"))
call DestroyEffect(AddSpecialEffectTarget("Objects\\Spawnmodels\\Orc\\OrcSmallDeathExplode\\OrcSmallDeathExplode.mdl",u2,"origin"))
set Id=GetHandleId(u)
if GetUnitLevel(u2)<=1 then
set dmg=(GetUnitState((u2),UNIT_STATE_MAX_LIFE))*2.0
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_UNIVERSAL,WEAPON_TYPE_WHOKNOWS)
set dmg=50+50*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.75
call SetUnitState(u,UNIT_STATE_LIFE,GetUnitState(u,UNIT_STATE_LIFE)+dmg)
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+2+GetUnitAbilityLevel(u,Skill))
else
set dmg=25+50*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.60
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+GetUnitAbilityLevel(u,Skill))
endif
```

## 狩獵 `A0D9`

俄文原名：Охота

```
英雄變得完全隱形，並可以毫無顧忌地攻擊敵人。使用此技能會為「深度創傷」技能增加額外充能。

持續時間：5 秒。
額外充能：3

冷卻：10 秒。
```

每級變動：
  - 第 3 行：5 / 6 / 7 / 8 / 9
  - 第 4 行：3 / 4 / 5 / 6 / 7
  - 第 6 行：10 / 11 / 12 / 13 / 14

物件欄位（原型 `Absk`）：`abuf = B00R`, `acdn = [10.0, 11.0, 12.0, 13.0, 14.0]`, `adur = [5.0, 6.0, 7.0, 8.0, 9.0]`, `ahdu = [5.0, 6.0, 7.0, 8.0, 9.0]`, `aher = 1`, `alev = 5`, `amcs = [45, 50, 55, 60, 65]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53247
```jass
elseif Skill=='A0D9' then
set Id=GetHandleId(u)
set dmg=4+I2R(GetUnitAbilityLevel(u,Skill))
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+2+GetUnitAbilityLevel(u,Skill))
call UnitAddAbility(u,'A0D8')
set t=CreateTimer()
call TimerStart(t,dmg,false,function RemoveBuff)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
call SaveInteger(hash,GetHandleId(t),2,'A0D8')
endif
```

`RemoveBuff`　war3map.j:2875
```jass
function RemoveBuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer Buff=LoadInteger(hash,Id,2)
call UnitRemoveAbility(u,Buff)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

## 深度創傷 `A08A`　—　吃技能強度

俄文原名：Глубокие раны

```
英雄每 3.5 秒獲得一層充能，攻擊時消耗充能對被攻擊的目標造成額外傷害並附帶流血效果，同時為狼回復生命值。充能可無上限累積。技能強度 200 以上時，充能造成的傷害會變為純粹傷害。

傷害：30 + （5% 技能強度）點
流血效果：75% 機率，150% 傷害
生命值回復：8 + （2% 技能強度）點
```

每級變動：
  - 第 3 行：30 / 45 / 60 / 75 / 90
  - 第 5 行：8 / 12 / 16 / 20 / 24

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

呼叫共用引擎函式：`BleedUnit` —— 完整內容見 `_engine.md`。

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53236
```jass
if GetUnitLevel(u2)<=1 then
set dmg=(GetUnitState((u2),UNIT_STATE_MAX_LIFE))*2.0
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_UNIVERSAL,WEAPON_TYPE_WHOKNOWS)
set dmg=50+50*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.75
call SetUnitState(u,UNIT_STATE_LIFE,GetUnitState(u,UNIT_STATE_LIFE)+dmg)
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+2+GetUnitAbilityLevel(u,Skill))
else
set dmg=25+50*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.60
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+GetUnitAbilityLevel(u,Skill))
endif
elseif Skill=='A0D9' then
set Id=GetHandleId(u)
set dmg=4+I2R(GetUnitAbilityLevel(u,Skill))
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+2+GetUnitAbilityLevel(u,Skill))
call UnitAddAbility(u,'A0D8')
set t=CreateTimer()
call TimerStart(t,dmg,false,function RemoveBuff)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
call SaveInteger(hash,GetHandleId(t),2,'A0D8')
endif
```

`Trig_HeroAttack21_Actions`　war3map.j:53269
```jass
function Trig_HeroAttack21_Actions takes nothing returns nothing
local unit u=GetAttacker()
local unit u2=GetTriggerUnit()
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real dmg
local integer count=LoadInteger(hash,GetHandleId(u),'A08A')
if count>0 then
set count=count-1
call SaveInteger(hash,GetHandleId(u),'A08A',count)
set dmg=15+15*I2R(GetUnitAbilityLevel(u,'A08A'))+udg_ItemBonusDMG[n]*0.05
if GetUnitAbilityLevel(u,'A08L')==1 and UnitLifePercent(u)>=50.00 then
set dmg=dmg*1.50
endif
if udg_ItemBonusDMG[n]>=200.00 then
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_UNIVERSAL,WEAPON_TYPE_WHOKNOWS)
else
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_HERO,DAMAGE_TYPE_NORMAL,WEAPON_TYPE_WHOKNOWS)
endif
call BleedUnit(u,u2,dmg*1.50,0.75)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\Stampede\\StampedeMissileDeath.mdl",u2,"origin"))
set dmg=4+4*I2R(GetUnitAbilityLevel(u,'A08A'))+udg_ItemBonusDMG[n]*0.02
call SetUnitState(u,UNIT_STATE_LIFE,GetUnitState(u,UNIT_STATE_LIFE)+dmg)
endif
set u=null
set u2=null
set pl=null
endfunction
```

`Hero21E`　war3map.j:53307
```jass
if GetUnitAbilityLevel(u,'A08L')==1 and UnitLifePercent(u)>=75.00 then
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+2)
else
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+1)
endif
```

`Trig_HeroGiveSkills21_Actions`　war3map.j:53318
```jass
if GetUnitAbilityLevel(u,'A08A')==1 then
set t=CreateTimer()
call TimerStart(t,3.5,true,function Hero21E)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
endif
```

`RemoveBuff`　war3map.j:2875
```jass
function RemoveBuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer Buff=LoadInteger(hash,Id,2)
call UnitRemoveAbility(u,Buff)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

## 頭狼 `A08L`

俄文原名：Альфа

```
生命值在 50% 以上時，「深度創傷」造成 150% 傷害；生命值在 75% 以上時，「深度創傷」每次累積 2 層充能。
```

物件欄位（原型 `Amgl`）：`aher = 1`

實作：

`Trig_HeroAttack21_Actions`　war3map.j:53280
```jass
if GetUnitAbilityLevel(u,'A08L')==1 and UnitLifePercent(u)>=50.00 then
set dmg=dmg*1.50
endif
```

`Hero21E`　war3map.j:53307
```jass
if GetUnitAbilityLevel(u,'A08L')==1 and UnitLifePercent(u)>=75.00 then
call SaveInteger(hash,Id,'A08A',LoadInteger(hash,Id,'A08A')+2)
```

## 選擇天賦 `A0V7`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，使英雄大幅強化並提升至新的力量位階。
```

**天賦選項**：
  - `A0YD` 野性之魂
    強度等級：T2 屬性加成／每級屬性成長加成： +5 / +1 +1 / +1 +0 / +0  英雄的復活時間為 15 秒，但每次死亡都會使該時間增加 2 秒。
  - `A0YF` 背包擴充
    強度等級：T3 屬性加成／每級屬性成長加成： +1 / +0 +1 / +1 +1 / +0  將道具欄位的數量增加至 6 格。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0YD,A0YF`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53205
```jass
if Skill=='A0YD' then
call SetHeroStr(u,GetHeroStr(u,false)+5,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0V7')
call UnitAddAbility(u,'A0YE')
call SaveReal(hash,GetHandleId(u),2,15.00)
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0YF' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0V7')
call UnitAddAbility(u,'A0YI')
set Id=0
loop
exitwhen Id>3
call SetItemPosition(UnitItemInSlot(u,Id),GetUnitX(u),GetUnitY(u))
set Id=Id+1
endloop
call UnitRemoveAbility(u,'A08K')
call UnitAddAbility(u,'AInv')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 掠食者本能 `A089`

俄文原名：Инстинкты хищника

```
英雄對當前生命值百分比低於自身當前生命值百分比的敵人造成 20% 額外傷害。
```

實作：

`Trig_HeroTakeDamage_Actions`　war3map.j:19825
```jass
if GetUnitAbilityLevel(a,'A089')==1 then
if DefLifePercent<UnitLifePercent(a)then
set DefCof=DefCof+0.20
endif
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 第二背包 `A08K`

俄文原名：物品欄

物件欄位（原型 `AInv`）：`inv1 = 4`

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53214
```jass
elseif Skill=='A0YF' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0V7')
call UnitAddAbility(u,'A0YI')
set Id=0
loop
exitwhen Id>3
call SetItemPosition(UnitItemInSlot(u,Id),GetUnitX(u),GetUnitY(u))
set Id=Id+1
endloop
call UnitRemoveAbility(u,'A08K')
call UnitAddAbility(u,'AInv')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

`Trig_HeroSkills26_Actions`　war3map.j:54702
```jass
if Skill=='A09W' then
call SetHeroStr(u,GetHeroStr(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A090')
call UnitAddAbility(u,'A09X')
set count=0
loop
exitwhen count>3
call SetItemPosition(UnitItemInSlot(u,count),GetUnitX(u),GetUnitY(u))
set count=count+1
endloop
call UnitRemoveAbility(u,'A08K')
call UnitAddAbility(u,'AInv')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 野性之魂 `A0YD`　—　來自天賦「選擇天賦」

俄文原名：Дикий дух

```
強度等級：T2
屬性加成／每級屬性成長加成：
+5 / +1
+1 / +1
+0 / +0

英雄的復活時間為 15 秒，但每次死亡都會使該時間增加 2 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acidbomb', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 15`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53205
```jass
if Skill=='A0YD' then
call SetHeroStr(u,GetHeroStr(u,false)+5,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0V7')
call UnitAddAbility(u,'A0YE')
call SaveReal(hash,GetHandleId(u),2,15.00)
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 背包擴充 `A0YF`　—　來自天賦「選擇天賦」

俄文原名：Расширение инвентаря

```
強度等級：T3
屬性加成／每級屬性成長加成：
+1 / +0
+1 / +1
+1 / +0

將道具欄位的數量增加至 6 格。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['faeriefireon', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills21_Actions`　war3map.j:53214
```jass
elseif Skill=='A0YF' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0V7')
call UnitAddAbility(u,'A0YI')
set Id=0
loop
exitwhen Id>3
call SetItemPosition(UnitItemInSlot(u,Id),GetUnitX(u),GetUnitY(u))
set Id=Id+1
endloop
call UnitRemoveAbility(u,'A08K')
call UnitAddAbility(u,'AInv')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 這隻碰到的 hash key


---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
