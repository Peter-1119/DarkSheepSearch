# 暴風使者 `N03S`（Буревестник）

主屬性 **智力** · 背包 **6 格** · 解鎖 50000 · 定位 法師

| | 初始 | 每級 |
|---|---|---|
| 力量 | 17 | （未覆寫） |
| 敏捷 | 19 | 2 |
| 智力 | 28 | 4 |

> 無情的法師，對敵方集群造成巨量傷害，但非常吃法力。

**縮放**：吃技能強度的技能 ['A0H3', 'A0H4', 'A0HC', 'A0HT', 'A0HU'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 致命閃電 `A0HU`　—　吃技能強度

俄文原名：Разящая молния

```
英雄的攻擊會引發分岔的閃電，擊中數名敵人。

傷害：70 +（15% 技能強度）點
目標數量：3 個
技能強度 200+：回復 2% MP
技能強度 400+：閃電最多擊中 4 個目標
技能強度 600+：回復 2% HP
技能強度 800+：閃電最多擊中 5 個目標

冷卻：10 秒
```

每級變動：
  - 第 3 行：70 / 110 / 150 / 190 / 230

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_Hero35DAttack_Actions`　war3map.j:57469
```jass
if GetUnitAbilityLevel(u,'A0HU')>=1 and LoadInteger(hash,GetHandleId(u),'A0HU')==0 and IsUnitType(u3,UNIT_TYPE_MECHANICAL)!=true and IsUnitType(u3,UNIT_TYPE_STRUCTURE)!=true then
if udg_ItemBonusDMG[n]>=200 then
set x=GetUnitState(u,UNIT_STATE_MANA)
set y=(GetUnitState((u),UNIT_STATE_MAX_MANA))
call SetUnitState(u,UNIT_STATE_MANA,x+y*0.02)
endif
if udg_ItemBonusDMG[n]>=600 then
set x=GetUnitState(u,UNIT_STATE_LIFE)
set y=(GetUnitState((u),UNIT_STATE_MAX_LIFE))
call SetUnitState(u,UNIT_STATE_LIFE,x+y*0.02)
endif
set dmg=30+40*GetUnitAbilityLevel(u,'A0HU')+udg_ItemBonusDMG[n]*0.20
set x=GetUnitX(u)
set y=GetUnitY(u)
set t=CreateTimer()
set CD=10.00
set dummy=CreateUnit(pl,'o010',x,y,0.00)
call SaveReal(hash,GetHandleId(dummy),13,dmg)
call SaveUnitHandle(hash,GetHandleId(dummy),13,u)
call UnitApplyTimedLife(dummy,'BTLF',2.00)
call UnitAddAbility(dummy,'A0HS')
if udg_ItemBonusDMG[n]>=800 then
call SetUnitAbilityLevel(dummy,'A0HS',3)
elseif udg_ItemBonusDMG[n]>=400 then
call SetUnitAbilityLevel(dummy,'A0HS',2)
endif
call IssueTargetOrderById(dummy,Order_forkedlightning,u3)
call SaveInteger(hash,GetHandleId(u),'A0HU',1)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveInteger(hash,Id,1,GetHandleId(u))
call SaveInteger(hash,Id,2,'A0HU')
call TimerStart(t,CD,false,function EndCooldown)
endif
```

## 吸收法球 `A0HT`　—　吃技能強度

俄文原名：Сфера поглощения

```
英雄在指定地點生成一個吸收法球，並於 10 秒後爆炸。法球會依據其內部的敵人累積自身的傷害。

初始傷害：200 + （75% 技能強度）點
傷害累積（每個敵方單位）：每秒 +0.5% 初始傷害
傷害累積（每個敵方英雄）：每秒 +2.5% 初始傷害

冷卻：60 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.699999988079071, 1.0]`, `Ncl2 = [1, 2]`, `Ncl3 = [3, 1]`, `Ncl4 = [0.699999988079071, 1.0]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'charm']`, `aare = 350.0`, `acdn = [60.0, 17.0]`, `alev = 1`, `amcs = [80, 90, 100, 110, 120, 175]`, `aran = [700.0, 800.0]`, `atar = air,ground,friend,neutral,self`

實作：

`Hero35R`　war3map.j:57259
```jass
function Hero35R takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,3)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real x=LoadReal(hash,Id,1)
local real y=LoadReal(hash,Id,2)
local real x2
local real y2
local real dmg=LoadReal(hash,Id,3)
local real dmg_final=LoadReal(hash,Id,4)
local integer count=LoadInteger(hash,Id,1)
local unit u3
local group ug
local texttag text=LoadTextTagHandle(hash,Id,4)
local force f=CreateForce()
call ForceAddPlayer(f,pl)
call DestroyTextTag(text)
set udg_p=Location(x,y)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,355,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
if IsUnitType(u3,UNIT_TYPE_HERO)then
set dmg_final=dmg_final+dmg*0.025
else
set dmg_final=dmg_final+dmg*0.005
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set count=count-1
if count==0 then
set text=CreateTextTagLocBJ("|cFF6496FF"+I2S(R2I(dmg_final)),udg_p,0,14.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,f)
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
set u3=LoadUnitHandle(hash,Id,1)
call KillUnit(u3)
set u3=LoadUnitHandle(hash,Id,2)
call KillUnit(u3)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
call DestroyEffect(AddSpecialEffect("war3mapImported\\Conflagrate Blue.mdx",x,y))
set count=1
loop
exitwhen count>6
set x2=PolarX(x,300,I2R(count*60))
set y2=PolarY(y,300,I2R(count*60))
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x2,y2))
call DestroyEffect(AddSpecialEffect("war3mapImported\\Conflagrate Blue.mdx",x2,y2))
set count=count+1
endloop
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,355,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg_final,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
set text=CreateTextTagLocBJ("|cFF6496FF"+I2S(R2I(dmg_final)),udg_p,0,14.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,f)
call RemoveLocation(udg_p)
call SaveTextTagHandle(hash,Id,4,text)
call SaveReal(hash,Id,4,dmg_final)
call SaveInteger(hash,Id,1,count)
endif
set t=null
set u=null
set u3=null
set ug=null
set pl=null
set text=null
set f=null
endfunction
```

`Trig_HeroSkills35_Actions`　war3map.j:57413
```jass
elseif Skill=='A0HT' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveReal(hash,Id,1,x)
call SaveReal(hash,Id,2,y)
set u3=CreateUnit(pl,'o025',x,y,GetRandomReal(0,360))
call SaveUnitHandle(hash,Id,1,u3)
set u3=CreateUnit(pl,'o026',x,y,GetRandomReal(0,360))
call SaveUnitHandle(hash,Id,2,u3)
set dmg=200+udg_ItemBonusDMG[n]*0.75
call SaveReal(hash,Id,3,dmg)
call SaveReal(hash,Id,4,dmg)
call SaveUnitHandle(hash,Id,3,u)
call SaveInteger(hash,Id,1,10)
call TimerStart(t,1,true,function Hero35R)
set f=CreateForce()
call ForceAddPlayer(f,pl)
set udg_p=Location(x,y)
set text=CreateTextTagLocBJ("|cFF6496FF"+I2S(R2I(dmg)),udg_p,0,14.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,f)
call RemoveLocation(udg_p)
call SaveTextTagHandle(hash,Id,4,text)
endif
```

`Trig_Hero35DAttack_Actions`　war3map.j:57504
```jass
if GetUnitAbilityLevel(u,'A0HT')==1 then
set a=5
```

## 閃電打擊 `A0H3`　—　吃技能強度

俄文原名：Удар молнии

```
以閃電擊中指定區域，對敵人造成傷害。

傷害：80 +（35% 技能強度）點

冷卻：15 秒
```

每級變動：
  - 第 3 行：80 / 120 / 160 / 200 / 240

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = cripple`, `aare = 250.0`, `acdn = 15.0`, `alev = 5`, `amcs = [90, 100, 110, 120, 130]`, `aran = 750.0`, `atar = air,ground,friend,neutral,self`

實作：

`Trig_HeroSkills35_Actions`　war3map.j:57367
```jass
if Skill=='A0H3' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
call DestroyEffect(AddSpecialEffect("war3mapImported\\Stormfall.mdl",x,y))
set dmg=40+40*GetUnitAbilityLevel(u,Skill)+udg_ItemBonusDMG[n]*0.35
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,255,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
```

## 連鎖引爆 `A0H4`　—　吃技能強度

俄文原名：Цепной подрыв

```
在近距離引爆指定區域。若爆炸擊中了敵人，就會在更遠的距離產生新的爆炸，而新的爆炸還能再引發下一次爆炸。每一次新的爆炸傷害提高 10%。

爆炸傷害：60 +（18% 技能強度）點

冷卻：22 秒
```

每級變動：
  - 第 3 行：60 / 90 / 120 / 150 / 180

物件欄位（原型 `ANcl`）：`Ncl1 = 0.800000011920929`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.800000011920929`, `Ncl5 = 0`, `Ncl6 = blizzard`, `aare = 200.0`, `acdn = 22.0`, `alev = 5`, `amcs = [115, 140, 165, 190, 215]`, `aran = 250.0`, `atar = air,ground,friend,neutral,self`

實作：

`Hero35W`　war3map.j:57216
```jass
function Hero35W takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real x=LoadReal(hash,Id,1)
local real y=LoadReal(hash,Id,2)
local real angle=LoadReal(hash,Id,3)
local real dmg=LoadReal(hash,Id,4)*1.1
local boolean B=false
local unit u3
local group ug
set x=PolarX(x,150,angle)
set y=PolarY(y,150,angle)
call DestroyEffect(AddSpecialEffect("war3mapImported\\Electric Spark - Classic.mdx",x,y))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,205,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
set B=true
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
if B==false then
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
call SaveReal(hash,Id,1,x)
call SaveReal(hash,Id,2,y)
call SaveReal(hash,Id,4,dmg)
endif
set t=null
set u=null
set u3=null
set ug=null
set pl=null
endfunction
```

`Trig_HeroSkills35_Actions`　war3map.j:57383
```jass
elseif Skill=='A0H4' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
call DestroyEffect(AddSpecialEffect("war3mapImported\\Electric Spark - Classic.mdx",x,y))
set dmg=30+30*GetUnitAbilityLevel(u,Skill)+udg_ItemBonusDMG[n]*0.18
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,205,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
set B=true
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
if B==true then
set x2=GetUnitX(u)
set y2=GetUnitY(u)
set x2=AngleXY(x2,y2,x,y)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveReal(hash,Id,1,x)
call SaveReal(hash,Id,2,y)
call SaveReal(hash,Id,3,x2)
call SaveReal(hash,Id,4,dmg)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,0.2,true,function Hero35W)
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 過載 `A0HC`　—　吃技能強度

俄文原名：Перегрузка

```
在 8 秒內，暴風使者的攻擊速度提升 75%，並在攻擊時獲得造成範圍傷害的小機率。

觸發機率：30%
傷害：（150% 智力）+ （20% 技能強度）點

冷卻：28 秒。
```

物件欄位（原型 `Absk`）：`abuf = B020`, `acdn = 28.0`, `adur = 8.0`, `ahdu = 8.0`, `amcs = 70`, `bsk1 = 0.0`, `bsk2 = 0.75`, `bsk3 = 0.0`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof
  - **4** — 受到傷害 −%〔受害者〕DefCof 減去它 → 值越大越耐打；電擊會扣它

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
