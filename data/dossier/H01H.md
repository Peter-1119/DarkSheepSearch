# 長工 `H01H`（Батрак）

主屬性 **力量** · 背包 **6 格** · 解鎖 50000 · 定位 輔助/召喚師

| | 初始 | 每級 |
|---|---|---|
| 力量 | 28 | 3 |
| 敏捷 | 12 | （未覆寫） |
| 智力 | 15 | 1 |

> 圍繞防禦塔運作的英雄，可以躲進碉堡。

**縮放**：吃技能強度的技能 ['A0AE', 'A0C3'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **狀態** —— 走 `Burn_Dmg` 那條，**外面包了 DisableTrigger** → 不吃 DefCof、不帶穿透、被狀態抗性擋。該買的是「狀態傷害 +%」「易燃」「機率倍率」。
- **技能直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且傷害事件數越多，穿透越划算。
- **召喚物** —— 召喚物**不繼承**主人的裝備觸發／狀態／傷害 +%，只吃主人技能公式裡明寫的屬性（通常是最大生命與技能強度）與原生光環。
- **治療** —— 直接寫血量，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。
- **屬性增益** —— 直接改屬性。注意有些是**永久**的（死亡不歸零），長局會滾雪球。

細節見 `data/dossier/_engine.md`。


---

## 廉價建設 `A0C8`

俄文原名：Дешевая застройка

```
建造或升級建築會返還少量金幣。

金幣返還：20 點
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_HeroGiveSkills28_Actions`　war3map.j:55386
```jass
if Skill=='A0C8' then
call SaveInteger(hash,GetHandleId(pl),Skill,GetUnitAbilityLevel(u,Skill))
```

`Trig_HeroBuild28_Conditions`　war3map.j:55399
```jass
function Trig_HeroBuild28_Conditions takes nothing returns boolean
return LoadInteger(hash,GetHandleId(GetOwningPlayer(GetTriggerUnit())),'A0C8')>0
endfunction
function Trig_HeroBuild28_Actions takes nothing returns nothing
local integer n=10+10*LoadInteger(hash,GetHandleId(GetOwningPlayer(GetTriggerUnit())),'A0C8')
set udg_p=GetUnitLoc(GetTriggerUnit())
call AdjustPlayerStateBJ(n,GetOwningPlayer(GetTriggerUnit()),PLAYER_STATE_RESOURCE_GOLD)
call CreateTextTagUnitBJ(("|cFFFFDC00+"+I2S(n)),GetTriggerUnit(),0,10.00,100,100,100,0)
call ShowTextTagForceBJ(false,GetLastCreatedTextTag(),GetPlayersAll())
call ShowTextTagForceBJ(true,GetLastCreatedTextTag(),GetForceOfPlayer(GetOwningPlayer(GetTriggerUnit())))
set udg_TEXT=GetLastCreatedTextTag()
call SetTextTagVelocityBJ(udg_TEXT,75.00,90.00)
call SetTextTagSuspendedBJ(udg_TEXT,false)
call SetTextTagPermanentBJ(udg_TEXT,false)
call SetTextTagLifespanBJ(udg_TEXT,3.00)
call SetTextTagFadepointBJ(udg_TEXT,2.00)
call RemoveLocation(udg_p)
endfunction
```

## 加固城牆 `A0C6`

俄文原名：Укрепление стен

```
提升指定建築的護甲，並緩慢回復其耐久度。持續時間 - 20 秒（特別強大的建築為 10 秒）

護甲加成：5 點
建築耐久度回復：每秒 1%

冷卻：20 秒。
```

每級變動：
  - 第 3 行：5 / 7 / 9 / 11 / 13

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = clusterrockets`, `acdn = 22.0`, `alev = 5`, `amcs = [50, 58, 66, 74, 82]`, `aran = 700.0`, `atar = friend,structure,mechanical`

實作：

`Hero28W`　war3map.j:55034
```jass
function Hero28W takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
local integer count=LoadInteger(hash,GetHandleId(u),'A0C6')
call SetWidgetLife(u,GetWidgetLife(u)+GetUnitState(u,UNIT_STATE_MAX_LIFE)*0.01)
set count=count-1
call SaveInteger(hash,GetHandleId(u),'A0C6',count)
if count<=0 then
call UnitRemoveAbility(u,'A0QH')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
endif
set u=null
set t=null
endfunction
```

`Trig_HeroSkills28_Actions`　war3map.j:55235
```jass
elseif Skill=='A0C6' then
set u2=GetSpellTargetUnit()
if GetUnitAbilityLevel(u2,'A0QH')==0 then
call UnitAddAbility(u2,'A0QH')
call SetUnitAbilityLevel(u2,'A0QH',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
if GetUnitLevel(u2)==5 then
call SaveInteger(hash,GetHandleId(u2),'A0C6',10)
else
call SaveInteger(hash,GetHandleId(u2),'A0C6',20)
endif
call TimerStart(t,1.,true,function Hero28W)
else
call SetUnitAbilityLevel(u2,'A0QH',GetUnitAbilityLevel(u,Skill))
if GetUnitLevel(u2)==5 then
call SaveInteger(hash,GetHandleId(u2),'A0C6',10)
else
call SaveInteger(hash,GetHandleId(u2),'A0C6',20)
endif
endif
```

`Trig_HeroSkills28_Actions`　war3map.j:55259
```jass
if GetUnitAbilityLevel(u,'A0QH')==0 then
call UnitAddAbility(u,'A0QH')
call SetUnitAbilityLevel(u,'A0QH',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,GetHandleId(u),'A0C6',20)
call TimerStart(t,1.,true,function Hero28W)
else
call SetUnitAbilityLevel(u,'A0QH',GetUnitAbilityLevel(u2,Skill))
call SaveInteger(hash,GetHandleId(u),'A0C6',20)
endif
```

## 碉堡 `A0C3`　—　吃技能強度

俄文原名：Бункер

```
英雄躲進碉堡，碉堡可以射擊附近的敵人。碉堡的持續時間無限制，英雄可以自行離開碉堡，或在碉堡被摧毀時離開。

碉堡的生命值：400 + 35% 英雄最大生命值
碉堡的攻擊力：30 + （10% 技能強度）點
碉堡的護甲：2 點

冷卻：60 秒。
```

每級變動：
  - 第 3 行：400 / 600 / 800 / 1000 / 1200
  - 第 4 行：30 / 40 / 50 / 60 / 70
  - 第 5 行：2 / 4 / 6 / 8 / 10

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acap = `, `acdn = 60.0`, `alev = 5`, `amcs = [90, 105, 120, 135, 150]`

呼叫共用引擎函式：`BurnUnit`, `FrostUnit` —— 完整內容見 `_engine.md`。

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55111
```jass
if Skill=='A0C3' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'o01E',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+200+200*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=20+10*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.10)
call SetUnitBaseDamage(u2,count-1)
set count=GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0CD')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A0CE')
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0YA')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0KC')
call SaveReal(hash,GetHandleId(u2),46,1.5)
endif
call SelectUnitForPlayerSingle(u2,pl)
```

`Trig_HeroSkills28_Actions`　war3map.j:55271
```jass
elseif Skill=='A0CD' then
set u2=LoadUnitHandle(hash,GetHandleId(u),1)
set dmg=25.+25.*I2R(GetUnitAbilityLevel(u2,'A0C3'))+udg_ItemBonusDMG[n]*0.15
set x=GetUnitX(u)
set y=GetUnitY(u)
set count=1
loop
exitwhen count>15
set x2=x+490*Cos(24*I2R(count)*bj_DEGTORAD)
set y2=y+490*Sin(24*I2R(count)*bj_DEGTORAD)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Other\\Incinerate\\FireLordDeathExplode.mdl",x2,y2))
set count=count+1
endloop
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,500.,null)
loop
set u=FirstOfGroup(ug)
exitwhen u==null
if UnitAlive(u)and IsUnitEnemy(u,pl)then
call UnitDamageTarget(u2,u,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call BurnUnit(u2,u,dmg*2.0,0.75)
endif
call GroupRemoveUnit(ug,u)
endloop
call DestroyGroup(ug)
elseif Skill=='A0ZS' then
set u2=LoadUnitHandle(hash,GetHandleId(u),1)
set dmg=50.+50.*I2R(GetUnitAbilityLevel(u2,'A0C3'))+udg_ItemBonusDMG[n]*0.30
set x=GetUnitX(u)
set y=GetUnitY(u)
set count=1
loop
exitwhen count>15
set x2=x+490*Cos(24*I2R(count)*bj_DEGTORAD)
set y2=y+490*Sin(24*I2R(count)*bj_DEGTORAD)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Undead\\FrostNova\\FrostNovaTarget.mdl",x2,y2))
set count=count+1
endloop
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,500.,null)
loop
set u=FirstOfGroup(ug)
exitwhen u==null
if UnitAlive(u)and IsUnitEnemy(u,pl)then
call UnitDamageTarget(u2,u,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call FrostUnit(u2,u,1.00)
endif
call GroupRemoveUnit(ug,u)
endloop
call DestroyGroup(ug)
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

## 建造據點 `A04H`

俄文原名：Построить точку

```
立即在指定地點建立一個基礎據點。

冷卻：300 秒
```

物件欄位（原型 `AIbt`）：`Ibl1 = n000,n000,n000,n000`, `acdn = 300.0`, `adur = 1.0`, `aher = 1`, `aite = 0`, `aran = 50.0`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 選擇天賦 `A0QL`

俄文原名：Выбрать талант

```
當玩家的經驗值達到足夠數量時，你可以選擇一項天賦，大幅強化英雄，使其提升至新的力量階級。
```

**天賦選項**：
  - `A0QP` 兵營建造者
    強度等級：T3 屬性加成／每級屬性成長加成： +3 / +1 +0 / +0 +0 / +0  你獲得 2 個用於建造生產據點的地基。
  - `A0QV` 礦坑建造者
    強度等級：T3+ 屬性加成／每級屬性成長加成： +6 / +2 +0 / +0 +2 / +0  你獲得用於建造收益據點的地基。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0QP,A0QV`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55082
```jass
if Skill=='A0QP' then
call SetHeroStr(u,GetHeroStr(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A0QL')
call UnitRemoveAbility(u,'A0ZQ')
call UnitAddAbility(u,'A0QR')
call UnitAddItemById(u,'I07Z')
call UnitAddItemById(u,'I07Z')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0QV' then
call SetHeroStr(u,GetHeroStr(u,false)+6,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A0QL')
call UnitRemoveAbility(u,'A0ZQ')
call UnitAddAbility(u,'A0QW')
call UnitAddItemById(u,'I09C')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 修理 `A0AE`　—　吃技能強度

俄文原名：Ремонтировать

物件欄位（原型 `Ahrp`）：`aran = 65.0`, `aubx = 0`, `auhk = Z`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55111
```jass
if Skill=='A0C3' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'o01E',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+200+200*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=20+10*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.10)
call SetUnitBaseDamage(u2,count-1)
set count=GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0CD')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A0CE')
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0YA')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0KC')
call SaveReal(hash,GetHandleId(u2),46,1.5)
endif
call SelectUnitForPlayerSingle(u2,pl)
elseif Skill=='A0WX' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'nntg',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+200+200*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=40+20*GetUnitAbilityLevel(u,Skill)+GetHeroInt(u,true)+R2I(udg_ItemBonusDMG[n]*0.20)
call SetUnitBaseDamage(u2,count-1)
set count=GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0WY')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A0X0')
call SetUnitLifeRegeneration(u2,GetUnitLifeRegeneration(u)+I2R(GetHeroStr(u,true))*0.05+1.50)
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0YB')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0X1')
endif
call SelectUnitForPlayerSingle(u2,pl)
elseif Skill=='A0HE' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'o02P',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+250+250*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=40+20*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.20)
call SetUnitBaseDamage(u2,count-1)
set count=2+GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0J8')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A06T')
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0IB')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0YC')
endif
call SelectUnitForPlayerSingle(u2,pl)
elseif Skill=='A0ZR' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'uzg2',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+200+200*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=25+25*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.20)
call SetUnitBaseDamage(u2,count-1)
set count=GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0ZS')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A0CE')
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0ZT')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0ZU')
call SaveReal(hash,GetHandleId(u2),28,250.)
endif
call SelectUnitForPlayerSingle(u2,pl)
```

`Trig_HeroKills28_Actions`　war3map.j:55363
```jass
function Trig_HeroKills28_Actions takes nothing returns nothing
local unit u=GetDyingUnit()
local unit hero=LoadUnitHandle(hash,GetHandleId(u),1)
local real x=GetUnitX(u)
local real y=GetUnitY(u)
call ShowUnit(hero,true)
call UnitAddAbility(hero,'A0AE')
call SetUnitInvulnerable(hero,false)
call SetUnitPosition(hero,x,y)
call SelectUnitForPlayerSingle(hero,GetOwningPlayer(hero))
set u=null
set hero=null
endfunction
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 兵營建造者 `A0QP`　—　來自天賦「選擇天賦」

俄文原名：Строитель бараков

```
強度等級：T3
屬性加成／每級屬性成長加成：
+3 / +1
+0 / +0
+0 / +0

你獲得 2 個用於建造生產據點的地基。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acidbomb', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55082
```jass
if Skill=='A0QP' then
call SetHeroStr(u,GetHeroStr(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A0QL')
call UnitRemoveAbility(u,'A0ZQ')
call UnitAddAbility(u,'A0QR')
call UnitAddItemById(u,'I07Z')
call UnitAddItemById(u,'I07Z')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 礦坑建造者 `A0QV`　—　來自天賦「選擇天賦」

俄文原名：Строитель шахт

```
強度等級：T3+
屬性加成／每級屬性成長加成：
+6 / +2
+0 / +0
+2 / +0

你獲得用於建造收益據點的地基。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['faeriefireon', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 32`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55091
```jass
elseif Skill=='A0QV' then
call SetHeroStr(u,GetHeroStr(u,false)+6,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A0QL')
call UnitRemoveAbility(u,'A0ZQ')
call UnitAddAbility(u,'A0QW')
call UnitAddItemById(u,'I09C')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 固守 `A0HE`　—　來自皮膚「森林守護者」

俄文原名：Укрепиться

```
守護者扎根入地並以額外的防護包覆自己，化為類似守衛塔的存在。此技能的持續時間無限制，英雄可以自行離開防護，或在防護被摧毀時離開。

生命值：500 + 35% 英雄 max HP 點
攻擊力：60 +（20% 技能強度）點
護甲：4 點

冷卻：60 秒
```

每級變動：
  - 第 3 行：500 / 750 / 1000 / 1250 / 1500
  - 第 4 行：60 / 80 / 100 / 120 / 140
  - 第 5 行：4 / 6 / 8 / 10 / 12

物件欄位（原型 `ANcl`）：`Ncl1 = 1.659999966621399`, `Ncl3 = 1`, `Ncl4 = 1.659999966621399`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acdn = 60.0`, `alev = 5`, `amcs = [90, 105, 120, 135, 150]`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55173
```jass
elseif Skill=='A0HE' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'o02P',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+250+250*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=40+20*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.20)
call SetUnitBaseDamage(u2,count-1)
set count=2+GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0J8')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A06T')
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0IB')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0YC')
endif
call SelectUnitForPlayerSingle(u2,pl)
```

`Trig_HeroSkills28_Actions`　war3map.j:55321
```jass
elseif Skill=='A0J8' then
set u2=LoadUnitHandle(hash,GetHandleId(u),1)
set dmg=6.+6.*I2R(GetUnitAbilityLevel(u2,'A0HE'))+udg_ItemBonusDMG[n]*0.10
set x=GetUnitX(u)
set y=GetUnitY(u)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,500.,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
set x2=GetUnitX(u2)
set y2=GetUnitY(u2)
set dummy=CreateUnit(pl,'o010',x2,y2,0.00)
call UnitAddAbility(dummy,'A0M3')
call IssueTargetOrderById(dummy,Order_entanglingroots,u2)
call UnitApplyTimedLife(dummy,'BTLF',5.00)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
call SaveReal(hash,Id,1,dmg)
call TimerStart(t,0.25,true,function Hero28QW)
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
endif
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

`Hero28QW`　war3map.j:55050
```jass
function Hero28QW takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u2=LoadUnitHandle(hash,Id,2)
local integer n=GetPlayerId(GetOwningPlayer(u))+1
local real dmg=LoadReal(hash,Id,1)*0.25
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if GetUnitAbilityLevel(u2,'BEer')==0 then
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set t=null
set u=null
set u2=null
endfunction
```

## 再生鱗片 `A0WZ`　—　來自皮膚「小貓」

俄文原名：Регенеративная чешуя

```
提升英雄的生命值回復，並縮短復活時間。

回復加成：+5 點
復活時間縮短：-2 秒
```

每級變動：
  - 第 3 行：5 / 10 / 15 / 20 / 25
  - 第 4 行：2 / 4 / 6 / 8 / 10

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_HeroGiveSkills28_Actions`　war3map.j:55388
```jass
elseif Skill=='A0WZ' then
call SetUnitLifeRegeneration(u,GetUnitLifeRegeneration(u)+5.00)
call SaveReal(hash,GetHandleId(u),2,LoadReal(hash,GetHandleId(u),2)-2.00)
endif
```

## 潮流守衛 `A0WX`　—　來自皮膚「小貓」

俄文原名：Страж течений

```
小貓召喚一座潮流守衛，它可以射擊附近的敵人。此技能的持續時間無限制，英雄可以自行離開建築，或在建築被摧毀時離開。

守衛的生命值：400 + 35% 英雄 max HP 點
守衛的攻擊力：60 +（100% 智力）+（20% 技能強度）點
守衛的護甲：2 點

冷卻：60 秒
```

每級變動：
  - 第 3 行：400 / 600 / 800 / 1000 / 1200
  - 第 4 行：60 / 80 / 100 / 120 / 140
  - 第 5 行：2 / 4 / 6 / 8 / 10

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acdn = 60.0`, `alev = 5`, `amcs = [90, 105, 120, 135, 150]`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55142
```jass
elseif Skill=='A0WX' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'nntg',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+200+200*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=40+20*GetUnitAbilityLevel(u,Skill)+GetHeroInt(u,true)+R2I(udg_ItemBonusDMG[n]*0.20)
call SetUnitBaseDamage(u2,count-1)
set count=GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0WY')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A0X0')
call SetUnitLifeRegeneration(u2,GetUnitLifeRegeneration(u)+I2R(GetHeroStr(u,true))*0.05+1.50)
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0YB')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0X1')
endif
call SelectUnitForPlayerSingle(u2,pl)
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

## 堅冰不摧 `A10E`　—　來自皮膚「寒冰魔像」

俄文原名：Незыблемость льда

```
對自己的建築與機械散發光環，提升它們的戰鬥能力。光環的範圍涵蓋整張地圖。

對攻擊與技能的防護：%；每 100 點技能強度 +1%
攻擊與技能造成的傷害：%；每 100 點技能強度 +1%
```

每級變動：
  - 第 3 行：%; +1% за каждые 100 силы умений / %; +1% за каждые 100 силы умений / %; +1% за каждые 100 силы умений / 0%; +1% за каждые 100 силы умений / 2%; +1% за каждые 100 силы умений
  - 第 4 行：%; +1% за каждые 100 силы умений / %; +1% за каждые 100 силы умений / %; +1% за каждые 100 силы умений / 0%; +1% за каждые 100 силы умений / 2%; +1% за каждые 100 силы умений

物件欄位（原型 `ACua`）：`Uau1 = 0.0`, `Uau2 = 0.0`, `aare = 99999.0`, `abuf = B048`, `aher = 1`, `alev = 5`, `atar = air,player,structure,mechanical,ground`

實作：

`Trig_HeroTakeDamage_Actions`　war3map.j:19851
```jass
if GetUnitAbilityLevel(a,'B048')>=1 then
set L=GetPlayerId(GetOwningPlayer(a))+1
set DefCof=DefCof+(0.02+0.02*I2R(GetUnitAbilityLevel(udg_Hero[L],'A10E'))+udg_ItemBonusDMG[L]*0.01)
endif
if GetUnitAbilityLevel(d,'B048')>=1 then
set L=GetPlayerId(GetOwningPlayer(d))+1
set DefCof=DefCof-(0.02+0.02*I2R(GetUnitAbilityLevel(udg_Hero[L],'A10E'))+udg_ItemBonusDMG[L]*0.01)
endif
```

## 寒冰通靈塔 `A0ZR`　—　來自皮膚「寒冰魔像」

俄文原名：Ледяной зиккурат

```
英雄躲進通靈塔，通靈塔可以射擊附近的敵人。通靈塔的持續時間無限制，英雄可以自行離開通靈塔，或在通靈塔被摧毀時離開。

通靈塔的生命值：400 + 35% 英雄 max HP 點
通靈塔的攻擊力：50 +（20% 技能強度）點
通靈塔的護甲：2 點

冷卻：60 秒
```

每級變動：
  - 第 3 行：400 / 600 / 800 / 1000 / 1200
  - 第 4 行：50 / 75 / 100 / 125 / 150
  - 第 5 行：2 / 4 / 6 / 8 / 10

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acap = `, `acdn = 60.0`, `alev = 5`, `amcs = [90, 105, 120, 135, 150]`

實作：

`Trig_HeroSkills28_Actions`　war3map.j:55202
```jass
elseif Skill=='A0ZR' then
set x=GetUnitX(u)
set y=GetUnitY(u)
call SetUnitInvulnerable(u,true)
call UnitRemoveAbility(u,'A0AE')
call ShowUnit(u,false)
set u2=CreateUnit(pl,'uzg2',x,y,GetUnitFacing(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
call SaveInteger(hash,GetHandleId(u2),11,1)
call TriggerRegisterUnitEvent(gg_trg_HeroSkills28,u2,EVENT_UNIT_SPELL_EFFECT)
set count=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.35)+200+200*GetUnitAbilityLevel(u,Skill)
call SetUnitLife(u2,count)
set count=25+25*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.20)
call SetUnitBaseDamage(u2,count-1)
set count=GetUnitAbilityLevel(u,Skill)*2
call SetUnitExtraArmor(u2,count)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Human\\Thunderclap\\ThunderClapCaster.mdl",x,y))
if udg_ItemBonusDMG[n]>=150 then
call UnitAddAbility(u2,'A0ZS')
endif
if udg_ItemBonusDMG[n]>=300 then
call UnitAddAbility(u2,'A0CE')
endif
if udg_ItemBonusDMG[n]>=500 then
call UnitAddAbility(u2,'A0ZT')
endif
if udg_ItemBonusDMG[n]>=1000 then
call UnitAddAbility(u2,'A0ZU')
call SaveReal(hash,GetHandleId(u2),28,250.)
endif
call SelectUnitForPlayerSingle(u2,pl)
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

---

## 這隻召喚／製造的單位

（技能程式碼裡的 `CreateUnit` 目標。數值取自 war3map.w3u，
沒列出的欄位代表地圖沒覆寫、沿用原型。）

### `nntg` nntg（原型 `None`）
  - 生命 100 ／ 骰子數 1 ／ 骰面 1 ／ 射程 800 ／ 技能 A0CA,A0C5
  - 技能 `A0CA` Закрытые умения　``
  - 技能 `A0C5` Выйти из бункера　`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['cannibalize', 'channel']`, `acdn = [None, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [0.0, 100.0]`, `atar = [None, 'air,ground,debris,enemy,neutral,organic']`

### `o01E` Бункер（原型 `otrb`）
  - 生命 100 ／ 防禦型 fort ／ 骰面 1 ／ 攻擊間隔 0.6000000238418579 ／ 射程 800 ／ 攻擊範圍 1000 ／ 技能 A0CA,A0C5
  - 技能 `A0CA` Закрытые умения　``
  - 技能 `A0C5` Выйти из бункера　`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['cannibalize', 'channel']`, `acdn = [None, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [0.0, 100.0]`, `atar = [None, 'air,ground,debris,enemy,neutral,organic']`

### `o02P` Хранитель лесов（原型 `otrb`）
  - 生命 325 ／ 防禦型 fort ／ 骰面 1 ／ 攻擊間隔 1.7999999523162842 ／ 射程 800 ／ 技能 Aro1,A0CA,A0C5
  - 技能 `Aro1` 　`Roo3 = 1`, `aubx = 0`, `auby = -11`, `auhk = `
  - 技能 `A0CA` Закрытые умения　``
  - 技能 `A0C5` Выйти из бункера　`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['cannibalize', 'channel']`, `acdn = [None, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [0.0, 100.0]`, `atar = [None, 'air,ground,debris,enemy,neutral,organic']`

### `uzg2` Ледяной зиккурат（原型 `None`）
  - 生命 100 ／ 骰面 1 ／ 射程 800 ／ 技能 A0CA,A0C5
  - 技能 `A0CA` Закрытые умения　``
  - 技能 `A0C5` Выйти из бункера　`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['cannibalize', 'channel']`, `acdn = [None, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [0.0, 100.0]`, `atar = [None, 'air,ground,debris,enemy,neutral,organic']`

---

## 皮膚

### 森林守護者 `Ewar` —— **會換技能**
  - 碉堡 `A0C3` → **固守** `A0HE`

### 小貓 `Obla`（預設未開放） —— **會換技能**
  - 廉價建設 `A0C8` → **再生鱗片** `A0WZ`
  - 碉堡 `A0C3` → **潮流守衛** `A0WX`

### 寒冰魔像 `H00L` —— **會換技能**
  - 廉價建設 `A0C8` → **堅冰不摧** `A10E`
  - 碉堡 `A0C3` → **寒冰通靈塔** `A0ZR`

---

## 同一組的其他實作函式

英雄的實作散在同編號的一組函式裡，上面按技能抽取時抓不到的補在這裡
（常見的是決定門檻、結算加成、清理 buff 的那幾支）。

`Trig_HeroKills28_Conditions`　war3map.j:55360
```jass
function Trig_HeroKills28_Conditions takes nothing returns boolean
return GetUnitTypeId(GetDyingUnit())=='o01E' or GetUnitTypeId(GetDyingUnit())=='o02P' or GetUnitTypeId(GetDyingUnit())=='nntg' or GetUnitTypeId(GetDyingUnit())=='uzg2'
endfunction
```

`Trig_HeroBuild28_Actions`　war3map.j:55402
```jass
function Trig_HeroBuild28_Actions takes nothing returns nothing
local integer n=10+10*LoadInteger(hash,GetHandleId(GetOwningPlayer(GetTriggerUnit())),'A0C8')
set udg_p=GetUnitLoc(GetTriggerUnit())
call AdjustPlayerStateBJ(n,GetOwningPlayer(GetTriggerUnit()),PLAYER_STATE_RESOURCE_GOLD)
call CreateTextTagUnitBJ(("|cFFFFDC00+"+I2S(n)),GetTriggerUnit(),0,10.00,100,100,100,0)
call ShowTextTagForceBJ(false,GetLastCreatedTextTag(),GetPlayersAll())
call ShowTextTagForceBJ(true,GetLastCreatedTextTag(),GetForceOfPlayer(GetOwningPlayer(GetTriggerUnit())))
set udg_TEXT=GetLastCreatedTextTag()
call SetTextTagVelocityBJ(udg_TEXT,75.00,90.00)
call SetTextTagSuspendedBJ(udg_TEXT,false)
call SetTextTagPermanentBJ(udg_TEXT,false)
call SetTextTagLifespanBJ(udg_TEXT,3.00)
call SetTextTagFadepointBJ(udg_TEXT,2.00)
call RemoveLocation(udg_p)
endfunction
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **28** — 實數＝冰凍傷害 +%〔施加者〕／整數＝抵抗冰凍旗標〔受害者〕
  - **46** — 易燃效果強化〔施加者〕影響易燃的機率倍率與跳數加成

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
