# 女獵手 `H01G`（Охотница）

主屬性 **敏捷** · 背包 **6 格** · 解鎖 0 · 定位 刺客

| | 初始 | 每級 |
|---|---|---|
| 力量 | 12 | 1.5 |
| 敏捷 | 22 | 3 |
| 智力 | 16 | 2 |

> 遠程英雄，擅長消滅單一目標，可短時間大幅提高攻擊速度。

**縮放**：吃技能強度的技能 無 ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **治療／增益** —— 直接寫數值，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。

細節見 `data/dossier/_engine.md`。


---

## 砲擊 `A08E`

俄文原名：Обстрел

```
啟動後提升英雄的攻擊速度，並賦予暫時的多重射擊。持續時間 - 6 秒。

攻擊速度加成：20%
多重射擊：3 個目標
持續時間：6 秒

冷卻：20 秒
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60
  - 第 4 行：3 / 4 / 5 / 6 / 7

物件欄位（原型 `Absk`）：`abuf = B00X`, `acdn = 20.0`, `adur = 6.0`, `ahdu = 6.0`, `aher = 1`, `alev = 5`, `amcs = [80, 90, 100, 110, 120]`, `bsk1 = 0.0`, `bsk2 = [0.20000000298023224, 0.30000001192092896, 0.4000000059604645, 0.5, 0.6000000238418579]`, `bsk3 = 0.0`

實作：

`HeroQ16_Endbuff`　war3map.j:51258
```jass
function HeroQ16_Endbuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local integer u_Id
local unit u=LoadUnitHandle(hash,Id,1)
local real r
if GetUnitTypeId(u)=='Hpb1' then
set r=LoadReal(hash,Id,1)
set u_Id=GetHandleId(u)
call SaveReal(hash,u_Id,3,LoadReal(hash,u_Id,3)+r)
call SaveReal(hash,u_Id,6,LoadReal(hash,u_Id,6)-r)
endif
call UnitRemoveAbility(u,'A0AN')
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills16_Actions`　war3map.j:51331
```jass
if Skill=='A08E' then
call UnitAddAbility(u,'A0AN')
call SetUnitAbilityLevel(u,'A0AN',lvl)
set t=CreateTimer()
call TimerStart(t,6,true,function HeroQ16_Endbuff)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
if GetUnitTypeId(u)=='Hpb1' then
set r=0.05+0.05*I2R(lvl)
set Id=GetHandleId(u)
call SaveReal(hash,Id,3,LoadReal(hash,Id,3)-r)
call SaveReal(hash,Id,6,LoadReal(hash,Id,6)+r)
call SaveReal(hash,GetHandleId(t),1,r)
endif
```

## 獵人印記 `A08Q`

俄文原名：Метка охотника

```
指定目標會從除了負面狀態以外的任何來源受到更多傷害。

對目標的額外傷害：20%
持續時間：9 秒

冷卻：18 秒
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = charm`, `acdn = 18.0`, `alev = 5`, `amcs = [65, 75, 85, 95, 105]`, `aran = 700.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`HeroW16_Endbuff`　war3map.j:51277
```jass
function HeroW16_Endbuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local real cof=LoadReal(hash,Id,1)
local integer count=LoadInteger(hash,Id,2)-1
local integer i=LoadInteger(hash,GetHandleId(u),'A08Q')
if count==0 or not UnitAlive(u)then
set i=i-1
if i==0 then
call UnitRemoveAbility(u,'A08C')
endif
call SaveInteger(hash,GetHandleId(u),'A08Q',i)
call SaveReal(hash,GetHandleId(u),4,LoadReal(hash,GetHandleId(u),4)+cof)
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
else
call SaveInteger(hash,Id,2,count)
endif
set t=null
set u=null
endfunction
```

`Trig_HeroSkills16_Actions`　war3map.j:51344
```jass
elseif Skill=='A08Q' then
set u2=GetSpellTargetUnit()
set r=0.1+(0.1*I2R(GetUnitAbilityLevel(u,Skill)))+I2R(LoadInteger(hash,GetHandleId(u),'TALE'))*0.01
call UnitAddAbility(u2,'A08C')
call SaveReal(hash,GetHandleId(u2),4,LoadReal(hash,GetHandleId(u2),4)-r)
set t=CreateTimer()
call TimerStart(t,1.,true,function HeroW16_Endbuff)
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
call SaveReal(hash,Id,1,r)
call SaveInteger(hash,Id,2,9)
call SaveInteger(hash,GetHandleId(u2),'A08Q',LoadInteger(hash,GetHandleId(u2),'A08Q')+1)
elseif Skill=='A0RU' then
set u2=GetSpellTargetUnit()
set r=0.175+(0.175*I2R(GetUnitAbilityLevel(u,Skill)))+I2R(LoadInteger(hash,GetHandleId(u),'TALE'))*0.01
call UnitAddAbility(u2,'A08C')
call SaveReal(hash,GetHandleId(u2),4,LoadReal(hash,GetHandleId(u2),4)-r)
set t=CreateTimer()
call TimerStart(t,1.,true,function HeroW16_Endbuff)
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
call SaveReal(hash,Id,1,r)
call SaveInteger(hash,Id,2,9)
call SaveInteger(hash,GetHandleId(u2),'A08Q',LoadInteger(hash,GetHandleId(u2),'A08Q')+1)
set x=GetUnitX(u2)
set y=GetUnitY(u2)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,205.,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)and not IsUnitType(u2,UNIT_TYPE_STRUCTURE)then
set r=0.1+(0.1*I2R(GetUnitAbilityLevel(u,Skill)))+I2R(LoadInteger(hash,GetHandleId(u),'TALE'))*0.01
call UnitAddAbility(u2,'A08C')
call SaveReal(hash,GetHandleId(u2),4,LoadReal(hash,GetHandleId(u2),4)-r)
set t=CreateTimer()
call TimerStart(t,1.,true,function HeroW16_Endbuff)
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
call SaveReal(hash,Id,1,r)
call SaveInteger(hash,Id,2,9)
call SaveInteger(hash,GetHandleId(u2),'A08Q',LoadInteger(hash,GetHandleId(u2),'A08Q')+1)
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
endif
```

## 毒藥 `A08B`

俄文原名：Яд

```
以毒藥毒害敵人，每秒造成傷害，並降低其移動速度與攻擊速度。毒藥持續時間 - 4 秒（英雄 2 秒）

每秒傷害：6 點
移動速度減速：20%。
攻擊速度減速：12%。
```

每級變動：
  - 第 3 行：6 / 12 / 18 / 24 / 30
  - 第 4 行：20 / 22 / 24 / 26 / 28
  - 第 5 行：12 / 13 / 14 / 15 / 16

物件欄位（原型 `Aspo`）：`Spo1 = [6.0, 12.0, 18.0, 24.0, 30.0]`, `Spo2 = [0.20000000298023224, 0.2199999988079071, 0.23999999463558197, 0.25999999046325684, 0.2800000011920929]`, `Spo3 = [0.11999999731779099, 0.12999999523162842, 0.14000000059604645, 0.15000000596046448, 0.1600000113248825]`, `Spo4 = [None, 1]`, `abuf = [None, 'Bspo,Bssd']`, `adur = 4.0`, `ahdu = 2.0`, `aher = 1`, `alev = 5`, `atar = air,ground,organic`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 精通 `A08H`

俄文原名：Мастерство

```
生命值高於 50% 時，英雄的攻擊有機率造成提高的傷害。

爆擊機率：20%
爆擊傷害：x4
```

物件欄位（原型 `Amgl`）：`aher = 1`

實作：

`Trig_HeroR16_Conditions`　war3map.j:51469
```jass
function Trig_HeroR16_Conditions takes nothing returns boolean
return GetLearnedSkill()=='A08H'
endfunction
function HeroR16_check takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer n=GetPlayerId(GetOwningPlayer(u))+1
if UnitAlive(u)then
if UnitLifePercent(u)>50.00 then
if GetUnitAbilityLevel(u,'A0RN')!=1 then
call UnitAddAbility(u,'A0RN')
endif
else
if GetUnitAbilityLevel(u,'A0RN')==1 then
call UnitRemoveAbility(u,'A0RN')
endif
endif
endif
set t=null
set u=null
endfunction
function Trig_HeroR16_Actions takes nothing returns nothing
local timer t=CreateTimer()
local integer Id=GetHandleId(t)
local unit u=GetLearningUnit()
call TimerStart(t,1,true,function HeroR16_check)
call SaveUnitHandle(hash,Id,1,u)
set t=null
set u=null
endfunction
```

## 倒鉤箭 `A0RP`

俄文原名：Зазубренные стрелы

```
女獵手對敵方英雄造成的傷害提高 15%。擊殺敵方英雄可使此加成再提高 2%，並額外提升 2 點攻擊力。輸入指令「-i」可顯示已累積的加成。
```

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 選擇天賦 `A08D`

俄文原名：Выбрать талант

```
當玩家經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的力量階級。
```

**天賦選項**：
  - `A0RQ` 巫術之箭
    強度等級：T2 屬性加成／每級屬性成長加成： +0 / +0 +2 / +1 +1 / +0  降低 20% 裝備技能冷卻。每 20 次擊殺可使裝備技能威力提高 2%。累積加成達到 100% 後，裝備技能威力的累積量降為 1%。
  - `A0RS` 獵殺印記
    強度等級：T3 屬性加成／每級屬性成長加成： +1 / +0 +4 / +2 +1 / +0  「獵人印記」的效果提高 75%，但需要多消耗 50% 的法力。此外「獵人印記」還會額外影響目標附近的敵人，對他們施加未強化的基礎版印記。  被女獵手擊殺、且處於印記效果下的敵方英雄，可提供 2 倍的「倒鉤箭」加成。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0RQ,A0RS`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills16_Actions`　war3map.j:51312
```jass
if Skill=='A0RQ' then
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A08D')
call UnitAddAbility(u,'A0RR')
set r=LoadReal(hash,GetHandleId(u),1)
set r=r-0.20
call SaveReal(hash,GetHandleId(u),1,r)
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0RS' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+4,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',2)
call UnitRemoveAbility(u,'A08D')
call UnitAddAbility(u,'A0RT')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 巫術之箭 `A0RQ`　—　來自天賦「選擇天賦」

俄文原名：Колдовские стрелы

```
強度等級：T2
屬性加成／每級屬性成長加成：
+0 / +0
+2 / +1
+1 / +0

降低 20% 裝備技能冷卻。每 20 次擊殺可使裝備技能威力提高 2%。累積加成達到 100% 後，裝備技能威力的累積量降為 1%。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acidbomb', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 15`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills16_Actions`　war3map.j:51312
```jass
if Skill=='A0RQ' then
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A08D')
call UnitAddAbility(u,'A0RR')
set r=LoadReal(hash,GetHandleId(u),1)
set r=r-0.20
call SaveReal(hash,GetHandleId(u),1,r)
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 獵殺印記 `A0RS`　—　來自天賦「選擇天賦」

俄文原名：Метка убийцы

```
強度等級：T3
屬性加成／每級屬性成長加成：
+1 / +0
+4 / +2
+1 / +0

「獵人印記」的效果提高 75%，但需要多消耗 50% 的法力。此外「獵人印記」還會額外影響目標附近的敵人，對他們施加未強化的基礎版印記。

被女獵手擊殺、且處於印記效果下的敵方英雄，可提供 2 倍的「倒鉤箭」加成。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acolyteharvest', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills16_Actions`　war3map.j:51322
```jass
elseif Skill=='A0RS' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+4,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',2)
call UnitRemoveAbility(u,'A08D')
call UnitAddAbility(u,'A0RT')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 皮膚

純外觀：追獵者、遊俠

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof
  - **4** — 受到傷害 −%〔受害者〕DefCof 減去它 → 值越大越耐打；電擊會扣它
  - **6** — 造成傷害 +%〔攻擊者〕；電擊會扣它 → 目標輸出下降

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
