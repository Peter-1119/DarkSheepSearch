# 美杜莎 `N00Z`（Медуза）

主屬性 **敏捷** · 背包 **6 格** · 解鎖 0 · 定位 刺客/詛咒者

| | 初始 | 每級 |
|---|---|---|
| 力量 | 17 | 1.0 |
| 敏捷 | 22 | 3.0 |
| 智力 | 16 | 2.0 |

> 遠程英雄，能降低單體防禦並擾亂敵方隊伍。

**縮放**：吃技能強度的技能 ['A035', 'A037'] ／ ◈ 吃裝備技能威力 ['A034'] ／ ⊕ 給裝備技能威力 無

---

## 腐蝕 `A035`　—　吃技能強度

俄文原名：Коррозия

```
降低指定目標的護甲數值。

護甲降低：3 + （2% 技能強度）點
持續時間（部隊）：12 秒。
持續時間（英雄）：6 秒。

冷卻：13 秒。
```

每級變動：
  - 第 3 行：3 / 6 / 9 / 12 / 15

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = channel`, `acdn = 13.0`, `alev = 5`, `amcs = [50, 60, 70, 80, 90]`, `aran = 700.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`SetUnitExtraArmor`　war3map.j:3846
```jass
function SetUnitExtraArmor takes unit u,integer a returns nothing
local integer Id=GetHandleId(u)
local integer p=10
local integer i=0
local integer r
if a>2047 then
set a=2047
endif
loop
call UnitRemoveAbility(u,setAttribute___abilityAddArmor[i])
call UnitRemoveAbility(u,setAttribute___abilityRemoveArmor[i])
exitwhen i==10
set i=i+1
endloop
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityAddArmor[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
elseif a<0 then
set r=-a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityRemoveArmor[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
endif
call SaveInteger(hash,Id,35,a)
endfunction
```

`Hero5Q`　war3map.j:47838
```jass
function Hero5Q takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer count=LoadInteger(hash,Id,3)-1
if count<=0 then
call DestroyEffect(LoadEffectHandle(hash,Id,2))
call SetUnitExtraArmor(u,GetUnitExtraArmor(u)+LoadInteger(hash,Id,2))
call PauseTimer(t)
call FlushChildHashtable(hash,Id)
call DestroyTimer(t)
else
call SaveInteger(hash,Id,3,count)
endif
set u=null
set t=null
endfunction
```

`Trig_HeroSkills5_Actions`　war3map.j:47898
```jass
elseif Skill=='A035' then
set u2=GetSpellTargetUnit()
set n2=3*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.02)
call SetUnitExtraArmor(u2,GetUnitExtraArmor(u2)-n2)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
call SaveInteger(hash,Id,2,n2)
call SaveEffectHandle(hash,Id,2,AddSpecialEffectTarget("war3mapImported\\Armor Penetration Blue.mdx",u2,"overhead"))
if IsUnitType(u2,UNIT_TYPE_HERO)==true then
call SaveInteger(hash,Id,3,6)
else
call SaveInteger(hash,Id,3,12)
endif
call TimerStart(t,1.,true,function Hero5Q)
```

## 美杜莎凝視 `A037`　—　吃技能強度

俄文原名：Взгляд медузы

```
移除指定範圍內敵人身上的所有正面效果，造成傷害，並有小機率對敵人施加虛弱。

作用範圍：200 點
造成的傷害：20 + （75% 技能強度）點
附加效果：30% 機率施加虛弱，30% 機率暈眩 1 秒。

冷卻：20 秒。
```

每級變動：
  - 第 3 行：200 / 250 / 300 / 350 / 400
  - 第 4 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = charm`, `aare = [200.0, 250.0, 300.0, 350.0, 400.0]`, `acdn = 20.0`, `alev = 5`, `amcs = [70, 80, 90, 100, 110]`, `aran = 700.0`, `atar = air,ground,friend,neutral,self`

實作：

`WeakUnit`　war3map.j:2398
```jass
function WeakUnit takes unit damager,unit target,real chanse returns nothing
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,GetHandleId(target),44)>0 then
set chanse=0.
endif
if LoadInteger(hash,GetHandleId(target),30)>0 then
if LoadInteger(hash,GetHandleId(target),30)>50 then
set chanse=0.
endif
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(target,'B043')==1 then
set t=LoadTimerHandle(hash,GetHandleId(target),'B043')
call TimerStart(t,8.,false,function RemoveWeak)
else
call UnitAddAbility(target,'S00N')
call SaveReal(hash,GetHandleId(target),49,LoadReal(hash,GetHandleId(target),49)-0.50)
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,GetHandleId(target),'B043',t)
call TimerStart(t,8.,false,function RemoveWeak)
endif
if UnitHasItemOfType(damager,'I044')then
call UnitDamageTarget(damager,target,I2R(GetHeroAgi(damager,true)),false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\SoulRitual.mdx",target,"origin"))
endif
if LoadInteger(hash,GetHandleId(damager),'I00Y')>=1 then
call FrostUnit(damager,target,0.50)
endif
endif
set t=null
endfunction
```

`VulnerabilityUnit`　war3map.j:2519
```jass
function VulnerabilityUnit takes unit damager,unit target,real chanse returns nothing
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,GetHandleId(target),44)>0 then
set chanse=0.
endif
if LoadInteger(hash,GetHandleId(target),30)>0 then
if LoadInteger(hash,GetHandleId(target),30)>50 then
set chanse=0.
endif
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(target,'B045')==1 then
set t=LoadTimerHandle(hash,GetHandleId(target),'B045')
call TimerStart(t,8.,false,function RemoveVulnerability)
else
call UnitAddAbility(target,'S014')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,GetHandleId(target),'B045',t)
call TimerStart(t,8.,false,function RemoveVulnerability)
endif
if LoadInteger(hash,GetHandleId(damager),'I00Y')>=1 then
call FrostUnit(damager,target,0.50)
endif
endif
set t=null
endfunction
```

`StunUnit`　war3map.j:2756
```jass
function StunUnit takes unit attacker,unit target,integer seconds,real chanse returns nothing
local integer random
local integer chanse_random
local unit u
local player pl=GetOwningPlayer(attacker)
local timer t=CreateTimer()
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random and IsUnitEnemy(target,pl)and UnitAlive(target)then
set u=CreateUnit(pl,'o010',GetUnitX(target),GetUnitY(target),0.)
call UnitAddAbility(u,'A0Y8')
call SetUnitAbilityLevel(u,'A0Y8',seconds)
call IssueTargetOrder(u,"creepthunderbolt",target)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
call TimerStart(t,2.,false,function RemoveDummy)
endif
set u=null
set pl=null
set t=null
endfunction
```

`Trig_HeroSkills5_Actions`　war3map.j:47913
```jass
elseif Skill=='A037' then
set dmg=10.+10.*GetUnitAbilityLevel(u,Skill)+udg_ItemBonusDMG[n]*0.75
set aoe=155.+50.*GetUnitAbilityLevel(u,Skill)
set x=GetLocationX(GetSpellTargetLoc())
set y=GetLocationY(GetSpellTargetLoc())
set ug=CreateGroup()
if GetUnitAbilityLevel(u,'A0U8')==1 then
call GroupEnumUnitsInRange(ug,x,y,aoe,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\DispelMagic\\DispelMagicTarget.mdl",u3,"origin"))
if GetUnitAbilityLevel(u3,'A02A')!=1 then
call UnitRemoveBuffs(u3,true,false)
endif
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call VulnerabilityUnit(u,u3,1.00)
call WeakUnit(u,u3,0.30)
call StunUnit(u,u3,1,0.30)
endif
call GroupRemoveUnit(ug,u3)
endloop
else
call GroupEnumUnitsInRange(ug,x,y,aoe,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\DispelMagic\\DispelMagicTarget.mdl",u3,"origin"))
if GetUnitAbilityLevel(u3,'A02A')!=1 then
call UnitRemoveBuffs(u3,true,false)
endif
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call WeakUnit(u,u3,0.30)
call StunUnit(u,u3,1,0.30)
endif
call GroupRemoveUnit(ug,u3)
endloop
endif
call DestroyGroup(ug)
endif
```

## 精湛射術 `A034`　—　◈ 吃裝備技能威力

俄文原名：Искусная стрельба

```
繼承裝備技能的加成。

英雄的攻擊會呈扇形射出箭矢，每支箭矢對一名敵人造成傷害並施加冰凍效果。

箭矢數量：3 支
每支箭矢的傷害：24 點
冰凍效果：50% 機率

冷卻：8 秒。
```

每級變動：
  - 第 6 行：24 / 36 / 48 / 60 / 72

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`StartModCooldown`　war3map.j:2914
```jass
function StartModCooldown takes integer u_Id,integer i_Id,real CD returns nothing
local real CDCof=LoadReal(hash,u_Id,1)
local timer t=CreateTimer()
if CDCof<0.20 then
set CDCof=0.20
endif
set CD=CD*CDCof
call SaveReal(hash,u_Id,i_Id,1.)
set t=CreateTimer()
call SaveInteger(hash,GetHandleId(t),1,u_Id)
call SaveInteger(hash,GetHandleId(t),2,i_Id)
call TimerStart(t,CD,false,function EndModCooldown)
set t=null
endfunction
```

`CreateProjectile`　war3map.j:3071
```jass
function CreateProjectile takes unit u,integer dummy_Id,real speed,real dist,real x,real y,real angle,real dmg,real aoe,real size,string eff,string eff2 returns nothing
local timer t=CreateTimer()
local integer Id=GetHandleId(t)
local unit u2
local player pl=GetOwningPlayer(u)
if eff !="none" then
call DestroyEffect(AddSpecialEffect(eff,x,y))
endif
set u2=CreateUnit(pl,dummy_Id,x,y,angle)
call SetUnitX(u2,x)
call SetUnitY(u2,y)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
call SaveReal(hash,Id,1,dmg)
call SaveReal(hash,Id,2,aoe)
call SaveInteger(hash,Id,2,-3)
call SaveReal(hash,Id,3,speed)
call SaveReal(hash,Id,4,dist)
call SaveReal(hash,Id,5,angle)
call SaveReal(hash,Id,6,0.)
call SaveReal(hash,Id,7,size)
call SaveStr(hash,Id,1,eff2)
call TimerStart(t,0.03,true,function ProjectileMove)
set t=null
set u2=null
set pl=null
endfunction
```

`Trig_HeroGiveSkills5_Conditions`　war3map.j:47967
```jass
function Trig_HeroGiveSkills5_Conditions takes nothing returns boolean
return GetLearnedSkill()=='A034'
endfunction
function Trig_HeroGiveSkills5_Actions takes nothing returns nothing
local unit u=GetLearningUnit()
call SetPlayerTechResearched(GetOwningPlayer(u),'R000',GetUnitAbilityLevel(u,'A034'))
set u=null
endfunction
```

`Trig_HeroAttacks5_Actions`　war3map.j:47981
```jass
function Trig_HeroAttacks5_Actions takes nothing returns nothing
local unit u=GetAttacker()
local integer u_Id=GetHandleId(u)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u2=GetTriggerUnit()
local integer i
local real dmg
local real cof
local real x
local real y
local real x2
local real y2
local integer lvl=GetUnitAbilityLevel(u,'A034')
local real angle
local real skill_angle
local real r
local integer count
if lvl>0 then
if LoadReal(hash,u_Id,'A034')==0. then
call StartModCooldown(u_Id,'A034',8.)
set cof=LoadReal(hash,u_Id,18)+1.
set dmg=(12.+12.*I2R(lvl))*cof
set x=GetUnitX(u)
set y=GetUnitY(u)
set x2=GetUnitX(u2)
set y2=GetUnitY(u2)
set angle=AngleXY(x,y,x2,y2)
set x=PolarX(x,30.,angle)
set y=PolarY(y,30.,angle)
if GetUnitAbilityLevel(u,'A0U8')==1 then
set count=7
set skill_angle=50.
set r=4.
set angle=angle-(skill_angle/I2R(count)*r)
set skill_angle=skill_angle/I2R(count)
else
set count=3
set skill_angle=32.
set r=2.
set angle=angle-(skill_angle/I2R(count)*r)
set skill_angle=skill_angle/I2R(count)
endif
set i=0
loop
set i=i+1
set angle=angle+skill_angle
call CreateProjectile(u,'o002',33.,1500.,x,y,angle,dmg,35.,35.,"none","none")
exitwhen i==count
endloop
endif
endif
set u=null
set pl=null
set u2=null
endfunction
```

## 壓制咒語 `A032`

俄文原名：Заклятие подавления

```
作用範圍內所有敵方單位失去施放法術的能力，並受到極強的減速。

減速效果：-80% 移動速度與攻擊速度
作用範圍：400 點。
持續時間：15 秒（英雄 5 秒）。

冷卻：80 秒。
```

物件欄位（原型 `ANsi`）：`Nsi3 = -0.800000011920929`, `Nsi4 = -0.800000011920929`, `aare = 400.0`, `acdn = 80.0`, `adur = 15.0`, `ahdu = 5.0`, `alev = 1`, `amcs = 175`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 蛇之敏捷 `A033`

俄文原名：Змеиная ловкость

```
短時間內提升英雄的敏捷。

敏捷加成：+100%
持續時間：15 秒。

冷卻：30 秒。
```

物件欄位（原型 `Absk`）：`abuf = B00N`, `adur = 15.0`, `ahdu = 15.0`, `amcs = 65`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Hero5Agi`　war3map.j:47826
```jass
function Hero5Agi takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer n=LoadInteger(hash,Id,1)
local integer agi=GetHeroAgi(u,false)
call SetHeroAgi(u,agi-n,true)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills5_Actions`　war3map.j:47890
```jass
if Skill=='A033' then
set t=CreateTimer()
set Id=GetHandleId(t)
set agi=GetHeroAgi(u,false)
call SetHeroAgi(u,agi*2,true)
call SaveInteger(hash,Id,1,agi)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,15.,false,function Hero5Agi)
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 選擇天賦 `A0U3`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，使英雄大幅強化並提升至新的力量位階。
```

**天賦選項**：
  - `A0U5` 磨利之箭
    強度等級：T2 屬性加成／每級屬性成長加成： +1 / +0 +1 / +1 +1 / +0  「精湛射術」的箭矢會額外以 100% 機率施加流血效果，最終傷害為 150%。
  - `A0U7` 蛇髮女妖
    強度等級：T3 屬性加成／每級屬性成長加成： +1 / +0 +3 / +1 +2 / +1  「美杜莎凝視」會額外以 100% 機率對敵人施加易傷。  「精湛射術」發射 7 支箭矢而非 3 支。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0U5,A0U7`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills5_Actions`　war3map.j:47872
```jass
if Skill=='A0U5' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0U3')
call UnitAddAbility(u,'A0A2')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0U7' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+3,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A0U3')
call UnitAddAbility(u,'A0U8')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 皮膚

純外觀：海之獵手

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **3** — 對英雄傷害 +%
  - **4** — 受到傷害 −%（被減的）
  - **5** — 對 0-1 級敵人傷害 +%
  - **6** — 造成傷害 +%
  - **18** — 裝備技能威力
  - **35** — 額外護甲
  - **44** — （狀態免疫旗標）
  - **49** — 流血抗性

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
