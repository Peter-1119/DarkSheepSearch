# 狂徒 `N05T`（Головорез）

主屬性 **敏捷** · 背包 **6 格** · 解鎖 50000 · 定位 刺客

| | 初始 | 每級 |
|---|---|---|
| 力量 | 19 | 2 |
| 敏捷 | 24 | 3 |
| 智力 | 16 | 1.5 |

> 著魔的刺客，攻擊與移動速度都很高，能累積力量。

**縮放**：吃技能強度的技能 ['A0J2', 'A0T2', 'A0T5'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **技能直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且傷害事件數越多，穿透越划算。
- **普攻** —— 有「攻擊時觸發」的機制 → 攻擊力／攻速／穿透有價值，但注意那類技能常有自己的內部冷卻，攻速超過內冷就沒用了。
- **屬性增益** —— 直接改屬性。注意有些是**永久**的（死亡不歸零），長局會滾雪球。

細節見 `data/dossier/_engine.md`。


---

## 致殘投擲 `A0T5`　—　吃技能強度

俄文原名：Увечащий бросок

```
向指定的敵人投擲斧頭，造成傷害並降低目標的護甲、攻擊速度與移動速度。

傷害：50 + （65% 技能強度）點
護甲降低：2 點
攻擊速度降低：15%
移動速度降低：30%
持續時間：25 秒（英雄 10 秒）

冷卻：10 秒。
```

每級變動：
  - 第 3 行：50 / 80 / 110 / 140 / 170
  - 第 4 行：2 / 3 / 4 / 5 / 6
  - 第 5 行：15 / 20 / 25 / 30 / 35
  - 第 6 行：30 / 40 / 50 / 60 / 70

物件欄位（原型 `ANab`）：`Nab1 = [-0.30000001192092896, -0.4000000059604645, -0.5, -0.6000000238418579, -0.699999988079071]`, `Nab2 = [-0.15000000596046448, -0.20000000298023224, -0.25, -0.30000001192092896, -0.3499999940395355]`, `Nab3 = [2, 3, 4, 5, 6]`, `Nab4 = 0.0`, `Nab5 = 0.0`, `Nab6 = 20.0`, `aare = 5.0`, `abuf = B035`, `acdn = 10.0`, `adur = 25.0`, `ahdu = 10.0`, `alev = 5`, `amac = 0.10000000149011612`, `amat = war3mapImported\WarchiefAxeMissile.mdx`, `amcs = [58, 67, 76, 85, 94]`, `amsp = 1000`, `aran = 600.0`

實作：

`Hero40Q`　war3map.j:58787
```jass
function Hero40Q takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u2=LoadUnitHandle(hash,Id,2)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real dmg=20+30*GetUnitAbilityLevel(u,'A0T5')+udg_ItemBonusDMG[n]*0.65
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
set u2=null
set pl=null
endfunction
```

`Trig_HeroSkills40_Actions`　war3map.j:58822
```jass
elseif Skill=='A0T5' then
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
set x=GetUnitX(u)
set y=GetUnitY(u)
set x2=GetUnitX(u2)
set y2=GetUnitY(u2)
set x=x2-x
set y=y2-y
set x=SquareRoot(x*x+y*y)/1000
call TimerStart(t,x,false,function Hero40Q)
endif
```

## 狩獵季 `A0T4`

俄文原名：Сезон охоты

```
連續擊殺會為英雄帶來加成。加成會依序觸發，在最後一項加成之後計數會重置，擊殺累積從零重新開始。

擊殺 6 名敵人：回復 8% 生命值
擊殺 12 名敵人：攻擊力 +1 點
擊殺 18 名敵人：敏捷 +1 點
```

物件欄位（原型 `Amgl`）：`aher = 1`

實作：

`Trig_HeroKills40_Actions`　war3map.j:58860
```jass
if GetUnitAbilityLevel(u,'A0T4')>0 and IsUnitAlly(u2,pl)==false then
set count=LoadInteger(hash,Id,21)+1
if count==6 then
set life=GetUnitState(u,UNIT_STATE_LIFE)
set heal=(GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.08
call SetUnitState(u,UNIT_STATE_LIFE,life+heal)
set text=CreateTextTagUnitBJ("|cFFFF4B398% heal|r",u,0,12.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,GetForceOfPlayer(pl))
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
elseif count==12 then
call SetUnitBaseDamage(u,GetUnitBaseDamage(u)+1)
set text=CreateTextTagUnitBJ("|cFFFF4B39+1 attack|r",u,0,12.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,GetForceOfPlayer(pl))
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
elseif count==18 then
set count=GetHeroAgi(u,false)
call SetHeroAgi(u,count+1,true)
set count=0
set text=CreateTextTagUnitBJ("|cFFFF4B39+1 agi|r",u,0,12.00,100,100,100,0)
call ShowTextTagForceBJ(false,text,bj_FORCE_ALL_PLAYERS)
call ShowTextTagForceBJ(true,text,GetForceOfPlayer(pl))
call SetTextTagVelocityBJ(text,75.00,90.00)
call SetTextTagSuspended(text,false)
call SetTextTagPermanent(text,false)
call SetTextTagLifespan(text,3.00)
call SetTextTagFadepoint(text,2.00)
endif
call SaveInteger(hash,Id,21,count)
endif
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

## 侵略者 `A0T2`　—　吃技能強度

俄文原名：Агрессор

```
被動效果：

英雄對近戰敵人造成反傷。

反傷：12 + （5% 技能強度）點

啟動時：

提高此技能的反傷，並使攻擊獲得吸血效果。

反傷提升：+60%
吸血效果：26%
持續時間：6 秒。

冷卻：19 秒。
```

每級變動：
  - 第 5 行：12 / 18 / 24 / 30 / 36
  - 第 11 行：60 / 90 / 120 / 150 / 180
  - 第 12 行：26 / 32 / 38 / 44 / 50

物件欄位（原型 `Absk`）：`abuf = B034`, `acdn = 19.0`, `adur = 6.0`, `ahdu = 6.0`, `aher = 1`, `alev = 5`, `amcs = [65, 73, 81, 89, 97]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Trig_HeroSkills40_Actions`　war3map.j:58814
```jass
if Skill=='A0T2' then
call UnitAddAbility(u,'A0T3')
call SetUnitAbilityLevel(u,'A0T3',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,2,'A0T3')
call TimerStart(t,6,false,function RemoveBuff)
```

`RemoveBuff`　war3map.j:2875
```jass
function RemoveBuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer Buff=LoadInteger(hash,Id,2)
call UnitRemoveAbility(u,Buff)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

## 失控之怒 `A0J2`　—　吃技能強度

俄文原名：Неконтролируемая ярость

```
英雄會週期性陷入狂怒，期間攻擊速度以及對非英雄敵人的傷害都會提升。狂怒期間，狂徒受到的傷害也會提高。

攻擊速度加成：30%
對非英雄的傷害加成：20%
受到的傷害：15%
持續時間：6 秒。

冷卻：8 秒。
```

每級變動：
  - 第 3 行：30 / 40 / 50 / 60 / 70
  - 第 4 行：20 / 30 / 40 / 50 / 60
  - 第 5 行：15 / 20 / 25 / 30 / 35

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_HeroAttack40_Actions`　war3map.j:58950
```jass
if GetUnitAbilityLevel(u,'A0J2')>0 then
set lvl=LoadInteger(hash,Id_u,20)
if lvl==0 then
call SaveInteger(hash,Id_u,20,1)
set lvl=GetUnitAbilityLevel(u,'A0J2')
set t=CreateTimer()
set Id=GetHandleId(t)
call TimerStart(t,6,false,function Hero40E)
call SetUnitVertexColor(u,255,100,100,255)
call UnitAddAbility(u,'A0T0')
call SetUnitAbilityLevel(u,'A0T0',lvl)
set dmg=0.10+0.10*I2R(lvl)
call SaveReal(hash,Id_u,6,LoadReal(hash,Id_u,6)+dmg)
call SaveReal(hash,Id_u,3,LoadReal(hash,Id_u,3)-dmg)
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,dmg)
set dmg=0.10+0.05*I2R(lvl)
call SaveReal(hash,Id_u,4,LoadReal(hash,Id_u,4)-dmg)
call SaveReal(hash,Id,2,dmg)
set t2=CreateTimer()
set Id=GetHandleId(t2)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t2,14,false,function Hero40E_Cd)
endif
endif
```

`Trig_HeroAttack40_Alt_Actions`　war3map.j:58989
```jass
function Trig_HeroAttack40_Alt_Actions takes nothing returns nothing
local unit u=GetTriggerUnit()
local unit u2=GetAttacker()
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local integer lvl=GetUnitAbilityLevel(u,'A0J2')
local real dmg
if lvl>0 then
if IsUnitType(u2,UNIT_TYPE_MELEE_ATTACKER)then
set dmg=6+6*I2R(lvl)+udg_ItemBonusDMG[n]*0.05
if GetUnitAbilityLevel(u,'B034')>0 then
set dmg=dmg*(1.30+0.30*I2R(lvl))
endif
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_NORMAL,WEAPON_TYPE_WHOKNOWS)
endif
endif
set u=null
set u2=null
set pl=null
endfunction
```

`Hero40E`　war3map.j:58914
```jass
function Hero40E takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer Id_u=GetHandleId(u)
local real r=LoadReal(hash,Id,1)
local real r2=LoadReal(hash,Id,2)
call SetUnitVertexColor(u,255,255,255,255)
call UnitRemoveAbility(u,'A0T0')
call SaveReal(hash,Id_u,6,LoadReal(hash,Id_u,6)-r)
call SaveReal(hash,Id_u,3,LoadReal(hash,Id_u,3)+r)
call SaveReal(hash,Id_u,4,LoadReal(hash,Id_u,4)+r2)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
function Hero40E_Cd takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
call SaveInteger(hash,GetHandleId(u),20,0)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

## 捕獵者 `A0T1`

俄文原名：Ловчий

```
英雄以網捕捉敵人，使其無法移動。受「捕獵者」影響的敵人受到英雄的傷害提高。效果不可疊加。

對被捕捉目標的額外傷害：+25%
持續時間：9 秒
持續時間（英雄）：4 秒

冷卻：20 秒
```

物件欄位（原型 `Aens`）：`abuf = B033`, `acdn = 20.0`, `adur = 9.0`, `ahdu = 4.0`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

---

## 以「單位型號」內聯的實作

這幾段不是靠技能 ID 分派的，而是直接用單位型號 `N05T` 寫在共用函式的條件式裡
（常見於寫進傷害管線的被動）。照技能抽取抓不到，所以單獨列出來。

`Trig_HeroTakeDamage_Actions`　war3map.j:19817
```jass
if GetUnitAbilityLevel(d,'B033')==1 and a_type=='N05T' then
set DefCof=DefCof+0.25
endif
```

---

## 同一組的其他實作函式

英雄的實作散在同編號的一組函式裡，上面按技能抽取時抓不到的補在這裡
（常見的是決定門檻、結算加成、清理 buff 的那幾支）。

`Trig_HeroKills40_Conditions`　war3map.j:58845
```jass
function Trig_HeroKills40_Conditions takes nothing returns boolean
return GetUnitTypeId(GetKillingUnit())=='N05T'
endfunction
```

`Trig_HeroAttack40_Conditions`　war3map.j:58911
```jass
function Trig_HeroAttack40_Conditions takes nothing returns boolean
return GetUnitTypeId(GetAttacker())=='N05T'
endfunction
```

`Hero40E_Cd`　war3map.j:58931
```jass
function Hero40E_Cd takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
call SaveInteger(hash,GetHandleId(u),20,0)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroAttack40_Alt_Conditions`　war3map.j:58986
```jass
function Trig_HeroAttack40_Alt_Conditions takes nothing returns boolean
return GetUnitTypeId(GetTriggerUnit())=='N05T'
endfunction
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof
  - **4** — 受到傷害 −%〔受害者〕DefCof 減去它 → 值越大越耐打；電擊會扣它
  - **6** — 造成傷害 +%〔攻擊者〕；電擊會扣它 → 目標輸出下降

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
