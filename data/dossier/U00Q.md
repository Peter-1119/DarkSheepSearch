# 黑暗獵手 `U00Q`（Тёмная охотница）

主屬性 **敏捷** · 背包 **6 格** · 解鎖 50000 · 定位 刺客/詛咒者

| | 初始 | 每級 |
|---|---|---|
| 力量 | 13 | 1.5 |
| 敏捷 | 23 | 3 |
| 智力 | 16 | 2 |

> 侵略性的遠程英雄，能擾亂敵方隊伍並高效清場。

**縮放**：吃技能強度的技能 ['A0BX', 'A0BY', 'A0BZ', 'A0C1'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 復仇之魂 `A0C1`　—　吃技能強度

俄文原名：Мстительный дух

```
黑暗獵手死亡後會出現復仇之魂，快速攻擊附近的敵人。此靈魂一次最多可命中 3 個目標。

靈魂的攻擊力：25 + （250% 穿透）點
靈魂的存在時間：12 + （2% 技能強度）秒。
```

每級變動：
  - 第 3 行：25 / 40 / 55 / 70 / 85

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_HeroKills27_Actions`　war3map.j:55002
```jass
elseif(GetUnitTypeId(u2)=='U00Q' or GetUnitTypeId(u2)=='H049')and GetUnitAbilityLevel(u2,'A0C1')>0 then
set dmg=10+15*I2R(GetUnitAbilityLevel(u2,'A0C1'))+LoadReal(hash,GetHandleId(u2),16)*2.50
set x=GetUnitX(u2)
set y=GetUnitY(u2)
set u=CreateUnit(GetOwningPlayer(u2),'o01D',x,y,270)
call SetUnitVertexColor(u,100,100,100,130)
call UnitApplyTimedLife(u,'BTLF',12+udg_ItemBonusDMG[n]*0.02)
call SaveReal(hash,GetHandleId(u),13,dmg)
call SaveUnitHandle(hash,GetHandleId(u),13,u2)
endif
```

## 毀滅砲擊 `A0C0`

俄文原名：Губительный обстрел

```
英雄的攻擊開始能夠命中多個目標，但英雄本身受到的傷害也會增加。

多重射擊：3 個目標
懲罰：受到的傷害 +15%
```

物件欄位（原型 `Aroc`）：`Efk1 = 0.0`, `Efk2 = 0.0`, `Efk3 = 1`, `aare = 650.0`, `acdn = 0.0`, `adur = 0.0`, `ahdu = 0.0`, `aher = 1`, `amat = Abilities\Spells\Other\BlackArrow\BlackArrowMissile.mdl`, `amsp = 1200`, `atar = air,enemies,ward,structure,ground,item,debris`

實作：

`Trig_HeroGiveSkills27_Actions`　war3map.j:55024
```jass
if GetUnitAbilityLevel(u,'A0C0')==1 then
call SaveReal(hash,GetHandleId(u),4,LoadReal(hash,GetHandleId(u),4)-0.15)
endif
```

## 女妖尖嘯 `A0BZ`　—　吃技能強度

俄文原名：Вой банши

```
使指定範圍內的敵人陷入混亂，令其不由自主地移動，同時造成傷害。此技能無法動搖敵方英雄的意志，但對他們造成的傷害提高 150%。

傷害：40 + （25% 技能強度）點
效果持續時間：5 秒。

冷卻：27 秒。
```

每級變動：
  - 第 3 行：40 / 65 / 90 / 115 / 140
  - 第 4 行：5 / 6 / 7 / 8 / 9

物件欄位（原型 `ANcl`）：`Ncl1 = 0.6000000238418579`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.6000000238418579`, `Ncl5 = 0`, `Ncl6 = charm`, `aare = 270.0`, `acap = `, `acdn = 27.0`, `alev = 5`, `amcs = [85, 96, 107, 118, 129]`, `aran = 650.0`

實作：

`RemoveFrost`　war3map.j:1527
```jass
function RemoveFrost takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call UnitRemoveAbility(u,'S00G')
call UnitRemoveAbility(u,'B02V')
call RemoveSavedHandle(hash,GetHandleId(u),'B02V')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set t=null
set u=null
endfunction
function FrostUnit takes unit damager,unit target,real chanse returns nothing
local real dmg
local real cof=1.0
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,t_Id,44)>0 then
set t=null
return
endif
if LoadInteger(hash,t_Id,28)>0 and LoadInteger(hash,GetHandleId(damager),'I07G')==0 then
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(damager,'A0AQ')==1 and GetUnitAbilityLevel(target,'B046')==1 then
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
endif
if GetUnitAbilityLevel(target,'B02V')==1 then
set cof=1.00
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
if GetPlayerTechCount(GetOwningPlayer(damager),'Rufb',true)==1 then
if GetUnitTypeId(damager)=='n041' or GetUnitTypeId(damager)=='n06L' or GetUnitTypeId(damager)=='n06S' or GetUnitTypeId(damager)=='n06U' or GetUnitTypeId(damager)=='n06V' then
set dmg=dmg+100.
endif
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
set t=LoadTimerHandle(hash,t_Id,'B02V')
call TimerStart(t,0.,false,function RemoveFrost)
else
call UnitAddAbility(target,'S00G')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,t_Id,'B02V',t)
call TimerStart(t,6.,false,function RemoveFrost)
endif
endif
set t=null
endfunction
```

`RemoveVulnerability`　war3map.j:2507
```jass
function RemoveVulnerability takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call UnitRemoveAbility(u,'S014')
call UnitRemoveAbility(u,'B045')
call RemoveSavedHandle(hash,GetHandleId(u),'B045')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set t=null
set u=null
endfunction
function VulnerabilityUnit takes unit damager,unit target,real chanse returns nothing
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,GetHandleId(target),44)>0 then
set chanse=0.
endif
if LoadInteger(hash,GetHandleId(target),30)>0 then
if LoadInteger(hash,GetHandleId(target),30)>50 then
set chanse=0.
endif
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(target,'B045')==1 then
set t=LoadTimerHandle(hash,GetHandleId(target),'B045')
call TimerStart(t,8.,false,function RemoveVulnerability)
else
call UnitAddAbility(target,'S014')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,GetHandleId(target),'B045',t)
call TimerStart(t,8.,false,function RemoveVulnerability)
endif
if LoadInteger(hash,GetHandleId(damager),'I00Y')>=1 then
call FrostUnit(damager,target,0.50)
endif
endif
set t=null
endfunction
```

`HeroW27`　war3map.j:54825
```jass
function HeroW27 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local group ug=LoadGroupHandle(hash,Id,1)
local group ug2=CreateGroup()
local integer count=LoadInteger(hash,Id,1)
local unit u3
local real x
local real y
local real angle
call GroupAddGroup(ug,ug2)
loop
set u3=FirstOfGroup(ug2)
exitwhen u3==null
if UnitAlive(u3)then
set angle=GetRandomReal(0,360)
set x=GetUnitX(u3)+200*Cos(angle*bj_DEGTORAD)
set y=GetUnitY(u3)+200*Sin(angle*bj_DEGTORAD)
call IssuePointOrderById(u3,Order_move,x,y)
else
call GroupRemoveUnit(ug,u3)
endif
call GroupRemoveUnit(ug2,u3)
endloop
set count=count-1
if count==0 then
call DestroyGroup(ug)
call FlushChildHashtable(hash,Id)
call DestroyTimer(t)
else
call SaveInteger(hash,Id,1,count)
call TimerStart(t,1.00,false,function HeroW27)
endif
set t=null
set u3=null
call DestroyGroup(ug2)
set ug=null
set ug2=null
endfunction
```

`Trig_HeroSkills27_Actions`　war3map.j:54903
```jass
elseif Skill=='A0BZ' or Skill=='A0ZZ' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Other\\HowlOfTerror\\HowlCaster.mdl",x,y))
set dmg=15+25*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.25
set ug=CreateGroup()
set ug2=CreateGroup()
if Skill=='A0BZ' then
call GroupEnumUnitsInRange(ug,x,y,275.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
if IsUnitType(u3,UNIT_TYPE_HERO)then
call UnitDamageTarget(u,u3,dmg*2.50,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
else
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call GroupAddUnit(ug2,u3)
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
else
call GroupEnumUnitsInRange(ug,x,y,330.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
if IsUnitType(u3,UNIT_TYPE_HERO)then
call UnitDamageTarget(u,u3,dmg*2.50,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
else
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call GroupAddUnit(ug2,u3)
endif
call VulnerabilityUnit(u,u3,1.00)
endif
call GroupRemoveUnit(ug,u3)
endloop
endif
call DestroyGroup(ug)
set t=CreateTimer()
set Id=GetHandleId(t)
call TimerStart(t,0.01,false,function HeroW27)
call SaveGroupHandle(hash,Id,1,ug2)
call SaveInteger(hash,Id,1,4+GetUnitAbilityLevel(u,Skill))
```

## 冷靜 `A0BY`　—　吃技能強度

俄文原名：Хладнокровие

```
英雄在 7 秒內逐漸獲得額外敏捷，之後最大的敏捷加成會維持 6 秒。

額外敏捷：14 + （10% 技能強度）點

冷卻：27 秒。
```

每級變動：
  - 第 3 行：14 / 21 / 28 / 35 / 42

物件欄位（原型 `Absk`）：`abuf = B018`, `acdn = 27.0`, `adur = 13.0`, `ahdu = 13.0`, `aher = 1`, `alev = 5`, `amcs = [80, 90, 100, 110, 120]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`HeroQ27`　war3map.j:54799
```jass
function HeroQ27 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer n=GetPlayerId(GetOwningPlayer(u))+1
local integer count=LoadInteger(hash,Id,1)
local integer BonusAgi=(7+7*GetUnitAbilityLevel(u,'A0BY')+R2I(udg_ItemBonusDMG[n]*0.10))/7
if GetUnitTypeId(u)=='H049' then
set BonusAgi=BonusAgi+(udg_KillsAmountNT[n])/7/5
endif
set count=count-1
if count==0 then
set BonusAgi=LoadInteger(hash,Id,2)
call SetHeroAgi(u,GetHeroAgi(u,false)-BonusAgi,true)
call FlushChildHashtable(hash,Id)
call DestroyTimer(t)
elseif count>6 then
call SetHeroAgi(u,GetHeroAgi(u,false)+BonusAgi,true)
call SaveInteger(hash,Id,2,LoadInteger(hash,Id,2)+BonusAgi)
call SaveInteger(hash,Id,1,count)
else
call SaveInteger(hash,Id,1,count)
endif
set t=null
set u=null
endfunction
```

`Trig_HeroSkills27_Actions`　war3map.j:54897
```jass
if Skill=='A0BY' then
set t=CreateTimer()
set Id=GetHandleId(t)
call TimerStart(t,1,true,function HeroQ27)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,1,14)
```

## 黑箭 `A0BX`　—　吃技能強度

俄文原名：Чёрные стрелы

```
擊殺敵人會引發爆炸，對死亡目標附近的敵人造成傷害。

爆炸傷害：（30% 敏捷）+（10% 技能強度）點
```

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 選擇天賦 `A09Y`

俄文原名：Выбрать талант

```
當玩家的經驗值達到足夠數量時，你可以選擇一項天賦，大幅強化英雄，使其提升至新的力量階級。
```

**天賦選項**：
  - `A0ZX` 威懾
    強度等級：T3 屬性加成／每級屬性成長加成： +1 / +0 +2 / +1 +1 / +0  使「女妖尖嘯」的作用範圍提高 20%，並增加 100% 機率對受影響的敵人施加易傷。  若「黑箭」擊殺了處於易傷狀態的敵人，其傷害範圍提高 50%。
  - `A100` 暗藏武器
    強度等級：T3+ 屬性加成／每級屬性成長加成： +2 / +0 +4 / +2 +2 / +1  你可以將弓、匕首、小刀、螫刺或任何其他輕型武器移入第二背包（F2）（劍類不算）。如此便可在第二背包中持有 1 件物品。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0ZX,A100`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`RemoveFrost`　war3map.j:1527
```jass
function RemoveFrost takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call UnitRemoveAbility(u,'S00G')
call UnitRemoveAbility(u,'B02V')
call RemoveSavedHandle(hash,GetHandleId(u),'B02V')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set t=null
set u=null
endfunction
function FrostUnit takes unit damager,unit target,real chanse returns nothing
local real dmg
local real cof=1.0
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,t_Id,44)>0 then
set t=null
return
endif
if LoadInteger(hash,t_Id,28)>0 and LoadInteger(hash,GetHandleId(damager),'I07G')==0 then
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(damager,'A0AQ')==1 and GetUnitAbilityLevel(target,'B046')==1 then
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
endif
if GetUnitAbilityLevel(target,'B02V')==1 then
set cof=1.00
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
if GetPlayerTechCount(GetOwningPlayer(damager),'Rufb',true)==1 then
if GetUnitTypeId(damager)=='n041' or GetUnitTypeId(damager)=='n06L' or GetUnitTypeId(damager)=='n06S' or GetUnitTypeId(damager)=='n06U' or GetUnitTypeId(damager)=='n06V' then
set dmg=dmg+100.
endif
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
set t=LoadTimerHandle(hash,t_Id,'B02V')
call TimerStart(t,0.,false,function RemoveFrost)
else
call UnitAddAbility(target,'S00G')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,t_Id,'B02V',t)
call TimerStart(t,6.,false,function RemoveFrost)
endif
endif
set t=null
endfunction
```

`Hero26E`　war3map.j:54529
```jass
function Hero26E takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real dmg
local unit u3=LoadUnitHandle(hash,Id,2)
set dmg=10+10*I2R(GetUnitAbilityLevel(u,'A0BB'))+udg_ItemBonusDMG[n]*0.10
if UnitAlive(u3)then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\Monsoon\\MonsoonBoltTarget.mdl",u3,"origin"))
if GetUnitAbilityLevel(u3,'B046')==1 then
call UnitDamageTarget(u,u3,dmg*2.0,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
else
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
if GetUnitAbilityLevel(u,'A0AQ')==1 then
call FrostUnit(u,u3,0.33)
endif
endif
call FlushChildHashtable(hash,Id)
call DestroyTimer(t)
set t=null
set u=null
set pl=null
set u3=null
endfunction
```

`Trig_HeroSkills26_Actions`　war3map.j:54748
```jass
if GetUnitAbilityLevel(u,'A09Y')==1 then
call TimerStart(t,GetRandomReal(0.25,1.50),true,function Hero26E)
```

`Trig_HeroSkills27_Actions`　war3map.j:54879
```jass
if Skill=='A0ZX' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A09Y')
call UnitAddAbility(u,'A0ZY')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A100' then
call SetHeroStr(u,GetHeroStr(u,false)+8,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+6,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',3)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A09Y')
call UnitAddAbility(u,'A101')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 威懾 `A0ZX`　—　來自天賦「選擇天賦」

俄文原名：Устрашение

```
強度等級：T3
屬性加成／每級屬性成長加成：
+1 / +0
+2 / +1
+1 / +0

使「女妖尖嘯」的作用範圍提高 20%，並增加 100% 機率對受影響的敵人施加易傷。

若「黑箭」擊殺了處於易傷狀態的敵人，其傷害範圍提高 50%。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['faeriefireon', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills27_Actions`　war3map.j:54879
```jass
if Skill=='A0ZX' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A09Y')
call UnitAddAbility(u,'A0ZY')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 暗藏武器 `A100`　—　來自天賦「選擇天賦」

俄文原名：Скрытое оружие

```
強度等級：T3+
屬性加成／每級屬性成長加成：
+2 / +0
+4 / +2
+2 / +1

你可以將弓、匕首、小刀、螫刺或任何其他輕型武器移入第二背包（F2）（劍類不算）。如此便可在第二背包中持有 1 件物品。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acolyteharvest', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 32`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills27_Actions`　war3map.j:54887
```jass
elseif Skill=='A100' then
call SetHeroStr(u,GetHeroStr(u,false)+8,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+6,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',3)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A09Y')
call UnitAddAbility(u,'A101')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 這隻召喚／製造的單位

（技能程式碼裡的 `CreateUnit` 目標。數值取自 war3map.w3u，
沒列出的欄位代表地圖沒覆寫、沿用原型。）

### `o01D` Мстительный дух（原型 `ocat`）
  - 生命 1 ／ 骰面 1 ／ 攻擊間隔 0.30000001192092896 ／ 射程 800 ／ 攻擊範圍 1200 ／ 技能 A0C2,Avul,Aloc
  - 技能 `A0C2` Мультишот　`Efk1 = 0.0`, `Efk2 = 0.0`, `Efk3 = 1`, `aare = 875.0`, `acdn = 0.0`, `adur = 0.0`, `ahdu = 0.0`, `amat = Abilities\Spells\Other\BlackArrow\BlackArrowMissile.mdl`, `amsp = 1200`, `atar = air,enemies,ward,structure,ground,item,debris`

---

## 皮膚

純外觀：女妖女王

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **4** — 受到傷害 −%〔受害者〕DefCof 減去它 → 值越大越耐打；電擊會扣它
  - **16** — 穿透〔攻擊者〕每次傷害事件後**另外**打一段 CHAOS/UNIVERSAL，不吃減傷
  - **28** — 實數＝冰凍傷害 +%〔施加者〕／整數＝抵抗冰凍旗標〔受害者〕
  - **44** — 狀態免疫旗標〔受害者〕>0 則所有狀態函式開頭直接 return，完全不判定
  - **48** — 冰凍抗性〔受害者〕；電擊 −1.00

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
