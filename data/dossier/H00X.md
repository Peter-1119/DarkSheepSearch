# 生命之靈 `H00X`（Дух жизни）

主屬性 **智力** · 背包 **6 格** · 解鎖 0 · 定位 治療/輔助

| | 初始 | 每級 |
|---|---|---|
| 力量 | 9 | （未覆寫） |
| 敏捷 | 20 | 1.2 |
| 智力 | 22 | 3 |

> 遠程英雄，單體治療能力極強，還能改造道具。

**縮放**：吃技能強度的技能 ['A02O', 'A02S'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且事件數越多穿透越划算。
- **召喚物** —— 召喚物**不繼承**主人的裝備觸發／狀態／傷害 +%，只吃主人技能公式裡明寫的屬性（通常是最大生命與技能強度）與原生光環。
- **治療／增益** —— 直接寫數值，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。

細節見 `data/dossier/_engine.md`。


---

## 能量傳輸 `A02T`

俄文原名：Передача энергии

```
英雄失去當前生命值的 10%，並依據失去的生命值為友軍目標回復生命值與法力值。

回復效率：110%
```

每級變動：
  - 第 3 行：110 / 130 / 150 / 170 / 190

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = cloudoffog`, `acdn = 30.0`, `alev = 5`, `aran = 600.0`, `atar = air,friend,ground,invulnerable,organic,vulnerable`

實作：

`Trig_HeroSkills7_Actions`　war3map.j:48431
```jass
elseif Skill=='A02T' then
set x=GetUnitState(u,UNIT_STATE_LIFE)
set dmg=x*0.10
call SetUnitState(u,UNIT_STATE_LIFE,x-dmg)
set dmg=dmg*(0.90+(0.20*I2R(GetUnitAbilityLevel(u,Skill))))
set u2=GetSpellTargetUnit()
call SetUnitState(u2,UNIT_STATE_LIFE,GetUnitState(u2,UNIT_STATE_LIFE)+dmg)
call SetUnitState(u2,UNIT_STATE_MANA,GetUnitState(u2,UNIT_STATE_MANA)+dmg)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\AIre\\AIreTarget.mdl",u2,"origin"))
```

## 能量爆發 `A02S`　—　吃技能強度

俄文原名：Энергетический всплеск

```
對小範圍內的敵人造成少量傷害。第一次爆炸有 90% 機率產生再次爆炸，每觸發一次該機率降低 10%。

傷害：40 +（20% 技能強度）點

冷卻：3 秒
```

每級變動：
  - 第 3 行：40 / 60 / 80 / 100 / 120

物件欄位（原型 `ANcl`）：`Ncl1 = 0.009999999776482582`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.009999999776482582`, `Ncl5 = 0`, `Ncl6 = charm`, `aare = 130.0`, `acdn = 3.0`, `alev = 5`, `amcs = [50, 60, 70, 80, 90]`, `aran = 600.0`

實作：

`Hero7W`　war3map.j:48237
```jass
function Hero7W takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u3
local integer count=LoadInteger(hash,Id,1)
local integer count2=LoadInteger(hash,Id,2)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real dmg=20+(20*I2R(GetUnitAbilityLevel(u,LoadInteger(hash,Id,3))))+udg_ItemBonusDMG[n]*0.20
local real x=LoadReal(hash,Id,2)
local real y=LoadReal(hash,Id,3)
local real x2
local real y2
local group ug
local integer Random
local real dist
local real angle
local real item_cof=1.00
if GetUnitAbilityLevel(u,'A02Q')==1 then
set item_cof=item_cof+0.20*I2R(LoadInteger(hash,GetHandleId(u),'iBLU'))
set dmg=dmg*item_cof
endif
set Random=GetRandomInt(1,10)
if Random<=count then
set dist=GetRandomReal(50,120)
set angle=GetRandomReal(0,360)
set x2=x+dist*Cos(angle*bj_DEGTORAD)
set y2=y+dist*Sin(angle*bj_DEGTORAD)
set count=count-1
call SaveInteger(hash,Id,1,count)
call DestroyEffect(AddSpecialEffect("Objects\\Spawnmodels\\NightElf\\NEDeathMedium\\NEDeath.mdl",x2,y2))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,130.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
if count2>0 then
set dist=GetRandomReal(120,150)
set angle=GetRandomReal(0,360)
set x2=x+dist*Cos(angle*bj_DEGTORAD)
set y2=y+dist*Sin(angle*bj_DEGTORAD)
set count2=count2-1
call SaveInteger(hash,Id,2,count2)
call DestroyEffect(AddSpecialEffect("Objects\\Spawnmodels\\NightElf\\NEDeathMedium\\NEDeath.mdl",x2,y2))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,130.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
if Random>count and count2==0 then
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set t=null
set u=null
set u3=null
set pl=null
set ug=null
endfunction
```

`Trig_HeroSkills7_Actions`　war3map.j:48371
```jass
elseif Skill=='A02S' then
set dmg=20+(20*I2R(GetUnitAbilityLevel(u,'A02S')))+udg_ItemBonusDMG[n]*0.20
set x=GetLocationX(GetSpellTargetLoc())
set y=GetLocationY(GetSpellTargetLoc())
call DestroyEffect(AddSpecialEffect("Objects\\Spawnmodels\\NightElf\\NEDeathMedium\\NEDeath.mdl",x,y))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,130,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set count=9
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,1,count)
call SaveReal(hash,Id,2,x)
call SaveReal(hash,Id,3,y)
call SaveInteger(hash,Id,3,Skill)
call TimerStart(t,0.1,true,function Hero7W)
```

## 靈魂治癒 `A02R`

俄文原名：Исцеление душ

```
此靈體立即為自身與所有友方英雄回復生命值，並清除負面狀態。

治療：35% 生命值

冷卻：100 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, None, 1.0]`, `Ncl2 = [None, 1]`, `Ncl3 = [1, None]`, `Ncl4 = [0.009999999776482582, None, 1.0]`, `Ncl5 = [0, None]`, `Ncl6 = ['chemicalrage', None, 'channel']`, `acap = `, `acdn = [100.0, None, 17.0]`, `alev = 1`, `amcs = [175, None, 80, 90, 100, 110, 120]`, `aran = [700.0, None]`, `atar = ['air,ground,friend,neutral,self', None]`

實作：

`Trig_HeroSkills7_Actions`　war3map.j:48440
```jass
elseif Skill=='A02R' then
set ug=CreateGroup()
call GroupAddGroup(udg_AllHeroes,ug)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)then
call ClearUnit(u3)
call SetUnitLifePercentBJ(u3,UnitLifePercent(u3)+35.00)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\Resurrect\\ResurrectTarget.mdl",u3,"origin"))
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
```

## 治療連結 `A02O`　—　吃技能強度

俄文原名：Целительная связь

```
指定目標獲得持續治療。在 300 點以內的距離，目標可獲得完整治療。英雄離目標越遠，治療越弱，在 1500 距離時最低為 15%。持續時間 - 10 秒。

完整治療量：400 +（150% 技能強度）點

冷卻：22 秒
```

每級變動：
  - 第 3 行：400 / 600 / 800 / 1000 / 1200

物件欄位（原型 `ANcl`）：`Ncl1 = 0.009999999776482582`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.009999999776482582`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acap = `, `acdn = 22.0`, `alev = 5`, `amcs = [80, 95, 110, 125, 140]`, `aran = 300.0`, `atar = air,friend,ground,invulnerable,organic,vulnerable`

實作：

`Hero7Q`　war3map.j:48206
```jass
if count>=0 then
set heal=(200+200*I2R(GetUnitAbilityLevel(u,'A02O'))+udg_ItemBonusDMG[n]*1.50)*0.02*item_cof
set x=x2-x
set y=y2-y
set dist=SquareRoot(x*x+y*y)
if dist<=300 then
set cof=1.00
elseif dist>=1500 then
set cof=0.15
else
set cof=1.00-((dist-300)/1500)
endif
set heal=heal*cof
call SetUnitState(u2,UNIT_STATE_LIFE,life+heal)
```

`Trig_HeroSkills7_Actions`　war3map.j:48361
```jass
if Skill=='A02O' then
set u2=GetSpellTargetUnit()
set count=50
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveEffectHandle(hash,Id,3,AddSpecialEffectTarget("Abilities\\Spells\\Other\\Drain\\DrainCaster.mdl",u2,"origin"))
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
call SaveInteger(hash,Id,1,count)
call TimerStart(t,0.2,true,function Hero7Q)
```

## 扭曲 `A0TC`

俄文原名：Искажение

```
將「唯一」類別的物品轉變為「扭曲」類別的物品。也可用於更換目前的扭曲物品。

冷卻：120 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [1.0, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [1.0, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['creepheal', 'channel']`, `acap = `, `acdn = [120.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [250, 95, 110, 125, 140, 155, 170]`, `aran = [200.0, 100.0]`, `atar = ['item', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ItemsUp3_Conditions`　war3map.j:26896
```jass
function Trig_ItemsUp3_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0BH' or GetSpellAbilityId()=='A0S2' or GetSpellAbilityId()=='A0TC'
endfunction
function Trig_ItemsUp3_Actions takes nothing returns nothing
local item Item=GetSpellTargetItem()
local integer ItemID=GetItemTypeId(Item)
local unit u=GetSpellAbilityUnit()
local unit u2
local real x=GetUnitX(u)
local real y=GetUnitY(u)
local item scroll
local boolean IsScroll=false
local integer L
local integer L2
if GetSpellAbilityId()=='A0BH' then
set scroll=GetItemOfTypeFromUnitBJ(u,'plcl')
set IsScroll=true
if ItemID=='plcl' then
if LoadInteger(hash,'asbl','asbl')!=1 then
set L=0
set L2=0
loop
exitwhen L>5
set Item=UnitItemInSlot(u,L)
set ItemID=GetItemTypeId(Item)
if ItemID=='plcl' then
set L2=L2+1
endif
set L=L+1
endloop
if L2>=2 then
call SaveInteger(hash,'asbl','asbl',1)
call RemoveItem(GetItemOfTypeFromUnitBJ(u,'plcl'))
call RemoveItem(GetItemOfTypeFromUnitBJ(u,'plcl'))
call UnitAddItemById(u,'asbl')
call DestroyEffect(AddSpecialEffect("war3mapImported\\EnergyBurst.mdx",x,y))
set Item=null
set u=null
set u2=null
set scroll=null
return
endif
endif
endif
endif
if ItemID=='belv' or ItemID=='rde1' or ItemID=='rde2' or ItemID=='rde3' or ItemID=='rlif' or ItemID=='sbch' or ItemID=='rin1' or ItemID=='rwiz' or ItemID=='mcou' or ItemID=='clfm' or ItemID=='ciri' then
call SetItemPosition(Item,x,y)
call RemoveItem(Item)
if GetSpellAbilityId()=='A0TC' and GetUnitTypeId(u)=='H046' then
set u2=CreateUnit(GetOwningPlayer(u),'o01K',x,y,0)
call UnitAddAbility(u2,'A0X5')
call UnitAddAbility(u2,'A0CF')
set L=GetRandomInt(1,PurpleItemsCount)
call UnitAddItem(u2,CreateItem(PurpleItems[L],x,y))
set L=L+GetRandomInt(1,5)
if L>PurpleItemsCount then
set L=L-PurpleItemsCount
endif
call UnitAddItem(u2,CreateItem(PurpleItems[L],x,y))
set L=L+GetRandomInt(1,5)
if L>PurpleItemsCount then
set L=L-PurpleItemsCount
endif
call UnitAddItem(u2,CreateItem(PurpleItems[L],x,y))
call SelectUnitForPlayerSingle(u2,GetOwningPlayer(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
else
set L=GetRandomInt(1,PurpleItemsCount)
set ItemID=PurpleItems[L]
if IsScroll==true then
call RemoveItem(scroll)
endif
set Item=CreateItem(ItemID,x,y)
call UnitAddItem(u,Item)
endif
else
set L=1
loop
exitwhen L>PurpleItemsCount
if ItemID==PurpleItems[L]then
call SetItemPosition(Item,x,y)
call RemoveItem(Item)
if GetSpellAbilityId()=='A0TC' and GetUnitTypeId(u)=='H046' then
set u2=CreateUnit(GetOwningPlayer(u),'o01K',x,y,0)
call UnitAddAbility(u2,'A0X5')
call UnitAddAbility(u2,'A0CF')
set L=GetRandomInt(1,PurpleItemsCount)
call UnitAddItem(u2,CreateItem(PurpleItems[L],x,y))
set L=L+GetRandomInt(1,5)
if L>PurpleItemsCount then
set L=L-PurpleItemsCount
endif
call UnitAddItem(u2,CreateItem(PurpleItems[L],x,y))
set L=L+GetRandomInt(1,5)
if L>PurpleItemsCount then
set L=L-PurpleItemsCount
endif
call UnitAddItem(u2,CreateItem(PurpleItems[L],x,y))
call SelectUnitForPlayerSingle(u2,GetOwningPlayer(u))
call SaveUnitHandle(hash,GetHandleId(u2),1,u)
else
if IsScroll==true then
call RemoveItem(scroll)
endif
set L2=1
set L=L+L2
if L>PurpleItemsCount then
set L=L-PurpleItemsCount
endif
set ItemID=PurpleItems[L]
set Item=CreateItem(ItemID,x,y)
call UnitAddItem(u,Item)
endif
set L=50
else
set L=L+1
endif
endloop
endif
set Item=null
set u=null
set u2=null
set scroll=null
endfunction
```

## 選擇天賦 `A0TA`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，使英雄大幅強化至新的力量等階。
```

**天賦選項**：
  - `A0TE` 覺醒
    強度等級：T2 屬性加成／每級屬性成長加成： +1 / +0 +1 / +0 +1 / +1  使用後將「唯一」類別的物品轉變為「覺醒」類別的物品。  背包中每擁有一件「傳說」類別的物品，「治療連結」的治療強度便提高 15%。
  - `A0TB` 折射
    強度等級：T3 屬性加成／每級屬性成長加成： +1 / +0 +3 / +1 +3 / +1  使用後將「聖物」類別的物品轉變為「折射」類別的物品。也可用於更換目前的折射物品。  背包中每擁有一件「折射」類別的物品，「治療連結」的治療強度便提高 10%，且「能量爆發」的傷害提高 20%。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0TE,A0TB`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills7_Actions`　war3map.j:48343
```jass
if Skill=='A0TE' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A0TA')
call UnitAddAbility(u,'A0TF')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0TB' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+3,true)
call SetHeroInt(u,GetHeroInt(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0TA')
call UnitAddAbility(u,'A02Q')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 覺醒 `A0TE`　—　來自天賦「選擇天賦」

俄文原名：Пробуждение

```
強度等級：T2
屬性加成／每級屬性成長加成：
+1 / +0
+1 / +0
+1 / +1

使用後將「唯一」類別的物品轉變為「覺醒」類別的物品。

背包中每擁有一件「傳說」類別的物品，「治療連結」的治療強度便提高 15%。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acolyteharvest', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 15`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills7_Actions`　war3map.j:48343
```jass
if Skill=='A0TE' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A0TA')
call UnitAddAbility(u,'A0TF')
call SaveInteger(hash,GetHandleId(pl),15,1)
```

## 折射 `A0TB`　—　來自天賦「選擇天賦」

俄文原名：Преломление

```
強度等級：T3
屬性加成／每級屬性成長加成：
+1 / +0
+3 / +1
+3 / +1

使用後將「聖物」類別的物品轉變為「折射」類別的物品。也可用於更換目前的折射物品。

背包中每擁有一件「折射」類別的物品，「治療連結」的治療強度便提高 10%，且「能量爆發」的傷害提高 20%。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['absorb', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills7_Actions`　war3map.j:48351
```jass
elseif Skill=='A0TB' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+3,true)
call SetHeroInt(u,GetHeroInt(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call SaveInteger(hash,GetHandleId(u),'aAGI',1)
call UnitRemoveAbility(u,'A0TA')
call UnitAddAbility(u,'A02Q')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 能量爆發（強化） `A0X4`　—　來自皮膚「生命守護者」

俄文原名：Энергетический всплеск (усиленный)

```
對小範圍內的敵人造成少量傷害。第一次爆炸有 100% 機率產生再次爆炸，每觸發一次該機率降低 10%。

傷害：40 +（20% 技能強度）點
額外強化：每 100 點技能強度，在作用範圍邊緣多產生一次爆炸

冷卻：3 秒
```

每級變動：
  - 第 3 行：40 / 60 / 80 / 100 / 120

物件欄位（原型 `ANcl`）：`Ncl1 = 0.009999999776482582`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.009999999776482582`, `Ncl5 = 0`, `Ncl6 = charm`, `aare = 130.0`, `acdn = 3.0`, `alev = 5`, `amcs = [50, 60, 70, 80, 90]`, `aran = 600.0`

實作：

`Hero7W`　war3map.j:48237
```jass
function Hero7W takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u3
local integer count=LoadInteger(hash,Id,1)
local integer count2=LoadInteger(hash,Id,2)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real dmg=20+(20*I2R(GetUnitAbilityLevel(u,LoadInteger(hash,Id,3))))+udg_ItemBonusDMG[n]*0.20
local real x=LoadReal(hash,Id,2)
local real y=LoadReal(hash,Id,3)
local real x2
local real y2
local group ug
local integer Random
local real dist
local real angle
local real item_cof=1.00
if GetUnitAbilityLevel(u,'A02Q')==1 then
set item_cof=item_cof+0.20*I2R(LoadInteger(hash,GetHandleId(u),'iBLU'))
set dmg=dmg*item_cof
endif
set Random=GetRandomInt(1,10)
if Random<=count then
set dist=GetRandomReal(50,120)
set angle=GetRandomReal(0,360)
set x2=x+dist*Cos(angle*bj_DEGTORAD)
set y2=y+dist*Sin(angle*bj_DEGTORAD)
set count=count-1
call SaveInteger(hash,Id,1,count)
call DestroyEffect(AddSpecialEffect("Objects\\Spawnmodels\\NightElf\\NEDeathMedium\\NEDeath.mdl",x2,y2))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,130.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
if count2>0 then
set dist=GetRandomReal(120,150)
set angle=GetRandomReal(0,360)
set x2=x+dist*Cos(angle*bj_DEGTORAD)
set y2=y+dist*Sin(angle*bj_DEGTORAD)
set count2=count2-1
call SaveInteger(hash,Id,2,count2)
call DestroyEffect(AddSpecialEffect("Objects\\Spawnmodels\\NightElf\\NEDeathMedium\\NEDeath.mdl",x2,y2))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x2,y2,130.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
if Random>count and count2==0 then
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set t=null
set u=null
set u3=null
set pl=null
set ug=null
endfunction
```

`Trig_HeroSkills7_Actions`　war3map.j:48396
```jass
elseif Skill=='A0X4' then
set dmg=20+(20*I2R(GetUnitAbilityLevel(u,'A0X4')))+udg_ItemBonusDMG[n]*0.20
set x=GetLocationX(GetSpellTargetLoc())
set y=GetLocationY(GetSpellTargetLoc())
call DestroyEffect(AddSpecialEffect("Objects\\Spawnmodels\\NightElf\\NEDeathMedium\\NEDeath.mdl",x,y))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,130,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set count=10
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,1,count)
call SaveReal(hash,Id,2,x)
call SaveReal(hash,Id,3,y)
call SaveInteger(hash,Id,3,Skill)
call TimerStart(t,0.1,true,function Hero7W)
set count2=0
set r=udg_ItemBonusDMG[n]
loop
exitwhen r<100.
set count2=count2+1
set r=r-100.
endloop
if count2>0 then
call SaveInteger(hash,Id,2,count2)
endif
```

## 犧牲 `A10Q`　—　來自皮膚「往昔幽魂」

俄文原名：Жертва

```
幽魂犧牲自己，將「吸收」技能累積的加成交給友方英雄。此技能的目標同時獲得生命值與法力值的完全回復，並清除身上的負面狀態。

冷卻：90 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, None, 1.0]`, `Ncl2 = [1, None]`, `Ncl3 = [1, None]`, `Ncl4 = [0.009999999776482582, None, 1.0]`, `Ncl5 = [0, None]`, `Ncl6 = ['chemicalrage', None, 'channel']`, `acap = `, `acdn = [90.0, None, 17.0]`, `alev = 1`, `amcs = [None, 80, 90, 100, 110, 120]`, `aran = [900.0, None, 700.0]`, `atar = ['hero,self,allies', None, 'air,ground,friend,neutral,self']`

實作：

`Trig_HeroSkills7_Actions`　war3map.j:48534
```jass
elseif Skill=='A10Q' then
set u2=GetSpellTargetUnit()
if not IsUnitEnemy(u2,pl)then
call SetUnitLifePercentBJ(u2,100)
call SetUnitManaPercentBJ(u2,100)
call ClearUnit(u2)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\TomeOfRetraining\\TomeOfRetrainingCaster.mdl",u,"origin"))
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\Resurrect\\ResurrectTarget.mdl",u2,"origin"))
set Id=GetHandleId(u)
set r=LoadReal(hash,Id,'wSPD')
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]-r
set udg_ItemBonusDMG[GetPlayerId(GetOwningPlayer(u2))+1]=udg_ItemBonusDMG[GetPlayerId(GetOwningPlayer(u2))+1]+r
set r=LoadReal(hash,Id,'wHPR')
call SetUnitLifeRegeneration(u,GetUnitLifeRegeneration(u)-r)
call SetUnitLifeRegeneration(u2,GetUnitLifeRegeneration(u2)+r)
set r=LoadReal(hash,Id,'wMPR')
call SetUnitManaRegeneration(u,GetUnitManaRegeneration(u)-r)
call SetUnitManaRegeneration(u2,GetUnitManaRegeneration(u2)+r)
set count=LoadInteger(hash,Id,'wSTR')
call SetHeroStr(u,GetHeroStr(u,false)-count,true)
call SetHeroStr(u2,GetHeroStr(u2,false)+count,true)
set count=LoadInteger(hash,Id,'wAGI')
call SetHeroAgi(u,GetHeroAgi(u,false)-count,true)
call SetHeroAgi(u2,GetHeroAgi(u2,false)+count,true)
set count=LoadInteger(hash,Id,'wINT')
call SetHeroInt(u,GetHeroInt(u,false)-count,true)
call SetHeroInt(u2,GetHeroInt(u2,false)+count,true)
call SaveInteger(hash,Id,'wSTR',0)
call SaveInteger(hash,Id,'wAGI',0)
call SaveInteger(hash,Id,'wINT',0)
call SaveReal(hash,Id,'wSPD',0.)
call SaveReal(hash,Id,'wHPR',0.)
call SaveReal(hash,Id,'wMPR',0.)
call KillUnit(u)
endif
```

## 幽靈幻惑 `A10R`　—　來自皮膚「往昔幽魂」

俄文原名：Призрачное наваждение

```
此技能會扣除你的生命值，數量為其法力消耗的 125%。

提升指定友軍的攻擊速度與攻擊力。

攻擊速度加成：40%
攻擊力加成：25 +（40% 技能強度）點
持續時間：10 秒

冷卻：25 秒
```

每級變動：
  - 第 5 行：40 / 60 / 80 / 100 / 120
  - 第 6 行：25 / 50 / 75 / 100 / 125

物件欄位（原型 `ANcl`）：`Ncl1 = 0.009999999776482582`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.009999999776482582`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acap = `, `acdn = 25.0`, `alev = 5`, `amcs = [100, 125, 150, 175, 200]`, `aran = 700.0`, `atar = air,friend,ground,invulnerable,organic,vulnerable`

實作：

`SetUnitExtraDamage`　war3map.j:3928
```jass
function SetUnitExtraDamage takes unit u,integer a returns nothing
local integer Id=GetHandleId(u)
local integer p=14
local integer index=0
local integer r
if a>8191 then
set a=8191
endif
loop
call UnitRemoveAbility(u,setAttribute___abilityAddDamage[index])
call UnitRemoveAbility(u,setAttribute___abilityRemoveDamage[index])
exitwhen index==14
set index=index+1
endloop
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityAddDamage[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
elseif a<0 then
set r=-a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityRemoveDamage[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
endif
call SaveInteger(hash,Id,34,a)
endfunction
```

`Hero7QQ`　war3map.j:48311
```jass
function Hero7QQ takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
local integer count=LoadInteger(hash,GetHandleId(t),2)-1
call SaveInteger(hash,GetHandleId(t),2,count)
if count<=0 or not UnitAlive(u)then
call SetUnitExtraDamage(u,GetUnitExtraDamage(u)-LoadInteger(hash,GetHandleId(t),3))
call UnitRemoveAbility(u,'A10S')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
endif
set u=null
set t=null
endfunction
```

`Trig_HeroSkills7_Actions`　war3map.j:48569
```jass
elseif Skill=='A10R' then
set u2=GetSpellTargetUnit()
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
set count=25*GetUnitAbilityLevel(u,Skill)+R2I(udg_ItemBonusDMG[n]*0.40)
call SetUnitExtraDamage(u2,GetUnitExtraDamage(u2)+count)
call SaveInteger(hash,Id,3,count)
call UnitAddAbility(u2,'A10S')
call SetUnitAbilityLevel(u2,'A10S',GetUnitAbilityLevel(u,Skill))
call SaveInteger(hash,Id,2,10)
call TimerStart(t,1.,true,function Hero7QQ)
set dmg=(75.+25.*I2R(GetUnitAbilityLevel(u,Skill)))*1.25
if GetWidgetLife(u)>dmg then
call SetWidgetLife(u,GetWidgetLife(u)-dmg)
else
call KillUnit(u)
endif
endif
```

---

## 這隻召喚／製造的單位

（技能程式碼裡的 `CreateUnit` 目標。數值取自 war3map.w3u，
沒列出的欄位代表地圖沒覆寫、沿用原型。）

### `o01K` Выберите предмет（原型 `ocat`）
  - 生命 1 ／ 骰面 1 ／ 攻擊範圍 1200 ／ 技能 Avul

---

## 皮膚

### 生命守護者 `H046` —— **會換技能**
  - 能量爆發 `A02S` → **能量爆發（強化）** `A0X4`

### 往昔幽魂 `E005` —— **會換技能**
  - 靈魂治癒 `A02R` → **犧牲** `A10Q`
  - 治療連結 `A02O` → **幽靈幻惑** `A10R`

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
