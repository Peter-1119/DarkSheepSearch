# 戰爭首領 `N027`（Варбосс）

主屬性 **力量** · 背包 **6 格** · 解鎖 0 · 定位 坦克/戰士

| | 初始 | 每級 |
|---|---|---|
| 力量 | 26 | 3.5 |
| 敏捷 | （未覆寫） | （未覆寫） |
| 智力 | （未覆寫） | 1.5 |

> 兇猛的近戰戰士，靠生產糧食來累積生命值上限。

**縮放**：吃技能強度的技能 無 ／ ◈ 吃裝備技能威力 ['A09A', 'A09D'] ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **技能直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且傷害事件數越多，穿透越划算。
- **治療** —— 直接寫血量，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。
- **屬性增益** —— 直接改屬性。注意有些是**永久**的（死亡不歸零），長局會滾雪球。

細節見 `data/dossier/_engine.md`。


---

## 終結 `A09D`　—　◈ 吃裝備技能威力

俄文原名：Добивание

```
繼承裝備技能的加成。

英雄的攻擊對生命值低於 50% 的敵人可造成提升的物理傷害，並回復少量生命值。

加成傷害：30 點
生命值回復：20 點

冷卻：3 秒
```

每級變動：
  - 第 5 行：30 / 45 / 60 / 75 / 90
  - 第 6 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`Trig_HeroAttack22_Actions`　war3map.j:53519
```jass
if GetUnitAbilityLevel(u,'A09D')>=1 then
if LoadReal(hash,u_Id,'A09D')==0. and UnitLifePercent(u2)<=50. then
call StartModCooldown(u_Id,'A09D',3.)
set cof=LoadReal(hash,u_Id,18)+1.
set dmg=(15.+15.*I2R(GetUnitAbilityLevel(u,'A09D')))*cof
call DestroyEffect(AddSpecialEffectTarget("Objects\\Spawnmodels\\Orc\\OrcSmallDeathExplode\\OrcSmallDeathExplode.mdl",u2,"origin"))
call UnitDamageTarget(u,u2,dmg,true,false,ATTACK_TYPE_HERO,DAMAGE_TYPE_NORMAL,WEAPON_TYPE_WHOKNOWS)
set dmg=(10.+10.*I2R(GetUnitAbilityLevel(u,'A09D')))*cof
call SetUnitState(u,UNIT_STATE_LIFE,GetUnitState(u,UNIT_STATE_LIFE)+dmg)
endif
endif
```

## 狂戰士 `A09C`

俄文原名：Берсерк

```
在短時間內獲得攻擊速度與攻擊力的加成。

攻擊速度提升：40%
攻擊力提升：（英雄最大生命值的 5%）點
持續時間：6 秒

冷卻：20 秒
```

每級變動：
  - 第 3 行：40 / 60 / 80 / 100 / 120

物件欄位（原型 `Absk`）：`abuf = B00S`, `acdn = 20.0`, `adur = 6.0`, `ahdu = 6.0`, `aher = 1`, `alev = 5`, `amcs = [60, 67, 74, 81, 88]`, `bsk1 = 0.0`, `bsk2 = [0.4000000059604645, 0.6000000238418579, 0.800000011920929, 1.0, 1.2000000476837158]`, `bsk3 = 0.0`

實作：

`Trig_HeroSkills22_Actions`　war3map.j:53404
```jass
elseif Skill=='A09C' then
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,6.,true,function Hero22Q)
set n2=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.05)
call SaveInteger(hash,Id,1,n2)
call SetUnitExtraDamage(u,GetUnitExtraDamage(u)+n2)
```

`SetUnitExtraDamage`　war3map.j:3928
```jass
function SetUnitExtraDamage takes unit u,integer a returns nothing
local integer Id=GetHandleId(u)
local integer p=14
local integer index=0
local integer r
if a>8191 then
set a=8191
endif
loop
call UnitRemoveAbility(u,setAttribute___abilityAddDamage[index])
call UnitRemoveAbility(u,setAttribute___abilityRemoveDamage[index])
exitwhen index==14
set index=index+1
endloop
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityAddDamage[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
elseif a<0 then
set r=-a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
call UnitAddAbility(u,setAttribute___abilityRemoveDamage[p])
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
endif
call SaveInteger(hash,Id,34,a)
endfunction
```

`Hero22Q`　war3map.j:53352
```jass
function Hero22Q takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
local integer n2=LoadInteger(hash,GetHandleId(t),1)
call SetUnitExtraDamage(u,GetUnitExtraDamage(u)-n2)
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set u=null
set t=null
endfunction
```

## 震地一擊 `A09B`

俄文原名：Удар по земле

```
英雄周圍的敵人受到傷害並暈眩 5 秒（英雄 2 秒）。

傷害：50 +（10% 英雄最大生命值）點

冷卻：9 秒
```

每級變動：
  - 第 3 行：50 / 100 / 150 / 200 / 250

物件欄位（原型 `AOw2`）：`Wrs1 = 0.10000000149011612`, `aare = [300.0, None]`, `acdn = 9.0`, `adur = [5.0, None]`, `ahdu = [None, 2.0]`, `alev = 5`, `amcs = [80, None, 100, 110, 120]`

實作：

`Trig_HeroSkills22_Actions`　war3map.j:53412
```jass
elseif Skill=='A09B' then
set x=GetUnitX(u)
set y=GetUnitY(u)
set ug=CreateGroup()
set dmg=50*I2R(GetUnitAbilityLevel(u,Skill))+(GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.10
call GroupEnumUnitsInRange(ug,x,y,305.,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
```

## 屠殺 `A09A`　—　◈ 吃裝備技能威力

俄文原名：Резня

```
繼承裝備技能的加成。

戰爭首領的攻擊會對附近 4 名隨機敵人造成物理傷害。

傷害：（8% 英雄最大生命值）點

冷卻：6 秒。
```

物件欄位（原型 `Amgl`）：`aher = 1`

實作：

`Trig_HeroAttack22_Actions`　war3map.j:53489
```jass
if GetUnitAbilityLevel(u,'A09A')==1 then
if LoadReal(hash,u_Id,'A09A')==0. then
call StartModCooldown(u_Id,'A09A',6.)
set x=GetUnitX(u)
set y=GetUnitY(u)
set cof=LoadReal(hash,u_Id,18)+1.
set dmg=(GetUnitState((u),UNIT_STATE_MAX_LIFE))*0.08*cof
set ug=CreateGroup()
set ug2=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,350.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call GroupAddUnit(ug2,u3)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set ug2=GetRandomSubGroup(4,ug2)
loop
set u3=FirstOfGroup(ug2)
exitwhen u3==null
call UnitDamageTarget(u,u3,dmg,true,false,ATTACK_TYPE_HERO,DAMAGE_TYPE_NORMAL,WEAPON_TYPE_WHOKNOWS)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\Stampede\\StampedeMissileDeath.mdl",u3,"origin"))
call GroupRemoveUnit(ug2,u3)
endloop
call DestroyGroup(ug2)
endif
endif
```

## 飽食的獸人 `A099`

俄文原名：Сытый орк

```
想要又大又壯，真正的戰爭首領就得多吃！對自己的養豬場、普通農場或珊瑚池使用此技能，讓英雄完全回復生命值並提升生命值上限。

生命值上限加成：目前人口上限的 +150%

冷卻：90 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [1.0, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [1.0, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['dreadlordinferno', 'channel']`, `aare = 200.0`, `acdn = [90.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [200.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_HeroSkills22_Actions`　war3map.j:53398
```jass
if Skill=='A099' then
set n2=GetPlayerState(pl,PLAYER_STATE_RESOURCE_FOOD_CAP)
set n2=n2+n2/2
call SetUnitState(u,UNIT_STATE_LIFE,(GetUnitState((u),UNIT_STATE_MAX_LIFE)))
call SetUnitLife(u,R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE)))+n2)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\AIsm\\AIsmTarget.mdl",u,"origin"))
```

`Trig_HeroSkillCheck22_Conditions`　war3map.j:53449
```jass
function Trig_HeroSkillCheck22_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A099'
endfunction
function Trig_HeroSkillCheck22_Actions takes nothing returns nothing
local unit u=GetSpellAbilityUnit()
local unit u2=GetSpellTargetUnit()
if GetOwningPlayer(u2)==GetOwningPlayer(u)and(GetUnitTypeId(u2)=='n00A' or GetUnitTypeId(u2)=='n00B' or GetUnitTypeId(u2)=='n00C' or GetUnitTypeId(u2)=='h006' or GetUnitTypeId(u2)=='h007' or GetUnitTypeId(u2)=='h008' or GetUnitTypeId(u2)=='h03W' or GetUnitTypeId(u2)=='n04J' or GetUnitTypeId(u2)=='n04K' or GetUnitTypeId(u2)=='n069' or GetUnitTypeId(u2)=='n06A')then
else
call IssueImmediateOrder(u,"stop")
endif
set u=null
set u2=null
endfunction
```

## 選擇天賦 `A0YJ`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的強度等級。
```

**天賦選項**：
  - `A0YK` 力量法則
    強度等級：T2 屬性加成／每級屬性成長加成： +2 / +1 +1 / +0 +0 / +0  技能強度的 500% 會作為額外加成套用至英雄的生命值上限。
  - `A0YM` 備用砍刀
    強度等級：T3 屬性加成／每級屬性成長加成： +4 / +2 +2 / +0 +1 / +0  你可以將斧、鎚或任何其他重型武器移入第二背包（F2）（劍類不算）。如此便可在第二背包中持有 1 件物品。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0YK,A0YM`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills22_Actions`　war3map.j:53379
```jass
if Skill=='A0YK' then
call SetHeroStr(u,GetHeroStr(u,false)+2,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A0YJ')
call UnitAddAbility(u,'A0YL')
set t=CreateTimer()
call TimerStart(t,0.5,true,function WarbossD2)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0YM' then
call SetHeroStr(u,GetHeroStr(u,false)+4,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A0YJ')
call UnitAddAbility(u,'A0YN')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

`WarbossD2`　war3map.j:53331
```jass
function WarbossD2 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer i=LoadInteger(hash,Id,1)
local integer i2
local real r
local real r2
local integer n=GetPlayerId(GetOwningPlayer(u))+1
local integer MainStat
if UnitAlive(u)then
set MainStat=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE)))
set i2=R2I(udg_ItemBonusDMG[n]*5.)
if i !=i2 then
call SetUnitLife(u,MainStat-i+i2)
endif
call SaveInteger(hash,Id,1,i2)
endif
set u=null
set t=null
endfunction
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 力量法則 `A0YK`　—　來自天賦「選擇天賦」

俄文原名：Принцип силы

```
強度等級：T2
屬性加成／每級屬性成長加成：
+2 / +1
+1 / +0
+0 / +0

技能強度的 500% 會作為額外加成套用至英雄的生命值上限。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acidbomb', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 15`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills22_Actions`　war3map.j:53379
```jass
if Skill=='A0YK' then
call SetHeroStr(u,GetHeroStr(u,false)+2,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A0YJ')
call UnitAddAbility(u,'A0YL')
set t=CreateTimer()
call TimerStart(t,0.5,true,function WarbossD2)
call SaveUnitHandle(hash,GetHandleId(t),1,u)
call SaveInteger(hash,GetHandleId(pl),15,1)
```

`WarbossD2`　war3map.j:53331
```jass
function WarbossD2 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer i=LoadInteger(hash,Id,1)
local integer i2
local real r
local real r2
local integer n=GetPlayerId(GetOwningPlayer(u))+1
local integer MainStat
if UnitAlive(u)then
set MainStat=R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE)))
set i2=R2I(udg_ItemBonusDMG[n]*5.)
if i !=i2 then
call SetUnitLife(u,MainStat-i+i2)
endif
call SaveInteger(hash,Id,1,i2)
endif
set u=null
set t=null
endfunction
```

## 備用砍刀 `A0YM`　—　來自天賦「選擇天賦」

俄文原名：Запасная чоппа

```
強度等級：T3
屬性加成／每級屬性成長加成：
+4 / +2
+2 / +0
+1 / +0

你可以將斧、鎚或任何其他重型武器移入第二背包（F2）（劍類不算）。如此便可在第二背包中持有 1 件物品。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = [None, 1]`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['acolyteharvest', 'channel']`, `acap = `, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = 100.0`, `arqa = 24`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`Trig_HeroSkills22_Actions`　war3map.j:53389
```jass
elseif Skill=='A0YM' then
call SetHeroStr(u,GetHeroStr(u,false)+4,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A0YJ')
call UnitAddAbility(u,'A0YN')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

---

## 同一組的其他實作函式

英雄的實作散在同編號的一組函式裡，上面按技能抽取時抓不到的補在這裡
（常見的是決定門檻、結算加成、清理 buff 的那幾支）。

`Trig_CreateHero22_Actions`　war3map.j:43671
```jass
function Trig_CreateHero22_Actions takes nothing returns nothing
local location p
local unit u
local integer i
if udg_WaveFase==7 then
set udg_EnemyHeroType[1]='Opgh'
set udg_EnemyHeroType[2]='Uwar'
elseif udg_WaveFase==8 then
set udg_EnemyHeroType[1]='U00R'
set udg_EnemyHeroType[2]='U00N'
elseif udg_WaveFase==9 then
set udg_EnemyHeroType[1]='U00H'
set udg_EnemyHeroType[2]='N063'
elseif udg_WaveFase==10 then
set udg_EnemyHeroType[1]='U010'
set udg_EnemyHeroType[2]='U00X'
elseif udg_WaveFase>=11 then
set udg_EnemyHeroType[1]='H03C'
set udg_EnemyHeroType[2]='Ubal'
endif
set udg_EnemyHeroLvl=udg_EnemyHeroLvl+1
set p=udg_SpawnPoints[GetRandomInt(1,4)]
set u=CreateUnitAtLoc(AI[GetRandomInt(1,3)],udg_EnemyHeroType[GetRandomInt(1,2)],p,GetRandomReal(0.,360.))
call SetHeroLevel(u,3+udg_EnemyHeroLvl,false)
call PrepareEnemyHero(u)
call GroupAddUnit(udg_AttackGroup,u)
call IssuePointOrderByIdLoc(u,Order_attack,udg_DefPoint)
if udg_WaveFase==2 then
call SpawnEnemy('u00I',p,udg_AttackGroup,0)
call SpawnEnemy('u00I',p,udg_AttackGroup,0)
call SpawnEnemy('n01X',p,udg_AttackGroup,0)
elseif udg_WaveFase==3 then
set i=1
loop
exitwhen i>4
call SpawnEnemy('n01Z',p,udg_AttackGroup,0)
set i=i+1
endloop
call SpawnEnemy('n038',p,udg_AttackGroup,0)
call SpawnEnemy('n038',p,udg_AttackGroup,0)
elseif udg_WaveFase==4 then
set i=1
loop
exitwhen i>5
call SpawnEnemy('n01F',p,udg_AttackGroup,0)
set i=i+1
endloop
call SpawnEnemy('u01F',p,udg_AttackGroup,0)
elseif udg_WaveFase==5 then
set i=1
loop
exitwhen i>4
call SpawnEnemy('n038',p,udg_AttackGroup,0)
call SpawnEnemy('n04X',p,udg_AttackGroup,0)
call SpawnEnemy('u00Y',p,udg_AttackGroup,0)
set i=i+1
endloop
elseif udg_WaveFase>=6 then
call SpawnEnemy('u00O',p,udg_AttackGroup,0)
call SpawnEnemy('u00O',p,udg_AttackGroup,0)
endif
call DisplayTimedTextToForce(bj_FORCE_ALL_PLAYERS,30.,"|cFFE60000Появился вражеский герой!|r")
set p=null
set u=null
endfunction
```

`Trig_CreateHero22_extra_Actions`　war3map.j:43741
```jass
function Trig_CreateHero22_extra_Actions takes nothing returns nothing
local location p
local unit u
local integer i
set udg_EnemyHeroLvl=udg_EnemyHeroLvl+1
set p=udg_SpawnPoints[GetRandomInt(1,2)]
set u=CreateUnitAtLoc(AI[GetRandomInt(1,3)],udg_EnemyHeroType[GetRandomInt(3,9)],p,GetRandomReal(0.,360.))
call SetHeroLevel(u,3+udg_EnemyHeroLvl,false)
call PrepareEnemyHero(u)
call GroupAddUnit(udg_AttackGroup,u)
call IssuePointOrderByIdLoc(u,Order_attack,udg_DefPoint)
set i=1
loop
exitwhen i>udg_WaveFase
call SpawnEnemy('n05E',p,udg_AttackGroup,0)
set i=i+1
endloop
set p=udg_SpawnPoints[GetRandomInt(2,3)]
set u=CreateUnitAtLoc(AI[GetRandomInt(1,3)],udg_EnemyHeroType[GetRandomInt(3,9)],p,GetRandomReal(0.,360.))
call SetHeroLevel(u,3+udg_EnemyHeroLvl,false)
call PrepareEnemyHero(u)
call GroupAddUnit(udg_AttackGroup,u)
call IssuePointOrderByIdLoc(u,Order_attack,udg_DefPoint)
set i=1
loop
exitwhen i>udg_WaveFase
call SpawnEnemy('n05E',p,udg_AttackGroup,0)
set i=i+1
endloop
set p=udg_SpawnPoints[GetRandomInt(3,4)]
set u=CreateUnitAtLoc(AI[GetRandomInt(1,3)],udg_EnemyHeroType[GetRandomInt(3,9)],p,GetRandomReal(0.,360.))
call SetHeroLevel(u,3+udg_EnemyHeroLvl,false)
call PrepareEnemyHero(u)
call GroupAddUnit(udg_AttackGroup,u)
call IssuePointOrderByIdLoc(u,Order_attack,udg_DefPoint)
set i=1
loop
exitwhen i>udg_WaveFase
call SpawnEnemy('n05E',p,udg_AttackGroup,0)
set i=i+1
endloop
set p=udg_SpawnPoints[GetRandomInt(1,4)]
set u=CreateUnitAtLoc(AI[GetRandomInt(1,3)],udg_EnemyHeroType[GetRandomInt(3,9)],p,GetRandomReal(0.,360.))
call SetHeroLevel(u,3+udg_EnemyHeroLvl,false)
call PrepareEnemyHero(u)
call GroupAddUnit(udg_AttackGroup,u)
call IssuePointOrderByIdLoc(u,Order_attack,udg_DefPoint)
set i=1
loop
exitwhen i>udg_WaveFase
call SpawnEnemy('n05E',p,udg_AttackGroup,0)
set i=i+1
endloop
call DisplayTimedTextToForce(bj_FORCE_ALL_PLAYERS,30.,"|cFFE60000Появились вражеские герои!|r")
set p=null
set u=null
endfunction
```

`Trig_HeroSkillCheck22_Actions`　war3map.j:53452
```jass
function Trig_HeroSkillCheck22_Actions takes nothing returns nothing
local unit u=GetSpellAbilityUnit()
local unit u2=GetSpellTargetUnit()
if GetOwningPlayer(u2)==GetOwningPlayer(u)and(GetUnitTypeId(u2)=='n00A' or GetUnitTypeId(u2)=='n00B' or GetUnitTypeId(u2)=='n00C' or GetUnitTypeId(u2)=='h006' or GetUnitTypeId(u2)=='h007' or GetUnitTypeId(u2)=='h008' or GetUnitTypeId(u2)=='h03W' or GetUnitTypeId(u2)=='n04J' or GetUnitTypeId(u2)=='n04K' or GetUnitTypeId(u2)=='n069' or GetUnitTypeId(u2)=='n06A')then
else
call IssueImmediateOrder(u,"stop")
endif
set u=null
set u2=null
endfunction
```

`Hero22E`　war3map.j:53467
```jass
function Hero22E takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call SaveInteger(hash,GetHandleId(u),21,0)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set t=null
set u=null
endfunction
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **18** — 裝備技能威力〔持有者〕道具觸發用 cof = key18 + 1

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
