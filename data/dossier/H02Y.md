# 吟遊詩人 `H02Y`（Менестрель）

主屬性 **智力** · 背包 **6 格** · 解鎖 500000 · 定位 輔助

| | 初始 | 每級 |
|---|---|---|
| 力量 | 16 | 1.5 |
| 敏捷 | 25 | 3 |
| 智力 | 31 | 3.5 |

> 特殊的輔助英雄，不直接參戰，專門強化隊友。

**縮放**：吃技能強度的技能 ['A0JW', 'A0K5'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 節奏 `A0K8`

俄文原名：Ритм

```
提升友方英雄的攻擊速度，並使其能夠在每次擊殺時回復 1% 生命值。

攻擊速度提升：50%
持續時間：12 秒

冷卻：19 秒
```

每級變動：
  - 第 3 行：50 / 75 / 100 / 125 / 150

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = deathcoil`, `acdn = 19.0`, `alev = 5`, `amcs = [90, 100, 110, 120, 130]`, `aran = 600.0`, `atar = hero,self,allies`

實作：

`Trig_UseSkillsEndcast_Actions`　war3map.j:28841
```jass
if u==u2 and(GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB')then
set u=null
set u2=null
set u3=null
set ug=null
set ug2=null
set pl=null
set Item=null
set t=null
return
endif
```

`Hero46E`　war3map.j:60598
```jass
function Hero46E takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
local integer count=LoadInteger(hash,GetHandleId(t),3)
set count=count-1
call SaveInteger(hash,GetHandleId(t),3,count)
if count==5 then
call DestroyEffect(LoadEffectHandle(hash,GetHandleId(t),2))
elseif count<=0 then
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)-LoadInteger(hash,GetHandleId(t),2))
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
endif
set u=null
set t=null
endfunction
```

`Trig_HeroSkills46_Actions`　war3map.j:60710
```jass
elseif Skill=='A0K8' then
set count=25+25*GetUnitAbilityLevel(u,Skill)
call SetUnitAttackSpeed(u2,GetUnitAttackSpeed(u2)+count)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
call SaveInteger(hash,Id,2,count)
call SaveEffectHandle(hash,Id,2,AddSpecialEffectTarget("war3mapImported\\MusicTarget.mdx",u2,"overhead"))
call SaveInteger(hash,Id,3,12)
call TimerStart(t,1,true,function Hero46E)
```

`Trig_HeroCheckSkills46_Actions`　war3map.j:60832
```jass
if GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB' then
if IsUnitEnemy(u2,pl)then
call IssueImmediateOrderById(u,Order_stop)
elseif u==u2 and GetUnitTypeId(u)=='H02Y' then
call IssueImmediateOrderById(u,Order_stop)
endif
endif
```

## 重金屬 `A0K5`　—　吃技能強度

俄文原名：Хэви метал

```
暈眩自身周圍的敵人並對其造成傷害。

傷害：全部屬性的 20% +（50% 技能強度）
暈眩：4 秒
暈眩（英雄）：2 秒

冷卻：9 秒
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60
  - 第 4 行：4 / 5 / 6 / 7 / 8
  - 第 5 行：2 / 2.5 / 3 / 3.5 / 4
  - 第 7 行：9 / 9.5 / 10 / 10.5 / 11

物件欄位（原型 `AOw2`）：`Wrs1 = 0.10000000149011612`, `aare = [300.0, None]`, `acdn = [9.0, 9.5, 10.0, 10.5, 11.0]`, `adur = [4.0, 5.0, 6.0, 7.0, 8.0]`, `ahdu = [None, 2.5, 3.0, 3.5, 4.0]`, `alev = 5`, `amcs = [80, None, 100, 110, 120]`

實作：

`Trig_HeroSkills46_Actions`　war3map.j:60675
```jass
elseif Skill=='A0K5' then
set dmg=udg_ItemBonusDMG[n]*0.50+I2R(GetHeroStr(u,true)+GetHeroAgi(u,true)+GetHeroInt(u,true))*(0.10+0.10*I2R(GetUnitAbilityLevel(u,Skill)))
set x=GetUnitX(u)
set y=GetUnitY(u)
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,305,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
```

## 頓悟 `A0K3`

俄文原名：Прозрение

```
永久提升友方英雄的智力，並賦予其持續 10 秒的法力回復光環。

智力提升：+1 點
光環效果：+3 MP regen
光環作用範圍：400

冷卻：30 秒
```

每級變動：
  - 第 4 行：3 / 5 / 7 / 9 / 11
  - 第 7 行：30 / 27 / 24 / 21 / 18

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = cloudoffog`, `acdn = [30.0, 27.0, 24.0, 21.0, 18.0]`, `alev = 5`, `amcs = [110, 125, 140, 155, 170]`, `aran = 600.0`, `atar = hero,self,allies`

實作：

`Trig_UseSkillsEndcast_Actions`　war3map.j:28841
```jass
if u==u2 and(GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB')then
set u=null
set u2=null
set u3=null
set ug=null
set ug2=null
set pl=null
set Item=null
set t=null
return
endif
```

`RemoveBuffHero46`　war3map.j:60615
```jass
function RemoveBuffHero46 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer check=LoadInteger(hash,GetHandleId(u),'A0K3')
set check=check-1
call SaveInteger(hash,GetHandleId(u),'A0K3',check)
if check==0 then
call UnitRemoveAbility(u,'A0K4')
endif
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills46_Actions`　war3map.j:60690
```jass
elseif Skill=='A0K3' then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\AIim\\AIimTarget.mdl",u2,"origin"))
call SetHeroInt(u2,GetHeroInt(u2,false)+1,true)
call UnitAddAbility(u2,'A0K4')
call SetUnitAbilityLevel(u2,'A0K4',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u2)
call SaveInteger(hash,GetHandleId(u2),'A0K3',LoadInteger(hash,GetHandleId(u2),'A0K3')+1)
call TimerStart(t,10,false,function RemoveBuffHero46)
elseif Skill=='A0WG' then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\AIim\\AIimTarget.mdl",u,"origin"))
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call UnitAddAbility(u,'A0K4')
call SetUnitAbilityLevel(u,'A0K4',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,GetHandleId(u),'A0K3',LoadInteger(hash,GetHandleId(u),'A0K3')+1)
call TimerStart(t,10,false,function RemoveBuffHero46)
```

`Trig_HeroCheckSkills46_Actions`　war3map.j:60832
```jass
if GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB' then
if IsUnitEnemy(u2,pl)then
call IssueImmediateOrderById(u,Order_stop)
elseif u==u2 and GetUnitTypeId(u)=='H02Y' then
call IssueImmediateOrderById(u,Order_stop)
endif
endif
```

## 宿命安魂曲 `A0JW`　—　吃技能強度

俄文原名：Роковой реквием

```
英雄開始持續維持法術，期間英雄周圍 2000 半徑內的隨機敵人會在每次觸發時受到傷害。

傷害：20 + （10% 技能強度） + （30% 全部屬性）
觸發間隔：0.5 秒
每次觸發的敵人數量：8 名
持續時間：20 秒

冷卻：120 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = 20.100000381469727`, `Ncl3 = 1`, `Ncl4 = 0.7300000190734863`, `Ncl5 = 0`, `Ncl6 = wispharvest`, `acap = `, `acdn = 120.0`, `alev = 1`, `amcs = 300`, `atar = player,structure`

實作：

`HeroR46_conditions`　war3map.j:60741
```jass
function HeroR46_conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0JW'
endfunction
```

`Trig_HeroR46_Stop_Conditions`　war3map.j:60807
```jass
function Trig_HeroR46_Stop_Conditions takes nothing returns boolean
return GetSpellAbilityId()=='A0JW'
endfunction
function Trig_HeroR46_Stop_Actions takes nothing returns nothing
local unit u=GetSpellAbilityUnit()
local integer Id=GetHandleId(u)
local timer t=LoadTimerHandle(hash,Id,20)
local integer Id_t=GetHandleId(t)
call UnitRemoveAbility(u,'A0K2')
call FlushChildHashtable(hash,Id_t)
call PauseTimer(t)
call DestroyTimer(t)
call RemoveSavedHandle(hash,Id,20)
set u=null
set t=null
endfunction
```

## （樂感） `S00A`

俄文原名：(музыкальность)

物件欄位（原型 `SCae`）：`Oae1 = -0.15000000596046448`, `Oae2 = 0.0`, `aare = 800.0`, `abuf = B027`, `atar = enemies`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 額外技能 `A0K9`

俄文原名：Дополнительные умения

```
開啟英雄的額外技能列表。
```

**天賦選項**：
  - `A0JV` 佔領據點
    佔領選定的據點。無法佔領敵方的建築。  冷卻：10 秒。
  - `A0KA` 治癒之歌
    為友方英雄回復 15% 生命值。技能強度 200 以上時，額外提升目標 25 點生命值上限。  冷卻：15 秒。
  - `A0KB` 戰歌
    使友方英雄的力量提升 1 點。技能強度 500 以上時，再額外提升友方英雄 1 點力量。  冷卻：20 秒。
  - `S00B` 樂感
    吟遊詩人提升附近友軍的移動速度，並減緩附近敵人的移動。作用範圍為 800 點。  友軍移動速度加成：10% 敵人移動速度降低：15%

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0JV,A0KA,A0KB,S00B`, `spb2 = 0`, `spb3 = 4`, `spb4 = 4`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 佔領據點 `A0JV`　—　來自天賦「額外技能」

俄文原名：Захватить точку

```
佔領選定的據點。無法佔領敵方的建築。

冷卻：10 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['uncoldarrows', 'channel']`, `acdn = [10.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [50, 95, 110, 125, 140, 155, 170]`, `aran = [250.0, 100.0]`, `atar = ['enemies,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_HeroSkills46_Actions`　war3map.j:60646
```jass
if Skill=='A0JV' and IsUnitType(u2,UNIT_TYPE_STRUCTURE)then
set x=GetUnitX(u2)
set y=GetUnitY(u2)
call DestroyEffect(AddSpecialEffect("Abilities\\Spells\\Other\\Charm\\CharmTarget.mdl",x,y))
call SetUnitOwner(u2,pl,true)
```

`Trig_HeroCheckSkills46_Actions`　war3map.j:60839
```jass
if GetSpellAbilityId()=='A0JV' then
if GetUnitTypeId(u2)!='n000' and GetUnitTypeId(u2)!='n002' and GetUnitTypeId(u2)!='n001' and GetUnitTypeId(u2)!='n003' and GetUnitTypeId(u2)!='n01U' and GetUnitTypeId(u2)!='n068' then
call IssueImmediateOrderById(u,Order_stop)
endif
endif
```

## 治癒之歌 `A0KA`　—　來自天賦「額外技能」

俄文原名：Целебная песнь

```
為友方英雄回復 15% 生命值。技能強度 200 以上時，額外提升目標 25 點生命值上限。

冷卻：15 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['transmute', 'channel']`, `acap = `, `acdn = [15.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [50, 95, 110, 125, 140, 155, 170]`, `aran = [600.0, 100.0]`, `atar = ['hero,self,allies', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_UseSkillsEndcast_Actions`　war3map.j:28841
```jass
if u==u2 and(GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB')then
set u=null
set u2=null
set u3=null
set ug=null
set ug2=null
set pl=null
set Item=null
set t=null
return
endif
```

`Trig_HeroSkills46_Actions`　war3map.j:60651
```jass
elseif Skill=='A0KA' then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Human\\HolyBolt\\HolyBoltSpecialArt.mdl",u2,"origin"))
call SetUnitState(u2,UNIT_STATE_LIFE,GetUnitState(u2,UNIT_STATE_LIFE)+(GetUnitState((u2),UNIT_STATE_MAX_LIFE))*0.15)
if udg_ItemBonusDMG[n]>=200 then
call SetUnitLife(u2,R2I((GetUnitState((u2),UNIT_STATE_MAX_LIFE)))+25)
endif
```

`Trig_HeroCheckSkills46_Actions`　war3map.j:60832
```jass
if GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB' then
if IsUnitEnemy(u2,pl)then
call IssueImmediateOrderById(u,Order_stop)
elseif u==u2 and GetUnitTypeId(u)=='H02Y' then
call IssueImmediateOrderById(u,Order_stop)
endif
endif
```

## 戰歌 `A0KB`　—　來自天賦「額外技能」

俄文原名：Боевая песнь

```
使友方英雄的力量提升 1 點。技能強度 500 以上時，再額外提升友方英雄 1 點力量。

冷卻：20 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.5, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.5, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['townbelloff', 'channel']`, `acdn = [20.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [50, 95, 110, 125, 140, 155, 170]`, `aran = [600.0, 100.0]`, `atar = ['hero,self,allies', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_UseSkillsEndcast_Actions`　war3map.j:28841
```jass
if u==u2 and(GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB')then
set u=null
set u2=null
set u3=null
set ug=null
set ug2=null
set pl=null
set Item=null
set t=null
return
endif
```

`Trig_HeroSkills46_Actions`　war3map.j:60663
```jass
elseif Skill=='A0KB' then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\AIsm\\AIsmTarget.mdl",u2,"origin"))
call SetHeroStr(u2,GetHeroStr(u2,false)+1,true)
if udg_ItemBonusDMG[n]>=500 then
call SetHeroStr(u2,GetHeroStr(u2,false)+1,true)
endif
```

`Trig_HeroCheckSkills46_Actions`　war3map.j:60832
```jass
if GetSpellAbilityId()=='A0K3' or GetSpellAbilityId()=='A0K8' or GetSpellAbilityId()=='A0KA' or GetSpellAbilityId()=='A0KB' then
if IsUnitEnemy(u2,pl)then
call IssueImmediateOrderById(u,Order_stop)
elseif u==u2 and GetUnitTypeId(u)=='H02Y' then
call IssueImmediateOrderById(u,Order_stop)
endif
endif
```

## 樂感 `S00B`　—　來自天賦「額外技能」

俄文原名：Музыкальность

```
吟遊詩人提升附近友軍的移動速度，並減緩附近敵人的移動。作用範圍為 800 點。

友軍移動速度加成：10%
敵人移動速度降低：15%
```

物件欄位（原型 `SCae`）：`Oae2 = 0.0`, `aare = 800.0`, `abuf = B028`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 節奏（對自己） `A0WH`　—　來自皮膚「搖滾樂手」

俄文原名：Ритм (на себя)

```
提升友方英雄的攻擊速度，並使其能夠在每次擊殺時回復 1% 生命值。

攻擊速度提升：50%
持續時間：12 秒

冷卻：19 秒
```

每級變動：
  - 第 3 行：50 / 75 / 100 / 125 / 150

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = deathcoil`, `acdn = 19.0`, `alev = 5`, `amcs = [90, 100, 110, 120, 130]`, `aran = 600.0`, `atar = hero,self,allies`

實作：

`Hero46E`　war3map.j:60598
```jass
function Hero46E takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
local integer count=LoadInteger(hash,GetHandleId(t),3)
set count=count-1
call SaveInteger(hash,GetHandleId(t),3,count)
if count==5 then
call DestroyEffect(LoadEffectHandle(hash,GetHandleId(t),2))
elseif count<=0 then
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)-LoadInteger(hash,GetHandleId(t),2))
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
endif
set u=null
set t=null
endfunction
```

`Trig_HeroSkills46_Actions`　war3map.j:60720
```jass
elseif Skill=='A0WH' then
set count=25+25*GetUnitAbilityLevel(u,Skill)
call SetUnitAttackSpeed(u,GetUnitAttackSpeed(u)+count)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,2,count)
call SaveEffectHandle(hash,Id,2,AddSpecialEffectTarget("war3mapImported\\MusicTarget.mdx",u,"overhead"))
call SaveInteger(hash,Id,3,12)
call TimerStart(t,1,true,function Hero46E)
endif
```

## 頓悟（對自己） `A0WG`　—　來自皮膚「搖滾樂手」

俄文原名：Прозрение (на себя)

```
永久提升友方英雄的智力，並賦予其持續 10 秒的法力回復光環。

智力提升：+1 點
光環效果：+3 MP regen
光環作用範圍：400 點

冷卻：30 秒
```

每級變動：
  - 第 4 行：3 / 5 / 7 / 9 / 11
  - 第 7 行：30 / 27 / 24 / 21 / 18

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = cloudoffog`, `acdn = [30.0, 27.0, 24.0, 21.0, 18.0]`, `alev = 5`, `amcs = [110, 125, 140, 155, 170]`, `aran = 600.0`, `atar = hero,self,allies`

實作：

`RemoveBuffHero46`　war3map.j:60615
```jass
function RemoveBuffHero46 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer check=LoadInteger(hash,GetHandleId(u),'A0K3')
set check=check-1
call SaveInteger(hash,GetHandleId(u),'A0K3',check)
if check==0 then
call UnitRemoveAbility(u,'A0K4')
endif
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills46_Actions`　war3map.j:60700
```jass
elseif Skill=='A0WG' then
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Items\\AIim\\AIimTarget.mdl",u,"origin"))
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call UnitAddAbility(u,'A0K4')
call SetUnitAbilityLevel(u,'A0K4',GetUnitAbilityLevel(u,Skill))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,GetHandleId(u),'A0K3',LoadInteger(hash,GetHandleId(u),'A0K3')+1)
call TimerStart(t,10,false,function RemoveBuffHero46)
```

---

## 皮膚

### 搖滾樂手 `Ocb2` —— **會換技能**
  - 節奏 `A0K8` → **節奏（對自己）** `A0WH`
  - 頓悟 `A0K3` → **頓悟（對自己）** `A0WG`

---

## 這隻碰到的 hash key

  - **3** — 對英雄傷害 +%〔攻擊者〕Trig_HeroTakeDamage_Actions 的 DefCof

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
