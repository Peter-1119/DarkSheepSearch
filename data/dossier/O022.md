# 老酋長 `O022`（Старый вождь）

主屬性 **力量** · 背包 **6 格** · 解鎖 50000 · 定位 戰士/刺客

| | 初始 | 每級 |
|---|---|---|
| 力量 | 25 | 3.5999999046325684 |
| 敏捷 | 18 | 2.0 |
| 智力 | 19 | 2.0 |

> 莽撞的近戰英雄，在戰鬥狂熱中能打出大量傷害。

**縮放**：吃技能強度的技能 ['A0NQ', 'A0NW'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 吞噬之刃 `A0NX`

俄文原名：Поглощающий клинок

```
擊殺敵人可提升英雄的生命值回復。這些效果可以疊加，但不會延長彼此的持續時間。

回復加成：4 + （被擊殺敵人的等級）點
加成持續時間：5 秒。
```

每級變動：
  - 第 3 行：4 / 6 / 8 / 10 / 12

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`SetUnitLifeRegeneration`　war3map.j:4167
```jass
function SetUnitLifeRegeneration takes unit u,real a returns nothing
local integer Id=GetHandleId(u)
if a>8191 then
set a=8191
endif
call SaveReal(hash,Id,32,a)
call GroupAddUnit(setAttribute___allUnits,u)
endfunction
```

`Hero38E`　war3map.j:58332
```jass
function Hero38E takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local real r=LoadReal(hash,Id,1)
call SetUnitLifeRegeneration(u,GetUnitLifeRegeneration(u)-r)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroKills38_Actions`　war3map.j:58349
```jass
if GetUnitAbilityLevel(u,'A0NX')>=1 then
set r=2+2*I2R(GetUnitAbilityLevel(u,'A0NX'))+I2R(GetUnitLevel(GetDyingUnit()))*2
call SetUnitLifeRegeneration(u,GetUnitLifeRegeneration(u)+r)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,r)
call TimerStart(t,5,false,function Hero38E)
endif
```

## 邪能之焰 `A0NW`　—　吃技能強度

俄文原名：Пламя скверны

```
毀滅之焰包覆英雄，對周圍的敵人以及英雄自身造成傷害。每次使用後技能傷害都會提升，此累積效果會在英雄死亡後重置。

傷害：18 + （10% 技能強度）點/秒。
對英雄自身的傷害：技能主要傷害的 80%
持續時間：10 秒。
每次使用的傷害加成：+8%

冷卻：18 秒。
```

每級變動：
  - 第 3 行：18 / 26 / 34 / 42 / 50
  - 第 4 行：80 / 75 / 70 / 65 / 60
  - 第 6 行：8 / 10 / 12 / 14 / 16

物件欄位（原型 `Absk`）：`abuf = B02M`, `acdn = 18.0`, `adur = 10.0`, `ahdu = 10.0`, `aher = 1`, `alev = 5`, `amcs = [70, 80, 90, 100, 110]`, `bsk1 = 0.0`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`Trig_DieHero_Actions`　war3map.j:46583
```jass
if GetUnitTypeId(u)=='O022' then
call SaveReal(hash,GetHandleId(u),'A0NW',1.00)
endif
```

`Hero38W`　war3map.j:58159
```jass
function Hero38W takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit hero=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(hero)
local integer n=GetPlayerId(pl)+1
local unit u3
local integer count=LoadInteger(hash,Id,1)
local real x=GetUnitX(hero)
local real y=GetUnitY(hero)
local group ug=CreateGroup()
local real dmg=((10+8*I2R(GetUnitAbilityLevel(hero,'A0NW'))+udg_ItemBonusDMG[n]*0.10)*LoadReal(hash,GetHandleId(hero),'A0NW'))*0.50
local real r=235
call GroupEnumUnitsInRange(ug,x,y,r,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
if GetUnitAbilityLevel(u3,'A0NV')==1 then
call UnitDamageTarget(hero,u3,dmg*1.30,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
else
call UnitDamageTarget(hero,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
call UnitDamageTarget(hero,hero,dmg*(0.85-0.05*I2R(GetUnitAbilityLevel(hero,'A0NW'))),false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if UnitAlive(hero)!=true then
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
set count=count-1
if count<=0 then
call SaveReal(hash,GetHandleId(hero),'A0NW',LoadReal(hash,GetHandleId(hero),'A0NW')+0.06+0.02*I2R(GetUnitAbilityLevel(hero,'A0NW')))
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
call SaveInteger(hash,Id,1,count)
endif
endif
set t=null
set u3=null
set hero=null
set ug=null
set pl=null
endfunction
```

`Trig_HeroSkills38_Actions`　war3map.j:58251
```jass
elseif Skill=='A0NW' then
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,1,20)
call TimerStart(t,0.50,true,function Hero38W)
```

## 徒手格鬥 `A0NQ`　—　吃技能強度

俄文原名：Кулачный бой

```
以近戰方式對目標施以重擊，將其擊退並暈眩數秒。

傷害：125 + 85% 技能強度
暈眩（一般部隊）：5 秒。
暈眩（英雄）：1.8 秒。

冷卻：11 秒。
```

每級變動：
  - 第 3 行：125 / 250 / 375 / 500 / 625
  - 第 4 行：5 / 5.6 / 6.2 / 6.8 / 7.4
  - 第 5 行：1.8 / 2.2 / 2.6 / 3 / 3.4

物件欄位（原型 `AHtb`）：`Htb1 = 0.10000000149011612`, `acdn = 11.0`, `adur = [5.599999904632568, 6.799999713897705, 7.399999618530273, 6.199999809265137]`, `ahdu = [1.7999999523162842, 2.200000047683716, 2.6000001430511475, 3.4000003337860107]`, `alev = 5`, `amat = `, `amcs = [70, 100, 110, 90, 80]`, `amsp = 2000`, `aran = 120.0`

實作：

`KnockBackUnit`　war3map.j:2849
```jass
function KnockBackUnit takes unit knockbacked,real distance,real time,real degrees,real period returns nothing
local integer amount=R2I(time/period)
local real portion=distance/amount
local timer t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),0,knockbacked)
call SaveInteger(hash,GetHandleId(t),1,amount)
call SaveReal(hash,GetHandleId(t),2,portion)
call SaveInteger(hash,GetHandleId(t),3,0)
call SaveReal(hash,GetHandleId(t),4,degrees)
call TimerStart(t,period,true,function KnockBackUnit_Timer)
set t=null
set knockbacked=null
endfunction
```

`Trig_HeroSkills38_Actions`　war3map.j:58234
```jass
if Skill=='A0NQ' then
set u2=GetSpellTargetUnit()
set dmg=125*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[n]*0.85
if GetUnitAbilityLevel(u2,'A0NV')==1 then
set dmg=dmg*1.30
endif
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
if UnitAlive(u2)then
set x2=GetUnitX(u2)
set y2=GetUnitY(u2)
set angle=AngleXY(x,y,x2,y2)
if IsUnitType(u2,UNIT_TYPE_HERO)then
call KnockBackUnit(u2,250,0.30,angle,0.03)
else
call KnockBackUnit(u2,500,0.40,angle,0.03)
endif
endif
```

## 戰鬥狂熱 `A0NY`

俄文原名：Боевое исступление

```
英雄暈眩自身周圍大範圍內的敵人，並暫時提升自己的攻擊力、獲得濺射效果，同時每次攻擊都會損失自身的生命值。

暈眩：8 秒
暈眩（英雄）：4 秒
攻擊力提升：+80%
濺射效果：對 400 點範圍內的敵人造成 30% 傷害
每次攻擊損失的生命值：當前生命值的 4%
加成持續時間：12 秒

冷卻：80 秒
```

物件欄位（原型 `AOw2`）：`Wrs1 = 0.10000000149011612`, `aare = [400.0, 300.0]`, `acdn = [80.0, 9.0]`, `adur = [8.0, 5.0]`, `ahdu = [2.0, 4.0]`, `alev = 1`, `amcs = [150, 100, 110, 120]`

實作：

`Hero38R`　war3map.j:58145
```jass
function Hero38R takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
call UnitRemoveAbility(u,'A0NZ')
call UnitRemoveAbility(u,'A0O0')
call UnitRemoveAbility(u,'B02N')
call SetUnitVertexColor(u,255,255,255,255)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills38_Actions`　war3map.j:58257
```jass
elseif Skill=='A0NY' then
call SetUnitVertexColor(u,255,0,0,255)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\NightElf\\BattleRoar\\RoarCaster.mdl",u,"chest"))
call UnitAddAbility(u,'A0NZ')
call UnitAddAbility(u,'A0O0')
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,12,false,function Hero38R)
endif
```

## 灌注恐懼 `A0NU`

俄文原名：Внушение страха

```
攻擊英雄的敵人有 10% 機率獲得持續 10 秒的恐懼效果。此效果會降低攻擊速度，並使該單位更容易受到基爾羅格的傷害。

攻擊速度降低：-20%
敵人受到英雄技能的傷害提升：+30%
英雄的攻擊對生命值低於 40% 的敵人造成額外傷害：（100% 力量）點；冷卻 1 秒
```

實作：

`EndCooldown`　war3map.j:2898
```jass
function EndCooldown takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local integer Key_1=LoadInteger(hash,Id,1)
local integer Key_2=LoadInteger(hash,Id,2)
local timerdialog td=LoadTimerDialogHandle(hash,Id,3)
if td !=null then
call DestroyTimerDialog(td)
endif
call SaveInteger(hash,Key_1,Key_2,0)
call PauseTimer(t)
call FlushChildHashtable(hash,Id)
call DestroyTimer(t)
set t=null
set td=null
endfunction
```

`Hero38D`　war3map.j:58276
```jass
function Hero38D takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
call UnitRemoveAbility(u,'A0NV')
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroAttack38_Actions`　war3map.j:58297
```jass
if GetUnitAbilityLevel(u2,'A0NV')==1 and LoadBoolean(hash,GetHandleId(u),'A0NU')==true and GetUnitLifePercent(u2)<=40.00 then
set dmg=I2R(GetHeroStr(u,true))
call UnitDamageTarget(u,u2,dmg,true,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_UNIVERSAL,WEAPON_TYPE_WHOKNOWS)
call DestroyEffect(AddSpecialEffectTarget("Objects\\Spawnmodels\\Orc\\OrcSmallDeathExplode\\OrcSmallDeathExplode.mdl",u2,"origin"))
call SaveBoolean(hash,GetHandleId(u),'A0NU',false)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveInteger(hash,Id,1,GetHandleId(u))
call SaveInteger(hash,Id,2,'A0NU')
call TimerStart(t,1,false,function EndCooldown)
endif
```

`Trig_HeroAttack38_Actions`　war3map.j:58309
```jass
if GetUnitAbilityLevel(u2,'A0NU')==1 and GetUnitAbilityLevel(u,'A0NV')!=1 then
set Id=GetRandomInt(1,10)
if Id==1 then
call UnitAddAbility(u,'A0NV')
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,10,false,function Hero38D)
endif
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **3** — 對英雄傷害 +%
  - **4** — 受到傷害 −%（被減的）

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
