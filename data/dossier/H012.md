# 秘法學者 `H012`（Арканолог）

主屬性 **智力** · 背包 **6 格** · 解鎖 0 · 定位 法師/輔助

| | 初始 | 每級 |
|---|---|---|
| 力量 | 12 | 1.7000000476837158 |
| 敏捷 | 16 | 1.2999999523162842 |
| 智力 | 23 | 2.799999952316284 |

> 遠程英雄，主要靠魔法傷害，能給自己或隊友套上秘法護盾。

**縮放**：吃技能強度的技能 ['A04S', 'A097', 'A0IT', 'A0IU'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 毀滅咒語 `A0IU`　—　吃技能強度

俄文原名：Разрушительное заклинание

```
英雄在身前生成不斷擴散的爆炸區域，在短暫延遲後造成傷害。

傷害：250 + （100% 技能強度）點

冷卻：60 秒。
```

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = ['channel', 'curse']`, `aare = 270.0`, `acdn = [60.0, 17.0]`, `alev = 1`, `amcs = [200, 80, 90, 100, 110, 120]`, `aran = [700.0, 900.0]`, `atar = air,ground,friend,neutral,self`

實作：

`ArcanologRRayStep`　war3map.j:48826
```jass
function ArcanologRRayStep takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local real cx=LoadReal(hash,Id,2)
local real cy=LoadReal(hash,Id,3)
local real angle=LoadReal(hash,Id,4)
local real stepDist=LoadReal(hash,Id,5)
local integer cur=LoadInteger(hash,Id,6)+1
local integer maxSteps=LoadInteger(hash,Id,7)
local real nx
local real ny
local location p
if cur>maxSteps or not UnitAlive(u)then
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
set nx=cx+stepDist*Cos(angle*bj_DEGTORAD)
set ny=cy+stepDist*Sin(angle*bj_DEGTORAD)
set p=Location(nx,ny)
set udg_u=u
set udg_p=p
call ArcanologRCreate(0.90)
call SaveReal(hash,Id,2,nx)
call SaveReal(hash,Id,3,ny)
set angle=angle+GetRandomReal(-18.5,18.5)
call SaveReal(hash,Id,4,angle)
call SaveInteger(hash,Id,6,cur)
endif
set t=null
set u=null
set p=null
endfunction
```

`Trig_HeroSkills8_Actions`　war3map.j:48929
```jass
elseif Skill=='A0IU' then
set r=GetUnitFacing(u)
set L=3
set maxDist=1200.00
set angleStep=25.00
set stepDist=100.00
set tick=0.08
if GetUnitAbilityLevel(u,'A0J4')==1 then
set L=5
set maxDist=1800.00
set angleStep=20.00
endif
set steps=R2I(maxDist/stepDist)
set ray=0
loop
exitwhen ray>=L
set rayAngle=r-(angleStep*(L-1)/2.0)+(angleStep*ray)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,2,x)
call SaveReal(hash,Id,3,y)
call SaveReal(hash,Id,4,rayAngle)
call SaveReal(hash,Id,5,stepDist)
call SaveInteger(hash,Id,6,0)
call SaveInteger(hash,Id,7,steps)
call TimerStart(t,tick,true,function ArcanologRRayStep)
set ray=ray+1
endloop
```

## 異常力場 `A0IT`　—　吃技能強度

俄文原名：Аномальное поле

```
創造一片異常力場，力場內的隨機敵人會受到傷害。持續時間 - 9 秒。

造成傷害：20 +（10% 技能強度）點
觸發間隔：每 0.75 秒 3 名隨機敵人

冷卻：22 秒
```

每級變動：
  - 第 3 行：20 / 30 / 40 / 50 / 60

物件欄位（原型 `ANcl`）：`Ncl1 = 0.8999999761581421`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.8999999761581421`, `Ncl5 = 0`, `Ncl6 = charm`, `aare = 300.0`, `acdn = 22.0`, `alev = 5`, `amcs = [85, 94, 103, 112, 121]`, `aran = 700.0`, `atar = air,ground,friend,neutral,self`

實作：

`Hero8Q`　war3map.j:48740
```jass
function Hero8Q takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u2
local integer count=LoadInteger(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real dmg=10+10*GetUnitAbilityLevel(u,'A0IT')+udg_ItemBonusDMG[n]*0.10
local group ug
local group ug2
local integer targets
local integer check
local real x=LoadReal(hash,Id,3)
local real y=LoadReal(hash,Id,4)
if GetUnitAbilityLevel(u,'A0IX')==1 then
set targets=10
else
set targets=3
endif
set count=count-1
set ug=CreateGroup()
set ug2=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,305,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
call GroupAddUnit(ug2,u2)
endif
call GroupRemoveUnit(ug,u2)
endloop
set check=0
set ug2=GetRandomSubGroup(targets,ug2)
loop
set u2=FirstOfGroup(ug2)
exitwhen u2==null
set check=check+1
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Undead\\DarkRitual\\DarkRitualTarget.mdl",u2,"origin"))
call GroupRemoveUnit(ug2,u2)
endloop
call DestroyGroup(ug)
call DestroyGroup(ug2)
if GetUnitAbilityLevel(u,'A0IX')==1 then
if check>=10 then
set count=count+1
endif
endif
call SaveInteger(hash,Id,1,count)
if count==0 then
set u2=LoadUnitHandle(hash,Id,2)
call RemoveUnit(u2)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set t=null
set u=null
set u2=null
set pl=null
set ug=null
set ug2=null
endfunction
```

`Trig_HeroSkills8_Actions`　war3map.j:48958
```jass
elseif Skill=='A0IT' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
set u2=CreateUnit(pl,'o01T',x,y,270)
set L=12
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
call SaveInteger(hash,Id,1,L)
call SaveReal(hash,Id,3,x)
call SaveReal(hash,Id,4,y)
call TimerStart(t,0.75,true,function Hero8Q)
```

## 秘法齊射 `A04S`　—　吃技能強度

俄文原名：Арканный залп

```
以魔法能量團轟擊指定區域，每一團都會造成範圍傷害。

每團能量的傷害：30 +（15% 技能強度）點
能量團數量：8 個（英雄每 500 點當前法力值 +1）

冷卻：13 秒
```

每級變動：
  - 第 3 行：30 / 45 / 60 / 75 / 90

物件欄位（原型 `ANcl`）：`Ncl1 = 0.10000000149011612`, `Ncl2 = 2`, `Ncl3 = 3`, `Ncl4 = 0.10000000149011612`, `Ncl5 = 0`, `Ncl6 = cripple`, `aare = 250.0`, `acap = `, `acdn = 13.0`, `alev = 5`, `amcs = [100, 115, 130, 145, 160]`, `aran = 800.0`, `atar = air,ground,friend,neutral,self`

實作：

`Hero8W`　war3map.j:48691
```jass
function Hero8W takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local timer t2=CreateTimer()
local integer Id2=GetHandleId(t2)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local real x=LoadReal(hash,Id,1)
local real y=LoadReal(hash,Id,2)
local real x2=LoadReal(hash,Id,3)
local real y2=LoadReal(hash,Id,4)
local integer count=LoadInteger(hash,Id,5)
local integer Skill=LoadInteger(hash,Id,6)
local integer check=LoadInteger(hash,Id,7)
local real dist
local real angle=GetRandomReal(0,360)
local unit u2
if check==1 then
set dist=GetRandomReal(25,500)
elseif Skill=='A0J5' then
set dist=GetRandomReal(25,320)
else
set dist=GetRandomReal(25,245)
endif
set count=count-1
set x2=x2+dist*Cos(angle*bj_DEGTORAD)
set y2=y2+dist*Sin(angle*bj_DEGTORAD)
set u2=CreateUnit(pl,'o00R',x,y,0)
call UnitApplyTimedLife(u2,'BTLF',2)
call IssuePointOrder(u2,"attackground",x2,y2)
set x=x2-x
set y=y2-y
set x=SquareRoot(x*x+y*y)/900
call SaveUnitHandle(hash,Id2,1,u)
call SaveReal(hash,Id2,1,x2)
call SaveReal(hash,Id2,2,y2)
call SaveInteger(hash,Id2,3,Skill)
call TimerStart(t2,x,false,function Hero8WW)
call SaveInteger(hash,Id,5,count)
if count==0 then
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set t=null
set t2=null
set u=null
set u2=null
set pl=null
endfunction
```

`Trig_HeroSkills8_Actions`　war3map.j:48985
```jass
elseif Skill=='A04S' or Skill=='A0J5' then
set x2=GetSpellTargetX()
set y2=GetSpellTargetY()
set L=8
set r=GetUnitState(u,UNIT_STATE_MANA)
loop
exitwhen r<500.
set L=L+1
set r=r-500.
endloop
if Skill=='A0J5' then
set L=L*2
endif
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,x)
call SaveReal(hash,Id,2,y)
call SaveReal(hash,Id,3,x2)
call SaveReal(hash,Id,4,y2)
call SaveInteger(hash,Id,5,L)
call SaveInteger(hash,Id,6,Skill)
call SaveInteger(hash,Id,7,0)
if Skill=='A0J5' then
call TimerStart(t,0.04,true,function Hero8W)
else
call TimerStart(t,0.05,true,function Hero8W)
endif
endif
```

## 秘法充能 `A097`　—　吃技能強度

俄文原名：Арканный заряд

```
向敵人發射一枚秘法投射物，造成傷害並暈眩目標。

傷害：100 +（40% 技能強度）+（英雄當前法力值的 15%）點
暈眩：100% 機率，1 秒

冷卻：10 秒
```

每級變動：
  - 第 3 行：100 + (40% сила умений) + (15 / 150 + (40% сила умений) + (20 / 200 + (40% сила умений) + (25 / 250 + (40% сила умений) + (30 / 300 + (40% сила умений) + (35

物件欄位（原型 `ANcl`）：`Ncl1 = 0.5`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 0.5`, `Ncl5 = 0`, `Ncl6 = tranquility`, `acdn = [10.0, 11.0]`, `alev = 5`, `amcs = [75, 90, 105, 120, 135, 140]`, `aran = 800.0`, `atar = ground,enemy,neutral,organic,air`

實作：

`Hero8E`　war3map.j:48626
```jass
function Hero8E takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local unit u2=LoadUnitHandle(hash,Id,2)
local unit u3=LoadUnitHandle(hash,Id,3)
local real x=GetUnitX(u3)
local real y=GetUnitY(u3)
local real x2=GetUnitX(u2)
local real y2=GetUnitY(u2)
local real dmg
local real r=AngleXY(x,y,x2,y2)
set x=PolarX(x,35,r)
set y=PolarY(y,35,r)
if DistanceNative(x,y,x2,y2)<=18 then
set dmg=LoadReal(hash,Id,1)
call SetUnitX(u3,x2)
call SetUnitY(u3,y2)
call KillUnit(u3)
call UnitDamageTarget(u,u2,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call StunUnit(u,u2,1,1.00)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
else
call SetUnitX(u3,x)
call SetUnitY(u3,y)
call SetUnitFacing(u2,r)
endif
set t=null
set u=null
set u2=null
set u3=null
endfunction
```

`Trig_HeroSkills8_Actions`　war3map.j:48971
```jass
elseif Skill=='A097' then
set t=CreateTimer()
set Id=GetHandleId(t)
set u2=GetSpellTargetUnit()
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
set x=GetUnitX(u)
set y=GetUnitY(u)
set x2=GetUnitX(u2)
set y2=GetUnitY(u2)
set u3=CreateUnit(pl,'o039',x,y,AngleXY(x,y,x2,y2))
call SaveUnitHandle(hash,Id,3,u3)
call SaveReal(hash,Id,1,50.+50.*I2R(GetUnitAbilityLevel(u,Skill))+udg_ItemBonusDMG[GetPlayerId(pl)+1]*0.40+GetUnitState(u,UNIT_STATE_MANA)*(0.10+0.05*I2R(GetUnitAbilityLevel(u,Skill))))
call TimerStart(t,0.03,true,function Hero8E)
```

## 秘法護盾 `A04P`

俄文原名：Арканный щит

```
對指定友軍施加護盾，並逐漸回復其法力值。

一般傷害防護：+15%
狀態傷害防護：+15%
法力值回復：每秒 1%
持續時間：10 秒（目標每一等級 +1 秒）

冷卻：20 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = cloudoffog`, `acap = `, `acdn = 20.0`, `aher = 0`, `alev = 1`, `aran = 700.0`, `atar = hero,friend,self`

實作：

`Hero8D`　war3map.j:48611
```jass
if count==0 or not UnitAlive(u)then
call DestroyEffect(LoadEffectHandle(hash,Id,2))
call SaveReal(hash,u_Id,4,LoadReal(hash,u_Id,4)-0.15)
call SaveReal(hash,u_Id,47,LoadReal(hash,u_Id,47)-0.15)
call SaveReal(hash,u_Id,48,LoadReal(hash,u_Id,48)-0.15)
call SaveReal(hash,u_Id,49,LoadReal(hash,u_Id,49)-0.15)
call SaveReal(hash,u_Id,50,LoadReal(hash,u_Id,50)-0.15)
call RemoveSavedHandle(hash,u_Id,'A04P')
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
```

`Trig_HeroSkills8_Actions`　war3map.j:48898
```jass
if Skill=='A04P' then
set u2=GetSpellTargetUnit()
set u_Id=GetHandleId(u)
set t=LoadTimerHandle(hash,u_Id,'A04P')
if t !=null then
set Id=GetHandleId(t)
if IsUnitType(u2,UNIT_TYPE_HERO)then
set L=10+GetHeroLevel(u2)
else
set L=10+GetUnitLevel(u2)
endif
call SaveInteger(hash,Id,1,L)
else
call SaveReal(hash,u_Id,4,LoadReal(hash,u_Id,4)+0.15)
call SaveReal(hash,u_Id,47,LoadReal(hash,u_Id,47)+0.15)
call SaveReal(hash,u_Id,48,LoadReal(hash,u_Id,48)+0.15)
call SaveReal(hash,u_Id,49,LoadReal(hash,u_Id,49)+0.15)
call SaveReal(hash,u_Id,50,LoadReal(hash,u_Id,50)+0.15)
if IsUnitType(u2,UNIT_TYPE_HERO)then
set L=10+GetHeroLevel(u2)
else
set L=10+GetUnitLevel(u2)
endif
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveEffectHandle(hash,Id,2,AddSpecialEffectTarget("Ubershield Arcane.mdx",u2,"chest"))
call SaveUnitHandle(hash,Id,1,u2)
call SaveInteger(hash,Id,1,L)
call TimerStart(t,1.,true,function Hero8D)
call SaveTimerHandle(hash,u_Id,'A04P',t)
endif
```

## 選擇天賦 `A0IV`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的強度等級。
```

**天賦選項**：
  - `A0IX` 不穩定力場
    強度等級：T2 屬性加成／每級屬性成長加成： +1 / +0 +0 / +0 +2 / +1  將技能「異常力場」可擊中的目標數量從 3 提升至 10。若技能的某次觸發擊中了 10 個目標，力場的作用便會再增加 1 次觸發。
  - `A0J1` 秘法砲擊
    強度等級：T3 屬性加成／每級屬性成長加成： +2 / +1 +1 / +0 +3 / +1  技能「秘法齊射」的施放距離提升 50%，作用範圍提升 30%。同時將該技能的彈體數量提升 100%。  技能「毀滅咒語」會產生更多爆炸，擴散距離增加 50%。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0IX,A0J1`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills8_Actions`　war3map.j:48881
```jass
if Skill=='A0IX' then
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call UnitRemoveAbility(u,'A0IV')
call UnitAddAbility(u,'A0IY')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0J1' then
call SetHeroStr(u,GetHeroStr(u,false)+2,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aINT',1)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A0IV')
call UnitAddAbility(u,'A0J4')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **3** — 對英雄傷害 +%
  - **4** — 受到傷害 −%（被減的）
  - **5** — 對 0-1 級敵人傷害 +%
  - **6** — 造成傷害 +%
  - **47** — 點燃抗性
  - **48** — 冰凍抗性
  - **49** — 流血抗性
  - **50** — 疾病抗性

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
