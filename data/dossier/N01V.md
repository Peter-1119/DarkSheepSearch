# 蠻族戰士 `N01V`（Варвар）

主屬性 **力量** · 背包 **6 格** · 解鎖 0 · 定位 戰士/坦克

| | 初始 | 每級 |
|---|---|---|
| 力量 | None | 3.0 |
| 敏捷 | None | None |
| 智力 | None | 1.5 |

> 獨立性強的近戰英雄，不需要隊友照顧也能撐住。

**縮放**：吃技能強度的技能 ['A07L', 'A07Z', 'A081', 'A082'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

---

## 求生 `A082`　—　吃技能強度

俄文原名：Выживание

```
英雄開始回復自身的生命值，但在技能持續期間，其攻擊速度與移動速度會降低。

治療強度：200 + （100% 技能強度）
攻擊速度與移動速度減緩：-40%
持續時間：10 秒。

冷卻：28 秒。
```

每級變動：
  - 第 3 行：200 / 300 / 400 / 500 / 600

物件欄位（原型 `Absk`）：`abuf = B00O`, `acdn = 28.0`, `adur = 10.0`, `ahdu = 10.0`, `aher = 1`, `alev = 5`, `amcs = [85, 95, 105, 115, 125]`, `bsk1 = -0.4000000059604645`, `bsk2 = -0.4000000059604645`, `bsk3 = 0.0`

實作：

`HeroW15`　war3map.j:51023
```jass
function HeroW15 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local integer count=LoadInteger(hash,Id,1)
local real life=GetUnitState(u,UNIT_STATE_LIFE)
local real heal=5+5*I2R(GetUnitAbilityLevel(u,'A082'))+udg_ItemBonusDMG[n]*0.05
if GetUnitAbilityLevel(u,'A0MM')==1 or GetUnitAbilityLevel(u,'A0XC')==1 then
set heal=heal*(1.00+(100.00-UnitLifePercent(u))*0.01)
endif
if UnitAlive(u)then
call SetUnitState(u,UNIT_STATE_LIFE,life+heal)
else
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set count=count-1
call SaveInteger(hash,Id,1,count)
if count<1 then
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
endif
set t=null
set u=null
set pl=null
endfunction
```

`Trig_HeroSkills15_Actions`　war3map.j:51102
```jass
elseif Skill=='A082' then
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,1,20)
call TimerStart(t,0.5,true,function HeroW15)
endif
```

## 野性戰士 `A081`　—　吃技能強度

俄文原名：Дикий воин

```
擊殺敵人可暫時提升英雄的力量。

力量加成：2 點
力量加成（技能強度 300 以上）：3 點
加成時間：5 秒。
```

每級變動：
  - 第 5 行：5 / 7 / 9 / 11 / 13

物件欄位（原型 `Amgl`）：`aher = 1`, `alev = 5`

實作：

`HeroE15`　war3map.j:51122
```jass
function HeroE15 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer n=LoadInteger(hash,Id,1)
local integer L
if GetUnitAbilityLevel(u,'A0MM')==1 or GetUnitAbilityLevel(u,'A0XC')==1 then
set L=GetRandomInt(1,100)
if LoadInteger(hash,GetHandleId(u),20)<100 then
if L<=5 then
call SaveInteger(hash,GetHandleId(u),20,LoadInteger(hash,GetHandleId(u),20)+1)
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
endif
else
if L<=3 then
call SaveInteger(hash,GetHandleId(u),20,LoadInteger(hash,GetHandleId(u),20)+1)
call SetHeroStr(u,GetHeroStr(u,false)+1,true)
endif
endif
endif
call SetHeroStr(u,GetHeroStr(u,false)-n,true)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroKills15_Actions`　war3map.j:51172
```jass
if GetUnitAbilityLevel(u,'A081')>=1 and IsUnitEnemy(u2,pl)then
set r=3+2*I2R(GetUnitAbilityLevel(u,'A081'))
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,r,false,function HeroE15)
if udg_ItemBonusDMG[n]>=300 then
call SetHeroStr(u,GetHeroStr(u,false)+3,true)
call SaveInteger(hash,Id,1,3)
else
call SetHeroStr(u,GetHeroStr(u,false)+2,true)
call SaveInteger(hash,Id,1,2)
endif
endif
```

## 斬擊者 `A080`

俄文原名：Слешер

```
蠻族戰士的攻擊對非英雄且非建築的普通敵人造成的傷害提高 75%。
```

物件欄位（原型 `ANde`）：`Nde2 = 1.0`, `Nde3 = 1.75`, `Nde4 = 1.0`, `atar = enemies`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 震懾咆哮 `A07L`　—　吃技能強度

俄文原名：Устрашающий рев

```
英雄發出震懾人心的咆哮，降低附近敵人的護甲與攻擊力。

護甲降低：3 點
攻擊力降低：15%
持續時間：30 秒（英雄 15 秒）
技能強度 200 以上：英雄攻擊速度 +75%，持續 8 秒。

冷卻：25 秒。
```

每級變動：
  - 第 3 行：3 / 4 / 5 / 6 / 7
  - 第 4 行：15 / 21 / 27 / 33 / 39

物件欄位（原型 `Aroa`）：`Roa1 = [-0.15000000596046448, -0.20999999344348907, -0.27000001072883606, -0.33000001311302185, -0.38999998569488525]`, `Roa2 = [-3, -4, -5, -6, -7]`, `aare = 500.0`, `abuf = B00M`, `acdn = 25.0`, `adur = 30.0`, `ahdu = 15.0`, `aher = 1`, `alev = 5`, `amcs = [85, 93, 101, 109, 117]`, `atar = air,ground,enemy,neutral`

實作：

`RemoveBuff`　war3map.j:2875
```jass
function RemoveBuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local integer Buff=LoadInteger(hash,Id,2)
call UnitRemoveAbility(u,Buff)
call PauseTimer(t)
call DestroyTimer(t)
call FlushChildHashtable(hash,Id)
set t=null
set u=null
endfunction
```

`Trig_HeroSkills15_Actions`　war3map.j:51079
```jass
if Skill=='A07L' or Skill=='A0MP' then
if udg_ItemBonusDMG[n]>=200 then
call UnitAddAbility(u,'A07Y')
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,1,u)
call SaveInteger(hash,Id,2,'A07Y')
call TimerStart(t,8.,false,function RemoveBuff)
endif
if Skill=='A0MP' then
set dmg=I2R(GetHeroStr(u,true))*1.25
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,750.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
endif
```

## 強健體魄 `A07Z`　—　吃技能強度

俄文原名：Крепкое здоровье

```
藥水對英雄的效果提高 30%。
```

實作：

`Trig_UseItem_Actions`　war3map.j:27372
```jass
if GetUnitAbilityLevel(u,'A07Z')==1 or GetUnitAbilityLevel(u,'A0XC')==1 then
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]+520.00
call SaveReal(hash,ID,2,520.00)
```

## 選擇天賦 `A0KP`

俄文原名：Выбрать талант

```
當玩家的經驗點數足夠時，你可以選擇一項天賦，將英雄大幅強化至新的強度等級。
```

**天賦選項**：
  - `A0ML` 猛士
    強度等級：T2 屬性加成／每級屬性成長加成： +3 / +1 +0 / +0 +0 / +0  技能「求生」每缺少 1% 生命值，就多回復 1% 生命值。  技能「野性戰士」在每次獲得的效果結束後，有 5% 機率保留 1 點力量。累積獲得 100 點力量後，機率降低至 3%。指令「-i」會顯示已獲得的力量。
  - `A0MN` 弒酋者
    強度等級：T3 屬性加成／每級屬性成長加成： +5 / +2 +1 / +0 +1 / +0  技能「震懾咆哮」的作用範圍提升 50%，施放時造成（125% 力量）點傷害。  技能「斬擊者」改為另一種效果——對敵方英雄造成的傷害提高 75%，受到英雄的傷害降低 25%。  擊殺敵方英雄可提升 5 點力量。每擊殺 3 名英雄，力量的累積量便減少 1 點，最低為 1 點。

物件欄位（原型 `Aspb`）：`aite = 0`, `spb1 = A0ML,A0MN`, `spb2 = 0`, `spb3 = 2`, `spb4 = 2`

實作：

`Trig_HeroSkills15_Actions`　war3map.j:51064
```jass
if Skill=='A0ML' then
call SetHeroStr(u,GetHeroStr(u,false)+3,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',1)
call UnitRemoveAbility(u,'A0KP')
call UnitAddAbility(u,'A0MM')
call SaveInteger(hash,GetHandleId(pl),15,1)
elseif Skill=='A0MN' then
call SetHeroStr(u,GetHeroStr(u,false)+5,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+1,true)
call SetHeroInt(u,GetHeroInt(u,false)+1,true)
call SaveInteger(hash,GetHandleId(u),'aSTR',2)
call UnitRemoveAbility(u,'A0KP')
call UnitAddAbility(u,'A0MO')
call SaveInteger(hash,GetHandleId(pl),15,1)
endif
```

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['channel', 'unburrow']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

---

## 皮膚

### 邪惡聖誕老人 `Hpb2` —— **會換技能**
  - 斬擊者 `A080` → **斬擊者** `A0MQ`
  - 震懾咆哮 `A07L` → **震懾咆哮** `A0MP`

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數
  - **20** — （雜項）

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
