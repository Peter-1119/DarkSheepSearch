# 死亡使者 `U01D`（Вестник смерти）

主屬性 **敏捷** · 背包 **6 格** · 解鎖 50000 · 定位 刺客

| | 初始 | 每級 |
|---|---|---|
| 力量 | （未覆寫） | 2 |
| 敏捷 | 27 | 3 |
| 智力 | 20 | 2.1 |

> 近戰英雄，普攻、技能與流血效果都能吸血，並有多種減益。

**縮放**：吃技能強度的技能 ['A0P7', 'A0P9', 'A0PB', 'A0PD'] ／ ◈ 吃裝備技能威力 無 ／ ⊕ 給裝備技能威力 無

**傷害／效果走哪條管線**（決定哪些裝備對這隻有用）：

- **狀態** —— 走 `Burn_Dmg` 那條，**外面包了 DisableTrigger** → 不吃 DefCof、不帶穿透、被狀態抗性擋。該買的是「狀態傷害 +%」「易燃」「機率倍率」。
- **直接傷害** —— 走 `Trig_HeroTakeDamage_Actions` → **吃 DefCof（key 3/5/6/9/40/41）也吃穿透**，而且事件數越多穿透越划算。
- **召喚物** —— 召喚物**不繼承**主人的裝備觸發／狀態／傷害 +%，只吃主人技能公式裡明寫的屬性（通常是最大生命與技能強度）與原生光環。
- **治療／增益** —— 直接寫數值，不經傷害事件 —— 全地圖沒有「治療加成」這種屬性，只能靠技能公式裡的係數（多半是技能強度）。

細節見 `data/dossier/_engine.md`。


---

## 恐懼召喚 `A0PD`　—　吃技能強度

俄文原名：Зов ужаса

```
對英雄周圍的敵人造成傷害，每擊中一名敵人便提升技能強度，並提高移動速度。

傷害：40 + （25% 技能強度）點
技能強度加成：每名敵人 +8 點
移動速度加成：30%
持續時間：8 秒

冷卻：11 秒
```

每級變動：
  - 第 3 行：40 / 60 / 80 / 100 / 120
  - 第 4 行：8 / 12 / 16 / 20 / 24
  - 第 5 行：30 / 40 / 50 / 60 / 70

物件欄位（原型 `Absk`）：`abuf = B02S`, `acdn = 11.0`, `adur = 8.0`, `ahdu = 8.0`, `aher = 1`, `alev = 5`, `amcs = [65, 75, 85, 95, 105]`, `bsk1 = [0.30000001192092896, 0.4000000059604645, 0.5, 0.6000000238418579, 0.7000000476837158]`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`HeroW39_endbuff`　war3map.j:58522
```jass
function HeroW39_endbuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real spd_total=LoadReal(hash,Id,1)
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]-spd_total
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
set u=null
set pl=null
set t=null
endfunction
```

`Trig_HeroSkills39_Actions`　war3map.j:58592
```jass
elseif Skill=='A0PD' or Skill=='A0LH' then
set dmg=20+(20*I2R(GetUnitAbilityLevel(u,Skill)))+(udg_ItemBonusDMG[n]*0.25)
set aoe=250
set x=GetUnitX(u)
set y=GetUnitY(u)
if GetUnitAbilityLevel(u,'A0PE')>=3 or GetUnitAbilityLevel(u,'A0XW')>=3 then
set aoe=aoe*1.25
endif
if Skill=='A0PD' then
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Call of Dread Red.mdx",u,"origin"))
else
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Divine Edict.mdx",u,"origin"))
endif
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,aoe,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
call UnitDamageTarget(u,u2,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
set spd_total=spd_total+4+4*I2R(GetUnitAbilityLevel(u,Skill))
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]+spd_total
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,spd_total)
call TimerStart(t,8.00,false,function HeroW39_endbuff)
```

## 判決 `A0PB`　—　吃技能強度

俄文原名：Приговор

```
對指定的敵人降下強力詛咒，持續造成高額週期傷害。若目標在「判決」效果下死亡，英雄的攻擊力會提升等同於死亡目標等級的數值。英雄必須存活才能獲得此攻擊力。

傷害：110 + （40% 技能強度）點/秒
持續時間：8 秒

冷卻：70 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.6000000238418579, None, 1.0]`, `Ncl2 = [1, None]`, `Ncl3 = [1, None]`, `Ncl4 = [0.6000000238418579, None, 1.0]`, `Ncl5 = [0, None]`, `Ncl6 = ['chemicalrage', None, 'channel']`, `acap = `, `acdn = [70.0, None, 17.0]`, `alev = 1`, `amcs = [150, None, 80, 90, 100, 110, 120]`, `aran = [700.0, None]`, `atar = ['air,ground,enemy,neutral,organic', None, 'air,ground,friend,neutral,self']`

實作：

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

`HeroR39`　war3map.j:58384
```jass
if count==0 or UnitAlive(u2)!=true then
call DestroyEffect(LoadEffectHandle(hash,Id,3))
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
call SaveUnitHandle(hash,GetHandleId(u2),'A0PB',null)
call SaveInteger(hash,GetHandleId(u2),'A0PB',0)
```

`Trig_HeroSkills39_Actions`　war3map.j:58622
```jass
elseif Skill=='A0PB' then
set u2=GetSpellTargetUnit()
if GetUnitAbilityLevel(u,'A0PE')>=4 or GetUnitAbilityLevel(u,'A0XW')>=4 then
call SaveInteger(hash,Id,1,32)
else
call SaveInteger(hash,Id,1,16)
endif
call SaveUnitHandle(hash,Id,1,u)
call SaveUnitHandle(hash,Id,2,u2)
call TimerStart(t,0.50,true,function HeroR39)
call SaveEffectHandle(hash,Id,3,AddSpecialEffectTarget("war3mapImported\\Omen Classic.mdx",u2,"overhead"))
if LoadInteger(hash,GetHandleId(u2),'A0PB')==1 then
call SaveInteger(hash,Id,2,1)
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFFA900Данный юнит уже приговорён - вы не получите бонусную атаку, но умение будет наносить на 25% больше урона.|r")
else
call SaveInteger(hash,Id,2,0)
call SaveUnitHandle(hash,GetHandleId(u2),'A0PB',u)
call SaveInteger(hash,GetHandleId(u2),'A0PB',1)
endif
```

`Trig_HeroKills39_Actions`　war3map.j:58669
```jass
if LoadInteger(hash,GetHandleId(u2),'A0PB')==1 then
if IsUnitType(u2,UNIT_TYPE_HERO)then
set n=GetHeroLevel(u2)
else
set n=GetUnitLevel(u2)
endif
set hero=LoadUnitHandle(hash,GetHandleId(u2),'A0PB')
if UnitAlive(hero)then
call SetUnitBaseDamage(hero,GetUnitBaseDamage(hero)+n)
endif
endif
```

## 死亡臨在 `A0PA`

俄文原名：Присутствие смерти

```
散發一道光環，降低英雄附近敵人的護甲。

護甲降低：3 點
作用範圍：200 點
```

每級變動：
  - 第 3 行：3 / 5 / 7 / 9 / 11

物件欄位（原型 `AHad`）：`Had1 = [-3.0, -5.0, -7.0, -9.0, -11.0]`, `aare = 200.0`, `abuf = B02R`, `adur = [None, 0.0]`, `ahdu = [None, 0.0]`, `alev = 5`, `atar = air,enemies,ground,invulnerable,vulnerable`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 撕裂 `A0P7`　—　吃技能強度

俄文原名：Разрыв

```
擊中指定的敵人以及其附近的敵人，造成傷害並施加流血。

主要傷害：30 + （15% 技能強度）點
流血：125% 機率，400% 傷害

冷卻：4 秒
```

每級變動：
  - 第 3 行：30 / 45 / 60 / 75 / 90

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acdn = 4.0`, `alev = 5`, `amcs = [45, 50, 55, 60, 65]`, `aran = 110.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`RemoveFrost`　war3map.j:1527
```jass
function RemoveFrost takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call UnitRemoveAbility(u,'S00G')
call UnitRemoveAbility(u,'B02V')
call RemoveSavedHandle(hash,GetHandleId(u),'B02V')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set t=null
set u=null
endfunction
function FrostUnit takes unit damager,unit target,real chanse returns nothing
local real dmg
local real cof=1.0
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,t_Id,44)>0 then
set t=null
return
endif
if LoadInteger(hash,t_Id,28)>0 and LoadInteger(hash,GetHandleId(damager),'I07G')==0 then
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(damager,'A0AQ')==1 and GetUnitAbilityLevel(target,'B046')==1 then
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
endif
if GetUnitAbilityLevel(target,'B02V')==1 then
set cof=1.00
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
if GetPlayerTechCount(GetOwningPlayer(damager),'Rufb',true)==1 then
if GetUnitTypeId(damager)=='n041' or GetUnitTypeId(damager)=='n06L' or GetUnitTypeId(damager)=='n06S' or GetUnitTypeId(damager)=='n06U' or GetUnitTypeId(damager)=='n06V' then
set dmg=dmg+100.
endif
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
set t=LoadTimerHandle(hash,t_Id,'B02V')
call TimerStart(t,0.,false,function RemoveFrost)
else
call UnitAddAbility(target,'S00G')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,t_Id,'B02V',t)
call TimerStart(t,6.,false,function RemoveFrost)
endif
endif
set t=null
endfunction
```

`Bleed_Dmg`　war3map.j:1990
```jass
function Bleed_Dmg takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u
local integer u_Id
local unit u2=LoadUnitHandle(hash,Id,0)
local integer u2_Id=GetHandleId(u2)
local integer count=LoadInteger(hash,u2_Id,'bled')
local integer i=0
local integer check=0
local integer L
local real dmg
local real dmg_cof=1.0
local real cof
local string str
if not UnitAlive(u2)then
call SaveInteger(hash,u2_Id,'bled',0)
call UnitRemoveAbility(u2,'A0Y7')
call UnitRemoveAbility(u2,'B041')
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
call RemoveSavedHandle(hash,u2_Id,'blud')
set u=null
set u2=null
set t=null
return
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
if LoadInteger(hash,u2_Id,'I07A')>=1 then
if UnitLifePercent(u2)<=25.0 then
set dmg_cof=dmg_cof-0.50
endif
endif
if LoadInteger(hash,u2_Id,'I048')>=1 then
if UnitLifePercent(u2)<=30.0 then
set dmg_cof=dmg_cof-0.25
endif
endif
if LoadInteger(hash,u2_Id,'tbak')>=1 then
if UnitLifePercent(u2)>=75.0 then
set dmg_cof=dmg_cof-0.25
endif
endif
if LoadInteger(hash,u2_Id,'pman')>=1 then
if UnitLifePercent(u2)>=75.0 then
set dmg_cof=dmg_cof-0.18
endif
endif
set dmg_cof=dmg_cof-LoadReal(hash,u2_Id,49)
loop
set i=i+1
set L=LoadInteger(hash,Id,i)
if L>0 then
set u=LoadUnitHandle(hash,Id,i)
set u_Id=GetHandleId(u)
set dmg=LoadReal(hash,Id,i)
set dmg=dmg*(1.-((100.-UnitLifePercent(u2))*0.0075))
set cof=dmg_cof
if LoadInteger(hash,u_Id,'shdt')>=1 then
if IsUnitType(u2,UNIT_TYPE_HERO)then
set cof=cof+(0.05*I2R(GetHeroLevel(u2)))
else
set cof=cof+(0.05*I2R(GetUnitLevel(u2)))
endif
endif
if LoadInteger(hash,u_Id,'gvsm')>=1 or LoadInteger(hash,u_Id,'I00S')>=1 then
if GetUnitAbilityLevel(u2,'B02V')==1 then
set cof=cof+0.35
endif
endif
if LoadInteger(hash,u_Id,'I086')>=1 then
if LoadInteger(hash,u_Id,29)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(u,'B01H')==1 then
set cof=cof+0.50
endif
set dmg_cof=dmg_cof+LoadReal(hash,u_Id,29)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(u,u2,dmg*cof,false,false,ATTACK_TYPE_CHAOS,DAMAGE_TYPE_UNIVERSAL,WEAPON_TYPE_WHOKNOWS)
if LoadInteger(hash,u2_Id,'I068')>=1 then
call UnitDamageTarget(u2,u,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
if LoadInteger(hash,GetHandleId(u),'VAMP')==1 then
call SetWidgetLife(u,GetWidgetLife(u)+dmg*LoadReal(hash,GetHandleId(u),'A0P9'))
endif
set L=L-1
if L>0 then
set check=1
else
call RemoveSavedHandle(hash,Id,i)
endif
call SaveInteger(hash,Id,i,L)
endif
exitwhen i==count
endloop
call EnableTrigger(gg_trg_HeroTakeDamage)
if check==0 then
call SaveInteger(hash,u2_Id,'bled',0)
call UnitRemoveAbility(u2,'A0Y7')
call UnitRemoveAbility(u2,'B041')
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
call RemoveSavedHandle(hash,u2_Id,'blud')
endif
set u=null
set u2=null
set t=null
endfunction
function BleedUnit takes unit damager,unit target,real dmg,real chanse returns nothing
local timer t
local integer Id
local integer count
local integer count2
local integer i
local integer i2
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local effect e
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,GetHandleId(target),44)>0 then
set chanse=0.
endif
if LoadInteger(hash,GetHandleId(target),29)>0 then
if LoadInteger(hash,GetHandleId(target),29)>50 then
set chanse=0.
endif
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'shdt')>=1 then
if IsUnitType(target,UNIT_TYPE_HERO)then
set count=GetHeroLevel(target)
else
set count=GetUnitLevel(target)
endif
set chanse=chanse*(1.0+0.05*I2R(count))
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
set dmg=dmg/16.
if GetUnitAbilityLevel(target,'A064')==1 then
set count=8
else
set count=16
endif
set count2=LoadInteger(hash,t_Id,'bled')
if count2==0 then
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,0,target)
call SaveUnitHandle(hash,Id,1,damager)
call SaveInteger(hash,Id,1,count)
call SaveReal(hash,Id,1,dmg)
call UnitAddAbility(target,'A0Y7')
call SaveInteger(hash,t_Id,'bled',1)
call SaveTimerHandle(hash,t_Id,'blud',t)
call TimerStart(t,0.5,true,function Bleed_Dmg)
else
set t=LoadTimerHandle(hash,t_Id,'blud')
set Id=GetHandleId(t)
set i2=0
set i=0
loop
set i=i+1
if LoadInteger(hash,Id,i)==0 then
call SaveUnitHandle(hash,Id,i,damager)
call SaveInteger(hash,Id,i,count)
call SaveReal(hash,Id,i,dmg)
call SaveInteger(hash,t_Id,'bled',i)
set i=count2
set i2=1
endif
exitwhen i==count2
endloop
if i2==0 then
set count2=count2+1
call SaveUnitHandle(hash,Id,count2,damager)
call SaveInteger(hash,Id,count2,count)
call SaveReal(hash,Id,count2,dmg)
call SaveInteger(hash,t_Id,'bled',count2)
endif
endif
if LoadInteger(hash,GetHandleId(damager),'I00Y')>=1 then
call FrostUnit(damager,target,0.50)
endif
endif
set t=null
set e=null
endfunction
```

`Trig_HeroSkills39_Actions`　war3map.j:58554
```jass
if Skill=='A0P7' then
set dmg=15.+(15.*I2R(GetUnitAbilityLevel(u,Skill)))+udg_ItemBonusDMG[n]*0.15
set aoe=225.
if GetUnitAbilityLevel(u,'A0PE')>=2 or GetUnitAbilityLevel(u,'A0XW')>=2 then
set r=10.00
else
set r=1.25
endif
set u2=GetSpellTargetUnit()
set x=GetUnitX(u2)
set y=GetUnitY(u2)
if GetUnitAbilityLevel(u,'A0PE')>=3 or GetUnitAbilityLevel(u,'A0XW')>=3 then
set aoe=aoe*1.25
endif
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Reaper's Claws Red.mdx",u,"hand left"))
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Reaper's Claws Red.mdx",u,"hand right"))
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,aoe,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
call DestroyEffect(AddSpecialEffectTarget("Objects\\Spawnmodels\\Human\\HumanBlood\\HumanBloodPeasant.mdl",u2,"origin"))
call UnitDamageTarget(u,u2,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
call BleedUnit(u,u2,dmg*4.0,r)
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
```

## 永恆飢餓 `A0P9`　—　吃技能強度

俄文原名：Вечный голод

```
攻擊、技能與流血效果具有 10% 吸血。

任務：擊殺 100 名敵人。完成任務後，吸血效果將提升至 15%。

使用指令「-i」可追蹤進度。
```

物件欄位（原型 `Amgl`）：`alev = 5`

實作：

`RemoveFrost`　war3map.j:1527
```jass
function RemoveFrost takes nothing returns nothing
local timer t=GetExpiredTimer()
local unit u=LoadUnitHandle(hash,GetHandleId(t),1)
call UnitRemoveAbility(u,'S00G')
call UnitRemoveAbility(u,'B02V')
call RemoveSavedHandle(hash,GetHandleId(u),'B02V')
call PauseTimer(t)
call FlushChildHashtable(hash,GetHandleId(t))
call DestroyTimer(t)
set t=null
set u=null
endfunction
function FrostUnit takes unit damager,unit target,real chanse returns nothing
local real dmg
local real cof=1.0
local timer t
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,t_Id,44)>0 then
set t=null
return
endif
if LoadInteger(hash,t_Id,28)>0 and LoadInteger(hash,GetHandleId(damager),'I07G')==0 then
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
if GetUnitAbilityLevel(damager,'A0AQ')==1 and GetUnitAbilityLevel(target,'B046')==1 then
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
endif
if GetUnitAbilityLevel(target,'B02V')==1 then
set cof=1.00
if IsUnitType(target,UNIT_TYPE_HERO)then
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.25
else
set dmg=GetUnitState(target,UNIT_STATE_MAX_LIFE)*0.05
endif
if LoadInteger(hash,d_Id,'I00R')>=1 then
set dmg=dmg+150.
endif
if GetPlayerTechCount(GetOwningPlayer(damager),'Rufb',true)==1 then
if GetUnitTypeId(damager)=='n041' or GetUnitTypeId(damager)=='n06L' or GetUnitTypeId(damager)=='n06S' or GetUnitTypeId(damager)=='n06U' or GetUnitTypeId(damager)=='n06V' then
set dmg=dmg+100.
endif
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Other\\CrushingWave\\CrushingWaveDamage.mdl",target,"chest"))
if LoadInteger(hash,d_Id,'I086')>=1 then
if LoadInteger(hash,d_Id,48)>=1 then
set cof=cof+0.50
endif
endif
if GetUnitAbilityLevel(damager,'B01H')==1 then
set cof=cof+0.50
endif
if LoadInteger(hash,t_Id,'I07A')>=1 then
if UnitLifePercent(target)<=25.0 then
set cof=cof-0.50
endif
endif
if LoadInteger(hash,t_Id,'I048')>=1 then
if UnitLifePercent(target)<=30.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'tbak')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.25
endif
endif
if LoadInteger(hash,t_Id,'pman')>=1 then
if UnitLifePercent(damager)>=75.0 then
set cof=cof-0.18
endif
endif
call DisableTrigger(gg_trg_HeroTakeDamage)
set cof=cof-LoadReal(hash,t_Id,48)+LoadReal(hash,d_Id,28)
if cof<0.20 then
set cof=0.20
endif
call UnitDamageTarget(damager,target,dmg*cof,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
if LoadInteger(hash,t_Id,'I068')>=1 then
call UnitDamageTarget(target,damager,dmg*cof*0.20,false,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_NORMAL,null)
endif
call EnableTrigger(gg_trg_HeroTakeDamage)
set t=LoadTimerHandle(hash,t_Id,'B02V')
call TimerStart(t,0.,false,function RemoveFrost)
else
call UnitAddAbility(target,'S00G')
set t=CreateTimer()
call SaveUnitHandle(hash,GetHandleId(t),1,target)
call SaveTimerHandle(hash,t_Id,'B02V',t)
call TimerStart(t,6.,false,function RemoveFrost)
endif
endif
set t=null
endfunction
```

`Bleed_Dmg`　war3map.j:2077
```jass
if LoadInteger(hash,GetHandleId(u),'VAMP')==1 then
call SetWidgetLife(u,GetWidgetLife(u)+dmg*LoadReal(hash,GetHandleId(u),'A0P9'))
endif
```

`BleedUnit`　war3map.j:2104
```jass
function BleedUnit takes unit damager,unit target,real dmg,real chanse returns nothing
local timer t
local integer Id
local integer count
local integer count2
local integer i
local integer i2
local integer t_Id=GetHandleId(target)
local integer d_Id=GetHandleId(damager)
local effect e
local integer random
local integer chanse_random
if IsUnitType(target,UNIT_TYPE_STRUCTURE)or IsUnitType(target,UNIT_TYPE_MECHANICAL)or not UnitAlive(target)or LoadInteger(hash,GetHandleId(target),44)>0 then
set chanse=0.
endif
if LoadInteger(hash,GetHandleId(target),29)>0 then
if LoadInteger(hash,GetHandleId(target),29)>50 then
set chanse=0.
endif
set chanse=chanse*0.25
endif
if GetUnitAbilityLevel(target,'B045')==1 then
set chanse=chanse*1.50
endif
if GetUnitAbilityLevel(target,'B00W')>0 then
set chanse=chanse*0.70
endif
if LoadInteger(hash,d_Id,'I09G')>=1 then
set chanse=chanse*1.50
endif
if LoadInteger(hash,d_Id,'shdt')>=1 then
if IsUnitType(target,UNIT_TYPE_HERO)then
set count=GetHeroLevel(target)
else
set count=GetUnitLevel(target)
endif
set chanse=chanse*(1.0+0.05*I2R(count))
endif
set chanse_random=R2I(chanse*100)
set random=GetRandomInt(1,100)
if random<=chanse_random then
set dmg=dmg/16.
if GetUnitAbilityLevel(target,'A064')==1 then
set count=8
else
set count=16
endif
set count2=LoadInteger(hash,t_Id,'bled')
if count2==0 then
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveUnitHandle(hash,Id,0,target)
call SaveUnitHandle(hash,Id,1,damager)
call SaveInteger(hash,Id,1,count)
call SaveReal(hash,Id,1,dmg)
call UnitAddAbility(target,'A0Y7')
call SaveInteger(hash,t_Id,'bled',1)
call SaveTimerHandle(hash,t_Id,'blud',t)
call TimerStart(t,0.5,true,function Bleed_Dmg)
else
set t=LoadTimerHandle(hash,t_Id,'blud')
set Id=GetHandleId(t)
set i2=0
set i=0
loop
set i=i+1
if LoadInteger(hash,Id,i)==0 then
call SaveUnitHandle(hash,Id,i,damager)
call SaveInteger(hash,Id,i,count)
call SaveReal(hash,Id,i,dmg)
call SaveInteger(hash,t_Id,'bled',i)
set i=count2
set i2=1
endif
exitwhen i==count2
endloop
if i2==0 then
set count2=count2+1
call SaveUnitHandle(hash,Id,count2,damager)
call SaveInteger(hash,Id,count2,count)
call SaveReal(hash,Id,count2,dmg)
call SaveInteger(hash,t_Id,'bled',count2)
endif
endif
if LoadInteger(hash,GetHandleId(damager),'I00Y')>=1 then
call FrostUnit(damager,target,0.50)
endif
endif
set t=null
set e=null
endfunction
```

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

`Trig_HeroLvlUp_Actions`　war3map.j:45516
```jass
if lvl==5 then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Beчный гoлoд [1/4]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SaveReal(hash,u_Id,'A0P9',0.15)
call SetUnitAbilityLevel(u,'A0XV',2)
```

`Trig_HeroLvlUp_Actions`　war3map.j:45523
```jass
elseif lvl==10 then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Beчный гoлoд [2/4]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SaveReal(hash,u_Id,'A0P9',0.20)
call SetUnitAbilityLevel(u,'A0XV',3)
elseif lvl==15 then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Beчный гoлoд [3/4]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SaveReal(hash,u_Id,'A0P9',0.25)
call SetUnitAbilityLevel(u,'A0XV',4)
```

`Trig_HeroLvlUp_Actions`　war3map.j:45534
```jass
elseif lvl==20 then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Beчный гoлoд [4/4]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SaveReal(hash,u_Id,'A0P9',0.30)
call SetUnitAbilityLevel(u,'A0XV',5)
```

`HeroR39`　war3map.j:58367
```jass
function HeroR39 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u2=LoadUnitHandle(hash,Id,2)
local integer count=LoadInteger(hash,Id,1)
local real vampiric=LoadReal(hash,GetHandleId(u),'A0P9')
local real dmg=55.00+udg_ItemBonusDMG[n]*0.20
if LoadInteger(hash,Id,2)==1 then
set dmg=dmg*1.25
endif
call DestroyEffect(AddSpecialEffectTarget("Abilities\\Spells\\Undead\\AnimateDead\\AnimateDeadTarget.mdl",u2,"origin"))
call UnitDamageTarget(u,u2,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
set count=count-1
if count==0 or UnitAlive(u2)!=true then
call DestroyEffect(LoadEffectHandle(hash,Id,3))
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
call SaveUnitHandle(hash,GetHandleId(u2),'A0PB',null)
call SaveInteger(hash,GetHandleId(u2),'A0PB',0)
else
call SaveInteger(hash,Id,1,count)
endif
set u=null
set u2=null
set pl=null
set t=null
endfunction
function HeroQ39 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u3
local integer count=LoadInteger(hash,Id,1)
local real dmg=20+20*I2R(GetUnitAbilityLevel(u,'A0LM'))+udg_ItemBonusDMG[n]*0.20
local real vampiric=LoadReal(hash,GetHandleId(u),'A0P9')
local real x=GetUnitX(u)
local real y=GetUnitY(u)
local real dist=GetRandomReal(20,200)
local real angle=GetRandomReal(0,360)
local group ug=CreateGroup()
set x=PolarX(x,dist,angle)
set y=PolarY(y,dist,angle)
call DestroyEffect(AddSpecialEffect("war3mapImported\\Shining Flare.mdx",x,y))
call GroupEnumUnitsInRange(ug,x,y,215.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set count=count-1
if count==0 then
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
else
call SaveInteger(hash,Id,1,count)
endif
set u=null
set u3=null
set pl=null
set t=null
set ug=null
endfunction
function HeroR39_Banhammer_Debuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u3
local group ug=LoadGroupHandle(hash,Id,1)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)then
call UnitRemoveAbility(u3,'A0LZ')
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set u3=null
set t=null
set ug=null
endfunction
function HeroR39_Banhammer takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u3
local group ug=CreateGroup()
local group ug2=CreateGroup()
local real x=LoadReal(hash,Id,1)
local real y=LoadReal(hash,Id,2)
local real dmg=750+udg_ItemBonusDMG[n]*2.00
local real vampiric=LoadReal(hash,GetHandleId(u),'A0P9')
if GetUnitAbilityLevel(u,'A0PE')>=4 or GetUnitAbilityLevel(u,'A0XW')>=4 then
set dmg=dmg*1.75
endif
call GroupEnumUnitsInRange(ug,x,y,225,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
```

## 無可避免的終局 `A0PE`

俄文原名：Неизбежный финал

```
任務：擊殺一名敵方英雄。完成任務後，「撕裂」技能將無視流血抗性。
```

物件欄位（原型 `Amgl`）：`alev = 4`

實作：

`HeroR39_Banhammer`　war3map.j:58473
```jass
if GetUnitAbilityLevel(u,'A0PE')>=4 or GetUnitAbilityLevel(u,'A0XW')>=4 then
set dmg=dmg*1.75
endif
```

`Trig_HeroSkills39_Actions`　war3map.j:58557
```jass
if GetUnitAbilityLevel(u,'A0PE')>=2 or GetUnitAbilityLevel(u,'A0XW')>=2 then
set r=10.00
```

`Trig_HeroSkills39_Actions`　war3map.j:58565
```jass
if GetUnitAbilityLevel(u,'A0PE')>=3 or GetUnitAbilityLevel(u,'A0XW')>=3 then
set aoe=aoe*1.25
endif
```

`Trig_HeroSkills39_Actions`　war3map.j:58585
```jass
if GetUnitAbilityLevel(u,'A0PE')>=2 or GetUnitAbilityLevel(u,'A0XW')>=2 then
call SaveInteger(hash,Id,1,12)
```

`Trig_HeroSkills39_Actions`　war3map.j:58597
```jass
if GetUnitAbilityLevel(u,'A0PE')>=3 or GetUnitAbilityLevel(u,'A0XW')>=3 then
set aoe=aoe*1.25
endif
```

`Trig_HeroSkills39_Actions`　war3map.j:58624
```jass
if GetUnitAbilityLevel(u,'A0PE')>=4 or GetUnitAbilityLevel(u,'A0XW')>=4 then
call SaveInteger(hash,Id,1,32)
```

`Trig_HeroKills39_Actions`　war3map.j:58744
```jass
if GetUnitAbilityLevel(u,'A0PE')==1 then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Неизбежный финал [1/3]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SetUnitAbilityLevel(u,'A0PE',2)
elseif GetUnitAbilityLevel(u,'A0PE')==2 and GetHeroLevel(u2)>=10 then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Неизбежный финал [2/3]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SetUnitAbilityLevel(u,'A0PE',3)
elseif GetUnitAbilityLevel(u,'A0PE')==3 and(GetUnitState((u2),UNIT_STATE_MAX_LIFE))>=10000. then
call DisplayTimedTextToPlayer(pl,0,0,15,"|cFFFDA601Неизбежный финал [3/3]:|r|cFF61B2A9 зaдaчa выпoлнeнa.|r")
call SetUnitAbilityLevel(u,'A0PE',4)
endif
```

## （吸血・死亡使者） `A0P6`

俄文原名：(вампиризм, вестник смерти)

物件欄位（原型 `AIva`）：`Ivam = [0.15000000596046448, 0.20000000298023224, 0.25, 0.30000001192092896]`, `alev = 4`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 轉移／移除據點 `A03V`

俄文原名：Передать/удалить точку

```
選擇自己的據點或建築，將其轉移給其他玩家，或在不損失地基的情況下摧毀它。可從任意距離施放。
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.009999999776482582, 0.8999999761581421]`, `Ncl2 = 1`, `Ncl3 = 1`, `Ncl4 = [0.009999999776482582, 0.8999999761581421]`, `Ncl5 = 0`, `Ncl6 = ['unburrow', 'channel']`, `acdn = [1.0, 16.0]`, `aher = 0`, `alev = 1`, `amcs = [None, 95, 110, 125, 140, 155, 170]`, `aran = [99999.0, 100.0]`, `atar = ['player,structure', 'air,ground,debris,enemy,neutral,organic']`

實作：

`Trig_ChangePoints_Actions`　war3map.j:17734
```jass
if GetSpellAbilityId()=='A03V' and GetUnitLevel(GetSpellTargetUnit())>0 then
set udg_CTPoint[n]=GetSpellTargetUnit()
call DialogClear(udg_CTWindow[n])
call DialogSetMessage(udg_CTWindow[n],"Передать/удалить")
set L=1
loop
exitwhen L>8
if Player(L-1)!=pl and GetPlayerController(Player(L-1))==MAP_CONTROL_USER and GetPlayerSlotState(Player(L-1))==PLAYER_SLOT_STATE_PLAYING then
set udg_CTButton[L+(n-1)*9]=DialogAddButton(udg_CTWindow[n],PlayerName[L],0)
endif
set L=L+1
endloop
set udg_CTButton[9]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Удалить строение|r",0)
set udg_CTButton[72]=DialogAddButton(udg_CTWindow[n],"|cFFFFDC00Отмена|r",0)
call DialogDisplay(pl,udg_CTWindow[n],true)
endif
```

## 聖光援助 `A0LH`　—　來自皮膚「光明使者」

俄文原名：Помощь Света

```
對英雄周圍的敵人造成傷害，每命中一名敵人便提升技能強度，並提升移動速度。

傷害：40 +（25% 技能強度）點
技能強度加成：每名敵人 +8 點
移動速度加成：30%
持續時間：8 秒

冷卻：11 秒
```

每級變動：
  - 第 3 行：40 / 60 / 80 / 100 / 120
  - 第 4 行：8 / 12 / 16 / 20 / 24
  - 第 5 行：30 / 40 / 50 / 60 / 70

物件欄位（原型 `Absk`）：`abuf = B03J`, `acdn = 11.0`, `adur = 8.0`, `ahdu = 8.0`, `aher = 1`, `alev = 5`, `amcs = [65, 75, 85, 95, 105]`, `bsk1 = [0.30000001192092896, 0.4000000059604645, 0.5, 0.6000000238418579, 0.7000000476837158]`, `bsk2 = 0.0`, `bsk3 = 0.0`

實作：

`HeroW39_endbuff`　war3map.j:58522
```jass
function HeroW39_endbuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local real spd_total=LoadReal(hash,Id,1)
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]-spd_total
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
set u=null
set pl=null
set t=null
endfunction
```

`Trig_HeroSkills39_Actions`　war3map.j:58592
```jass
elseif Skill=='A0PD' or Skill=='A0LH' then
set dmg=20+(20*I2R(GetUnitAbilityLevel(u,Skill)))+(udg_ItemBonusDMG[n]*0.25)
set aoe=250
set x=GetUnitX(u)
set y=GetUnitY(u)
if GetUnitAbilityLevel(u,'A0PE')>=3 or GetUnitAbilityLevel(u,'A0XW')>=3 then
set aoe=aoe*1.25
endif
if Skill=='A0PD' then
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Call of Dread Red.mdx",u,"origin"))
else
call DestroyEffect(AddSpecialEffectTarget("war3mapImported\\Divine Edict.mdx",u,"origin"))
endif
set ug=CreateGroup()
call GroupEnumUnitsInRange(ug,x,y,aoe,null)
loop
set u2=FirstOfGroup(ug)
exitwhen u2==null
if UnitAlive(u2)and IsUnitEnemy(u2,pl)then
call UnitDamageTarget(u,u2,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
set spd_total=spd_total+4+4*I2R(GetUnitAbilityLevel(u,Skill))
endif
call GroupRemoveUnit(ug,u2)
endloop
call DestroyGroup(ug)
set udg_ItemBonusDMG[n]=udg_ItemBonusDMG[n]+spd_total
call SaveUnitHandle(hash,Id,1,u)
call SaveReal(hash,Id,1,spd_total)
call TimerStart(t,8.00,false,function HeroW39_endbuff)
```

## 至高審判 `A0LG`　—　來自皮膚「光明使者」

俄文原名：Высший суд

```
朝指定區域降下光明之鎚，對敵人造成傷害。以鎚擊殺可強化英雄。鎚會在敵人身上留下印記，使其在之後一段時間內（7.5 秒）被擊殺時仍能提供屬性。「無可避免的終局」的第三項任務使鎚的傷害提升 75%。

傷害：750 +（200% 技能強度）點
鎚擊殺 0-1 級的敵人：+1 點攻擊力
鎚擊殺 2-4 級的敵人：+2 點攻擊力
鎚擊殺 5-8 級的敵人：+2 點攻擊力，+50 HP
鎚擊殺 9 級以上的敵人：+2 點攻擊力，+50 HP，+1 點護甲
鎚擊殺英雄：+2 力量、敏捷與智力

冷卻：60 秒
```

物件欄位（原型 `ANcl`）：`Ncl1 = [0.6000000238418579, None, 1.0]`, `Ncl2 = [2, None, 1]`, `Ncl3 = [3, None, 1]`, `Ncl4 = [0.6000000238418579, None, 1.0]`, `Ncl5 = [0, None]`, `Ncl6 = ['chemicalrage', None, 'channel']`, `aare = 220.0`, `acdn = [60.0, None, 17.0]`, `alev = 1`, `amcs = [125, None, 80, 90, 100, 110, 120]`, `aran = [800.0, None, 700.0]`, `atar = ['air,ground,enemy,neutral,organic', None, 'air,ground,friend,neutral,self']`

實作：

`SetUnitBaseDamage`　war3map.j:3887
```jass
function SetUnitBaseDamage takes unit u,integer a returns nothing
local integer p=12
local integer r
local integer Id=GetHandleId(u)
local integer pId=LoadInteger(hash,Id,31)
local boolean q=false
if GetUnitAbilityLevel(u,'AInv')<=0 then
set q=true
call UnitAddAbility(u,'AInv')
endif
if a>8191 then
set a=8191
endif
if pId+a<0 then
set a=-pId
else
if pId !=0 and pId+a>0 then
set a=-(pId-a)
endif
endif
if a>0 then
set r=a
loop
exitwhen r<=0
if R2I(Pow(2,p))>r then
set p=p-1
elseif R2I(Pow(2,p))<=r then
set bj_lastCreatedItem=CreateItem(setAttribute___itemAddBaseDamage[p],GetUnitX(u),GetUnitY(u))
call UnitAddItem(u,bj_lastCreatedItem)
call RemoveItem(bj_lastCreatedItem)
set r=r-R2I(Pow(2,p))
set p=p-1
endif
endloop
set pId=pId+a
call SaveInteger(hash,Id,31,pId)
endif
if GetUnitAbilityLevel(u,'AInv')>0 and q==true then
call UnitRemoveAbility(u,'AInv')
endif
endfunction
```

`HeroR39_Banhammer_Debuff`　war3map.j:58442
```jass
function HeroR39_Banhammer_Debuff takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u3
local group ug=LoadGroupHandle(hash,Id,1)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)then
call UnitRemoveAbility(u3,'A0LZ')
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set u3=null
set t=null
set ug=null
endfunction
function HeroR39_Banhammer takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u3
local group ug=CreateGroup()
local group ug2=CreateGroup()
local real x=LoadReal(hash,Id,1)
local real y=LoadReal(hash,Id,2)
local real dmg=750+udg_ItemBonusDMG[n]*2.00
local real vampiric=LoadReal(hash,GetHandleId(u),'A0P9')
if GetUnitAbilityLevel(u,'A0PE')>=4 or GetUnitAbilityLevel(u,'A0XW')>=4 then
set dmg=dmg*1.75
endif
call GroupEnumUnitsInRange(ug,x,y,225,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
if UnitAlive(u3)!=true then
if IsUnitType(u3,UNIT_TYPE_HERO)then
call SetHeroStr(u,GetHeroStr(u,false)+2,true)
call SetHeroAgi(u,GetHeroAgi(u,false)+2,true)
call SetHeroInt(u,GetHeroInt(u,false)+2,true)
else
set n=GetUnitLevel(u3)
if n<=1 then
call SetUnitBaseDamage(u,GetUnitBaseDamage(u)+1)
elseif n<=4 then
call SetUnitBaseDamage(u,GetUnitBaseDamage(u)+2)
elseif n<=8 then
call SetUnitBaseDamage(u,GetUnitBaseDamage(u)+2)
call SetUnitLife(u,R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE)))+50)
else
call SetUnitBaseDamage(u,GetUnitBaseDamage(u)+2)
call SetUnitLife(u,R2I((GetUnitState((u),UNIT_STATE_MAX_LIFE)))+50)
call SetUnitExtraArmor(u,GetUnitExtraArmor(u)+1)
endif
endif
else
call GroupAddUnit(ug2,u3)
call UnitAddAbility(u3,'A0LZ')
endif
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set t=CreateTimer()
set Id=GetHandleId(t)
call SaveGroupHandle(hash,Id,1,ug2)
call TimerStart(t,7.50,false,function HeroR39_Banhammer_Debuff)
set u=null
set u3=null
set pl=null
set t=null
set ug=null
set ug2=null
endfunction
```

`Trig_HeroSkills39_Actions`　war3map.j:58641
```jass
elseif Skill=='A0LG' then
set x=GetSpellTargetX()
set y=GetSpellTargetY()
set u2=CreateUnit(pl,'o02Q',x,y,270)
call UnitApplyTimedLife(u2,'BTLF',4.5)
call SaveReal(hash,Id,1,x)
call SaveReal(hash,Id,2,y)
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,0.80,false,function HeroR39_Banhammer)
endif
```

## 無可迴避的報應 `A0LD`　—　來自皮膚「光明使者」

俄文原名：Неотвратимое возмездие

```
散發光環，降低英雄附近敵人的護甲。

護甲降低：3 點
作用範圍：200 點
```

每級變動：
  - 第 3 行：3 / 5 / 7 / 9 / 11

物件欄位（原型 `AHad`）：`Had1 = [-3.0, -5.0, -7.0, -9.0, -11.0]`, `aare = 200.0`, `abuf = B02X`, `adur = [None, 0.0]`, `ahdu = [None, 0.0]`, `alev = 5`, `atar = air,enemies,ground,invulnerable,vulnerable`

*（JASS 裡沒有對應實作 —— 這是原生技能，效果看上面的物件欄位）*

## 天罰 `A0LM`　—　來自皮膚「光明使者」

俄文原名：Кара небесная

```
英雄周圍開始出現爆炸，對敵人造成傷害。「無可避免的終局」技能第一項任務的加成會為此技能增加 4 次爆炸。

每次爆炸的傷害：40 +（20% 技能強度）點
爆炸次數：8 次

冷卻：10 秒
```

每級變動：
  - 第 3 行：40 / 60 / 80 / 100 / 120

物件欄位（原型 `ANcl`）：`Ncl1 = 1.0`, `Ncl3 = 1`, `Ncl4 = 1.0`, `Ncl5 = 0`, `Ncl6 = [None, 'channel']`, `acdn = 10.0`, `alev = 5`, `amcs = [90, 100, 110, 120, 130]`, `aran = 110.0`, `atar = air,ground,debris,enemy,neutral,organic`

實作：

`HeroQ39`　war3map.j:58399
```jass
function HeroQ39 takes nothing returns nothing
local timer t=GetExpiredTimer()
local integer Id=GetHandleId(t)
local unit u=LoadUnitHandle(hash,Id,1)
local player pl=GetOwningPlayer(u)
local integer n=GetPlayerId(pl)+1
local unit u3
local integer count=LoadInteger(hash,Id,1)
local real dmg=20+20*I2R(GetUnitAbilityLevel(u,'A0LM'))+udg_ItemBonusDMG[n]*0.20
local real vampiric=LoadReal(hash,GetHandleId(u),'A0P9')
local real x=GetUnitX(u)
local real y=GetUnitY(u)
local real dist=GetRandomReal(20,200)
local real angle=GetRandomReal(0,360)
local group ug=CreateGroup()
set x=PolarX(x,dist,angle)
set y=PolarY(y,dist,angle)
call DestroyEffect(AddSpecialEffect("war3mapImported\\Shining Flare.mdx",x,y))
call GroupEnumUnitsInRange(ug,x,y,215.,null)
loop
set u3=FirstOfGroup(ug)
exitwhen u3==null
if UnitAlive(u3)and IsUnitEnemy(u3,pl)then
call UnitDamageTarget(u,u3,dmg,true,false,ATTACK_TYPE_NORMAL,DAMAGE_TYPE_MAGIC,WEAPON_TYPE_WHOKNOWS)
call SetWidgetLife(u,GetWidgetLife(u)+dmg*vampiric)
endif
call GroupRemoveUnit(ug,u3)
endloop
call DestroyGroup(ug)
set count=count-1
if count==0 then
call FlushChildHashtable(hash,Id)
call PauseTimer(t)
call DestroyTimer(t)
else
call SaveInteger(hash,Id,1,count)
endif
set u=null
set u3=null
set pl=null
set t=null
set ug=null
endfunction
```

`Trig_HeroSkills39_Actions`　war3map.j:58584
```jass
elseif Skill=='A0LM' then
if GetUnitAbilityLevel(u,'A0PE')>=2 or GetUnitAbilityLevel(u,'A0XW')>=2 then
call SaveInteger(hash,Id,1,12)
else
call SaveInteger(hash,Id,1,8)
endif
call SaveUnitHandle(hash,Id,1,u)
call TimerStart(t,0.40,true,function HeroQ39)
```

---

## 皮膚

純外觀：幽靈騎士

### 光明使者 `Udth` —— **會換技能**
  - 恐懼召喚 `A0PD` → **聖光援助** `A0LH`
  - 判決 `A0PB` → **至高審判** `A0LG`
  - 死亡臨在 `A0PA` → **無可迴避的報應** `A0LD`
  - 撕裂 `A0P7` → **天罰** `A0LM`

---

## 這隻碰到的 hash key

  - **1** — 裝備技能冷卻乘數〔持有者〕StartModCooldown 讀，CD×它，下限 0.20
  - **28** — 實數＝冰凍傷害 +%〔施加者〕／整數＝抵抗冰凍旗標〔受害者〕
  - **29** — 實數＝流血傷害 +%〔施加者〕／整數＝抵抗流血旗標〔受害者〕（加成寫錯變數，實際無效 —— 見 地圖問題回報 A-4）
  - **44** — 狀態免疫旗標〔受害者〕>0 則所有狀態函式開頭直接 return，完全不判定
  - **48** — 冰凍抗性〔受害者〕；電擊 −1.00
  - **49** — 流血抗性〔受害者〕；電擊 −1.00

---

*由 `tools/build_dossier.py` 從 UD_v3.81 地圖檔產生。*
*機制通則、配裝規則與輸出格式見 `tools/BUILD_BRIEF.md`；*
*道具數值見 `data/dossier/_items.md`。*
