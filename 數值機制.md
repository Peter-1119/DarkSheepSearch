# 數值機制全貌：加法、乘法，以及狀態之間的連動

> 全部逐條對照 `UD_v3.81` 地圖的 `war3map.j`（66226 行）原始碼，不是從道具說明推測。
> 括號裡的 `key N` 是地圖內部 hashtable 的欄位編號，方便日後自行查證。
> 解地圖的工具在 `tools/mpq.py`，任何一條都能自己複驗。

---

## 0. 先看這張圖：傷害其實有四條獨立管線

```
                  ┌─────────────────────────────────────────────┐
  普通攻擊         │  管線 A：DefCof                              │
  英雄技能    ───▶ │  一個係數，所有百分比「加減」進去，最後乘一次    │
  裝備直接傷害     │  下限 0.20（最多減傷 80%），上限無             │
                  └─────────────────────────────────────────────┘

  點燃 / 流血      ┌─────────────────────────────────────────────┐
  / 疾病 的跳傷 ─▶ │  管線 B：狀態係數（每種狀態各一條）             │
                  │  施加時算總傷害 → 切 16 跳 → 每跳各自算係數     │
                  │  ★ 執行前會把 DefCof 那個觸發器整個關掉 ★      │
                  └─────────────────────────────────────────────┘

  被攻擊時的反擊 ─▶ │  管線 C：反傷  = 反傷值 ×(1+反傷加成) × 情境倍率 │

  命中時的追加   ─▶ │  管線 D：穿透  = 穿透值 × 情境倍率（獨立一包）   │
```

**四條互不共用係數。** 這是理解所有「為什麼我加了 X 卻沒效果」的關鍵。

---

## 1. 核心規則：全地圖沒有乘法疊乘

`Trig_HeroTakeDamage_Actions`（受傷結算）對 `DefCof` 的操作實測統計：

> **加法 30 次、減法 23 次、乘法 0 次。**

所以：

```
+20% 和 +20%  →  +40%      （不是 1.2×1.2 = +44%）
−20% 和 −20%  →  −40%      （不是 0.8×0.8 = −36%）
```

**加法對堆疊減傷比較有利，對堆疊輸出比較不利。**

唯二用乘法的地方是「係數本身乘上基數」的那一步：
`實際傷害 = 原始傷害 × DefCof`、`實際冷卻 = 基礎冷卻 × 冷卻係數`。

---

## 2. 技能強度（сила умений）

最單純：**純加法數值池**，存在 `udg_ItemBonusDMG[玩家編號]`。

- 全地圖 **85 件道具**往這個池子加值
- 技能再按**自己的係數**取用，係數寫死在各技能裡

```jass
// 某個 R 技能
set dmg = 200. + udg_ItemBonusDMG[n]*0.90 + LoadReal(hash,Id,16)*9.00
//        基礎     技能強度 × 0.90              穿透 × 9.00
```

實測係數範圍 **0.02 ～ 1.00**。所以：

| 你有 | 對係數 0.90 的技能 | 對係數 0.05 的技能 |
|---|---|---|
| +100 技能強度 | +90 傷害 | +5 傷害 |

**53 位英雄中有 50 位**至少一個技能吃技能強度。網站英雄頁有標。

### ★ 技能強度和裝備技能威力是「相乘」的

這是全地圖唯一一處真正的兩層結構。最清楚的一行（`Trig_HeroSkills11_Actions`）：

```jass
set dmg = (50 + 50*技能等級 + udg_ItemBonusDMG[n]*0.50) * cof
//         └──── 括號內是加法：基礎 + 技能強度×係數 ────┘   × (1 + 裝備技能威力×0.33)
```

```
技能傷害 = (基礎值 + 技能強度 × 該技能係數) × (1 + 裝備技能威力 × 0.33)
```

- **技能強度加進括號裡**（跟基礎值同一層）
- **裝備技能威力乘在括號外**

所以兩者**會互相放大**：+200 技能強度和 +100% 裝備技能威力一起帶，
效果是 `(基礎+200×係數) × 1.33`，不是各自獨立相加。

> 兩者仍是各自獨立累積的：多件技能強度道具彼此是加法，
> 多件裝備技能威力道具彼此也是加法，只有這兩「群」之間才是乘法。

> 補充：英雄技能施加的狀態，其總傷害也是先照上面算好再丟進 `BurnUnit`
> （例如 `call BurnUnit(u,u3,dmg*cof,0.60)`），所以**英雄技能造成的狀態傷害
> 是吃技能強度的**（道具造成的則不吃）。

---

## 3. 裝備技能威力（сила модификаторов，key 18）

最容易誤解的一項，因為**作用範圍是分段的**。

各道具的 `+X%` 先**加總**進 `key 18`，再依觸發路徑換算：

| 觸發路徑 | 公式 | 吃幾成 |
|---|---|---|
| 裝備觸發：攻擊時 / 被攻擊時 / 施法結束 / 擊殺時 | `cof = 1 + key18` | **100%** |
| 英雄技能 | `cof = 1 + key18 × 0.33` | **33%** |
| **每 5 秒的週期道具** | 不使用 `cof` | **0%** |

第三列最容易踩雷。`Check_5sec_Item`（腐化之冠、熾熱核心、寒冰之球那類
「每 5 秒對周圍敵人…」）整個函式裡 `cof` 出現 **0 次**。

```jass
// 裝甲護手：攻擊觸發 → 吃滿
set dmg = 50*cof                  // cof = 1 + 裝備技能威力
call BurnUnit(a,d,dmg,0.75)

// 腐化之冠：每 5 秒週期 → 完全不吃
set dmg = 250.                    // 沒有 *cof
call DiseaseUnit(u,u3,dmg,0.60)
```

### 隱藏門檻

有些道具會**檢查**裝備技能威力有沒有到某個值才給額外效果：

```jass
// 詛咒者之弩：每 5 秒
call VulnerabilityUnit(a,d,1.00)      // 100% 施加易傷（無條件）
if cof >= 2.00 then                   // 需要裝備技能威力 ≥ 100%
  call CurseUnit(a,d,0.50)            // 才會額外 50% 機率補一層詛咒
endif
```

---

## 4. 裝備技能冷卻（key 1）

**加法累積係數，乘法套用在秒數上。**

```jass
function StartModCooldown takes integer u_Id, integer i_Id, real CD
  local real CDCof = LoadReal(hash,u_Id,1)    // 英雄出生時設為 1.00
  if CDCof < 0.20 then
    set CDCof = 0.20                           // 下限
  endif
  set CD = CD * CDCof
```

- 「−10% 裝備技能冷卻」= `key1 −= 0.10`
- 三件 −10% → 係數 0.70 → 冷卻變原本的 **70%**（不是 0.9³ = 72.9%）
- **下限 0.20**：堆到 −90% 也只會縮到 20%，超過的部分白費
- 也有道具是**加**冷卻的懲罰（`+20%`、`+50%`），同樣走加法

---

## 5. 狀態傷害（管線 B）

### 5-1　只有三種狀態會造成傷害

| 狀態 | 每跳間隔 | 跳數 | 總持續 | 傷害加成欄位 | 抗性欄位 |
|---|---|---|---|---|---|
| **點燃** | 0.25 秒 | 16 | 4 秒 | key 27 | key 47 |
| **流血** | 0.5 秒 | 16 | 8 秒 | key 29 | key 49 |
| **疾病** | 0.75 秒 | 16 | 12 秒 | key 45 | key 50 |
| （冰凍） | — | — | — | key 28 | key 48 |

易燃／冰凍／電擊／詛咒／虛弱／易傷本身**不造成傷害**，全都是放大器或減益。
（冰凍有一組欄位是給「二次冰凍」的斬血傷害用的。）

### 5-2　施加時：道具寫的數字是「總傷害」

```jass
set dmg = dmg/16.        // 一律切成 16 跳
set count = 16
```

所以道具說明的「50 點傷害」是**整段 4 秒的總和**，每跳只有 3.1。
若目標帶 `A064`，跳數減半（8 跳）→ **總傷害直接砍半**。

### 5-3　每跳的係數：又是加法

```
每跳傷害 = (總傷害 ÷ 16) × 係數

係數 = 1.00
     − 目標的該狀態抗性       (key 47 / 48 / 49 / 50)
     + 攻擊者的該狀態傷害加成 (key 27 / 28 / 29 / 45)
     + 情境加成（目標血量、特定道具…）

係數下限 0.20
```

**抗性是被「減」的，所以負抗性 = 加傷害。** 這就是電擊和詛咒的運作方式。

---

## 6. ★ 狀態 debuff 之間、以及對其他傷害的連動 ★

這是最容易被漏掉的一層。**有些狀態會直接改對方的屬性欄位**，
所以它們的效果會流進**別的管線**。

### 6-1　完整連動表

| 狀態 | 對狀態傷害 | 對一般傷害（管線 A） | 對其他管線 | 機制 |
|---|---|---|---|---|
| **電擊** | 四種狀態 **+100%** | 目標受到的傷害 **+20%**<br>目標造成的傷害 **−30%** | — | 直接改 key47/48/49/50 各 −1.00<br>**以及 key4 −0.20、key6 −0.30** |
| **詛咒** | 疾病 **+50%** | 目標受到的傷害 **+20%** | — | key50 −0.50、**key4 −0.20** |
| **虛弱** | 流血 **+50%** | — | — | key49 −0.50 |
| **易傷** | 所有狀態的**施加機率** ×1.5 | — | 受到的**反傷 ×1.5** | 讀 buff `B045`，不改欄位 |
| **易燃** | 點燃機率 ×1.5<br>點燃跳數 16 → **32**（總傷害 ×2） | — | — | 讀 buff `B042` |
| **冰凍** | — | — | 受到的**穿透 ×1.5**<br>（對英雄且持復仇之手可到 ×2） | 讀 buff `B02V` |

### 6-2　關鍵：電擊和詛咒會讓一般傷害也變痛

```jass
// ShockUnit —— 施加電擊時
call SaveReal(hash,u_Id,4,  LoadReal(hash,u_Id,4)  - 0.20*cof)   // 非狀態減傷 −20%
call SaveReal(hash,u_Id,6,  LoadReal(hash,u_Id,6)  - 0.30*cof)   // 造成傷害   −30%
call SaveReal(hash,u_Id,47, LoadReal(hash,u_Id,47) - 1.00*cof)   // 點燃抗性 −100%
call SaveReal(hash,u_Id,48, ... - 1.00*cof)                      // 冰凍抗性 −100%
call SaveReal(hash,u_Id,49, ... - 1.00*cof)                      // 流血抗性 −100%
call SaveReal(hash,u_Id,50, ... - 1.00*cof)                      // 疾病抗性 −100%

// CurseUnit —— 施加詛咒時
call SaveReal(hash,...,50, ... - 0.50)                           // 疾病抗性 −50%
call SaveReal(hash,u_Id,4, LoadReal(hash,u_Id,4) - 0.20)         // 非狀態減傷 −20%
```

`key 4` 是 `DefCof` 減項之一（無條件生效），`key 6` 是加項之一。
所以**電擊等於同時做三件事**：放大狀態傷害、讓目標更脆、讓目標更弱。

> 上面那個 `cof` **不是**裝備技能威力，是 `ShockUnit` 自己的區域變數，
> 預設 1.0。唯一會改變它的是：**施加電擊的人**身上有「風暴之靴 `I09A`」時變成 0.50，
> 整組效果減半。
>
> ⚠️ 值得注意：風暴之靴的道具說明寫的是「**減弱英雄身上的電擊負面效果 50%**」，
> 聽起來應該是防禦性道具（自己被電擊時減輕），但程式碼檢查的是
> `GetHandleId(damager)` —— 也就是**施加者**。照現在的程式碼，
> 你帶著它反而會讓自己打出去的電擊效果砍半。這看起來是地圖的筆誤，
> 但我只能陳述程式碼實際的行為，不臆測作者本意。

### 6-3　狀態之間會不會互相疊乘？—— 不會，全部加在同一條線上

目標同時中了**電擊 + 詛咒**時的疾病係數：

```
係數 = 1.00 − (−1.00)  − (−0.50)  = 2.50
        基礎    電擊       詛咒
```

**2.5 倍，不是 2×1.5 = 3 倍。** 因為兩者都是往同一個 `key 50` 累加。

同理，目標同時中電擊 + 詛咒時受到的**一般**傷害：

```
DefCof = 1.00 − key4    where key4 = 原本的減傷 − 0.20(電擊) − 0.20(詛咒)
       → 等於受到的傷害 +40%（假設原本沒有減傷）
```

### 6-4　易燃是唯一會改「跳數」的

```jass
if GetUnitAbilityLevel(target,'B042')==1 then           // 目標處於易燃
  set chanse = chanse*(1.00+(0.50*(1.00+key46)))        // 點燃機率 ×1.5
endif
...
if GetUnitAbilityLevel(target,'B042')==1 then
  set count = count + R2I(8.0*(1.00+(1.00+key46)))      // 16 → 32 跳
endif
```

`key46` 平常是 **0**（只有天賦 `A0ZK` 會給 +0.50），所以代入後：

```
count = 16 + R2I(8.0 × (1.00 + (1.00 + 0))) = 16 + 16 = 32
```

每跳傷害是固定的（總傷害 ÷ 16），**跳數 16 → 32 就等於總傷害翻倍**，
持續時間也從 4 秒變 **8 秒**。

**這是全地圖唯一真正接近「乘法」的狀態放大。**

### ⚠️ 遊戲內說明與程式碼不一致

遊戲 F2 圖鑑寫的是：

> 「易燃」使點燃機率 +50%，並**延長 2 秒**點燃時間（總點燃傷害 **+50%**）

但程式碼實際是：

| | 遊戲說明 | 程式碼實際 |
|---|---|---|
| 點燃機率 | +50% | **+50%** ✅ 一致 |
| 延長時間 | +2 秒 | **+4 秒**（4 秒 → 8 秒） |
| 總點燃傷害 | +50% | **+100%** |

機率那一項是對的，時間與總傷害是說明少寫了一半。
公式裡多包了一層 `(1.00 + ...)`：寫成 `8.0*(1.00+key46)` 才會是說明講的 +2 秒。

> 網站上的翻譯維持照抄遊戲原文，不改。這裡只記錄實測差異。
> 若帶天賦 `A0ZK`（key46 = 0.5）：機率 ×1.75、跳數 16 → 36（9 秒）。

### 6-5　有些道具會針對「目標處於某狀態」給額外加成

這些不是狀態內建的，是道具自己判斷的：

| 道具 | 條件 | 效果 |
|---|---|---|
| 極光 `ktrm` | 目標**冰凍** | `DefCof +0.35` |
| 附魔之爪 `I07G` | 目標**冰凍** | `DefCof +0.20` |
| 石英法杖 / 寒冰之球 | 目標**冰凍** | 狀態每跳係數 `+0.35` |
| 幸運馬蹄鐵 `I09G` | — | 自己施加狀態的機率 ×1.5 |
| 惡魔鎧甲 `shhn` | — | 反傷改成 60% 機率的點燃 |
| 苦難面具 `I068` | — | 受到的狀態傷害 20% 反彈給對方 |

---

## 7. 非狀態傷害／減傷（管線 A 的細節）

一次普攻或技能命中時，地圖用**一個** `DefCof` 收集雙方所有百分比：

```jass
local real DefCof = 1.00

// ── 攻擊者的加成（加）
if 目標是英雄        then DefCof += key3(攻擊者)    // 對英雄傷害
if 目標等級 ≤ 1      then DefCof += key5(攻擊者)    // 對 0-1 級傷害
if 目標是近戰        then DefCof += key40(攻擊者)   // 對近戰傷害
if 目標是遠程        then DefCof += key41(攻擊者)   // 對遠程傷害
                          DefCof += key6(攻擊者)    // 造成傷害（無條件）
if 目標是不死族      then DefCof += key9(攻擊者)    // 對不死族傷害

// ── 防守者的減傷（減）
                          DefCof -= key4(防守者)    // 非狀態減傷（無條件）
if 攻擊者是英雄      then DefCof -= key8(防守者)    // 受英雄傷害減免
if 攻擊者是近戰      then DefCof -= key42(防守者)   // 受近戰減免
if 攻擊者是遠程      then DefCof -= key43(防守者)   // 受遠程減免
if 攻擊者等級 < 2    then DefCof -= key12(防守者)   // 受 0-1 級減免
if 攻擊者是不死族    then DefCof -= key15(防守者)   // 受不死族減免

// ── 另外還有 ~40 條情境判斷（血量、特定道具、特定 buff…）也都是加減
```

最後套用一次：

```jass
if DefCof < 1.00 then
  if DefCof < 0.20 then set DefCof = 0.20 endif    // 下限 = 最多減傷 80%
  set life = life + dmg*(1.00-DefCof)               // 把減掉的還回去
elseif DefCof > 1.00 then
  call UnitDamageTarget(a,d,dmg*(DefCof-1.00))      // 超出的再打一次
endif
```

淨效果 `實際傷害 = 原始傷害 × DefCof`。**沒有上限**，下限 0.20。

---

## 8. 「非狀態」三個字是機制上的硬區隔

不是文案修辭。第 7 節那些加成全部寫在「受傷時」觸發器裡，
而**所有狀態傷害在打出去之前都會把這個觸發器關掉**：

```jass
call DisableTrigger(gg_trg_HeroTakeDamage)
call UnitDamageTarget(u,u2,dmg*cof,...)      // 狀態的每一跳
call EnableTrigger(gg_trg_HeroTakeDamage)
```

實測 `Burn_Dmg`／`Bleed_Dmg`／`Disease_Dmg`／`BurnUnit`／`FrostUnit` **全部都有這一行**。

> **「+X% 非狀態傷害」對點燃、流血、疾病的跳傷完全沒有作用**
> —— 那些傷害根本不經過那段程式碼。

反過來，**英雄技能不關這個觸發器**（實測），所以技能傷害算「非狀態傷害」，
吃 `DefCof` 的全部加成與減免。

### 各種傷害走哪條路

| 傷害種類 | 走 DefCof？ | 吃非狀態傷害/減傷？ | 吃狀態傷害加成？ | 吃裝備技能威力？ | 吃技能強度？ |
|---|---|---|---|---|---|
| 普通攻擊 | ✅ | ✅ | ✖ | ✖ | ✖ |
| 英雄技能 | ✅ | ✅ | ✖ | ✅ 33% | ✅ 各技能係數 |
| 裝備觸發的直接傷害 | ✅ | ✅ | ✖ | ✅ 100% | ✖ |
| **狀態跳傷** | ✖ | ✖ | ✅ | 只在施加當下乘一次 | ✖ |
| **反傷** | ✖ | ✖ | ✖ | ✖ | ✖ |
| **穿透** | ✖ | ✖ | ✖ | ✖ | ✖ |

---

## 9. 反傷（管線 C）與穿透（管線 D）

### 反傷

```
反傷 = key17 × (1 + key19)
     × 0.50   若目標是建築
     × 1.50   若攻擊者身上有【易傷】
     × 0.50   若攻擊者有 B00W
     × (0.90 − 0.10×等級)  若攻擊者有 B01S
```

**注意這裡是乘法**，因為每一項是獨立的情境倍率，不是同類百分比累加。
`key19`（反傷加成）本身仍是各道具**加總**後才乘一次。

若持有惡魔鎧甲 `shhn`，反傷不直接造成傷害，改成 60% 機率的點燃。

### 穿透

在主傷害結算完之後，**另外打一包**：

```jass
set life = key16(攻擊者)                       // 穿透值
if 目標不是建築 then
  if 目標【冰凍】then
    life *= (持復仇之手 且 目標是英雄) ? 2.00 : 1.50
  else
    life *= (持復仇之手 且 目標是英雄) ? 1.50 : 1.00
  endif
  if 目標有 B01S then life *= (0.90 − 0.10×等級)
  UnitDamageTarget(a, d, life, ...)            // 獨立一包，DAMAGE_TYPE_UNIVERSAL
endif
```

所以穿透**不吃**任何 `DefCof` 加成、不吃技能強度、不吃裝備技能威力。
它唯一的放大來源就是**冰凍**（和復仇之手）。

---

## 10. 已知的地圖 bug：流血與疾病的傷害加成失效

| 函式 | 加成寫進哪個變數 | 實際拿來算傷害的 |
|---|---|---|
| `Burn_Dmg` | `cof = cof + key27` ✅ | `dmg * cof` |
| `Bleed_Dmg` | `dmg_cof = dmg_cof + key29` ❌ | `dmg * cof` |
| `Disease_Dmg` | `dmg_cof = dmg_cof + key45` ❌ | `dmg * cof` |

`cof` 在迴圈開頭就已經從 `dmg_cof` 複製走了，加成卻寫回 `dmg_cof`。

**後果：「流血傷害 +X%」和「疾病傷害 +X%」對你自己施加的第一段狀態完全無效**
（只會外溢到同一個目標身上的下一個傷害來源）。**只有點燃傷害是正常的。**

受影響：荊棘戒指的 +60% 流血傷害、屍毒的 +50% 疾病傷害、
魔術師的面具轉出來的那一份。

**不受影響**：電擊、詛咒、虛弱 —— 因為它們走「抗性」那一側（在複製之前就減掉了）。

---

## 11. 實例計算

### 例一：裝甲護手（每 4 秒，攻擊觸發）

道具寫「每種狀態 75% 機率、50 點傷害」。假設 **+65% 裝備技能威力**、目標乾淨：

```
cof        = 1 + 0.65 = 1.65
點燃總傷害 = 50 × 1.65 = 82.5   → 16 跳 × 0.25 秒，每跳 5.2
流血總傷害 = 50 × 1.65 = 82.5   → 16 跳 × 0.5 秒，每跳 5.2
每次觸發期望 = 2 × 0.75 × 82.5 ≈ 124 傷害 / 4 秒 ≈ 31 DPS
```

同樣情境，目標身上有**電擊**：

```
每跳係數 = 1.00 − (−1.00) = 2.00
總計 165 × 2.00 = 330      → 約 82 DPS
```

再加上**易燃**（只影響點燃那半邊）：

```
點燃跳數 16 → 32，總傷害 82.5 × 2 = 165，再 ×2.00（電擊）= 330
流血維持 82.5 × 2.00 = 165
合計 495 傷害 / 4 秒 ≈ 124 DPS
```

而且電擊同時讓目標的**一般傷害**也多吃 20%。

### 例二：腐化之冠（每 5 秒，範圍 300）

「疾病 60% 機率、250 點傷害」。**週期觸發 → 不吃裝備技能威力**：

```
總傷害 = 250（固定）→ 16 跳 × 0.75 秒 = 12 秒，每跳 15.6
單一敵人期望 = 0.6 × 250 / 5 秒 = 30 DPS
但它是 AoE，範圍內每個敵人獨立判定 → 打 10 隻怪 ≈ 300 DPS
```

它同時 40% 機率上詛咒 → 疾病係數 `1 + 0.50 = 1.50` → 該段變 **375 總傷害**，
而且目標的一般減傷同時被扣 20%。

### 例三：減傷疊加與天花板

三件裝備各給 −20%、−15%、−10% 受到的傷害（無條件的 key 4）：

```
DefCof = 1.00 − 0.20 − 0.15 − 0.10 = 0.55   → 受到 55% 傷害
```

**不是** `0.8 × 0.85 × 0.9 = 0.612`。加法對堆減傷比較划算。

但堆到 `DefCof < 0.20` 就被夾住 —— **減傷超過 80% 之後完全沒有效益**，
而且此時若對方給你上電擊 + 詛咒（key4 −0.40），你會直接掉出天花板保護。

---

## 12. 內部欄位對照表

想自己查證時對照用。`SaveReal(hash, 英雄handle, key, 值)`。

| key | 意義 | 進哪條管線 |
|---|---|---|
| 1 | 裝備技能冷卻係數（起始 1.00） | 乘上冷卻秒數 |
| 3 | 對英雄傷害 | DefCof + |
| 4 | 非狀態減傷（無條件） | DefCof − |
| 5 | 對 0-1 級傷害 | DefCof + |
| 6 | 造成傷害（無條件） | DefCof + |
| 8 | 受英雄傷害減免 | DefCof − |
| 9 | 對不死族傷害 | DefCof + |
| 12 | 受 0-1 級單位減免 | DefCof − |
| 15 | 受不死族減免 | DefCof − |
| **16** | **穿透** | 獨立一包 |
| **17** | **反傷** | 獨立一包 |
| **18** | **裝備技能威力** | 乘進道具/技能的傷害基數 |
| 19 | 反傷加成 | 乘進反傷 |
| 27 / 47 | 點燃傷害加成 / 點燃抗性 | 狀態係數 |
| 28 / 48 | 冰凍傷害加成 / 冰凍抗性 | 狀態係數 |
| 29 / 49 | 流血傷害加成 / 流血抗性 | 狀態係數 |
| 45 / 50 | 疾病傷害加成 / 疾病抗性 | 狀態係數 |
| 40 / 42 | 對近戰傷害 / 受近戰減免 | DefCof |
| 41 / 43 | 對遠程傷害 / 受遠程減免 | DefCof |

> 道具寫「+X% 狀態傷害」時，會**同時**寫 key 27／28／29／45 四格。
> 道具寫「+X% 狀態防護」時，會**同時**寫 key 47／48／49／50 四格。

---

## 13. 速查

**想讓狀態打更痛**
1. 上**電擊** —— 四種狀態 +100%，沒有任何裝備擋得住，還順便讓對方一般傷害多吃 20%
2. 上**易燃** —— 點燃跳數翻倍，是唯一接近乘法的放大
3. 上**詛咒**（疾病 +50%）或**虛弱**（流血 +50%）
4. 疊**裝備技能威力** —— 只對「攻擊／技能／擊殺觸發」有效，**週期道具完全沒用**
5. 「點燃傷害 +X%」有效；「流血／疾病傷害 +X%」因地圖 bug 幾乎無效

**想讓技能打更痛**
1. 疊**技能強度**（每個技能的吃取係數不同，0.02～1.00，看英雄頁）
2. 疊**裝備技能威力** —— 技能只吃 33%
3. 疊「造成傷害」「對英雄傷害」等 DefCof 加成

**想讓普攻打更痛**
1. DefCof 那一組（造成傷害、對英雄／對近戰／對遠程傷害）
2. **穿透** —— 完全獨立，不被減傷吃掉；配冰凍再 ×1.5

**想更耐打**
1. 「非狀態減傷」全部相加，**實質上限 80%**
2. 狀態傷害要另外用「狀態防護」擋，跟上面是完全兩套系統
3. 小心電擊／詛咒 —— 它們直接扣你的 key4，等於穿透你的減傷堆疊

---

---

## 14. 方法論的限制：不是所有效果都在 war3map.j 裡

本文的結論全部來自觸發器程式碼 `war3map.j`，但**有一類效果不在那裡**：
掛在道具或英雄身上的**原版魔獸技能**，由遊戲引擎直接執行，觸發器完全不會提到它。

實例 —— **大法師權杖 `fgrd`**：

| | |
|---|---|
| 道具說明 | 「每 0.75 秒對附近隨機敵人造成 125 點傷害」 |
| 在 `war3map.j` 裡搜 `'fgrd'` | 只有屬性套用與給予道具兩處，**沒有任何觸發** |
| 真相 | 道具的 `iabi` 欄位掛著技能 `A0GW`，衍生自暴雪原版的鳳凰之火 `Apxf` |
| 技能欄位 | `acdn = 0.75`（冷卻）、`pxf1 = 125.0`（傷害）、`aare = 700`（範圍） |

所以效果是真的，只是由引擎執行。

> ⚠️ **我曾因此判斷錯誤**：先前只搜 JASS 沒找到，就結論「大法師權杖是純屬性、沒有觸發」，
> 那是錯的。要確認一件道具有沒有效果，除了搜 `war3map.j`，
> 還要看 `war3map.w3t` 的 `iabi` 欄位與對應的 `war3map.w3a` 技能資料。

**這個限制影響哪些結論？**

- 第 3 節「裝備技能威力的作用範圍」**不受影響** —— `key 18` 只有 JASS 讀得到，
  引擎執行的原版技能不可能吃到它（這也表示大法師權杖那 125 點**不吃**裝備技能威力）。
- 第 5～8 節的狀態與 `DefCof` 管線**不受影響** —— 那些全是自訂觸發器實作的。
- 會受影響的是「這件道具／這個技能到底有沒有額外效果」這類問題，
  需要同時查物件資料才算完整。

同理，英雄頁上「技能吃不吃裝備技能威力」的標記，是掃 JASS 得到的；
若某個技能純粹由引擎執行，它本來就不會吃裝備技能威力，所以標記仍然正確。

---

*資料來源：`UD_v3.81` 的 `war3map.j`、`war3map.w3t`、`war3map.w3a`。*
*解地圖：`python tools/mpq.py <地圖路徑>`；所有公式與統計皆可自行複驗。*
