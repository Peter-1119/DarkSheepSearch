# 設計筆記 — 這個網站為什麼這樣做

你問「頂尖前端會怎麼呈現」，這份筆記把 `index.html` 裡每個設計決策的理由寫出來。
重點不是技巧，而是**每個決定要解決哪個具體問題**。

---

## 一、先定義真正的問題

原始需求拆開來其實有四個彼此衝突的目標：

| 需求 | 衝突對象 |
|---|---|
| 474 件裝備要能瀏覽 | 不要滑很久 |
| 不要排得太擠 | 不要滑很久 |
| 點擊要看詳細 | 詳細內容會擠壓瀏覽空間 |
| 三種語言 | 版面寬度（俄文字最長） |

**「不要太擠」和「不要滑太久」在數學上不可能同時最大化** — 同樣的螢幕面積，
留白多就一定裝得少。所以正確的做法不是選一邊，而是**把選擇權交給使用者，
並且讓「找東西」不再依賴滑動**。

具體對應：

- **密度切換（寬鬆／緊湊／純圖）** — 直接把這個取捨變成一個開關。
  純圖模式一屏可看 6 個品質階層，寬鬆模式看 2 個。
- **左側品質導覽列** — 點一下就只顯示該階層，不用捲。這才是真正解決「滑很久」的方法：
  **減少捲動的正解不是塞更多，是讓人不需要捲**。
- **搜尋改用平鋪版面** — 搜尋「freeze」有 16 筆結果，若照品質分段會變成 11 個標題各配 1 個圖示，
  佔掉三個螢幕高度。所以一偵測到搜尋就切換成單一平鋪網格，16 筆一屏看完。
  品質資訊沒有消失 —— 它在圖示邊框顏色和左側計數裡。

---

## 二、注意力焦點：三層訊號，一次只給一個主角

這是你特別問的部分。互動式介面的焦點管理，關鍵是**訊號要分層、不要打架**。

### 第 1 層：選取本身
被點的格子拿到「全額」視覺權重 —— 品質色外框 + 光暈 + 上浮 2px。
全畫面只有這一個元素有這種強度。

### 第 2 層：關聯高亮（這個網站最有價值的互動）
點下 `判官`，它的三個素材（分離者、利斧、鍍金匕首）**在網格裡同時亮起綠框並帶 ＋ 標記**，
可以合成出去的裝備亮起琥珀框並帶 ↑ 標記。

> 這一步把「靜態列表」變成「活的合成圖」。
> 使用者不用讀文字就知道「這東西跟誰有關」。
> 一個裝備合成攻略網站的核心價值就在這裡 —— 而它是**用顏色講的，不是用文字講的**。

### 第 3 層：其餘元素降噪
沒關係的格子降到 `opacity: .34` + 輕微去飽和。

注意這裡刻意**用「調暗」而不是「隱藏」**：

- 隱藏會讓版面重排，使用者失去空間記憶（「剛剛那個東西在左上角」）
- 調暗保留了版面骨架，眼睛自動被亮的東西吸走，但要找暗的仍然找得到
- 滑鼠移上去暗的格子會恢復到 `.85` — 降噪不等於封鎖

**曾經踩到的坑**：一開始降到 `.26` 並且 `saturate(.5)`，實際看起來像網頁壞掉。
調到 `.34 / .62` 之後才是「聚光燈」而不是「斷電」。這種數值只能看著螢幕調，不能用想的。

### 一個真實的 bug 與修正
搜尋時如果選取的裝備被過濾掉了，畫面上「沒有任何亮點，但所有東西都是暗的」——
聚光燈失去了它照的對象。修正方式是：**只有當被選取的格子確實在畫面上時才啟用降噪**。

```js
const onScreen = !!(sel && main.querySelector(`.tile[data-id="${sel}"]`));
grid.setAttribute('data-focus', onScreen ? '1' : '0');
```

這類「狀態組合」的 bug 是互動介面最常見的失誤來源。

---

## 三、視覺語言直接取自素材本身

沒有自己發明配色。地圖檔的道具名稱本身就帶顏色碼：

```
"|cFFFF5800Судья|r"   →  神器 = #FF5800
```

於是：**每個格子的邊框顏色 = 遊戲裡該裝備名稱的顏色**，左側導覽的圓點、
右側面板的光暈、品質標籤也全部沿用同一個值。

| 品質 | 色碼 | | 品質 | 色碼 |
|---|---|---|---|---|
| 神器 | `#FF5800` | | 扭曲 | `#FF00FF` |
| 傳說 | `#FFDC00` | | 折射 | `#01FFFF` |
| 獨特 | `#07EDE6` | | 聖物／完美 | `#FFFF00` |
| 稀有 | `#00C850` | | 耳環 | `#FF427D` |
| 普通／特殊 | `#7D9664` | | 儀式 | `#00C8AF` |

好處有三個：玩家零學習成本、配色天然協調（同一個美術定的）、
而且套裝件會自動顯示套裝色（深淵紫、英勇銅、風暴藍、地獄橘），不用另外寫規則。

> **可以學的一條原則**：當你的內容有既存的視覺系統，先去挖出來用，
> 不要另外發明一套。自創配色幾乎一定比原作者的差，而且會和素材打架。

---

## 四、版面與動線

```
┌──────────────────────────────────────────────────────────┐
│ 頂列：搜尋（主要動作，放中間偏左）· 密度 · 語言（次要，靠右） │
├──────────┬─────────────────────────────┬─────────────────┤
│ 導覽列    │  網格（主要內容）             │ ⇔ 詳細面板       │
│ 品質+計數 │  黏性分段標題                 │   （可拖曳）     │
└──────────┴─────────────────────────────┴─────────────────┘
```

- **搜尋框放左中而非靠右** — 它是最高頻動作，靠近視線起點（左上）。
  語言和密度是低頻設定，推到右邊。
- **黏性分段標題** — 捲動時永遠知道自己在哪一階，不用往回捲確認。
- **左欄計數會跟著搜尋變動** — 打字時左欄立刻變成「結果分佈表」，
  一個元件同時做導覽和統計兩件事。
- **面板從右側進場** — 因為點擊來源在左／中，內容往右展開符合閱讀方向；
  而且不遮蔽剛剛點的東西。

### 開關面板的三種方式
同一件事給多個出口，使用者用哪個都行：

1. **再點一次同一個格子** → 收合（最省手，不用移動滑鼠去找按鈕）
2. `Esc`
3. 右上角 ✕

第 1 種是最重要的：瀏覽時手本來就在格子上，要求使用者把游標移到面板右上角才能關閉，
等於每看一件裝備就多一趟來回。**關閉的入口應該離「打開的地方」越近越好。**

### 可拖曳面板的細節
- 用 `pointer` 事件（同時支援滑鼠／觸控／手寫筆），不是 `mousedown`
- 寬度鉗制在 320–760px，避免拖成不可用的狀態
- `setPointerCapture` 確保拖到視窗外也不會斷
- 拖曳時整個 `body` 加 `cursor:col-resize` 和 `user-select:none`，
  否則會選到文字、游標會閃
- 寬度存進 `localStorage`，下次打開維持
- 雙擊把手重設寬度；把手可 focus，方向鍵也能調（鍵盤可用性）
- **面板變寬時，配方清單自動從 1 欄變 2–3 欄**（`auto-fill / minmax(210px,1fr)`）——
  變寬要有實際回報，不然使用者只是得到更多空白

---

## 四之二、排行檢視：換內容就要換版面

新增「依屬性排行」時，最容易犯的錯是**只把格子重新排序**。但排行和瀏覽的「主角」不同：

| | 主角 | 適合的版面 |
|---|---|---|
| 瀏覽 | 圖示（辨識「這是什麼」） | 網格 |
| 排行 | 數值與順序（比較「差多少」） | 橫列 ＋ 長度條 |

所以排行模式整個換成橫列。網格排序看不出「第 1 名是第 2 名的兩倍」，長度條可以。

### 一個真正的設計取捨：長條要不要用稀有度顏色

第一版我讓長條沿用裝備的稀有度顏色 —— 結果整個清單變成彩虹，**色相變化干擾了長度判讀**。

問題出在一條原則：

> **一個視覺通道只承載一個變數。**
> 長條同時編碼「長度＝數值」和「顏色＝稀有度」時，兩者會互相干擾。
> 眼睛想比長度，卻一直被顏色拉走。

修正後把兩個變數拆到不同通道：

| 變數 | 通道 |
|---|---|
| 數值大小 | 長條長度（**統一琥珀色**） |
| 稀有度 | 圖示外框 ＋ 品質文字顏色 ＋ 長條右端的細線 |

長條右端加一條 2px 實線，給眼睛一個明確的比較邊緣 —— 漸層的尾端很模糊，實線才好對齊。

### 其他決定

- **前三名只加重顏色，不放大字級** —— 放大會讓右側數字欄對不齊，反而破壞「一路掃下來比大小」的能力
- **負值不隱藏**，排在最後並轉紅（例如玄鐵手套 -30% 攻速）。這是真實的取捨資訊
- **只計算固定加成**。「+15% 對 0-1 級敵人的傷害」這種條件加成不列入 —— 跟裸數值放一起比大小會誤導，這點直接寫在標題下方
- **品質篩選仍然有效**，可以問「神器裡技能強度最高的是哪件」
- **面板顯示名次**（`#3 / 78`），排行和詳細兩個檢視互相呼應

---

## 五、動態與節奏

| 元素 | 時間 | 理由 |
|---|---|---|
| 格子 hover | 160ms | 快到感覺是「即時反應」 |
| 面板開啟 | 160ms | 夠慢能看出「這是我點擊造成的」，夠快不覺得拖 |
| Tooltip 延遲 | 260ms | 滑鼠掃過時不會一路閃出提示 |
| 捲動至選取項 | smooth | 保留空間連續性，使用者知道畫面移去哪 |

緩動用 `cubic-bezier(.22,.7,.3,1)` — 起步快、收尾慢，接近物理減速，比 `ease` 有精神。

並且完整支援 `prefers-reduced-motion`：使用者若在系統關掉動畫，全部動畫降到 0.01ms，
平滑捲動也改成瞬移。**這不是加分項，是基本責任**（前庭功能障礙者看到大量動態會不適）。

---

## 六、其他該做但常被忽略的事

- **深層連結**：`index.html#hval` 直接開啟判官。攻略網站一定要能分享單一裝備。
- **鍵盤操作**：`/` 聚焦搜尋、`Esc` 關閉、方向鍵在網格移動、`1/2/3` 切語言。
- **`:focus-visible`** 而非 `:focus` — 只有鍵盤操作才顯示外框，滑鼠點擊不會出現。
- **數值自動高亮**：效果敘述裡的數字用琥珀色標出，掃視時先抓到數值。
  用一條 regex 完成，不用在資料裡標記。
- **缺圖降級**：54 件裝備沿用暴雪內建圖示、沒有匯出，顯示道具 ID 佔位而不是破圖。
- **`loading="lazy"`**：420 張圖不會一次全載。
- **單檔部署**：資料內嵌進 HTML，直接雙擊就能開，不需要架伺服器。

---

## 七、如果要再往下做

1. **合成樹展開** — 面板裡的素材可以往下再展一層，看到完整合成鏈
2. **對比模式** — 選兩件裝備並排比較數值
3. **build 規劃** — 選 6 件裝備，加總屬性
4. **虛擬捲動** — 目前 474 個 DOM 節點還很輕鬆，若破千再考慮
5. **補齊缺失圖示** — 從 War3 本體匯出那 54 張內建圖示

---

## 檔案

| 檔案 | 說明 |
|---|---|
| `index.html` | 網站本體（資料已內嵌，可直接開啟） |
| `tools/site_template.html` | 原始模板，`__DATA__` 為資料佔位 |
| `tools/build_site.py` | 把 `data/site.json` 內嵌進模板產生 `index.html` |
| `tools/build_site_data.py` | 產生三語資料 `data/site.json` |
| `images/` | 420 張裝備圖示 |
